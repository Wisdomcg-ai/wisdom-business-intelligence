/**
 * Phase 44 — Wave 0 scaffold (created by plan 44-01).
 * All tests are it.todo until plan 44-04 implements the sync orchestrator
 * (covers D-06, D-07, D-09, D-10 per 44-VALIDATION.md).
 * See .planning/phases/44-forecast-pipeline-fix/44-VALIDATION.md for the
 * decision-to-test map.
 */
import { describe, it, expect, vi } from 'vitest'

describe('Sync Orchestrator', () => {
  it.todo('two FY windows')
  it.todo('advisory lock')
  it.todo('idempotent upsert')
  it.todo('natural key uniqueness')
  it.todo('coverage record')
})

void expect
void vi
