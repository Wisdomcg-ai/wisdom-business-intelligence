/**
 * Phase 44 — Wave 0 scaffold (created by plan 44-01).
 * All tests are it.todo until plan 44-11 wires the sparse-tenant UX
 * into the wizard (covers D-15 per 44-VALIDATION.md).
 * See .planning/phases/44-forecast-pipeline-fix/44-VALIDATION.md for the
 * decision-to-test map.
 */
import { describe, it, expect, vi } from 'vitest'
import { render, screen } from '@testing-library/react'

describe('Step3RevenueCOGS — sparse tenant rendering', () => {
  it.todo('renders em-dash for missing months')
  it.todo('renders $0 for real-zero months')
  it.todo('renders coverage banner when coverage less than expected')
  it.todo('renders Based on N months subtext with N matching real-data months')
})

void expect
void vi
void render
void screen
