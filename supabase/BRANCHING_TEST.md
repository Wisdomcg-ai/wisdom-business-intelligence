# Supabase Branching smoke test

Tiny change to trigger Supabase's preview-branch creator for the first time.
Touches `supabase/` so the GitHub integration's "Supabase changes only" filter
fires.

If you're reading this on a preview branch: the branching pipeline works.

Created during the Phase 34 setup — can be deleted once branching is proven.
