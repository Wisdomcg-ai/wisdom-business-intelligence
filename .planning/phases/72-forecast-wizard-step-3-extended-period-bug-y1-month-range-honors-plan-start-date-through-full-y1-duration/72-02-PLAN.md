---
phase: 72-forecast-wizard-step-3-extended-period-bug-y1-month-range-honors-plan-start-date-through-full-y1-duration
plan: 02
type: execute
wave: 2
depends_on:
  - "72-01"
files_modified:
  - src/app/finances/forecast/components/wizard-v4/steps/Step3RevenueCOGS.tsx
  - src/app/finances/forecast/components/wizard-v4/ForecastWizardV4.tsx
  - src/__tests__/forecast/phase-72-step3-extended-period.test.tsx
  - src/lib/forecast/extended-period.ts
autonomous: true
requirements:
  - PHASE-72-FIX
must_haves:
  truths:
    - "Armstrong wizard config (plan_start_date=2026-06-01, is_extended_period=true, Y1=13 months) renders 13 monthly inputs in Step 3 (Jun 2026 - Jun 2027)."
    - "Standard non-extended client (FY26 = Jul 2025 - Jun 2026) Step 3 continues to render exactly 12 months — zero regression."
    - "Extended period starting at FY boundary (plan_start_date === fiscal year start) renders correctly with no remainder overlap."
    - "Extended period >12 and ≤15 months renders the correct month count (Phase 14 max)."
    - "Scoped vitest `vitest run src/__tests__/forecast/phase-72-step3-extended-period.test.tsx` passes 4/4."
    - "Adjacent phase-51/phase-54/phase-55 forecast tests continue to pass (no regression to Step 3-related test files)."
  artifacts:
    - path: "src/__tests__/forecast/phase-72-step3-extended-period.test.tsx"
      provides: "4 regression tests locking the extended-period month-range behavior"
      contains: "describe"
      min_lines: 80
    - path: "src/app/finances/forecast/components/wizard-v4/steps/Step3RevenueCOGS.tsx"
      provides: "Step 3 wizard component honoring is_extended_period + plan_start_date"
      contains: "isExtendedPeriod"
    - path: "src/lib/forecast/extended-period.ts"
      provides: "Shared util for computing extended-period Y1 month keys (created IFF 72-DIAGNOSIS recommends extraction; otherwise skip and inline)"
      contains: "export"
  key_links:
    - from: "src/app/finances/forecast/components/wizard-v4/ForecastWizardV4.tsx"
      to: "src/app/finances/forecast/components/wizard-v4/steps/Step3RevenueCOGS.tsx"
      via: "props plumbing of is_extended_period + plan_start_date + Y1 boundaries"
      pattern: "isExtendedPeriod|extendedPeriod"
    - from: "src/__tests__/forecast/phase-72-step3-extended-period.test.tsx"
      to: "src/app/finances/forecast/components/wizard-v4/steps/Step3RevenueCOGS.tsx"
      via: "renders Step3RevenueCOGS with mocked extended-period props + asserts month count"
      pattern: "Step3RevenueCOGS"
---

<objective>
Apply the fix named in 72-DIAGNOSIS.md and lock the behavior with 4 regression tests. After this plan ships, Matt opens Armstrong's forecast wizard Step 3 and sees 13 monthly revenue/COGS inputs (Jun 2026 - Jun 2027) instead of 3.

Purpose: Close the Phase 14 extended-period gap that left the forecast wizard out of sync with the goals wizard (which was fixed in Phase 68 B15/B16). One platform, one extended-period contract.

Output: Wizard fix + 4 regression tests + (optionally) a new shared util at src/lib/forecast/extended-period.ts. Atomic commits: RED (failing tests) → GREEN (fix) → optional REFACTOR.

**Scope guardrails (locked by CONTEXT):**
- Step 3 ONLY. Step 4/5/6/7 same-family bugs are documented for follow-up phase per 72-DIAGNOSIS `## Related Bugs Found` — DO NOT fix here.
- Non-extended (standard) client behavior MUST remain byte-identical to current.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/72-forecast-wizard-step-3-extended-period-bug-y1-month-range-honors-plan-start-date-through-full-y1-duration/72-CONTEXT.md
@.planning/phases/72-forecast-wizard-step-3-extended-period-bug-y1-month-range-honors-plan-start-date-through-full-y1-duration/72-DIAGNOSIS.md
@.planning/phases/72-forecast-wizard-step-3-extended-period-bug-y1-month-range-honors-plan-start-date-through-full-y1-duration/72-01-SUMMARY.md
@src/app/finances/forecast/components/wizard-v4/steps/Step3RevenueCOGS.tsx
@src/app/finances/forecast/components/wizard-v4/ForecastWizardV4.tsx
@src/app/finances/forecast/components/wizard-v4/types.ts
@src/app/goals/utils/quarters.ts

<interfaces>
<!-- Contract reference for the test scaffold (extracted ahead of executor read) -->

From src/app/goals/utils/quarters.ts (B15 — the adjacent extended-period solution to model the math on):
```typescript
// B15 trim logic — apply same pattern to Y1 month-key derivation:
if (isExtendedPeriod && planStartDate instanceof Date && planStartDate < fyEnd) {
  fyEnd = new Date(planStartDate.getTime() - 24 * 60 * 60 * 1000);
}
```

Existing test file convention (per memory `feedback_executor_scoped_tests`):
- Path: `src/__tests__/forecast/phase-72-step3-extended-period.test.tsx`
- Style: matches `src/__tests__/forecast/phase-51-step3-seasonality.test.tsx` (tsx, React Testing Library, Vitest)

Test data fixtures (canonical extended-period scenarios — use these EXACT values):
- **Armstrong (the symptom):** plan_start_date='2026-06-01', is_extended_period=true, year_1_duration_months=13, fiscal_year_start=7 (FY=July) → expected Y1 monthKeys = ['2026-06', '2026-07', ..., '2027-06'] (13 entries)
- **Standard non-extended:** is_extended_period=false, fiscal_year_start=7, fiscalYear=2026 → expected Y1 monthKeys = ['2025-07', ..., '2026-06'] (12 entries, UNCHANGED from current behavior)
- **FY-boundary extended:** plan_start_date='2025-07-01' (exactly FY26 start), is_extended_period=true, year_1_duration_months=12, fiscal_year_start=7 → expected 12 entries (no remainder overlap), no edge-case crash
- **15-month extended:** plan_start_date='2026-04-01', is_extended_period=true, year_1_duration_months=15, fiscal_year_start=7 → expected 15 entries ['2026-04' through '2027-06']
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — write 4 failing regression tests</name>
  <files>
    src/__tests__/forecast/phase-72-step3-extended-period.test.tsx
  </files>
  <read_first>
    - .planning/phases/72-forecast-wizard-step-3-extended-period-bug-y1-month-range-honors-plan-start-date-through-full-y1-duration/72-DIAGNOSIS.md (locks fix shape + decides shared-util vs inline)
    - src/app/finances/forecast/components/wizard-v4/steps/Step3RevenueCOGS.tsx (current contract — what props it accepts today)
    - src/__tests__/forecast/phase-51-step3-seasonality.test.tsx (test style template — match imports/setup)
    - src/__tests__/forecast/phase-55-step4-year-filter.test.tsx (additional test style reference for extended-period-adjacent assertions)
  </read_first>
  <behavior>
    Test 1 — "Armstrong extended-period config renders 13 monthly inputs in Step 3"
      Given: plan_start_date='2026-06-01', is_extended_period=true, year_1_duration_months=13, fiscal_year_start=7
      When: Step3RevenueCOGS renders with activeYear=1
      Then: exactly 13 month columns are visible AND month labels = ['Jun 2026', 'Jul 2026', ..., 'Jun 2027']

    Test 2 — "Standard non-extended client renders 12 monthly inputs in Step 3 (no regression)"
      Given: is_extended_period=false, fiscalYear=2026, fiscal_year_start=7
      When: Step3RevenueCOGS renders with activeYear=1
      Then: exactly 12 month columns are visible (Jul 2025 - Jun 2026)

    Test 3 — "Extended period starting exactly at FY boundary renders 12 months with no remainder overlap"
      Given: plan_start_date='2025-07-01' (= FY26 start), is_extended_period=true, year_1_duration_months=12, fiscal_year_start=7
      When: Step3RevenueCOGS renders with activeYear=1
      Then: exactly 12 month columns are visible (Jul 2025 - Jun 2026); no crash on FY-aligned edge

    Test 4 — "Extended period of 15 months (Phase 14 max) renders 15 monthly inputs"
      Given: plan_start_date='2026-04-01', is_extended_period=true, year_1_duration_months=15, fiscal_year_start=7
      When: Step3RevenueCOGS renders with activeYear=1
      Then: exactly 15 month columns are visible (Apr 2026 - Jun 2027)
  </behavior>
  <action>
    **TDD RED phase.** Tests MUST fail before Task 2 starts.

    1. Read 72-DIAGNOSIS.md `## Fix Scope` section to learn the final prop shape the fix will introduce on Step3RevenueCOGS (e.g. `extendedPeriod={ isExtendedPeriod, planStartDate, year1EndDate, year1DurationMonths }` or whatever the diagnosis settled on). Match the test mocks to that prop shape.

    2. Read existing test files (src/__tests__/forecast/phase-51-step3-seasonality.test.tsx, phase-55-step4-year-filter.test.tsx) to copy:
       - Imports (vitest, @testing-library/react, the component under test)
       - Mock setup patterns (mocking ForecastContext / actions / required upstream hooks)
       - Render harness (the props bag that Step 3 minimally needs to mount)

    3. Create src/__tests__/forecast/phase-72-step3-extended-period.test.tsx with EXACTLY the 4 tests defined in `<behavior>` above. Use the fixtures from the `<interfaces>` block — do NOT invent new numbers; Matt's diagnosis depends on those specific values.

    4. Assertion shape: prefer counting rendered month columns via `getAllByRole('columnheader')` or a stable test-id, with month-label substring assertions (e.g. `expect(screen.getByText(/Jun 2026/)).toBeInTheDocument()`). Avoid brittle DOM-tree position assertions.

    5. Run the file:
       `npx vitest run src/__tests__/forecast/phase-72-step3-extended-period.test.tsx`
       Expected: ALL 4 FAIL (test 2 may pass since standard behavior is unchanged — that's acceptable as a "regression lock"; tests 1, 3, 4 MUST fail because the fix is not yet in).

       If test 2 also fails (suggesting unexpected breakage in standard path), STOP and re-read 72-DIAGNOSIS.md — the test scaffold might be hitting a setup bug rather than the real fix surface.

    6. Commit RED state:
       `git commit -m "test(72-02): add failing regression tests for Step 3 extended-period (RED)"`
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/forecast/phase-72-step3-extended-period.test.tsx 2>&1 | grep -E "(FAIL|failed|Tests)" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - Test file exists at src/__tests__/forecast/phase-72-step3-extended-period.test.tsx
    - 4 tests defined matching the `<behavior>` spec exactly
    - At least 3 of 4 tests fail (Armstrong test 1, FY-boundary test 3, 15-month test 4)
    - Test 2 (standard 12-month) passes or fails — either is acceptable; if it fails the scaffold needs correction before Task 2
    - RED commit landed with message containing "RED"
  </acceptance_criteria>
  <done>Failing tests locked in CI. Task 2 has a green-target to hit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — apply the fix per 72-DIAGNOSIS</name>
  <files>
    src/app/finances/forecast/components/wizard-v4/steps/Step3RevenueCOGS.tsx
    src/app/finances/forecast/components/wizard-v4/ForecastWizardV4.tsx
    src/lib/forecast/extended-period.ts
  </files>
  <read_first>
    - .planning/phases/72-forecast-wizard-step-3-extended-period-bug-y1-month-range-honors-plan-start-date-through-full-y1-duration/72-DIAGNOSIS.md (`## Fix Scope` is the binding spec for this task)
    - src/app/finances/forecast/components/wizard-v4/steps/Step3RevenueCOGS.tsx (current state)
    - src/app/finances/forecast/components/wizard-v4/ForecastWizardV4.tsx (prop-plumbing source)
    - src/app/goals/utils/quarters.ts deriveCurrentRemainderColumn (B15 — math reference)
    - src/__tests__/forecast/phase-72-step3-extended-period.test.tsx (the contract the GREEN code must satisfy)
  </read_first>
  <behavior>
    After this task:
    - All 4 tests in phase-72-step3-extended-period.test.tsx pass
    - Step3RevenueCOGS accepts extended-period props (or reads them from a context already plumbed by ForecastWizardV4)
    - When activeYear===1 AND is_extended_period===true: monthKeys = [plan_start_date month, ..., plan_start_date + year_1_duration_months - 1] (inclusive both ends)
    - When activeYear===1 AND is_extended_period===false: monthKeys = existing 12-month FY-based sequence (UNCHANGED — verified by test 2)
    - When activeYear===2 or 3: existing behavior UNCHANGED (the bug is Y1-only per 72-CONTEXT)
  </behavior>
  <action>
    **TDD GREEN phase.** Apply the fix named in 72-DIAGNOSIS.md `## Fix Scope`.

    1. **Shared util OR inline (read diagnosis decision):**
       - If 72-DIAGNOSIS says "extract": create src/lib/forecast/extended-period.ts with a pure function `computeY1MonthKeys({ fiscalYear, activeYear, fiscalYearStart, isExtendedPeriod, planStartDate, year1DurationMonths }): string[]`. Add unit tests for the util in the same test file (or extract to src/__tests__/lib/forecast/extended-period.test.ts — Claude's discretion).
       - If 72-DIAGNOSIS says "inline": skip the new file entirely and embed the logic in Step3RevenueCOGS.tsx as a `useMemo` near the existing `generateMonthKeys` call.
       - **Remove the src/lib/forecast/extended-period.ts entry from files_modified frontmatter** (in this PLAN.md, post-decision) if the decision is "inline" — keep the actual file modifications honest.

    2. **Prop plumbing (ForecastWizardV4 → Step3RevenueCOGS):**
       - Source the extended-period signals from {business_profiles row | forecast config | API call} per 72-DIAGNOSIS finding
       - Pass to Step3RevenueCOGS as a single grouped prop or as individual props (match the test mocks from Task 1)
       - If the wizard already has business_profile in scope: thread it through. If not: extend the upstream data fetch minimally — name the route + line being modified

    3. **The math change in Step3RevenueCOGS:**
       - Around line ~311 (current site): replace `const monthKeys = generateMonthKeys(fiscalYear - 1 + (activeYear - 1));` with a branch:
         ```typescript
         const monthKeys = useMemo(() => {
           if (activeYear === 1 && extendedPeriod?.isExtendedPeriod && extendedPeriod.planStartDate && extendedPeriod.year1DurationMonths) {
             return computeY1MonthKeysExtended(extendedPeriod.planStartDate, extendedPeriod.year1DurationMonths);
           }
           return generateMonthKeys(fiscalYear - 1 + (activeYear - 1), DEFAULT_YEAR_START_MONTH);
         }, [activeYear, fiscalYear, extendedPeriod]);
         ```
       - **CRITICAL — actuals lock side:** the bug also affects line ~547 `remainingMonthsCount = 12 - completedMonthsCount`. For extended Y1:
         - "actuals" are months in monthKeys that ALSO appear in `currentYTD.revenue_by_month`
         - "editable" = monthKeys.length - count(actuals-in-monthKeys)
         - Update isActualMonth() and remainingMonthsCount derivation per 72-DIAGNOSIS

    4. **Run the regression tests:**
       `npx vitest run src/__tests__/forecast/phase-72-step3-extended-period.test.tsx`
       Expected: ALL 4 PASS.

    5. **Run scoped forecast suite to catch collateral regressions:**
       `npx vitest run src/__tests__/forecast/`
       Expected: zero new failures vs baseline. If any pre-existing failures appear that are unrelated to this fix (memory `feedback_executor_scoped_tests`: "ignore local-only timezone-shaped failures"), document them in the SUMMARY but proceed.

    6. **Typecheck + lint on touched files only:**
       - `npx tsc --noEmit` (full project — non-negotiable)
       - `npx eslint src/app/finances/forecast/components/wizard-v4/steps/Step3RevenueCOGS.tsx src/app/finances/forecast/components/wizard-v4/ForecastWizardV4.tsx src/lib/forecast/extended-period.ts 2>/dev/null || true`

    7. **GREEN commit:**
       `git commit -m "fix(72-02): forecast wizard Step 3 honors is_extended_period + plan_start_date (GREEN)"`

    8. **Optional REFACTOR (only if `<behavior>` is satisfied and tests still pass):**
       Clean up. Commit separately as `refactor(72-02): ...` if any cleanup was done.

    **Memory constraints (apply throughout):**
    - `feedback_testing`: trace root cause fully — the diagnosis was Task 72-01's job; do NOT re-investigate here, just implement
    - `feedback_executor_schema_deviations`: if 72-DIAGNOSIS named a schema column that does not actually exist in production, STOP and surface to Matt before forcing a workaround
    - `feedback_executor_scoped_tests`: run the scoped vitest, not the full suite
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/forecast/phase-72-step3-extended-period.test.tsx 2>&1 | tail -20 | grep -E "Tests.*passed|Test Files.*passed"</automated>
  </verify>
  <acceptance_criteria>
    - All 4 regression tests pass (`npx vitest run src/__tests__/forecast/phase-72-step3-extended-period.test.tsx` → 4 pass, 0 fail)
    - Scoped suite `npx vitest run src/__tests__/forecast/` shows zero NEW failures vs pre-fix baseline
    - `npx tsc --noEmit` clean (or only pre-existing errors unrelated to this fix — must enumerate in SUMMARY if any)
    - Step3RevenueCOGS.tsx consumes the extended-period signal (grep confirms `isExtendedPeriod` is read in the Y1 month-key derivation branch)
    - ForecastWizardV4.tsx plumbs the extended-period props (grep confirms)
    - GREEN commit landed with message containing "GREEN"
    - Standard (non-extended) wizard path: code-path branch preserves byte-identical `generateMonthKeys(fiscalYear - 1, ...)` call for activeYear=2/3 AND for activeYear=1 when is_extended_period=false
  </acceptance_criteria>
  <done>
    Bug fixed. Tests green. Matt can manually verify by opening Armstrong's forecast wizard Step 3 and counting 13 month columns. Step 4/5/6/7 same-family bugs remain documented in 72-DIAGNOSIS `## Related Bugs Found` for a future phase per CONTEXT scope lock.
  </done>
</task>

</tasks>

<verification>
- All 4 tests in src/__tests__/forecast/phase-72-step3-extended-period.test.tsx pass
- Scoped forecast vitest suite shows no new failures
- `npx tsc --noEmit` clean (or pre-existing-only)
- Manual smoke test (post-merge, Matt-driven): Armstrong wizard Step 3 shows 13 month columns; Fit2Shine wizard Step 3 shows 12 month columns
</verification>

<success_criteria>
Armstrong's Step 3 renders 13 months. Standard clients render 12. Phase 14 extended-period contract is now end-to-end consistent across goals wizard (B15/B16, Phase 68) and forecast wizard (this phase).
</success_criteria>

<output>
After completion, create `.planning/phases/72-forecast-wizard-step-3-extended-period-bug-y1-month-range-honors-plan-start-date-through-full-y1-duration/72-02-SUMMARY.md` recording:
- Fix shape applied (inline vs shared util — final decision)
- Test results (4/4 pass + scoped suite delta)
- Any deviations from 72-DIAGNOSIS (e.g. schema column name discovered during fix)
- Files touched + atomic commit hashes (RED → GREEN → optional REFACTOR)
- Step 4/5/6/7 follow-up note: link to 72-DIAGNOSIS `## Related Bugs Found` section so the next planner can pick up the audit
- Manual smoke test instructions for Matt (Armstrong + 1 standard client)
</output>
