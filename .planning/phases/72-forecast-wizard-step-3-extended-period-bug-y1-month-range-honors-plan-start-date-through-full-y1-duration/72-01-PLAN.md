---
phase: 72-forecast-wizard-step-3-extended-period-bug-y1-month-range-honors-plan-start-date-through-full-y1-duration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/72-forecast-wizard-step-3-extended-period-bug-y1-month-range-honors-plan-start-date-through-full-y1-duration/72-DIAGNOSIS.md
autonomous: true
requirements:
  - PHASE-72-DIAG
must_haves:
  truths:
    - "72-DIAGNOSIS.md exists at the correct phase path with a `## Root Cause` heading."
    - "Diagnosis names exactly ONE primary root cause (file path + line number) for Step 3 rendering ≠ Y1 duration."
    - "Diagnosis enumerates the props/data sources Step 3 currently reads (activeYear, fiscalYear, currentYTD.months_count, generateMonthKeys output) and which of `is_extended_period`, `plan_start_date`, `year1_end_date`, `year_1_duration_months` it does NOT read."
    - "Diagnosis specifies the exact fix shape 72-02 must implement (data plumbing changes + math changes), naming the function signatures to change."
    - "Diagnosis lists evidence of any same-family bug found in Step 4/5/6/7 as `## Related Bugs Found` (informational; 72-02 will NOT fix these)."
  artifacts:
    - path: ".planning/phases/72-forecast-wizard-step-3-extended-period-bug-y1-month-range-honors-plan-start-date-through-full-y1-duration/72-DIAGNOSIS.md"
      provides: "Named root cause + concrete fix scope for 72-02"
      contains: "## Root Cause"
      min_lines: 80
  key_links:
    - from: "72-DIAGNOSIS.md `## Fix Scope`"
      to: "72-02-PLAN.md Task 2 action"
      via: "Plan 72-02's first task reads 72-DIAGNOSIS.md and implements the named fix"
      pattern: "72-DIAGNOSIS"
---

<objective>
Investigate the forecast wizard Step 3 extended-period bug and produce a named, evidence-backed diagnosis with a concrete fix scope for 72-02. Read-only — no source code changes.

Purpose: Lock the root cause and fix shape BEFORE touching the wizard. The draft analysis at `docs/forecast-wizard-extended-period-bug.md` is a preliminary hypothesis; this plan promotes it to a phase-grade diagnosis with verified line numbers, exact prop wiring, and the precise math change 72-02 must apply. Honors the per-MEMORY rule "Go deep before deploying fixes — trace root cause fully, plan before coding."

Output: `72-DIAGNOSIS.md` in the phase directory with `## Root Cause`, `## Evidence`, `## Fix Scope`, `## Related Bugs Found` sections.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/72-forecast-wizard-step-3-extended-period-bug-y1-month-range-honors-plan-start-date-through-full-y1-duration/72-CONTEXT.md
@docs/forecast-wizard-extended-period-bug.md
@src/app/finances/forecast/components/wizard-v4/steps/Step3RevenueCOGS.tsx
@src/app/finances/forecast/components/wizard-v4/types.ts
@src/app/finances/forecast/components/wizard-v4/ForecastWizardV4.tsx
@src/app/goals/utils/quarters.ts
@src/app/finances/forecast/types.ts

<interfaces>
<!-- Pre-extracted contracts so the investigator does not need a discovery hunt. -->

From src/app/finances/forecast/components/wizard-v4/types.ts:
```typescript
// Deprecated but currently in use by Step3RevenueCOGS:
export function generateMonthKeys(fiscalYearStart: number, yearStartMonth: number = 7): string[];
// Always returns exactly 12 months. No extended-period awareness.
```

From src/app/finances/forecast/types.ts (around lines 143-144):
```typescript
forecast_start_month: string // e.g., "2025-11" (next month when rolling)
forecast_end_month: string   // e.g., "2026-06" (FY26 end)
```

From src/app/finances/forecast/components/wizard-v4/steps/Step3RevenueCOGS.tsx (current Y1 month-range computation):
```typescript
// Line ~311: generates 12 months for activeYear=1 based on fiscalYear only
const monthKeys = generateMonthKeys(fiscalYear - 1 + (activeYear - 1));

// Line ~545-547: actuals vs forecast split based on currentYTD only
const ytdActualTotal = currentYTD?.total_revenue || 0;
const completedMonthsCount = currentYTD?.months_count || 0;
const remainingMonthsCount = 12 - completedMonthsCount;
```

From src/app/goals/utils/quarters.ts (adjacent solution for goals wizard — Phase 68 B15):
```typescript
export function deriveCurrentRemainderColumn(
  today: Date,
  planYear: number,
  fiscalYearStart: number,
  thresholdMonths: number = 3,
  isExtendedPeriod: boolean = false,
  planStartDate: Date | null = null,
): QuarterInfo | null;
// Trims "Now" remainder to plan_start_date - 1 day when isExtendedPeriod=true.
```

Schema fields the wizard does NOT currently consult (per CONTEXT.md):
- business_profiles.is_extended_period
- business_profiles.plan_start_date
- business_profiles.year_1_duration_months (or equivalent)
- business_profiles.fiscal_year_start
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify line-numbered evidence and produce 72-DIAGNOSIS.md</name>
  <files>.planning/phases/72-forecast-wizard-step-3-extended-period-bug-y1-month-range-honors-plan-start-date-through-full-y1-duration/72-DIAGNOSIS.md</files>
  <read_first>
    - .planning/phases/72-forecast-wizard-step-3-extended-period-bug-y1-month-range-honors-plan-start-date-through-full-y1-duration/72-CONTEXT.md (FULL — locked decisions)
    - docs/forecast-wizard-extended-period-bug.md (preliminary hypothesis)
    - src/app/finances/forecast/components/wizard-v4/steps/Step3RevenueCOGS.tsx (entire file)
    - src/app/finances/forecast/components/wizard-v4/types.ts (focus on generateMonthKeys + ForecastState shape)
    - src/app/finances/forecast/components/wizard-v4/ForecastWizardV4.tsx (focus on props passed to Step 3 + currentYTD assembly + extended-period prop plumbing if any)
    - src/app/goals/utils/quarters.ts deriveCurrentRemainderColumn (Phase 68 B15 — adjacent solution to potentially reuse)
    - src/app/finances/forecast/types.ts (ForecastState/ForecastConfig shape — search for is_extended_period, plan_start_date, year1_end_date, year_1_duration_months, forecast_start_month, forecast_end_month)
    - src/types/database.ts business_profiles row type (grep for is_extended_period, plan_start_date, year_1_duration_months)
    - src/lib/utils/fiscal-year-utils.ts (the non-deprecated month-key generator referenced by the draft analysis)
  </read_first>
  <action>
    Read-only investigation. NO source code changes in this plan.

    **Step 1 — Confirm the Step 3 month-range computation site (the suspected bug location).**
    The draft analysis at docs/forecast-wizard-extended-period-bug.md names two suspect sites:
    (a) Step3RevenueCOGS.tsx:311 `const monthKeys = generateMonthKeys(fiscalYear - 1 + (activeYear - 1));`
    (b) Step3RevenueCOGS.tsx:547 `const remainingMonthsCount = 12 - completedMonthsCount;`
    Re-grep the live file to confirm exact line numbers (may have drifted) and quote the surrounding 5-10 lines for evidence.

    **Step 2 — Trace what Step 3 actually receives as props.**
    In ForecastWizardV4.tsx, find where Step3RevenueCOGS is rendered. Document:
    - What props are passed (fiscalYear, activeYear, currentYTD, priorYear, goals, etc.)
    - Whether any extended-period signal (is_extended_period, plan_start_date, year1_end_date, year_1_duration_months, forecast_start_month, forecast_end_month) is currently plumbed in
    - Where currentYTD is sourced from (likely an API call to /api/finances/forecast/current-pl or similar — name the route)

    **Step 3 — Check ForecastState / ForecastConfig for extended-period awareness.**
    Read src/app/finances/forecast/types.ts. Document:
    - Does the forecast config hold is_extended_period / plan_start_date / year1_end_date / year_1_duration_months? (the CONTEXT says forecast_start_month + forecast_end_month exist at lines 143-144)
    - Does any wizard-internal state carry the extended-period flag?
    - If the data IS available in state but not consumed by Step 3 — the fix is plumbing-only. If the data is NOT in state at all — the fix is plumbing + data-fetch.

    **Step 4 — Confirm the schema fields exist on business_profiles.**
    Grep src/types/database.ts (and src/lib/database.types.ts if it exists) for the four schema fields. Document presence/absence + column types. If a field is missing from the generated DB types but referenced elsewhere in the codebase, note the actual column name.

    **Step 5 — Verify the Phase 68 B15 adjacent solution.**
    Read deriveCurrentRemainderColumn in src/app/goals/utils/quarters.ts. Document:
    - Whether its math (trim to plan_start_date - 1 day) is directly reusable, or only conceptually similar
    - Whether extracting a shared util into src/lib/forecast/extended-period.ts makes sense (Claude's discretion per CONTEXT.md), with a YES/NO recommendation + 2-sentence justification

    **Step 6 — Probe Step 4/5/6/7 for same-family bugs (informational only).**
    Grep for `generateMonthKeys` and `currentYTD?.months_count` usage in:
    - src/app/finances/forecast/components/wizard-v4/steps/Step4*.tsx
    - src/app/finances/forecast/components/wizard-v4/steps/Step5*.tsx
    - src/app/finances/forecast/components/wizard-v4/steps/Step6*.tsx
    - src/app/finances/forecast/components/wizard-v4/steps/Step7*.tsx
    For each match, briefly note (file:line) whether the same hardcoded-12-month assumption is present. DO NOT fix; just enumerate. Per CONTEXT.md: "If 72-01 surfaces evidence the same bug affects Steps 4-7, document but DO NOT expand scope — note for follow-up phase."

    **Step 7 — Write 72-DIAGNOSIS.md.**
    File path: `.planning/phases/72-forecast-wizard-step-3-extended-period-bug-y1-month-range-honors-plan-start-date-through-full-y1-duration/72-DIAGNOSIS.md`

    Required structure:
    ```markdown
    # Phase 72 — Diagnosis

    **Phase:** 72 (forecast wizard Step 3 extended-period bug)
    **Diagnosed:** 2026-05-31
    **Investigator:** Claude (Plan 72-01)
    **Symptom:** Armstrong's forecast wizard Step 3 shows only 3 editable months instead of 13 (Jun 2026 - Jun 2027)
    **Armstrong config:** plan_start_date=2026-06-01, is_extended_period=true, Y1=13 months

    ## Root Cause

    {ONE primary root cause, named precisely. Format:
    `Step3RevenueCOGS.tsx:{line} — {what the code does} — {why it fails for extended-period plans}`}

    {2-4 sentence explanation of why the conflation of "current FY YTD actuals" with "plan Y1 actuals" produces the 3-month symptom for Armstrong.}

    ## Evidence

    ### What Step 3 currently reads
    - {line N}: `{exact code line}` — generates monthKeys for `fiscalYear - 1 + (activeYear - 1)` (always 12 months from FY start)
    - {line N}: `{exact code line}` — locks the first `currentYTD.months_count` months as actuals
    - {line N}: `{exact code line}` — derives remainingMonthsCount as `12 - completedMonthsCount`

    ### What Step 3 does NOT read
    - `business_profiles.is_extended_period` — {confirmed: not consumed by Step 3 or any ancestor prop}
    - `business_profiles.plan_start_date` — {…}
    - `business_profiles.year_1_duration_months` — {…or note actual column name found}
    - `business_profiles.fiscal_year_start` — {note: DEFAULT_YEAR_START_MONTH constant currently hardcoded — confirm or refute}
    - `forecast_start_month` / `forecast_end_month` — {present in state per types.ts:143-144 but not consulted by Step 3 month-range computation — confirm}

    ### Where the extended-period signal IS available (but not used)
    - business_profiles row: {confirmed/refuted columns + schema source}
    - ForecastState/ForecastConfig: {confirmed/refuted fields + line numbers}
    - ForecastWizardV4 prop flow: {trace from page → wizard → Step 3 — what's missing}

    ### Walked-through Armstrong case
    Given: today=2026-05-31, fiscalYear=2026 (current FY end June 2026), Y1 plan=Jun-2026 → Jun-2027 (13 months), is_extended_period=true.
    Step 3 with `activeYear=1` computes:
    - monthKeys = generateMonthKeys(2025) = [2025-07 ... 2026-06] (12 months, wrong — should be [2026-06 ... 2027-06] = 13 months)
    - currentYTD.months_count ≈ 10 (Jul 2025 - Apr 2026)
    - remainingMonthsCount = 12 - 10 = 2 editable months (May/Jun 2026)
    - Visible symptom: ~2-3 editable cells instead of 13

    ## Fix Scope (for 72-02)

    ### Data plumbing changes
    {Specific list. Examples:
    - Add `extendedPeriod: { isExtendedPeriod: boolean; planStartDate: string | null; year1EndDate: string | null; year1DurationMonths: number | null }` prop to Step3RevenueCOGS
    - Source from {business_profiles row | forecast config | API X} — name the upstream source
    - Thread through ForecastWizardV4 from {line N where page passes business_profile} to {line N where Step 3 is rendered}}

    ### Math changes
    {Specific list. Examples:
    - Replace line 311 `generateMonthKeys(fiscalYear - 1 + (activeYear - 1))` with a new helper `computeY1MonthKeys({ fiscalYear, activeYear, isExtendedPeriod, planStartDate, year1EndDate, fiscalYearStart })` that:
      - Standard path (activeYear !== 1 OR !isExtendedPeriod): returns existing 12-month sequence (UNCHANGED behavior for non-extended clients)
      - Extended path (activeYear === 1 AND isExtendedPeriod): returns monthKeys from planStartDate through year1EndDate inclusive
    - Replace line 547 `12 - completedMonthsCount` with `monthKeys.length - <count of monthKeys that fall in currentYTD>` (note: actuals lock should ONLY apply to months within the Y1 range that have already passed)}

    ### Shared util recommendation
    {YES extract to src/lib/forecast/extended-period.ts | NO inline in Step3RevenueCOGS}: {2-sentence justification}

    ### File touchset for 72-02
    - src/app/finances/forecast/components/wizard-v4/steps/Step3RevenueCOGS.tsx (modify)
    - src/app/finances/forecast/components/wizard-v4/ForecastWizardV4.tsx (modify — prop plumbing)
    - {src/lib/forecast/extended-period.ts (create) | OR no new file if inline}
    - src/__tests__/forecast/phase-72-step3-extended-period.test.tsx (create — 4 regression tests per CONTEXT)

    ## Related Bugs Found

    {Enumerate any Step 4/5/6/7 sites with the same generateMonthKeys/currentYTD pattern. Format:
    - Step4Team.tsx:N — {what it does} — same family, defer to follow-up
    - Step5OpEx.tsx:N — {…} — same family, defer
    - {OR "None found — Step 4-7 use different month-range logic"} }

    Per CONTEXT.md, these are documented for a follow-up phase and explicitly OUT OF SCOPE for 72-02.

    ## Open Questions for Matt (if any)

    {List anything that genuinely needs operator input. Examples:
    - Confirm year_1_duration_months column name (database.ts shows X, code references Y)
    - {OR "None — diagnosis is unambiguous and 72-02 can proceed as scripted"} }
    ```

    **Step 8 — Self-check before completion.**
    - [ ] All line numbers in the diagnosis match the live source (re-grep after writing)
    - [ ] `## Root Cause` heading present
    - [ ] `## Fix Scope` names specific function signatures + file touchset
    - [ ] `## Related Bugs Found` section is present (even if empty)
    - [ ] No source code changes were made (run `git status` — only .planning/ file should appear)

    Commit per gsd-tools convention (one atomic commit for the diagnosis doc).
  </action>
  <verify>
    <automated>test -f .planning/phases/72-forecast-wizard-step-3-extended-period-bug-y1-month-range-honors-plan-start-date-through-full-y1-duration/72-DIAGNOSIS.md && grep -q "^## Root Cause" .planning/phases/72-forecast-wizard-step-3-extended-period-bug-y1-month-range-honors-plan-start-date-through-full-y1-duration/72-DIAGNOSIS.md && grep -q "^## Fix Scope" .planning/phases/72-forecast-wizard-step-3-extended-period-bug-y1-month-range-honors-plan-start-date-through-full-y1-duration/72-DIAGNOSIS.md && grep -q "^## Evidence" .planning/phases/72-forecast-wizard-step-3-extended-period-bug-y1-month-range-honors-plan-start-date-through-full-y1-duration/72-DIAGNOSIS.md && grep -q "^## Related Bugs Found" .planning/phases/72-forecast-wizard-step-3-extended-period-bug-y1-month-range-honors-plan-start-date-through-full-y1-duration/72-DIAGNOSIS.md && [ "$(git status --porcelain src/ | wc -l)" -eq 0 ]</automated>
  </verify>
  <acceptance_criteria>
    - 72-DIAGNOSIS.md exists at the locked path
    - File contains `## Root Cause`, `## Evidence`, `## Fix Scope`, `## Related Bugs Found` headings
    - Root Cause names ONE primary site with file:line precision
    - Fix Scope specifies function signatures + file touchset for 72-02
    - Zero source code modifications (read-only investigation per autonomous=true contract)
    - All line numbers in diagnosis match live source code (no drift)
  </acceptance_criteria>
  <done>
    Diagnosis written and committed. Plan 72-02 has a named root cause + concrete fix shape to implement against.
  </done>
</task>

</tasks>

<verification>
- 72-DIAGNOSIS.md exists at the phase path
- All four required headings present (`## Root Cause`, `## Evidence`, `## Fix Scope`, `## Related Bugs Found`)
- Root cause names file:line for the primary bug site
- Fix scope is concrete enough for 72-02 to execute without re-investigating
- No source code changes (this plan is read-only)
- `git status` shows only the new .planning/ file
</verification>

<success_criteria>
72-02 can proceed without re-reading Step3RevenueCOGS.tsx or re-tracing the wizard — the diagnosis is the contract.
</success_criteria>

<output>
After completion, create `.planning/phases/72-forecast-wizard-step-3-extended-period-bug-y1-month-range-honors-plan-start-date-through-full-y1-duration/72-01-SUMMARY.md` recording: investigation steps taken, root cause found, fix scope decided, any deviations from the planned investigation protocol, any open questions surfaced for Matt.
</output>
