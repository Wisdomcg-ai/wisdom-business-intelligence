---
phase: 73-annual-plan-reset
plan: 03
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/app/quarterly-review/utils/annual-reset-entry.ts
  - src/__tests__/quarterly-review/annual-reset-entry.test.ts
  - src/app/quarterly-review/page.tsx
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Entry detection is a pure function of (planningQuarter start date, year1_end_date, hasPlanDates) → one of three states: initial-setup | needs-reset | normal-review"
    - "A client whose planningQuarter start is AFTER year1_end_date is routed to RESET; within Year1 → normal review; no plan dates → initial setup"
    - "Armstrong & Fit2Shine (year1_end_date = 2027-06-29) are classified normal-review for the current planning quarter and NEVER shown the reset prompt"
    - "Detection is read-only and side-effect-free — it issues no writes; the landing only routes when the user clicks"
    - "The prompt is visible to both client and coach (no role gate on the detection)"
  artifacts:
    - path: "src/app/quarterly-review/utils/annual-reset-entry.ts"
      provides: "detectAnnualResetState pure decision function"
      min_lines: 40
      exports: ["detectAnnualResetState", "AnnualResetState"]
    - path: "src/app/quarterly-review/page.tsx"
      provides: "read-only landing prompt that branches CTA on detected state"
      contains: "detectAnnualResetState"
  key_links:
    - from: "src/app/quarterly-review/page.tsx"
      to: "src/app/quarterly-review/utils/annual-reset-entry.ts"
      via: "import + call with year1_end_date and planningQuarter start"
      pattern: "detectAnnualResetState"
    - from: "src/app/quarterly-review/page.tsx"
      to: "/goals (rollover mode)"
      via: "reset CTA routes to /goals?reset=annual"
      pattern: "/goals\\?reset"
---

<objective>
Replace the calendar-gated "isQ4 → Start Annual Review" entry with a DATA-DRIVEN one. A pure
function reads the client's `year1_end_date` and the start date of the quarter being planned and
returns exactly one of three states. The quarterly-review landing renders the right CTA per state.
It is strictly read-only — it never mutates anything; a reset only begins when the client clicks
through to `/goals?reset=annual` (Plan 04 handles the wizard side).

Purpose: This is the zero-impact gate that guarantees already-planned clients (Armstrong, Fit2Shine)
are never prompted or touched, while finished-year clients get routed to the reset.

Output: `detectAnnualResetState` (pure, tested for all 3 states incl. Armstrong/Fit2Shine) wired
into the landing page CTA. The legacy isQ4 annual button is removed in Plan 05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/codebase/ANNUAL-RESET-DESIGN.md
@.planning/phases/73-annual-plan-reset-full-year-reset-reusing-the-goals-wizard-data-driven-entry-off-year1-end-date-snapshot-before-overwrite-zero-impact-to-clients-with-a-current-plan/73-CONTEXT.md

<interfaces>
<!-- From the codebase. The landing already computes planningQuarter + resolves goals row. -->

EXISTING landing logic (src/app/quarterly-review/page.tsx):
```typescript
import { getPlanningQuarter, getNextQuarterOf, getPreviousQuarterOf, getQuarterLabel,
         isLastQuarterOfYear, type YearType, type QuarterNumber } from './types';
// quarter/year = the quarter being PLANNED:
const [quarter] = useState(() => getPlanningQuarter().quarter);  // resolved with yearType after goals load
const [year]    = useState(() => getPlanningQuarter().year);
const isQ4 = isLastQuarterOfYear(quarter);   // <-- legacy gate to be REPLACED by detection
// It already loads year_type from business_financial_goals via business_profiles.id:
//   profile.id → business_financial_goals.select('year_type').eq('business_id', goalsBusinessId)
// EXTEND that same query to also select 'year1_end_date'.
const startNewReview = (type: 'quarterly' | 'annual' = 'quarterly') => { ... router.push(.../workshop?...) }
```

PLANNING-QUARTER START DATE. The quarter being planned has a calendar start. Compute it from
(quarter, year, yearType) using the existing fiscal quarter math:
```typescript
// src/app/goals/utils/quarters.ts → calculateQuarters(yearType, planYear) returns QuarterInfo[]
//   each has { id:'q1'|..., startDate: Date, endDate: Date }
// For FY, planYear is the FY the quarter belongs to (e.g. Q1 FY27 startDate = 2026-07-01).
```
Use calculateQuarters(yearType, year).find(q => q.id === `q${quarter}`).startDate as the planningQuarter start.

DATA SIGNAL (verified prod):
  - 10 FY26 clients: year1_end_date = 2026-06-30 → needs FY27 reset (planningQuarter Q1 FY27 start 2026-07-01 > 2026-06-30).
  - Armstrong, Fit2Shine: year1_end_date = 2027-06-29 → already FY27. For the current planning quarter
    (≤ Q1 FY27, start 2026-07-01), 2026-07-01 < 2027-06-29 ⇒ within Year1 ⇒ normal-review. MUST NOT reset.
  - Oh Nine (CY): year1_end_date = 2026-12-31.
  - JVJ: no plan dates → initial-setup.

DECISION RULE (locked):
  if !hasPlanDates (year1_end_date null/absent) → 'initial-setup'
  else if planningQuarterStart > year1EndDate     → 'needs-reset'
  else                                            → 'normal-review'
Comparison is date-only (strip time). > is strict: start exactly on year1End counts as within Year1.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure detectAnnualResetState + tests for all 3 states (RED→GREEN)</name>
  <read_first>
    - src/app/quarterly-review/types/index.ts (getPlanningQuarter, QuarterNumber, YearType)
    - src/app/goals/utils/quarters.ts (calculateQuarters startDate per quarter id)
    - src/app/quarterly-review/__tests__/quarter-helpers.test.ts (test style for this module)
    - src/app/quarterly-review/utils/annual-reset-entry.ts (file being created)
  </read_first>
  <behavior>
    - detectAnnualResetState({ planningQuarterStart: Date, year1EndDate: Date | null }): 'initial-setup' | 'needs-reset' | 'normal-review'
      - year1EndDate null → 'initial-setup'
      - planningQuarterStart (date-only) > year1EndDate (date-only) → 'needs-reset'
      - otherwise → 'normal-review'
    - Cases tested:
      - FY26 client: start=2026-07-01, year1End=2026-06-30 → 'needs-reset'
      - Armstrong/Fit2Shine: start=2026-07-01, year1End=2027-06-29 → 'normal-review'  (CRITICAL: never reset)
      - Oh Nine CY: start=2027-01-01, year1End=2026-12-31 → 'needs-reset'; start=2026-10-01, year1End=2026-12-31 → 'normal-review'
      - JVJ: year1End=null → 'initial-setup'
      - boundary: start === year1End → 'normal-review' (strict >)
  </behavior>
  <action>
    Create src/app/quarterly-review/utils/annual-reset-entry.ts exporting the `AnnualResetState`
    union type and the pure `detectAnnualResetState`. Create
    src/__tests__/quarterly-review/annual-reset-entry.test.ts covering every case above with
    explicit Armstrong/Fit2Shine assertions. Commits: `test(73-03): entry detection 3 states + already-planned guard (RED)`
    then `feat(73-03): detectAnnualResetState (GREEN)`.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/quarterly-review/annual-reset-entry.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "export function detectAnnualResetState" src/app/quarterly-review/utils/annual-reset-entry.ts`
    - Test asserts Armstrong/Fit2Shine (year1End 2027-06-29, start 2026-07-01) → 'normal-review'
    - Test asserts a FY26 client (year1End 2026-06-30) → 'needs-reset' and JVJ (null) → 'initial-setup'
    - `npx vitest run src/__tests__/quarterly-review/annual-reset-entry.test.ts` passes
  </acceptance_criteria>
  <done>Detection is pure, side-effect-free, and proven for all three states incl. the already-planned guard.</done>
</task>

<task type="auto">
  <name>Task 2: Wire read-only detection into the quarterly-review landing CTA</name>
  <read_first>
    - src/app/quarterly-review/page.tsx (the goals-load effect at ~L66-86 and the CTA block at ~L257-288)
    - src/app/goals/utils/quarters.ts (calculateQuarters to derive planningQuarter startDate)
    - src/app/quarterly-review/utils/annual-reset-entry.ts (Task 1)
    - src/app/quarterly-review/types/index.ts (getQuarterLabel for FY label)
  </read_first>
  <action>
    Edit src/app/quarterly-review/page.tsx:
    1. Extend the existing goals query (the `.select('year_type')` at ~L74-78) to
       `.select('year_type, year1_end_date')`; store `year1_end_date` in new state `year1EndDate: string | null`.
    2. Compute `planningQuarterStart` from `calculateQuarters(yearType, year).find(q => q.id === 'q'+quarter)?.startDate`.
    3. Compute `resetState = detectAnnualResetState({ planningQuarterStart, year1EndDate: year1EndDate ? new Date(year1EndDate) : null })`.
       Default to 'normal-review' while loading (so nothing flashes the reset prompt before data resolves).
    4. In the CTA region (where the green "Start {Q} Review" button + isQ4 annual button live):
       - resetState === 'needs-reset' AND no in-progress review for this quarter:
         render a primary CTA `Set your {FY label} Annual Plan` that routes to
         `getPath('/goals?reset=annual')` (read-only until click). Keep the normal "Start Review" as a secondary link.
       - resetState === 'initial-setup': primary CTA `Set up your Annual Plan` → `getPath('/goals')`.
       - resetState === 'normal-review': unchanged normal "Start {Q} Review" CTA + optional secondary
         "Adjust annual plan" link → `getPath('/goals')`.
    5. Do NOT remove the isQ4 annual button yet (Plan 05 removes it after the reset path is verified end-to-end);
       but gate it so it does NOT show when resetState==='needs-reset' (avoid two competing annual CTAs).
    No data is mutated anywhere in this component.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v "node_modules" | grep -E "quarterly-review/page" ; echo "tsc-checked"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "detectAnnualResetState" src/app/quarterly-review/page.tsx`
    - `grep -q "year1_end_date" src/app/quarterly-review/page.tsx` (query extended)
    - `grep -q "/goals?reset=annual" src/app/quarterly-review/page.tsx` (reset CTA target)
    - No `.update(`/`.insert(`/`.upsert(`/`.delete(` added in page.tsx (read-only): `! grep -E "\.(update|insert|upsert|delete)\(" src/app/quarterly-review/page.tsx`
    - `npx tsc --noEmit` reports no new errors in page.tsx
    - `npx vitest run src/__tests__/quarterly-review/annual-reset-entry.test.ts` still green
  </acceptance_criteria>
  <done>Landing shows the correct CTA per detected state; reset only routes on click; no writes occur.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/__tests__/quarterly-review/annual-reset-entry.test.ts` passes (3 states incl. Armstrong/Fit2Shine).
- Landing renders reset CTA only for needs-reset; initial CTA for no-plan; normal CTA otherwise — all read-only.
- `npx tsc --noEmit` clean.
</verification>

<success_criteria>
A finished-year client sees "Set your FY27 Annual Plan"; an already-planned client (Armstrong/Fit2Shine)
sees the normal quarterly CTA and is never offered a reset; a no-plan client sees initial setup. Nothing
is written until they click into the wizard.
</success_criteria>

<output>
After completion, create `.planning/phases/73-annual-plan-reset-full-year-reset-reusing-the-goals-wizard-data-driven-entry-off-year1-end-date-snapshot-before-overwrite-zero-impact-to-clients-with-a-current-plan/73-03-SUMMARY.md`
</output>
