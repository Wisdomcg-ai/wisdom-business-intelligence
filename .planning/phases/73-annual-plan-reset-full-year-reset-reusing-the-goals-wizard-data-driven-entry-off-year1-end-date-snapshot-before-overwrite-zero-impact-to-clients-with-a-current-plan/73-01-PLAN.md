---
phase: 73-annual-plan-reset
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/app/goals/services/annual-reset-snapshot-service.ts
  - src/__tests__/goals/annual-reset-snapshot.test.ts
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Before any reset overwrite, the full ending-year plan (3-year ladder + quarterly_targets + KPIs + initiatives) is captured into plan_snapshots"
    - "The captured snapshot is restorable: `restoreAnnualResetSnapshot` writes the captured ladder/quarterly_targets/year_type back to business_financial_goals (load-bearing financial reversibility); KPI and initiative restore is documented as a follow-up in the service header"
    - "Capturing a snapshot never mutates business_financial_goals, business_kpis, or strategic_initiatives (read + insert into plan_snapshots only)"
  artifacts:
    - path: "src/app/goals/services/annual-reset-snapshot-service.ts"
      provides: "captureAnnualResetSnapshot + restoreAnnualResetSnapshot"
      min_lines: 80
      exports: ["AnnualResetSnapshotService", "annualResetSnapshotService"]
    - path: "src/__tests__/goals/annual-reset-snapshot.test.ts"
      provides: "round-trip capture→restore test + read-only assertion"
      min_lines: 60
  key_links:
    - from: "src/app/goals/services/annual-reset-snapshot-service.ts"
      to: "plan_snapshots"
      via: "supabase.from('plan_snapshots').insert"
      pattern: "from\\(['\"]plan_snapshots['\"]\\)"
    - from: "src/app/goals/services/annual-reset-snapshot-service.ts"
      to: "business_financial_goals"
      via: "restore writes ladder back via update"
      pattern: "from\\(['\"]business_financial_goals['\"]\\)"
---

<objective>
Build the reversibility foundation for the annual reset: a service that captures the
COMPLETE ending-year plan into `plan_snapshots` BEFORE any overwrite, plus a documented
restore path that writes it back. This is the zero-impact safety gate — every later plan that
mutates `business_financial_goals` will call `captureAnnualResetSnapshot` first.

Purpose: Matt's #1 constraint is "snapshot before any overwrite; fully reversible." This plan
delivers that capability and its proof (round-trip test) in isolation, before any mutation code
exists.

Output: `AnnualResetSnapshotService` with `captureAnnualResetSnapshot(...)` and
`restoreAnnualResetSnapshot(...)`, plus a passing capture→restore round-trip test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/codebase/ANNUAL-RESET-DESIGN.md
@.planning/phases/73-annual-plan-reset-full-year-reset-reusing-the-goals-wizard-data-driven-entry-off-year1-end-date-snapshot-before-overwrite-zero-impact-to-clients-with-a-current-plan/73-CONTEXT.md

<interfaces>
<!-- Extracted from codebase. Executor should use these directly — no exploration needed. -->

EXISTING SNAPSHOT INFRA (reuse the table + insert pattern, NOT the OnePagePlanData shape):
From src/app/one-page-plan/services/plan-snapshot-service.ts:
```typescript
// plan_snapshots row written via:
supabase.from('plan_snapshots').insert({
  business_id, user_id, version_number,  // next version = (max for business)+1
  snapshot_type,                          // CHECK-constrained — see below
  quarter, year, quarterly_review_id, plan_data, label,
})
// Next version: select version_number from plan_snapshots where business_id=... order desc limit 1
```

plan_snapshots SCHEMA CONSTRAINT (baseline_schema.sql:3840) — DO NOT add a new snapshot_type:
```sql
CONSTRAINT plan_snapshots_snapshot_type_check CHECK (snapshot_type = ANY (ARRAY[
  'goals_wizard_complete','quarterly_review_pre_sync','quarterly_review_post_sync']))
-- UNIQUE (business_id, version_number)
-- plan_data is jsonb (free-form) — store the full restorable plan here.
```
DECISION (locked): use snapshot_type='quarterly_review_pre_sync' (an ALLOWED value) and put the
reset tag in `label` = `annual_reset_FY<endingFY>` (e.g. "annual_reset_FY2026"). Storing a free-form
restorable blob in `plan_data` jsonb. This avoids a CHECK migration (zero schema risk).

business_financial_goals KEY + COLUMNS (baseline_schema.sql:1720+, plan_period_columns.sql).
KEYED ON business_profiles.id via UNIQUE(business_id). Columns to capture AND restore:
```
revenue_current/_year1/_year2/_year3
gross_profit_current/_year1/_year2/_year3
gross_margin_current/_year1/_year2/_year3
net_profit_current/_year1/_year2/_year3
net_margin_current/_year1/_year2/_year3
customers_current/_year1/_year2/_year3
employees_current/_year1/_year2/_year3
leads_per_month_*  conversion_rate_*  avg_transaction_value_*  team_headcount_*  owner_hours_per_week_*   (each _current/_year1/_year2/_year3)
quarterly_targets (jsonb)   year_type ('FY'|'CY')
plan_start_date  plan_end_date  year1_end_date  (date, YYYY-MM-DD)
is_extended_period  year1_months  current_year_remaining_months
```

business_kpis KEYED ON businesses.id (text). Capture: id, kpi_id, name, year1_target, year2_target, year3_target, current_value, is_active.
strategic_initiatives KEYED ON businesses.id (uuid). Capture: id, title, step_type, status, fiscal_year, selected, quarter_assigned, category, order_index.

Supabase client: import { createClient } from '@/lib/supabase/client'  (same as snapshot-service.ts)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write capture→restore round-trip + read-only tests (RED)</name>
  <read_first>
    - src/app/one-page-plan/services/plan-snapshot-service.ts (version-number + insert pattern)
    - src/app/goals/services/snapshot-service.ts (mock-supabase test style — chained builder)
    - src/__tests__/goals/plan-period-persistence.test.ts (existing goals service test harness + supabase mock shape)
  </read_first>
  <behavior>
    - captureAnnualResetSnapshot({ businessId(profileId), businessesId, userId, endingFY }):
      reads business_financial_goals (by profileId), business_kpis + strategic_initiatives (by businessesId),
      inserts ONE plan_snapshots row with snapshot_type='quarterly_review_pre_sync',
      label='annual_reset_FY<endingFY>', year=endingFY, and plan_data = { goals:{...full ladder + quarterly_targets + year_type + plan dates}, kpis:[...], initiatives:[...] }.
      Returns { success:true, snapshotId, versionNumber }.
    - version_number = (max existing for business)+1; first snapshot = 1.
    - restoreAnnualResetSnapshot({ businessId(profileId), snapshotId }): loads the snapshot,
      writes plan_data.goals back to business_financial_goals via .update().eq('business_id', profileId).
      Returns { success:true }.
    - Round-trip: capture a known ladder → mutate the goals row → restore → goals row equals the captured ladder.
    - READ-ONLY assertion: during capture, NO .update/.upsert/.delete is issued against
      business_financial_goals / business_kpis / strategic_initiatives (only .select on those + .insert on plan_snapshots).
  </behavior>
  <action>
    Create src/__tests__/goals/annual-reset-snapshot.test.ts using the existing vitest + mocked
    `@/lib/supabase/client` pattern from plan-period-persistence.test.ts. Spy on the from() builder
    so the test can assert which tables received insert/update vs select. Tests fail because the
    service does not exist yet (RED). Commit: `test(73-01): failing capture/restore round-trip + read-only guard`.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/goals/annual-reset-snapshot.test.ts 2>&1 | grep -qE "fail|FAIL|Cannot find module|annual-reset-snapshot-service"</automated>
  </verify>
  <done>Test file exists; suite RED because the service module is absent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement AnnualResetSnapshotService (GREEN)</name>
  <read_first>
    - src/app/goals/services/annual-reset-snapshot-service.ts (the file being created — re-read after write if iterating)
    - src/app/one-page-plan/services/plan-snapshot-service.ts (version-number query + insert)
    - src/app/goals/services/financial-service.ts (exact business_financial_goals column list to capture/restore)
  </read_first>
  <action>
    Create src/app/goals/services/annual-reset-snapshot-service.ts:
    - `export class AnnualResetSnapshotService` with `createClient()` (from '@/lib/supabase/client').
    - `async captureAnnualResetSnapshot({ businessId, businessesId, userId, endingFY }: { businessId: string; businessesId: string; userId: string; endingFY: number })`:
        1. `select('*')` from business_financial_goals `.eq('business_id', businessId).maybeSingle()`.
        2. `select(...)` from business_kpis `.eq('business_id', businessesId).eq('is_active', true)`.
        3. `select(...)` from strategic_initiatives `.eq('business_id', businessesId)`.
        4. Compute nextVersion = (max version_number for businessId in plan_snapshots)+1 (reuse the query shape from plan-snapshot-service.ts).
        5. `insert` ONE plan_snapshots row: business_id=businessId, user_id=userId, version_number=nextVersion,
           snapshot_type='quarterly_review_pre_sync', year=endingFY, label=`annual_reset_FY${endingFY}`,
           plan_data = { kind:'annual_reset', endingFY, goals: <full goals row>, kpis: <rows>, initiatives: <rows> }.
        6. Return { success:true, snapshotId, versionNumber: nextVersion }. On any error return { success:false, error }.
       NEVER call update/upsert/delete on goals/kpis/initiatives in this method.
    - `async restoreAnnualResetSnapshot({ businessId, snapshotId }: { businessId: string; snapshotId: string })`:
        1. `select('plan_data')` from plan_snapshots `.eq('id', snapshotId).single()`.
        2. Extract `plan_data.goals`, strip `id`/`created_at`/`updated_at`, set `updated_at = new Date().toISOString()`.
        3. `update(goalsPayload).eq('business_id', businessId)` on business_financial_goals.
        4. Return { success:true } / { success:false, error }.
       (KPI + initiative restore are documented as a follow-up in the file header JSDoc — goals ladder
        restore is the load-bearing reversibility path for the financial plan; note this explicitly.)
    - `export const annualResetSnapshotService = new AnnualResetSnapshotService()`.
    Commit: `feat(73-01): annual-reset snapshot capture + goals restore`.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/goals/annual-reset-snapshot.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "annualResetSnapshotService" src/app/goals/services/annual-reset-snapshot-service.ts`
    - `grep -q "from('plan_snapshots')" src/app/goals/services/annual-reset-snapshot-service.ts`
    - `grep -q "annual_reset_FY" src/app/goals/services/annual-reset-snapshot-service.ts`
    - capture method contains NO `.update(` / `.upsert(` / `.delete(` against business_financial_goals/business_kpis/strategic_initiatives (grep the method body)
    - `npx vitest run src/__tests__/goals/annual-reset-snapshot.test.ts` passes (round-trip + read-only guard green)
    - `npx tsc --noEmit` clean for the new file
  </acceptance_criteria>
  <done>Round-trip test green; capture is read-only on plan tables; restore rewrites the goals ladder.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/__tests__/goals/annual-reset-snapshot.test.ts` passes.
- Capture writes exactly one plan_snapshots row (snapshot_type allowed value + annual_reset_FY label) and touches no plan table with a write.
- Restore round-trips the captured 3-year ladder + quarterly_targets + year_type back into business_financial_goals.
</verification>

<success_criteria>
The reset is provably reversible: a captured FY26 plan can be restored byte-for-byte into the
goals ladder. No mutation of client plan tables occurs during capture.
</success_criteria>

<output>
After completion, create `.planning/phases/73-annual-plan-reset-full-year-reset-reusing-the-goals-wizard-data-driven-entry-off-year1-end-date-snapshot-before-overwrite-zero-impact-to-clients-with-a-current-plan/73-01-SUMMARY.md`
</output>
