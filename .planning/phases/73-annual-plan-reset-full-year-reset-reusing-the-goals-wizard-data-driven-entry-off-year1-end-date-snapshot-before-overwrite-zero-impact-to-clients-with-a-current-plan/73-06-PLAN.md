---
phase: 73-annual-plan-reset
plan: 06
type: tdd
wave: 5
depends_on: [73-01, 73-02, 73-03, 73-04, 73-05]
files_modified:
  - src/__tests__/goals/annual-reset-integration.test.ts
autonomous: true
user_setup: []

must_haves:
  truths:
    - "An integration test proves the full flow: needs-reset client → snapshot captured → ladder/dates rolled → snapshot restores the pre-reset plan exactly"
    - "Entry detection is verified for all three states INCLUDING already-planned (Armstrong/Fit2Shine year1_end_date=2027-06-29 classified normal-review, never reset)"
    - "Rollover math is verified at the FY boundary AND the CY boundary"
    - "The full vitest suite passes (no regressions introduced by Phase 73)"
  artifacts:
    - path: "src/__tests__/goals/annual-reset-integration.test.ts"
      provides: "snapshot→rollover→restore round-trip + zero-impact-on-already-planned assertions"
      min_lines: 80
  key_links:
    - from: "src/__tests__/goals/annual-reset-integration.test.ts"
      to: "annualResetService.executeAnnualReset + annualResetSnapshotService + detectAnnualResetState"
      via: "exercises the three units together against a mocked supabase"
      pattern: "executeAnnualReset"
---

<objective>
Lock the phase's safety guarantees in CI with one cross-cutting integration test that wires the
three real units together (detection → snapshot → rollover → restore) against a mocked Supabase,
then prove the whole repo suite is green. This is the durable regression net for Matt's zero-impact
constraint.

Purpose: Unit tests (01-03) prove each piece; this proves they compose correctly and that the
already-planned clients are untouched end-to-end. Plus the full-suite gate catches any collateral.

Output: `annual-reset-integration.test.ts` + a clean `npx vitest run` (whole repo).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/codebase/ANNUAL-RESET-DESIGN.md
@src/app/goals/services/annual-reset-service.ts
@src/app/goals/services/annual-reset-snapshot-service.ts
@src/app/quarterly-review/utils/annual-reset-entry.ts

<interfaces>
<!-- The three units under integration. Use a single mocked supabase that simulates the goals row,
     plan_snapshots, kpis, and initiatives so capture→roll→restore can be observed on the same store. -->

detectAnnualResetState({ planningQuarterStart, year1EndDate }) → 'initial-setup'|'needs-reset'|'normal-review'
annualResetSnapshotService.captureAnnualResetSnapshot({ businessId, businessesId, userId, endingFY }) → { success, snapshotId, versionNumber }
annualResetSnapshotService.restoreAnnualResetSnapshot({ businessId, snapshotId }) → { success }
annualResetService.executeAnnualReset({ businessId, businessesId, userId, priorRow?|read-internally, yearStartMonth }) → { success, snapshotId, newFY, carriedForwardCount }

Mock store shape: an in-memory map keyed by table → rows, with a from()-builder mock supporting
select/.eq/.maybeSingle/.single/.insert/.update/.order/.limit, mutating the in-memory store so a
capture-then-restore actually round-trips. Model after the supabase mock in
src/__tests__/goals/plan-period-persistence.test.ts (reuse its builder if exported, else replicate).

Boundary fixtures:
  FY26 client: year_type 'FY', year1_end_date '2026-06-30', revenue_year1=1_000_000, revenue_year2=1_300_000, revenue_year3=1_700_000.
  After roll: revenue_current=1_000_000, revenue_year1=1_300_000, revenue_year2=1_700_000, revenue_year3=1_700_000,
              plan_start_date '2026-07-01', year1_end_date '2027-06-30', plan_end_date '2029-06-30', quarterly_targets {}.
  CY client (Oh Nine shape): year_type 'CY', year1_end_date '2026-12-31' → start '2027-01-01', year1End '2027-12-31', planEnd '2029-12-31'.
  Already-planned (Armstrong/Fit2Shine): year1_end_date '2027-06-29', planningQuarterStart '2026-07-01' → detect 'normal-review'.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cross-unit integration test — snapshot→rollover→restore + zero-impact guard</name>
  <read_first>
    - src/__tests__/goals/plan-period-persistence.test.ts (supabase mock/builder to reuse)
    - src/__tests__/goals/annual-reset-snapshot.test.ts (capture/restore expectations from 73-01)
    - src/__tests__/goals/annual-reset-service.test.ts (rollover expectations from 73-02)
    - src/__tests__/quarterly-review/annual-reset-entry.test.ts (detection cases from 73-03)
    - src/app/goals/services/annual-reset-service.ts + annual-reset-snapshot-service.ts (final signatures as built)
  </read_first>
  <behavior>
    - Full round-trip (FY26 fixture): executeAnnualReset → assert (a) a plan_snapshots row exists with
      label annual_reset_FY2026 BEFORE the goals row changed; (b) goals row now holds the rolled ladder +
      dates + empty quarterly_targets; (c) calling restoreAnnualResetSnapshot with that snapshotId returns
      the goals row to the pre-reset FY26 ladder exactly (revenue_year1 back to 1_000_000, etc.).
    - Snapshot-gate: configure the snapshot insert to fail → executeAnnualReset returns success:false AND
      the goals row is byte-for-byte unchanged (no partial roll).
    - Detection matrix: assert all three states, with explicit Armstrong/Fit2Shine 'normal-review' (never reset)
      and a FY26 'needs-reset' and a null-dates 'initial-setup'.
    - CY boundary: roll a CY fixture and assert 2027-01-01 / 2027-12-31 / 2029-12-31.
    - Already-planned no-op: detection 'normal-review' ⇒ executeAnnualReset is NOT invoked by the flow logic
      (assert at the call-decision level — the test calls detect first, then only rolls when needs-reset,
      mirroring the hook gate in 73-04).
  </behavior>
  <action>
    Create src/__tests__/goals/annual-reset-integration.test.ts exercising the real
    detectAnnualResetState + annualResetSnapshotService + annualResetService against one shared in-memory
    mocked supabase. Cover every behavior above. Commit:
    `test(73-06): annual-reset integration round-trip + zero-impact + FY/CY boundaries`.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/goals/annual-reset-integration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "executeAnnualReset" src/__tests__/goals/annual-reset-integration.test.ts`
    - `grep -q "restoreAnnualResetSnapshot" src/__tests__/goals/annual-reset-integration.test.ts`
    - Test proves snapshot-insert failure ⇒ goals row unchanged (zero-impact gate)
    - Test proves Armstrong/Fit2Shine (2027-06-29) ⇒ 'normal-review' ⇒ no roll
    - Test proves FY (2026-06-30→2027-06-30→2029-06-30) AND CY (2026-12-31→2027-12-31→2029-12-31) boundaries
    - `npx vitest run src/__tests__/goals/annual-reset-integration.test.ts` passes
  </acceptance_criteria>
  <done>The composed flow is proven reversible and zero-impact on already-planned clients in CI.</done>
</task>

<task type="auto">
  <name>Task 2: Full-suite green gate</name>
  <read_first>
    - vitest.config.ts (suite scope)
  </read_first>
  <action>
    Run the entire vitest suite. Fix any Phase-73-introduced regressions (do NOT touch unrelated
    pre-existing local-only timezone-shaped failures — per MEMORY, ignore local-only TZ failures but
    confirm they are pre-existing by checking they fail on a clean main checkout if any appear).
    Record the pass count in the SUMMARY. Commit only if a fix to Phase-73 code was needed:
    `fix(73-06): resolve regression surfaced by full-suite run` (otherwise no commit).
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` reports 0 failures attributable to Phase 73 files
      (goals/quarterly-review annual-reset units + the integration test all green)
    - Any remaining failures are confirmed pre-existing and local-only (documented in SUMMARY with rationale)
    - `npx tsc --noEmit` clean across all Phase-73 files
  </acceptance_criteria>
  <done>Whole-repo vitest is green (modulo documented pre-existing local-only TZ failures); Phase 73 adds no regressions.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/__tests__/goals/annual-reset-integration.test.ts` passes.
- `npx vitest run` whole-suite green (Phase 73 adds zero regressions).
- Integration test pins: reversibility, snapshot-before-overwrite gate, all 3 entry states incl. already-planned, FY+CY rollover.
</verification>

<success_criteria>
CI permanently enforces the phase's two non-negotiables: every reset is restorable from its snapshot, and
clients already on the new FY (Armstrong, Fit2Shine) are never reset or mutated.
</success_criteria>

<output>
After completion, create `.planning/phases/73-annual-plan-reset-full-year-reset-reusing-the-goals-wizard-data-driven-entry-off-year1-end-date-snapshot-before-overwrite-zero-impact-to-clients-with-a-current-plan/73-06-SUMMARY.md`
</output>
