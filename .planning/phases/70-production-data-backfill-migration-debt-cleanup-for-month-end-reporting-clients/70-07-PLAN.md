---
phase: 70-production-data-backfill-migration-debt-cleanup-for-month-end-reporting-clients
plan: 07
type: execute
wave: 3
depends_on: [70-01, 70-02]
files_modified:
  - scripts/70-07-B3-iict-onboarding-completion.mjs
autonomous: false
requirements:
  - PHASE-70-B3
must_haves:
  truths:
    - "IICT business_profiles has non-null industry, annual_revenue, gross_profit, net_profit + derived margin percentages"
    - "IICT businesses.consolidation_budget_mode = 'consolidated' (was 'single')"
    - "IICT has subscription_budgets rows for the known subscriptions Matt entered interactively (currently 0 rows in production)"
    - "IICT has exactly one canonical active forecast (FY27 dedupe resolved via the same canonical-selection rule as A1)"
    - "IICT has monthly_report_snapshots rows for 2026-04 and 2026-05 produced via the existing report-generation flow (NOT bypassed)"
  artifacts:
    - path: "scripts/70-07-B3-iict-onboarding-completion.mjs"
      provides: "IICT-specific interactive onboarding completion (profile, mode, subs, FY27 dedupe, snapshots)"
      min_lines: 200
  key_links:
    - from: "scripts/70-07-B3-iict-onboarding-completion.mjs"
      to: "business_profiles + businesses + subscription_budgets + financial_forecasts + monthly_report_snapshots (IICT)"
      via: "step-by-step interactive flow; each step independently apply-gated"
      pattern: "fbc6dffd-677d-47ec-8277-7157982938e7|6c0dfadb-4229-4fc2-89eb-ec064d24511b"
    - from: "scripts/70-07-B3-iict-onboarding-completion.mjs"
      to: "existing report-generation flow"
      via: "POST /api/monthly-report/generate (do not bypass)"
      pattern: "monthly-report.*generate"
---

<objective>
Complete IICT's onboarding so the month-end report pack can render. IICT is essentially un-onboarded per the audit:
- business_profiles missing industry, revenue, profit, margins
- businesses.consolidation_budget_mode = 'single' despite 3 tenants (NZ + NZ + HK)
- 0 subscription_budgets rows
- duplicate FY27 forecast rows (post-70-02 dedupe may have resolved this; verify)
- 0 monthly_report_snapshots ever generated

Purpose: IICT is the highest-lift client. Each step is independently risky and requires Matt's input (profile values are business-specific). This is the longest interactive checkpoint of Phase 70.

Output: A script with multiple --step flags so Matt can run each step independently in sequence, reviewing each step's effect before moving to the next.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/70-production-data-backfill-migration-debt-cleanup-for-month-end-reporting-clients/70-CONTEXT.md
@.planning/phases/70-production-data-backfill-migration-debt-cleanup-for-month-end-reporting-clients/70-01-SUMMARY.md
@.planning/phases/70-production-data-backfill-migration-debt-cleanup-for-month-end-reporting-clients/70-02-SUMMARY.md
@scripts/onboard-fit2shine.mjs
@scripts/phase-70-data-audit.mjs

<interfaces>
<!-- IICT IDs (locked in CONTEXT.md): -->
businesses.id        = fbc6dffd-677d-47ec-8277-7157982938e7
business_profiles.id = 6c0dfadb-4229-4fc2-89eb-ec064d24511b

<!-- IICT has 3 Xero tenants (NZ + NZ + HK per memory `reference_xero_reconciliation_verifier`): -->
<!-- xero_connections keyed by businesses.id; check current rows for tenant_id + tenant_name + functional_currency -->

<!-- consolidation_budget_mode is on the `businesses` table (keyed by businesses.id) -->

<!-- business_profiles fields to fill (from audit script line 57): -->
industry, annual_revenue, gross_profit, net_profit,
gross_profit_margin, net_profit_margin, current_revenue

<!-- monthly_report_snapshots existing flow — verify the endpoint name: -->
<!--   `grep -rn "monthly_report_snapshots" src/app/api/monthly-report/` to find current insert path -->
<!--   Per memory `feedback_save_state_legacy`: trace the button → API call BEFORE assuming any path -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the IICT multi-step interactive script</name>
  <files>scripts/70-07-B3-iict-onboarding-completion.mjs</files>
  <read_first>
    - scripts/onboard-fit2shine.mjs (the patch + insert helper functions are the EXACT pattern for the profile fill-out step)
    - scripts/phase-70-data-audit.mjs lines 50-100 (the audit's identity + xero sync + forecast sections — IICT's broken state is visible here)
    - .planning/phases/70-.../70-CONTEXT.md decisions B3 (the full locked list of fields + the consolidation_budget_mode flip + the subscription-entry interactive step + the FY27 dedupe + the snapshot generation requirement)
    - .planning/phases/70-.../70-02-SUMMARY.md (what 70-02 did to IICT FY27 dedupe — if already resolved, this script's forecast step is a no-op)
    - The current monthly_report_snapshots schema and the API endpoint that produces snapshots — find via `grep -rn "monthly_report_snapshots" src/app/api/monthly-report/`
  </read_first>
  <action>
Create `scripts/70-07-B3-iict-onboarding-completion.mjs` accepting `--step=N` (1-5) + `--apply` flags. Each step is independent — Matt runs them in order, reviewing each.

Hardcode IICT IDs:
```js
const IICT_BUSINESSES_ID = 'fbc6dffd-677d-47ec-8277-7157982938e7';
const IICT_PROFILES_ID   = '6c0dfadb-4229-4fc2-89eb-ec064d24511b';
```

STEPS:

**Step 1 — business_profiles fill-out (INTERACTIVE)**:
- Read current business_profiles WHERE id = IICT_PROFILES_ID. Print existing values.
- For each NULL field in `[industry, annual_revenue, gross_profit, net_profit]`:
  - Prompt at stdin: `IICT — <field> (current: NULL) — enter value (or 'skip')?`
  - For numeric fields: validate (must parse to number, must be > 0); reprompt on bad input.
  - For industry: free-text accept (Matt knows the right industry tag — hint: per memory the existing context suggests "Information Technology / IT Consulting" but defer to Matt).
- Compute derived fields after collecting answers:
  - `gross_profit_margin = round((gross_profit / annual_revenue) * 100, 1)`
  - `net_profit_margin = round((net_profit / annual_revenue) * 100, 1)`
  - `current_revenue = annual_revenue` (per onboard-fit2shine.mjs line 251 pattern)
- Dry-run: show the proposed UPDATE payload. --apply: execute UPDATE business_profiles SET ... WHERE id = IICT_PROFILES_ID.
- Idempotency: if all fields are already populated (re-run), print "already populated" and exit 0 for this step.

**Step 2 — consolidation_budget_mode flip (DETERMINISTIC)**:
- Read `businesses WHERE id = IICT_BUSINESSES_ID`. Check current `consolidation_budget_mode`.
- If already 'consolidated': print "already consolidated" and exit 0.
- Verify IICT has > 1 xero_connection row (sanity — flipping to consolidated for a single-tenant business would break things). If only 1 connection: print warning, exit 1.
- If 'single' and > 1 connection: dry-run shows proposed UPDATE; --apply executes `UPDATE businesses SET consolidation_budget_mode = 'consolidated' WHERE id = IICT_BUSINESSES_ID`.

**Step 3 — Initial subscription_budgets entry (INTERACTIVE)**:
- Check current subscription_budgets count for IICT — if > 0, abort with "subscriptions already exist — review manually" (avoids accidental duplicates).
- Interactive loop: `Vendor name? (or 'done')` → `Frequency (monthly|annual)?` → `Budget amount?` → `Renewal month (1-12, or skip)?` → `Account codes (comma-separated, or skip)?`
- Each row: dry-run prints the proposed INSERT payload; --apply executes the INSERT.
- Use `createVendorKey` from `src/lib/utils/vendor-normalization.ts` to verify Matt's vendor names normalize cleanly (warn on edge cases).
- Loop until Matt types 'done'.

**Step 4 — FY27 forecast dedupe (CONDITIONAL)**:
- Read IICT forecasts: `SELECT * FROM financial_forecasts WHERE business_id = IICT_PROFILES_ID ORDER BY created_at DESC`.
- If exactly 1 active FY27 forecast: print "already deduped (likely by 70-02)" and exit 0.
- If > 1 active FY27: apply the same canonical-selection rule as 70-02 (updated_at DESC, pl_lines DESC, payroll_summary nonzero, created_at DESC). Print winner + losers. --apply: deactivate losers (set is_active=false).
- This is essentially "70-02 scoped to IICT" — if 70-02 already ran successfully, this step is idempotent zero.

**Step 5 — Baseline monthly_report_snapshots generation (CRITICAL — DO NOT BYPASS)**:
- Per CONTEXT.md decisions B3: "use the existing report generation flow (do not bypass)".
- Per memory `feedback_save_state_legacy`: trace the button → API call BEFORE assuming any path.
- This step's --apply DOES NOT write directly to monthly_report_snapshots. Instead:
  - For each target month (2026-04 and 2026-05): print the EXACT curl command Matt needs to run, e.g.:
    ```
    To generate the 2026-04 baseline snapshot for IICT:
    curl -X POST -H "Cookie: <Matt's session cookie>" \
         -d '{"business_id":"fbc6dffd-...","period_month":"2026-04-01"}' \
         https://wisdombi.ai/api/monthly-report/generate
    ```
    OR (if such an endpoint exists for triggering snapshot writes server-side; verify by `grep -rn "monthly_report_snapshots.*insert" src/app/api/monthly-report/`):
    print the equivalent.
  - The script does NOT execute these — Matt runs them from the browser/CLI with his session.
  - After Matt confirms execution: the script re-reads monthly_report_snapshots and verifies 2 rows now exist for IICT (one per target month). If not: log and exit non-zero.
- This step's `--apply` flag is interpreted as "verify post-Matt-execution" rather than "write".

Per-step verify command suggestion at end of each step output:
```
After step N completes, run:
  node scripts/phase-70-data-audit.mjs
to confirm the IICT section now passes this dimension.
```

Avoid (and WHY):
- Do NOT bypass the report-generation flow for snapshots — the serializer has the D4 issue (numeric vs named section keys) being fixed in the code-fixes phase; bypassing here means future code paths can't read the rows correctly. Use the existing flow.
- Do NOT touch the Xero tokens for IICT — those are Phase 69's concern. If tokens are expired here (likely, per audit), the snapshot generation will fail with 401 from the in-app Xero calls; that surfaces the cron-health issue (70-09 will gate this).
- Do NOT auto-fill profile values from any heuristic. Matt must enter them. Anything else risks shipping wrong numbers to a CFO-level report.
- Do NOT touch any client other than IICT.
- Do NOT delete any forecast row.
- Do NOT skip Step 5 even though it requires browser-side action. The snapshot writes are how the audit script confirms IICT is reporting-ready.

Use stdin readline pattern:
```js
import { createInterface } from 'node:readline/promises';
const rl = createInterface({ input: process.stdin, output: process.stdout });
const ans = await rl.question('IICT — industry (current: NULL) — enter value (or skip)? ');
```
  </action>
  <verify>
    <automated>node scripts/70-07-B3-iict-onboarding-completion.mjs --step=2 2>&1 | grep -E "step=|IICT|consolidation_budget_mode" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - File `scripts/70-07-B3-iict-onboarding-completion.mjs` exists, > 8 KB
    - `grep -c "fbc6dffd-677d-47ec-8277-7157982938e7" scripts/70-07-B3-iict-onboarding-completion.mjs` ≥ 1
    - `grep -c "6c0dfadb-4229-4fc2-89eb-ec064d24511b" scripts/70-07-B3-iict-onboarding-completion.mjs` ≥ 1
    - `grep -cE "step=1|step=2|step=3|step=4|step=5" scripts/70-07-B3-iict-onboarding-completion.mjs` ≥ 5 (all 5 step branches present)
    - `grep -c "consolidation_budget_mode" scripts/70-07-B3-iict-onboarding-completion.mjs` ≥ 2
    - `grep -c "industry\|annual_revenue\|gross_profit\|net_profit" scripts/70-07-B3-iict-onboarding-completion.mjs` ≥ 4
    - `grep -c "readline\|rl.question\|stdin" scripts/70-07-B3-iict-onboarding-completion.mjs` ≥ 2 (interactive prompts)
    - `grep -c "monthly_report_snapshots" scripts/70-07-B3-iict-onboarding-completion.mjs` ≥ 2 (step 5 read verification)
    - `grep -c "monthly-report/generate\|monthly-report.*generate" scripts/70-07-B3-iict-onboarding-completion.mjs` ≥ 1 (step 5 references the existing flow)
    - `grep -c "\.delete\(" scripts/70-07-B3-iict-onboarding-completion.mjs` = 0
    - Running with no --step prints usage + step descriptions; running with `--step=2` prints current consolidation_budget_mode value
  </acceptance_criteria>
  <done>
    Script exists, IICT-scoped, 5-step interactive flow, idempotent per-step.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Matt drives the 5-step IICT onboarding interactively</name>
  <files>(checkpoint — no file changes; Matt drives execution interactively)</files>
  <action>HALT and present the human-verification flow described in the elements below. Wait for Matt to drive the dry-run, review, --apply, and post-apply verification cycle described in <how-to-verify> / <acceptance_criteria>. The executor (Claude) only runs the script commands Matt asks for; the human is the decision-maker.</action>
  <what-built>The IICT onboarding completion script with 5 independent steps Matt drives in sequence.</what-built>
  <how-to-verify>
    Run each step in order, reviewing dry-run before --apply:

    STEP 1 — Profile values:
      `node scripts/70-07-B3-iict-onboarding-completion.mjs --step=1` (dry-run prompts and shows planned UPDATE)
      `node scripts/70-07-B3-iict-onboarding-completion.mjs --step=1 --apply` (after reviewing)

    STEP 2 — Consolidation mode:
      `node scripts/70-07-B3-iict-onboarding-completion.mjs --step=2`
      `node scripts/70-07-B3-iict-onboarding-completion.mjs --step=2 --apply`

    STEP 3 — Initial subscriptions:
      `node scripts/70-07-B3-iict-onboarding-completion.mjs --step=3 --apply` (interactive entry loop)

    STEP 4 — FY27 dedupe:
      `node scripts/70-07-B3-iict-onboarding-completion.mjs --step=4`
      `node scripts/70-07-B3-iict-onboarding-completion.mjs --step=4 --apply` (likely no-op if 70-02 ran)

    STEP 5 — Snapshot generation:
      `node scripts/70-07-B3-iict-onboarding-completion.mjs --step=5`
      → script prints curl commands to run from Matt's browser/CLI
      → Matt runs them
      `node scripts/70-07-B3-iict-onboarding-completion.mjs --step=5 --apply` (verify the snapshots exist)

    After all 5 steps complete, run: `node scripts/phase-70-data-audit.mjs` — IICT section should show:
      - industry, annual_revenue, gross_profit, net_profit, margins all non-null
      - consolidation_budget_mode = 'consolidated'
      - subscription_budgets row count > 0
      - financial_forecasts active count = 1
      - monthly_report_snapshots row count for 2026-04 and 2026-05 each ≥ 1
  </how-to-verify>
  <acceptance_criteria>
    - All 5 steps executed with --apply
    - audit re-run: IICT section no longer shows ⚠ industry NULL / profile_completed false / consolidation_budget_mode=single warnings
    - audit re-run: IICT shows subscription_budgets count > 0
    - audit re-run: IICT shows monthly_report_snapshots count ≥ 2 (one per target month)
    - audit re-run: IICT has exactly 1 active forecast
    - Each step idempotent: re-running with --apply after success produces no further writes
  </acceptance_criteria>
  <resume-signal>Type "step N complete" after each step (where N is 1-5), and "all 5 complete" at the end.</resume-signal>  <verify>See <how-to-verify> below — Matt walks through it manually with executor support.</verify>
  <done>All checks in <acceptance_criteria> satisfied; Matt has explicitly confirmed completion.</done>

</task>

</tasks>

<verification>
- Audit re-run confirms IICT identity + Xero (where token allows) + forecasts + subs + snapshots dimensions all healthy
- Per-step idempotency: re-running each step --apply produces 0 mutations
- Snapshot rows exist for 2026-04 and 2026-05 produced by the existing generation flow (not bypassed)
</verification>

<success_criteria>
- IICT business_profiles populated with Matt-provided real values
- consolidation_budget_mode flipped to 'consolidated'
- Initial subscription_budgets rows inserted
- FY27 forecast deduped
- 2 baseline monthly_report_snapshots rows exist
</success_criteria>

<output>
After completion, create `.planning/phases/70-.../70-07-SUMMARY.md` recording:
- Per-step outcome (which fields Matt entered for profile; how many subs added; how the dedupe resolved; how many snapshots generated)
- The exact UPDATE/INSERT statements executed per step
- Final audit-script output for IICT (paste the relevant section verbatim)
- Any deferred work (e.g., if a step skipped due to Xero token expiry — links to 70-09)
- Follow-up note: Calxa migration is unblocked for IICT once these gaps are closed
</output>
