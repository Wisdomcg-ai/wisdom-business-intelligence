---
phase: 70-production-data-backfill-migration-debt-cleanup-for-month-end-reporting-clients
plan: 06
type: execute
wave: 3
depends_on: [70-01, 70-02]
files_modified:
  - scripts/70-06-B2-jds-profile-and-forecast.mjs
autonomous: false
requirements:
  - PHASE-70-B2
must_haves:
  truths:
    - "JDS business_profiles.profile_completed = true"
    - "JDS has exactly one canonical active forecast for the current fiscal year, with populated forecast_pl_lines (Matt's choice: either FY26 backfilled via re-materialize, OR FY26 deactivated in favour of FY27)"
    - "The decision Matt made (Option A or B) is recorded in 70-06-SUMMARY.md"
  artifacts:
    - path: "scripts/70-06-B2-jds-profile-and-forecast.mjs"
      provides: "JDS-specific profile flip + FY26 forecast decision execution"
      min_lines: 100
  key_links:
    - from: "scripts/70-06-B2-jds-profile-and-forecast.mjs"
      to: "business_profiles (JDS)"
      via: "UPDATE business_profiles SET profile_completed = true WHERE id = JDS_PROFILES_ID"
      pattern: "profile_completed.*true"
    - from: "scripts/70-06-B2-jds-profile-and-forecast.mjs"
      to: "financial_forecasts (JDS FY26)"
      via: "either UPDATE is_active=false on FY26 (Option B) OR a re-materialize API call (Option A)"
      pattern: "fiscal_year.*2026|FY26|FY27"
---

<objective>
JDS-specific cleanup: (1) flip `business_profiles.profile_completed` from false to true; (2) resolve the FY26 forecast ambiguity — audit found the active FY26 forecast has 0 `forecast_pl_lines` while FY27 has 92 lines. Matt must choose Option A (backfill FY26) or Option B (deactivate FY26 in favour of FY27).

Purpose: Without `profile_completed=true`, JDS triggers onboarding gates in the app. Without a populated active forecast, the wages tab + variance dashboard render blank for JDS specifically — even after 70-03 backfills payroll_summary, the underlying forecast_pl_lines must exist for budget comparisons. This is the one client whose dimension D1+D2 gaps are not fully resolved by the cross-client scripts alone.

Output: A two-mode script that (a) flips profile_completed, (b) executes Matt's FY26 decision (interactive checkpoint). Default recommendation per CONTEXT.md decisions: Option B (deactivate FY26, FY27 already has populated data).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/70-production-data-backfill-migration-debt-cleanup-for-month-end-reporting-clients/70-CONTEXT.md
@.planning/phases/70-production-data-backfill-migration-debt-cleanup-for-month-end-reporting-clients/70-01-SUMMARY.md
@.planning/phases/70-production-data-backfill-migration-debt-cleanup-for-month-end-reporting-clients/70-02-SUMMARY.md
@scripts/onboard-fit2shine.mjs
@scripts/phase-70-data-audit.mjs

<interfaces>
<!-- JDS IDs (locked in CONTEXT.md): -->
businesses.id        = fea253dd-3dfa-447b-8f9b-8dff68aeac0a
business_profiles.id = 900aa935-ae8c-4913-baf7-169260fa19ef

<!-- business_profiles keyed by business_profiles.id (its own primary key). -->
<!-- financial_forecasts keyed by business_profiles.id (in the business_id column). -->

<!-- 70-02 (A1) will have run by the time this plan executes — JDS may already have its FY26 deactivated by the cross-client dedupe. CHECK FIRST: -->
<!--   If 70-02 already deactivated JDS FY26 because FY27 won the canonical-selection rule, Option B is already complete and this script's forecast step is a no-op. -->
<!--   If 70-02 KEPT FY26 active (because it was more recently updated than FY27, even though it has no PL lines), THIS plan's interactive decision still matters. -->
<!--   Either way: the script must START by reading current state, not assuming. -->

<!-- forecast_pl_lines count read pattern (from audit script line 104): -->
sb.from('forecast_pl_lines').select('id', { count: 'exact', head: true }).eq('forecast_id', F.id)

<!-- B2 INTERACTIVE PROMPT — the script MUST print this exact string verbatim to stdout when reaching the decision point (no abbreviation, no paraphrase): -->
<!-- "FY26 active forecast has 0 forecast_pl_lines. Options: (A) Backfill via forecast re-materialize, (B) Deactivate FY26 in favour of FY27 (92 lines, ready). Recommend (B). Reply 'A' or 'B'." -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the JDS profile + FY26 decision script (defaults to Option B)</name>
  <files>scripts/70-06-B2-jds-profile-and-forecast.mjs</files>
  <read_first>
    - scripts/onboard-fit2shine.mjs (the patch-pattern for business_profiles.profile_completed flip — see lines 305-317)
    - .planning/phases/70-.../70-CONTEXT.md decisions B2 (the locked options A vs B + default recommendation)
    - .planning/phases/70-.../70-02-SUMMARY.md (read the output of 70-02 — confirms which JDS forecast is currently active AFTER cross-client dedupe)
  </read_first>
  <action>
Create `scripts/70-06-B2-jds-profile-and-forecast.mjs` accepting zero flags (dry-run) or `--apply`, plus `--option=A` / `--option=B` to specify Matt's choice (REQUIRED for --apply; absent in dry-run means "preview both options").

Hardcode JDS IDs:
```js
const JDS_BUSINESSES_ID = 'fea253dd-3dfa-447b-8f9b-8dff68aeac0a';
const JDS_PROFILES_ID   = '900aa935-ae8c-4913-baf7-169260fa19ef';
```

**Interactive prompt string (locked verbatim — DO NOT paraphrase, DO NOT abbreviate):**
```
FY26 active forecast has 0 forecast_pl_lines. Options: (A) Backfill via forecast re-materialize, (B) Deactivate FY26 in favour of FY27 (92 lines, ready). Recommend (B). Reply 'A' or 'B'.
```

Define this as a const at the top of the script:
```js
const B2_DECISION_PROMPT = "FY26 active forecast has 0 forecast_pl_lines. Options: (A) Backfill via forecast re-materialize, (B) Deactivate FY26 in favour of FY27 (92 lines, ready). Recommend (B). Reply 'A' or 'B'.";
```

When the script reaches the decision branch in dry-run mode (no `--option=` flag supplied), it MUST print `B2_DECISION_PROMPT` verbatim as the last informational line BEFORE printing the both-options preview. When invoked with `--apply` but missing `--option=`, it MUST print `B2_DECISION_PROMPT` and exit non-zero.

Algorithm:

STEP 1 — Profile completion flip (always, no Matt input needed):
1. Read `business_profiles WHERE id = JDS_PROFILES_ID`. If `profile_completed === true`: log "already complete" and skip.
2. If false: dry-run shows the planned UPDATE; --apply executes `UPDATE business_profiles SET profile_completed = true, profile_updated_at = NOW() WHERE id = JDS_PROFILES_ID`.

STEP 2 — Read current forecast state (post-70-02 dedupe):
1. Read all JDS forecasts: `SELECT id, name, fiscal_year, is_active, created_at, updated_at FROM financial_forecasts WHERE business_id = JDS_PROFILES_ID ORDER BY created_at DESC`.
2. For each, fetch forecast_pl_lines count.
3. Print:
```
JDS current forecast state (post 70-02):
  • FY26 v3   id=...  active=true   pl_lines=0    updated=2026-04-...
  • FY27 v1   id=...  active=false  pl_lines=92   updated=2026-03-...
```

STEP 3 — Decision branch:

If invoked WITHOUT `--option=` (dry-run preview OR --apply without choice):
- Print the both-options preview (what each branch would do)
- Print the locked `B2_DECISION_PROMPT` verbatim as the final line
- If `--apply` was specified without --option: exit 1
- If pure dry-run (no flags): exit 0

If `--option=A` (BACKFILL FY26):
- This is the "re-materialize FY26 forecast" path. The forecast wizard's save flow produces forecast_pl_lines; calling it from a script requires either:
  (a) hitting the existing API endpoint (e.g., `POST /api/forecast/<id>/materialize` if such exists — verify by `grep -rn "materialize\|regenerate" src/app/api/forecast/`), OR
  (b) copying forecast_pl_lines from FY27 with month-key remapping (FY27 months → FY26 months)
- If neither path is straightforward in 30 lines of script: ABORT with clear message — Option A requires more work than a backfill script; route to a separate code-phase task. Print: "Option A is not implementable as a one-shot backfill. Recommend Option B. Or escalate to a dedicated phase."
- This avoids quietly half-implementing a complex path.

If `--option=B` (DEACTIVATE FY26, KEEP FY27 ACTIVE):
- If FY26 is currently active: `UPDATE financial_forecasts SET is_active = false WHERE id = <FY26.id>`
- If FY27 is currently inactive: `UPDATE financial_forecasts SET is_active = true WHERE id = <FY27.id>`
- If 70-02 already did this (FY27 active, FY26 inactive): print "Option B is already the current state — no mutation needed" and exit success.
- If multiple FY26 rows or multiple FY27 rows exist: print "JDS forecast state ambiguous — please run scripts/70-02-A1-active-forecast-remediation.mjs --apply first" and exit non-zero.

STEP 4 — Verification post-write (only if --apply succeeded):
- Re-read JDS forecasts. Confirm: exactly one is_active=true, and that one has > 0 forecast_pl_lines.
- If verification fails: print loud ERROR and exit 1 (do NOT silently succeed).

Avoid (and WHY):
- Do NOT touch any client other than JDS.
- Do NOT auto-execute Option A. The CONTEXT explicitly says Matt chooses; default recommendation is B; A requires materialize/copy logic worth its own plan.
- Do NOT delete any forecast row. Deactivate only. (Matches the locked A1 rule from 70-02.)
- Do NOT touch FY27's underlying forecast_pl_lines, employees, or payroll_summary. Those are already populated and correct.
- Do NOT proceed past Step 1 if `business_profiles WHERE id = JDS_PROFILES_ID` returns null (unexpected — abort with clear message).
- Do NOT paraphrase the B2_DECISION_PROMPT. Use the locked string verbatim — downstream tooling (and Matt's review flow) recognizes it by exact match.
  </action>
  <verify>
    <automated>node scripts/70-06-B2-jds-profile-and-forecast.mjs 2>&1 | grep -E "JDS current forecast state|FY26|FY27|profile_completed|Reply 'A' or 'B'" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - File `scripts/70-06-B2-jds-profile-and-forecast.mjs` exists, > 4 KB
    - `grep -c "fea253dd-3dfa-447b-8f9b-8dff68aeac0a" scripts/70-06-B2-jds-profile-and-forecast.mjs` ≥ 1
    - `grep -c "900aa935-ae8c-4913-baf7-169260fa19ef" scripts/70-06-B2-jds-profile-and-forecast.mjs` ≥ 1
    - `grep -c "profile_completed" scripts/70-06-B2-jds-profile-and-forecast.mjs` ≥ 2
    - `grep -c "option=A\|option=B\|--option" scripts/70-06-B2-jds-profile-and-forecast.mjs` ≥ 3 (flag parsing + both branches)
    - `grep -c "\.delete\(" scripts/70-06-B2-jds-profile-and-forecast.mjs` = 0
    - `grep -c "is_active.*false\|is_active: false" scripts/70-06-B2-jds-profile-and-forecast.mjs` ≥ 1 (Option B path)
    - The locked B2 prompt string is present VERBATIM: `grep -F "FY26 active forecast has 0 forecast_pl_lines. Options: (A) Backfill via forecast re-materialize, (B) Deactivate FY26 in favour of FY27 (92 lines, ready). Recommend (B). Reply 'A' or 'B'." scripts/70-06-B2-jds-profile-and-forecast.mjs` returns exactly 1 match
    - Default execution prints "DRY RUN" + the FY26/FY27 state report + the both-options preview + the verbatim B2_DECISION_PROMPT
    - Default execution does NOT write to either business_profiles or financial_forecasts
  </acceptance_criteria>
  <done>
    Script exists, JDS-scoped, two-mode + --option-gated, defaults to safe dry-run preview, prints the locked B2 prompt verbatim.
  </done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Matt chooses Option A or B for JDS FY26 then approves --apply</name>
  <files>(checkpoint — no file changes; Matt drives execution interactively)</files>
  <action>HALT and present the human-verification flow described in the elements below. Wait for Matt to drive the dry-run, review, --apply, and post-apply verification cycle described in <how-to-verify> / <acceptance_criteria>. The executor (Claude) only runs the script commands Matt asks for; the human is the decision-maker.</action>
  <decision>FY26 forecast resolution for JDS — backfill or deactivate?</decision>
  <context>
FY26 active forecast has 0 forecast_pl_lines. Options: (A) Backfill via forecast re-materialize, (B) Deactivate FY26 in favour of FY27 (92 lines, ready). Recommend (B). Reply 'A' or 'B'.

JDS has an active FY26 forecast with 0 forecast_pl_lines. A separate FY27 forecast exists with 92 lines but is inactive. The cross-client dedupe in 70-02 may have already chosen between them per the canonical-selection rule. This decision finalizes: either populate FY26's missing data, or keep FY27 as the canonical active.

Run the dry-run first: `node scripts/70-06-B2-jds-profile-and-forecast.mjs`
Review the current state shown.

Reply with: 'A' or 'B' followed by 'approved' once you have a choice.
  </context>
  <options>
    <option id="option-a">
      <name>A — Backfill FY26 forecast_pl_lines</name>
      <pros>Preserves FY26 as the active forecast (matches calendar reality if today is FY26)</pros>
      <cons>Implementation is non-trivial — the backfill script will print "not implementable as one-shot" and recommend escalating to a dedicated phase. Choosing A means deferring the actual FY26 population to follow-up work.</cons>
    </option>
    <option id="option-b">
      <name>B — Deactivate FY26 in favour of FY27 (DEFAULT RECOMMENDED)</name>
      <pros>FY27 already has 92 populated forecast_pl_lines and is the operationally healthier forecast. One UPDATE flips is_active. Idempotent if 70-02 already did this. Phase 70 is data backfill — keeping scope tight.</pros>
      <cons>If JDS is currently mid-FY26 reporting cycle, the active forecast jumps a year forward — but the audit explicitly recommends B because FY26 has no usable budget data anyway.</cons>
    </option>
  </options>
  <how-to-verify>
    1. Run dry-run: `node scripts/70-06-B2-jds-profile-and-forecast.mjs`
    2. Note the current FY26 / FY27 active flags + line counts
    3. The script will print the locked B2 prompt verbatim:
       `FY26 active forecast has 0 forecast_pl_lines. Options: (A) Backfill via forecast re-materialize, (B) Deactivate FY26 in favour of FY27 (92 lines, ready). Recommend (B). Reply 'A' or 'B'.`
    4. Choose A or B
    5. Run --apply with the chosen option:
       - Option A: `node scripts/70-06-B2-jds-profile-and-forecast.mjs --option=A --apply` (will likely print "not implementable, escalate" — that's expected)
       - Option B: `node scripts/70-06-B2-jds-profile-and-forecast.mjs --option=B --apply`
    6. Re-run dry-run; should show:
       - profile_completed=true (always — Step 1 runs in both options)
       - Exactly one is_active=true forecast for JDS with > 0 forecast_pl_lines
  </how-to-verify>
  <acceptance_criteria>
    - Matt explicitly chose A or B; choice recorded in resume-signal
    - --apply executed with the chosen option
    - Final state: business_profiles.profile_completed = true (verify: `node scripts/phase-70-data-audit.mjs` no longer prints "⚠ business_profiles.profile_completed = false" for JDS)
    - Final state: exactly one is_active=true forecast for JDS with forecast_pl_lines > 0
    - The chosen option is documented in 70-06-SUMMARY.md with Matt's stated rationale
  </acceptance_criteria>
  <resume-signal>Reply with 'A approved' or 'B approved', or describe an alternative.</resume-signal>  <verify>See <how-to-verify> below — Matt walks through it manually with executor support.</verify>
  <done>All checks in <acceptance_criteria> satisfied; Matt has explicitly confirmed completion.</done>

</task>

</tasks>

<verification>
- Audit re-run: JDS section no longer shows "profile_completed = false" warning
- Audit re-run: JDS active forecast count = 1 AND that forecast has forecast_pl_lines > 0
- Idempotency: re-running the script with the same --option flag is a no-op
- The locked B2 prompt string is printed verbatim in dry-run output
</verification>

<success_criteria>
- profile_completed flipped to true
- FY26/FY27 ambiguity resolved per Matt's recorded choice
- JDS now has exactly one active forecast with populated lines
</success_criteria>

<output>
After completion, create `.planning/phases/70-.../70-06-SUMMARY.md` recording:
- Initial JDS state (profile_completed value, FY26 vs FY27 active + line counts)
- Matt's choice (Option A or B) and rationale
- The exact UPDATE statements executed
- Final state confirmation (profile_completed=true, one active forecast with N lines)
- If Option A was chosen and the script escalated: link to the proposed follow-up phase
</output>
