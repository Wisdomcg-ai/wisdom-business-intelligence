---
phase: 70-production-data-backfill-migration-debt-cleanup-for-month-end-reporting-clients
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/70-01-snapshot-pre-write.mjs
  - .planning/phases/70-production-data-backfill-migration-debt-cleanup-for-month-end-reporting-clients/snapshots/
autonomous: true
requirements:
  - PHASE-70-D1
  - PHASE-70-D2
  - PHASE-70-D3
  - PHASE-70-B1
  - PHASE-70-B2
  - PHASE-70-B3
must_haves:
  truths:
    - "A timestamped JSON snapshot exists capturing every row in every table that 70-02..70-07 will mutate, BEFORE any --apply has been run"
    - "Snapshot file is keyed by ISO timestamp so re-runs do not overwrite the rollback baseline"
    - "Snapshot captures both businesses.id-keyed and business_profiles.id-keyed reads for every dual-ID table, so dual-ID drift cannot hide rows"
  artifacts:
    - path: "scripts/70-01-snapshot-pre-write.mjs"
      provides: "Read-only snapshot script following 68-01-snapshot-armstrong.mjs pattern"
      min_lines: 80
    - path: ".planning/phases/70-production-data-backfill-migration-debt-cleanup-for-month-end-reporting-clients/snapshots/70-pre-write-{ISO-timestamp}.json"
      provides: "Versioned rollback baseline; produced by running the script"
      contains: "tables.financial_forecasts, tables.forecast_payroll_summary, tables.subscription_budgets, tables.business_profiles, tables.businesses, tables.monthly_report_snapshots"
  key_links:
    - from: "scripts/70-01-snapshot-pre-write.mjs"
      to: "supabase REST"
      via: "@supabase/supabase-js with SUPABASE_SECRET_KEY"
      pattern: "createClient.*SUPABASE_SECRET_KEY"
    - from: "scripts/70-01-snapshot-pre-write.mjs"
      to: ".planning/phases/70.../snapshots/"
      via: "writeFileSync with mkdirSync recursive"
      pattern: "writeFileSync.*snapshots.*70-pre-write"
---

<objective>
Create the pre-write rollback baseline that gates all of Phase 70's mutating work. Reads every table that 70-02 through 70-07 will mutate — for ALL production businesses (cross-client A1/A2/A3) AND specifically Envisage, JDS, IICT (per-client B1/B2/B3) — and writes a versioned JSON snapshot under `.planning/phases/70-.../snapshots/`.

Purpose: If any --apply run in waves 2-3 produces a bad result, this snapshot is the only reliable rollback artifact. Phase 70 has no schema-level audit log — the JSON IS the audit log.

Output: One JSON snapshot file with ISO-timestamped filename. Script is idempotent because the filename includes a timestamp — re-runs produce a new file, never overwrite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/70-production-data-backfill-migration-debt-cleanup-for-month-end-reporting-clients/70-CONTEXT.md
@scripts/68-01-snapshot-armstrong.mjs
@scripts/phase-70-data-audit.mjs

<interfaces>
<!-- Dual-ID convention (verified by scripts/phase-70-data-audit.mjs lines 14-17) -->
businesses.id KEYS:        xero_connections, subscription_budgets, monthly_report_snapshots, businesses
business_profiles.id KEYS: xero_pl_lines (and v2), xero_bs_lines, financial_forecasts,
                           forecast_pl_lines, forecast_payroll_summary, forecast_employees,
                           business_profiles

<!-- Production client IDs (from scripts/phase-70-data-audit.mjs lines 33-37) -->
Envisage:     businesses.id=8c8c63b2-bdc4-4115-9375-8d0fd89acc00  business_profiles.id=fa0a80e8-e58e-40aa-b34a-8db667d4b221
Just Digital: businesses.id=fea253dd-3dfa-447b-8f9b-8dff68aeac0a  business_profiles.id=900aa935-ae8c-4913-baf7-169260fa19ef
IICT:         businesses.id=fbc6dffd-677d-47ec-8277-7157982938e7  business_profiles.id=6c0dfadb-4229-4fc2-89eb-ec064d24511b

<!-- Env vars (per memory feedback_executor_schema_deviations) -->
SUPABASE_SECRET_KEY (legacy SUPABASE_SERVICE_KEY fallback; disabled 2026-05-19)
NEXT_PUBLIC_SUPABASE_URL
Loaded from .env.local via dotenv config({ path: '.env.local' })
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build pre-write snapshot script</name>
  <files>scripts/70-01-snapshot-pre-write.mjs</files>
  <read_first>
    - scripts/68-01-snapshot-armstrong.mjs (THE pattern — copy structure exactly: dotenv → createClient → table loop → writeFileSync with ISO timestamp)
    - scripts/phase-70-data-audit.mjs (lines 14-17 for dual-ID convention comments + lines 33-37 for client IDs)
    - .planning/phases/70-production-data-backfill-migration-debt-cleanup-for-month-end-reporting-clients/70-CONTEXT.md (the locked dual-ID list in `<decisions>` block)
  </read_first>
  <action>
Create `scripts/70-01-snapshot-pre-write.mjs` as a READ-ONLY snapshot script (no --apply flag, no write paths). Mirror `scripts/68-01-snapshot-armstrong.mjs` exactly for the env-loading + fetch + write-file scaffolding. Differences from 68-01:

1. Cross-client scope (not single-tenant):
   - For tables keyed by `businesses.id`: snapshot ALL rows (no `business_id=eq.X` filter) for `businesses`, `xero_connections`, `subscription_budgets`, `monthly_report_snapshots`.
   - For tables keyed by `business_profiles.id`: snapshot ALL rows for `business_profiles`, `financial_forecasts`, `forecast_employees`, `forecast_payroll_summary`. For `forecast_pl_lines` snapshot ONLY rows where `forecast_id IN (active forecasts)` — full table would be too large. Use a two-pass: first SELECT active forecast IDs, then SELECT `forecast_pl_lines` filtered by those IDs in batches of 100 via `forecast_id=in.(...)` PostgREST syntax.

2. Per-client deep snapshot (Envisage + JDS + IICT only): for the 3 IDs in the CLIENTS array below, ALSO snapshot via BOTH key conventions (drift defence — same pattern 68-01 uses with `_by_profiles_id` / `_by_businesses_id` suffixes). Specifically: `xero_pl_lines` filtered by both `business_id=eq.{businesses.id}` AND `business_id=eq.{business_profiles.id}` so that if dual-ID drift has put rows under the wrong key, both sets are captured.

3. Output path: `.planning/phases/70-production-data-backfill-migration-debt-cleanup-for-month-end-reporting-clients/snapshots/70-pre-write-{ISO-timestamp}.json` (note: under `.planning/`, NOT `scripts/snapshots/` — the rollback artifact lives with the phase doc).

4. Use `mkdirSync(snapshotsDir, { recursive: true })` then `existsSync` guard then `writeFileSync` — refuse to overwrite (same as 68-01 line 107-109).

5. CLIENTS constant matching scripts/phase-70-data-audit.mjs:
```js
const CLIENTS = [
  { label: 'Envisage',     business_id: '8c8c63b2-bdc4-4115-9375-8d0fd89acc00', business_profile_id: 'fa0a80e8-e58e-40aa-b34a-8db667d4b221' },
  { label: 'Just Digital', business_id: 'fea253dd-3dfa-447b-8f9b-8dff68aeac0a', business_profile_id: '900aa935-ae8c-4913-baf7-169260fa19ef' },
  { label: 'IICT',         business_id: 'fbc6dffd-677d-47ec-8277-7157982938e7', business_profile_id: '6c0dfadb-4229-4fc2-89eb-ec064d24511b' },
];
```

6. Top-of-file purpose comment (matching 68-01 style) documenting: this is the Phase 70 rollback baseline; 70-02..70-07 must NOT --apply until this snapshot exists; the file is the only audit log for the phase.

7. Print row counts per table (matching 68-01 line 92 format: `  ${label.padEnd(40)} ${String(rows.length).padStart(6)} rows`) so the human inspecting dry-run output can confirm capture completeness.

8. Top-level payload shape (matching 68-01 lines 111-123):
```js
{
  capturedAt: ISO,
  phase: 70,
  plan: '01',
  purpose: 'Pre-write snapshot before Phase 70 cross-client + per-client backfills',
  clients: CLIENTS,
  tables: { /* label → rows[] */ }
}
```

Use `SUPABASE_SECRET_KEY` with `SUPABASE_SERVICE_KEY` fallback per memory `feedback_executor_schema_deviations`. NO `--apply` flag; no write paths to Supabase; this script's ONLY side effect is writing the JSON file locally.

Avoid (and WHY):
- Do NOT batch the cross-client `subscription_budgets` / `xero_connections` reads through PostgREST `in.()` filters with the production business IDs — the full table is small enough (44+47+0 budgets visible in audit, dozens of connections) that an unfiltered SELECT is simpler and matches the "snapshot everything mutable" promise.
- Do NOT compress or transform the data — raw rows only. Bigger file is fine; tiny rollback friction loss is unacceptable.
- Do NOT add a `--apply` flag — having one would invite future readers to think this script mutates. Read-only by signature.
  </action>
  <verify>
    <automated>node scripts/70-01-snapshot-pre-write.mjs && ls .planning/phases/70-production-data-backfill-migration-debt-cleanup-for-month-end-reporting-clients/snapshots/70-pre-write-*.json | head -1</automated>
  </verify>
  <acceptance_criteria>
    - File `scripts/70-01-snapshot-pre-write.mjs` exists, size > 4 KB, contains NO `--apply` flag handling (`grep -c "apply" scripts/70-01-snapshot-pre-write.mjs` returns ≤ 2 — only in the comments/error message, never in argv parsing)
    - Running `node scripts/70-01-snapshot-pre-write.mjs` produces a new file matching glob `.planning/phases/70-.../snapshots/70-pre-write-*.json` whose size is > 200 KB (Envisage alone has 44 sub budgets + multiple forecasts)
    - The JSON has top-level keys `capturedAt`, `phase: 70`, `tables`, `clients`
    - `jq '.tables | keys' .planning/phases/70-.../snapshots/70-pre-write-*.json` includes at minimum: `businesses`, `business_profiles`, `xero_connections`, `subscription_budgets`, `financial_forecasts`, `forecast_employees`, `forecast_payroll_summary`, `monthly_report_snapshots`
    - Running the script a second time produces a second file (different ISO timestamp), does NOT throw, does NOT overwrite the first file
    - Re-running the script produces files whose content (excluding `capturedAt`) is byte-identical for the same Supabase state (idempotent read)
  </acceptance_criteria>
  <done>
    A timestamped JSON snapshot file exists under `.planning/phases/70-.../snapshots/`, contains rows for all 8 mutable tables, and the script can be re-run safely.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run the snapshot and confirm row counts match the audit</name>
  <files>(no file changes — verification only)</files>
  <read_first>
    - The snapshot JSON produced by Task 1
    - docs/phase-70-month-end-audit.md (the per-client status table) — to confirm counts roughly align
  </read_first>
  <action>
Execute `node scripts/70-01-snapshot-pre-write.mjs` once and capture stdout (the per-table row counts). Cross-check key counts against `docs/phase-70-month-end-audit.md`:

- `subscription_budgets` total rows ≥ (44 Envisage + 47 JDS + 0 IICT) = 91. If significantly less, dual-ID drift may be hiding rows — investigate before proceeding.
- `financial_forecasts` total — must include at minimum 2 active rows for Envisage + 2 active rows for JDS (per audit).
- `forecast_payroll_summary` total — audit reports 0 rows for all 3 sampled clients; expect very low total count (0 is plausible).
- `monthly_report_snapshots` total — audit reports 0 for IICT, 1 for JDS, 0+ for Envisage; expect very low count.

Document the row counts in a one-line note appended to STATE.md under "Active operational notes" so subsequent plans can reference them as the baseline.

NO --apply flag exists on this script. NO Matt approval required to run — it is read-only.
  </action>
  <verify>
    <automated>node scripts/70-01-snapshot-pre-write.mjs 2>&1 | grep -E "rows$" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Script stdout reports at least 8 lines ending in "rows" (one per snapshotted table label)
    - Snapshot file size > 200 KB AND < 50 MB (sanity bounds)
    - `subscription_budgets` row count from stdout ≥ 90 (per audit)
    - At least one snapshot file exists under `.planning/phases/70-.../snapshots/`
    - STATE.md has a new line under "Active operational notes" documenting the snapshot filename + key row counts
  </acceptance_criteria>
  <done>
    Snapshot captured, row counts validated against audit, STATE.md baseline note written.
  </done>
</task>

</tasks>

<verification>
- Snapshot file exists with correct structure (jq validation)
- Row counts are plausible given audit findings (no zero-row anomalies indicating dual-ID drift)
- Re-running script is safe (timestamp-unique filenames)
</verification>

<success_criteria>
- One JSON file exists under `.planning/phases/70-.../snapshots/`, > 200 KB, with the 8 expected top-level table keys
- STATE.md updated with the snapshot baseline reference
</success_criteria>

<output>
After completion, create `.planning/phases/70-production-data-backfill-migration-debt-cleanup-for-month-end-reporting-clients/70-01-SUMMARY.md` recording:
- The exact ISO-timestamped snapshot filename
- The per-table row counts captured
- Any anomalies (zero-row tables, drift candidates) flagged for downstream plans
- Confirmation that 70-02..70-07 may now proceed
</output>
