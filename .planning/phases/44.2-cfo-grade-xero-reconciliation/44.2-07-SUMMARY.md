---
phase: 44.2-cfo-grade-xero-reconciliation
plan: 07
status: complete
---

# 44.2-07 — ForecastReadService.data_quality — Summary

## What shipped

- **`src/lib/services/forecast-read-service.ts`**:
  - New `DataQuality` enum (`verified | partial | failed | no_sync | stale`).
  - New `PerTenantQuality` interface (per-tenant breakdown for the 44.2-09 drawer).
  - Exported `QUALITY_RANK` const for worst-of severity ordering.
  - `MonthlyComposite` extended with `data_quality` + `per_tenant_quality`.
  - Private `computeDataQuality(businessIds[])` reads `xero_connections` (active tenants) → `sync_jobs` LATEST per tenant → maps status to enum (with 24h stale window for `success` → `stale`) → worst-of rolls per-tenant up to business level.
  - Public `getDataQualityForBusiness(businessIds[])` wrapper for consumers without an active forecast (cashflow fallback, coach dashboard).
  - Standalone `aggregateDataQualityAcrossBusinesses(supabase, businessIds[])` for the coach dashboard compact-aggregate banner variant.
- **`src/__tests__/services/forecast-read-service-data-quality.test.ts`** — 8 tests covering each enum value + multi-tenant worst-of + public-wrapper shape.

## Tests

8/8 green. tsc clean. 41/41 services tests still pass.

## Notes

- Reconciliation discrepancy count surfaces both shapes: post-06D `pl.discrepant_accounts` + `bs.unbalanced_dates`, plus legacy `discrepant_accounts` for pre-06D `sync_jobs` rows still in the table.
- 24h staleness threshold per the plan; threshold is private to `computeDataQuality`. If clients want it tunable later, lift to a constructor param.
