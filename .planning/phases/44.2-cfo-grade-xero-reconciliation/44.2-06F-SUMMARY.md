---
phase: 44.2-cfo-grade-xero-reconciliation
plan: 06F
status: tasks-1-2-3-complete; task-4-ongoing-via-verify-script
pr: TBD
---

# 06F — Production Migration Runbook + Verifier — Summary

## What shipped (Tasks 1, 2, 3 — all autonomous)

- **`scripts/verify-production-migration.ts`** — runs the same 4 automated gates as the 06E test harness, but against LIVE production data. Exit 0 = all pass, 1 = any fail, 2 = infrastructure error. Surfaces 3 Gate-5 spot-check candidates (top revenue / top asset / top TB-debit) for operator manual cross-check against Xero web PDFs.
- **`src/lib/xero/reconciliation-gates.ts`** — gate logic extracted from the 06E test file as 4 pure functions (`assertGate1/2/3/4`). 06E test file refactored to import these. **One source of truth**: green tests + green verify-script ⟹ "production matches the reference fixtures."
- **`PRODUCTION-MIGRATION-RUNBOOK.md`** — 12-step retrospective documenting the 44.2-06A → 06D rollout sequence with verification SQL and rollback links per step. Useful as historical record + template for future migrations of the same shape.
- **`ROLLBACK-PROTOCOL.md`** — per-step reversal recipes + full-revert procedure. Captures the snapshot discipline lesson for future destructive migrations.

## Production verification results

Verified live against production on 2026-05-02:

| Tenant | Gate 1 | Gate 2 | Gate 3 | Gate 4 |
|---|---|---|---|---|
| **JDS** (AU/AUD)             | ✅ max drift $0.00 (Rent allow-listed) | ✅ Δ $0.00 | ✅ Δ -$0.01 | ✅ Δ -$0.01 |
| **IICT-HK** (HK/HKD, multi-currency) | ✅ max drift $0.00 (FX allow-listed) | ✅ Δ $0.00 | ✅ Δ $0.00 | ✅ Δ $0.00 |

Both Gate 4 results match Xero web BS PDF to the cent.

JDS Gate 5 BS evidence: 15 spot-checks all match Xero PDF to the cent (recorded in `RECONCILIATION-EVIDENCE.md`). Mastercard Aeris correctly persisted as `liability` per JDS layout (verifies 06D.1 hot-fix in production).

## What's left (Task 4 — operator-driven, ongoing)

Task 4 in the plan was "Production cutover (canary → full rollout)." That cutover is **already complete** — 06A through 06D-and-the-06D.1-hot-fix all merged to main and have been writing to production for days. The verify script (Task 3) is the ongoing health check; the runbook + rollback docs are the historical record.

Remaining ongoing work — none of it blocks 06F sign-off:

1. **Periodic regression check** — run `verify-production-migration.ts` weekly per tenant (or wire into a cron). Failures = immediate triage signal.
2. **Envisage verify run** — Envisage's connections show `is_active=false`; run with `--include-inactive` to spot-check.
3. **Gate 5 manual evidence** — `RECONCILIATION-EVIDENCE.md` has JDS BS rows complete; PL + TB rows for JDS, plus Envisage and IICT-HK rows, are operator-driven incremental.
4. **(Optional) feature flag for Step 8** — adding `XERO_SYNC_PATH_A` env var would make the orchestrator deploy flag-flippable rollback-able. Documented in `ROLLBACK-PROTOCOL.md` § Step 8 as a defensive future addition.

## Sign-off

> **Phase 44.2 06A-06F complete — production matches Xero to the cent.** All four reconciliation gates verified live against production for the canary (JDS) and multi-currency (IICT-HK) tenants. Path A per-month single-period architecture proven end-to-end (PL: $237k FY-YTD-Mar to the cent; BS: Net Assets == Equity to the cent across 3 month-ends; TB: Σ debit == Σ credit; per-account oracle agreement with documented allow-list for two known Xero quirks). Production verifier script + runbook + rollback protocol in place for ongoing health checks and future migrations of the same shape.
