---
phase: 71-month-end-reporting-code-fixes-production-bugs-calxa-parity-scope-gaps-snapshot-serializer-remap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/utils/vendor-normalization.ts
  - src/app/api/monthly-report/commentary/route.ts
  - src/__tests__/lib/vendor-normalization-roundtrip.test.ts
autonomous: true
requirements: [B2]
must_haves:
  truths:
    - "Only ONE definition of createVendorKey exists in the repo (src/lib/utils/vendor-normalization.ts)"
    - "Both subscription-detail and commentary routes use createVendorKey() for keying (so a budgeted vendor key matches an extracted Xero vendor key)"
    - "Round-trip vendor_name → vendor_key → display_name → vendor_key is idempotent"
    - "A regression test fails if anyone re-implements vendor normalization inline in a route"
  artifacts:
    - path: src/lib/utils/vendor-normalization.ts
      provides: "Canonical createVendorKey + extractVendorName + extractVendorInfo + VENDOR_MAPPINGS"
      contains: "export function createVendorKey"
    - path: src/__tests__/lib/vendor-normalization-roundtrip.test.ts
      provides: "Round-trip + single-source-of-truth tests"
      contains: "createVendorKey"
  key_links:
    - from: src/app/api/monthly-report/commentary/route.ts
      to: src/lib/utils/vendor-normalization.ts
      via: "import { extractVendorInfo, createVendorKey }"
      pattern: "createVendorKey"
    - from: src/app/api/monthly-report/subscription-detail/route.ts
      to: src/lib/utils/vendor-normalization.ts
      via: "import { extractVendorName, createVendorKey } (already exists)"
      pattern: "createVendorKey"
---

<objective>
B2 — Vendor-key normalization consolidation.

Purpose: subscription_budgets vendors never key-match against Xero-extracted vendors because the two routes used different normalization at different times. Make `createVendorKey` the single source of truth, ensure both monthly-report routes use it for keying, and lock the invariant with a regression test.

Output: Hardened vendor-normalization util (already exists from Phase 70 70-04) + commentary route using createVendorKey for keying + new regression test file that fails if anyone re-implements normalization inline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/71-month-end-reporting-code-fixes-production-bugs-calxa-parity-scope-gaps-snapshot-serializer-remap/71-CONTEXT.md

<interfaces>
From src/lib/utils/vendor-normalization.ts (confirmed via grep — already exists):
```typescript
export type VendorSource = 'contact' | 'description' | 'mapping'
export interface VendorInfo { vendor: string; context: string; source: VendorSource }
export const VENDOR_MAPPINGS: Record<string, string>
export function extractVendorInfo(contactName: string, description: string): VendorInfo
export function extractVendorName(contactName: string, description: string): string
export function createVendorKey(vendorName: string): string
```

Current state (verified 2026-05-31):
- subscription-detail/route.ts:6 — `import { extractVendorName, createVendorKey } from '@/lib/utils/vendor-normalization'` (already canonical for keying — uses createVendorKey at line 184).
- commentary/route.ts:6 — `import { extractVendorInfo } from '@/lib/utils/vendor-normalization'` (uses `info.vendor` as the aggregation key at lines 293+312 — NOT normalized via createVendorKey, so budget rows keyed by createVendorKey('Stripe Au') won't match extracted "Stripe Au"). **This is the actual B2 mismatch the audit identified.**
- Zero duplicate `function createVendorKey` definitions exist anywhere else (grep confirmed `:0` matches in both routes).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write round-trip + single-source-of-truth regression tests (RED)</name>
  <files>src/__tests__/lib/vendor-normalization-roundtrip.test.ts</files>
  <read_first>
    - src/lib/utils/vendor-normalization.ts (full file — understand createVendorKey + extractVendorName + extractVendorInfo + VENDOR_MAPPINGS)
    - src/__tests__/forecast/phase-51-helpers.test.ts (pattern reference for pure-helper vitest style in this repo)
  </read_first>
  <behavior>
    - Test 1: round-trip — for each vendor_name in a fixture array (`['Stripe Au', 'STRIPE AU PTY LTD', '  stripe  ', 'Paypal Australia 1043714034893']`), `createVendorKey(extractVendorName('', vendor_name))` returns the same value when re-run twice (idempotent).
    - Test 2: createVendorKey collapses whitespace + punctuation + case so `createVendorKey('Stripe Au')` === `createVendorKey('STRIPE AU')` === `createVendorKey('  stripe-au  ')`.
    - Test 3: single-source-of-truth invariant — grep-style fs.readFileSync of `src/app/api/monthly-report/subscription-detail/route.ts` and `src/app/api/monthly-report/commentary/route.ts` and assert NEITHER file contains a string match for `^(function|const) createVendorKey` (multiline regex). If a future executor inlines a duplicate, this test fails.
    - Test 4: extractVendorInfo + createVendorKey alignment — assert `createVendorKey(extractVendorInfo('Stripe Au', '').vendor)` === `createVendorKey('Stripe Au')` (so commentary keying matches subscription keying).
  </behavior>
  <action>
    Create `src/__tests__/lib/vendor-normalization-roundtrip.test.ts` with vitest describe block `'B2 — vendor-normalization single source of truth'`. Use `import { createVendorKey, extractVendorName, extractVendorInfo } from '@/lib/utils/vendor-normalization'` and `import { readFileSync } from 'node:fs'` + `import { resolve } from 'node:path'`.

    Test 3 regex (multiline + global): `/^\s*(function|const)\s+createVendorKey\b/gm`.

    All tests should be pure (no mocks, no network, no Supabase). Run via scoped vitest only.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/lib/vendor-normalization-roundtrip.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    Test file exists with 4 tests. Tests 1, 2, 3 PASS (createVendorKey is already canonical + no inline duplicates today). Test 4 FAILS because commentary route uses `info.vendor` not `createVendorKey(info.vendor)` for keying — Task 2 will make it pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate commentary route to use createVendorKey for keying (GREEN)</name>
  <files>src/app/api/monthly-report/commentary/route.ts</files>
  <read_first>
    - src/app/api/monthly-report/commentary/route.ts (lines 280-330 — addToVendor + extractVendorInfo call sites)
    - src/app/api/monthly-report/subscription-detail/route.ts (lines 180-220 — reference implementation that already uses createVendorKey for keying)
    - src/lib/utils/vendor-normalization.ts (full util)
  </read_first>
  <action>
    1. Update import on line 6 from `import { extractVendorInfo } from '@/lib/utils/vendor-normalization'` to `import { extractVendorInfo, createVendorKey } from '@/lib/utils/vendor-normalization'`.
    2. In `addToVendor(vendor: string, txn: VendorTransaction)` (line 280), change the map key from raw `vendor` to `createVendorKey(vendor)`. Keep the display value (`info.vendor`) intact for UI rendering — only the MAP KEY changes.
    3. Where `vendorData` is iterated later for sorting/output (look around line 326 `Array.from(vendorData.entries())`), the returned `vendor` field should remain the display name (preserve via storing display name alongside transactions in the map value, OR set display name on first insert and keep it).
    4. Verify B2 round-trip test (`src/__tests__/lib/vendor-normalization-roundtrip.test.ts` Test 4) now PASSES.
    5. Run the commentary route's existing unit tests if any exist; otherwise no other test regressions to worry about (route is integration-tested in production).
    Reference: per CONTEXT D-B2 — "Delete the duplicate in subscription-detail/route.ts and commentary/route.ts; import from the shared util." Since both files already import from the shared util, the residual B2 work is the keying mismatch in commentary (`info.vendor` not normalized via createVendorKey).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/lib/vendor-normalization-roundtrip.test.ts</automated>
  </verify>
  <done>
    All 4 tests in vendor-normalization-roundtrip.test.ts PASS. Commentary route uses createVendorKey for keying so a budget row keyed by createVendorKey('Stripe Au') matches an extracted Xero vendor keyed by createVendorKey('STRIPE AU'). Grep confirms ONLY ONE `createVendorKey` definition in the repo: `grep -rcn "^export function createVendorKey\|^function createVendorKey" src/ | grep -v ":0$" | wc -l` returns 1.
  </done>
</task>

</tasks>

<verification>
- `npx vitest run src/__tests__/lib/vendor-normalization-roundtrip.test.ts` — all tests pass.
- `grep -rn "function createVendorKey" src/` returns exactly ONE line (src/lib/utils/vendor-normalization.ts).
- `grep -n "createVendorKey" src/app/api/monthly-report/commentary/route.ts` returns at least 2 lines (import + use in addToVendor key).
- `npx tsc --noEmit` clean on touched files.
</verification>

<success_criteria>
- Round-trip + single-source-of-truth + keying-alignment tests all pass.
- Commentary route + subscription-detail route share the same vendor key scheme.
- Future inline createVendorKey duplicates will fail Test 3.
</success_criteria>

<output>
After completion, create `.planning/phases/71-month-end-reporting-code-fixes-production-bugs-calxa-parity-scope-gaps-snapshot-serializer-remap/71-01-SUMMARY.md`.
</output>
