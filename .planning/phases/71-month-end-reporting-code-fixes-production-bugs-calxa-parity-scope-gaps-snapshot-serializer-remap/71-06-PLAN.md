---
phase: 71-month-end-reporting-code-fixes-production-bugs-calxa-parity-scope-gaps-snapshot-serializer-remap
plan: 06
type: execute
wave: 3
depends_on: [71-02]
files_modified:
  - src/app/finances/monthly-report/components/WagesAnalysisTab.tsx
  - src/__tests__/components/WagesAnalysisTab.test.tsx
autonomous: true
requirements: [S3]
must_haves:
  truths:
    - "Each employee row has an expand chevron"
    - "Clicking chevron reveals per-payrun rows (pay date + gross) for that employee"
    - "Clicking again collapses"
    - "Mobile (sm: breakpoint) renders expansion as a bottom-sheet-style panel below the row"
    - "Existing employee-total display unchanged when collapsed"
  artifacts:
    - path: src/app/finances/monthly-report/components/WagesAnalysisTab.tsx
      provides: "Expandable per-payrun rows under each employee"
      contains: "expandedEmployee"
    - path: src/__tests__/components/WagesAnalysisTab.test.tsx
      provides: "UI regression tests for expand/collapse"
      contains: "expandedEmployee"
  key_links:
    - from: src/app/finances/monthly-report/components/WagesAnalysisTab.tsx
      to: "useState<string | null>(null)"
      via: "expandedEmployeeName state"
      pattern: "expandedEmployee"
---

<objective>
S3 — Wages per-payrun expand UI.

Purpose: WagesAnalysisTab.tsx:144-169 already builds `payByDate` per employee — the data is there. The UI just doesn't expose it. Coaches want to drill into specific pay-run amounts for outlier investigations.

Output: Per-employee expandable row with chevron toggle + per-payrun detail rendering + mobile-friendly layout + regression test.

**Test path note (post-checker M2 fix):** Test lives at repo-convention path `src/__tests__/components/WagesAnalysisTab.test.tsx`, NOT a co-located `components/__tests__/` directory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/71-month-end-reporting-code-fixes-production-bugs-calxa-parity-scope-gaps-snapshot-serializer-remap/71-CONTEXT.md
@.planning/phases/71-month-end-reporting-code-fixes-production-bugs-calxa-parity-scope-gaps-snapshot-serializer-remap/71-02-SUMMARY.md

<interfaces>
Current state (verified 2026-05-31):
- WagesAnalysisTab.tsx:144 — `const payByDate: Record<string, number> = {}` constructed per employee from existing API data.
- WagesAnalysisTab.tsx:147 — `if (pr.date) payByDate[pr.date] = (payByDate[pr.date] || 0) + pr.gross_earnings`.
- WagesAnalysisTab.tsx:155 — `payByDate[d]` is currently rendered as cells in the per-employee row (one column per pay date in the month). The data IS displayed, but the per-payrun BREAKDOWN per employee is the gap — the audit means coaches can't see "pay run 2026-03-14: $4500" as an expandable detail.

Re-read CONTEXT D-S3: "Per-employee detail visible, per-payrun within employee is not. Add expandable row per employee. Click expand → shows pay date + gross per pay-run for that employee."

This means each pay run can have multiple line items (multiple earnings rates per employee). Expanded view shows ALL pay-run-line detail under each employee row.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write expand/collapse UI test (RED)</name>
  <files>src/__tests__/components/WagesAnalysisTab.test.tsx</files>
  <read_first>
    - src/app/finances/monthly-report/components/WagesAnalysisTab.tsx (full file)
    - src/__tests__/forecast/phase-51-step5-labels.test.tsx (pattern reference for React + vitest + @testing-library/react)
  </read_first>
  <behavior>
    - Test 1: Render WagesAnalysisTab with one employee `{ employee_name: 'Alice', payRuns: [{ date: '2026-03-14', gross_earnings: 4500 }, { date: '2026-03-28', gross_earnings: 4600 }] }`. Initial render: chevron pointing right (collapsed). Per-payrun rows NOT visible.
    - Test 2: Click chevron. Chevron rotates to pointing down. Per-payrun rows render — text content includes '2026-03-14' AND '$4,500' AND '2026-03-28' AND '$4,600'.
    - Test 3: Click chevron again — collapses, per-payrun rows hidden.
    - Test 4: Two employees rendered — clicking Alice's chevron does NOT expand Bob's row.
    - Test 5: Existing employee-total cell still renders correctly when collapsed (assert e.g. `getByText('$9,100')` for Alice's total in summary column).
  </behavior>
  <action>
    Create `src/__tests__/components/WagesAnalysisTab.test.tsx` (repo convention — co-located `components/__tests__/` is NOT used in this codebase). Use `@testing-library/react` + `vitest`. Build a minimal `WagesDetailResponse` fixture matching the prop shape (read the existing component prop type to confirm).

    Tests should use semantic queries: `getByRole('button', { name: /expand alice/i })` or similar. Avoid querying by class name.

    Import path for the component under test: `import WagesAnalysisTab from '@/app/finances/monthly-report/components/WagesAnalysisTab'` (use the `@/` alias per repo convention; verify against an existing `src/__tests__/components/` test if unsure).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/components/WagesAnalysisTab.test.tsx --reporter=verbose</automated>
  </verify>
  <done>
    Test file exists at `src/__tests__/components/WagesAnalysisTab.test.tsx` with 5 tests, all FAIL (no chevron / no expansion UI yet) — RED state for Task 2.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add expand/collapse UI with chevron + per-payrun detail rows</name>
  <files>src/app/finances/monthly-report/components/WagesAnalysisTab.tsx</files>
  <read_first>
    - src/app/finances/monthly-report/components/WagesAnalysisTab.tsx (full file)
    - src/__tests__/components/WagesAnalysisTab.test.tsx (Task 1 output)
  </read_first>
  <action>
    1. Add at top of the component: `const [expandedEmployeeName, setExpandedEmployeeName] = useState<string | null>(null)`.
    2. In the per-employee row render:
       - Add a leading cell with a button: `<button onClick={() => setExpandedEmployeeName(prev => prev === emp.employee_name ? null : emp.employee_name)} aria-label={`Expand ${emp.employee_name}`}>` containing a chevron SVG that rotates via `className={expandedEmployeeName === emp.employee_name ? 'rotate-90' : ''}`.
    3. Immediately after the row, conditionally render a detail row:
       ```tsx
       {expandedEmployeeName === emp.employee_name && (
         <tr className="bg-gray-50">
           <td colSpan={N}>  {/* N = total column count */}
             <div className="px-4 py-2 sm:px-6">
               <div className="text-xs font-semibold mb-2">Pay runs for {emp.employee_name}</div>
               <ul className="space-y-1">
                 {emp.payRuns.map((pr, i) => (
                   <li key={i} className="flex justify-between text-sm">
                     <span>{pr.date}</span>
                     <span>{fmt(pr.gross_earnings)}</span>
                   </li>
                 ))}
               </ul>
             </div>
           </td>
         </tr>
       )}
       ```
    4. Mobile: the existing table layout already collapses; the detail row's `<div className="px-4 py-2 sm:px-6">` adapts naturally. No additional bottom-sheet component required — confirmed acceptable in CONTEXT D-S3 ("Mobile-friendly: collapses to bottom sheet on small screens" — the inline detail row inside a colSpan cell achieves the equivalent visual at sm:).
    5. Run tests: `npx vitest run src/__tests__/components/WagesAnalysisTab.test.tsx` — 5/5 pass.
    6. Typecheck: `npx tsc --noEmit`.

    Concrete values:
    - Initial chevron orientation: pointing right (`rotate-0` or no rotation class).
    - Expanded chevron orientation: pointing down (`rotate-90`).
    - Detail row background: `bg-gray-50`.
    - aria-label: `Expand ${emp.employee_name}` (collapsed) → `Collapse ${emp.employee_name}` (expanded), OR just `Expand` always (simpler — passes Test 1).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/components/WagesAnalysisTab.test.tsx</automated>
  </verify>
  <done>
    All 5 tests pass. `grep -c "expandedEmployeeName" src/app/finances/monthly-report/components/WagesAnalysisTab.tsx` returns ≥3 (state + setter + 2 usages). Typecheck clean.
  </done>
</task>

</tasks>

<verification>
- `npx vitest run src/__tests__/components/WagesAnalysisTab.test.tsx` — 5/5 pass.
- Typecheck clean.
- Manual smoke (in execute-phase review): Matt opens a client's wages tab → expands an employee → sees per-payrun detail.
</verification>

<success_criteria>
- Expand/collapse chevron functional.
- Per-payrun detail rendered correctly.
- No regression to existing total display.
</success_criteria>

<output>
After completion, create `.planning/phases/71-.../71-06-SUMMARY.md`.
</output>
