---
phase: 71-month-end-reporting-code-fixes-production-bugs-calxa-parity-scope-gaps-snapshot-serializer-remap
plan: 05
type: execute
wave: 3
depends_on: [71-01]
files_modified:
  - src/app/api/monthly-report/subscription-detail/route.ts
  - src/app/finances/monthly-report/components/SubscriptionAnalysisTab.tsx
  - src/__tests__/api/subscription-detail-budget-only.test.ts
autonomous: true
requirements: [S2]
must_haves:
  truths:
    - "Budgeted vendors with zero transactions in the month appear in the response with actual=0, prior_actual=null, transaction_count=0"
    - "These vendors are visually flagged with a 'not billed this month' badge in the UI"
    - "Vendors with actual transactions render unchanged (no regression)"
  artifacts:
    - path: src/app/api/monthly-report/subscription-detail/route.ts
      provides: "Response includes budget-only vendors with $0 actuals"
      contains: "transaction_count"
    - path: src/app/finances/monthly-report/components/SubscriptionAnalysisTab.tsx
      provides: "Badge UI for $0-actual budget-only vendors"
      contains: "not billed this month"
    - path: src/__tests__/api/subscription-detail-budget-only.test.ts
      provides: "Regression test for budget-only vendor visibility"
      contains: "budget-only"
  key_links:
    - from: src/app/finances/monthly-report/components/SubscriptionAnalysisTab.tsx
      to: src/app/api/monthly-report/subscription-detail/route.ts
      via: "vendor.transaction_count === 0 → render badge"
      pattern: "transaction_count.*0"
---

<objective>
S2 — Subscription budget-only vendors visible.

Purpose: subscription-detail/route.ts filters out vendor rows with zero current-month transactions. Budget-only vendors (budgeted but not billed this month, e.g. annual subs in their off-month) become invisible — the coach can't tell if a subscription is genuinely $0 or just unmapped.

Fix per CONTEXT D-S2: render budgeted vendors with `actual=$0, prior_actual=null, transaction_count=0`, flag visually with a "not billed this month" badge.

Output: Route returns full vendor set including zero-actual budget rows + UI badge for unbilled-this-month + regression test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/71-month-end-reporting-code-fixes-production-bugs-calxa-parity-scope-gaps-snapshot-serializer-remap/71-CONTEXT.md
@.planning/phases/71-month-end-reporting-code-fixes-production-bugs-calxa-parity-scope-gaps-snapshot-serializer-remap/71-01-SUMMARY.md

<interfaces>
Current state (verified 2026-05-31):
- subscription-detail/route.ts:181 — `addLineItem(accountCode, vendorName, amount, isCurrent)` populates `vendorData` Map keyed by createVendorKey.
- subscription-detail/route.ts:406 — final response construction filters out zero-actual rows (the bug per CONTEXT).
- subscription_budgets table — `{ vendor_name, account_code, frequency, monthly_amount, renewal_month, is_active }` (vendor_name is the budgeted display name; createVendorKey(vendor_name) is its key).
- SubscriptionAnalysisTab.tsx — renders the vendor table; needs a badge cell for transaction_count === 0.
- After 71-01, both routes use createVendorKey for keying so budget rows match Xero-extracted rows by key.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write regression test for budget-only vendor visibility (RED)</name>
  <files>src/__tests__/api/subscription-detail-budget-only.test.ts</files>
  <read_first>
    - src/app/api/monthly-report/subscription-detail/route.ts (full file — focus on lines 380-450 response build)
    - src/lib/utils/vendor-normalization.ts (createVendorKey behavior)
  </read_first>
  <behavior>
    - Test 1 (budget-only vendor appears): Mock Xero returns ZERO BankTransactions for account 415. subscription_budgets has one row `{ vendor_name: 'Lastpass', account_code: '415', monthly_amount: 25, frequency: 'monthly', is_active: true }`. Call route → response includes one vendor under account 415 with `{ vendor_name: 'Lastpass', actual: 0, prior_actual: null, transaction_count: 0, budget: 25 }`.
    - Test 2 (existing transacted vendors preserved): Mock Xero returns 1 SPEND BankTransaction `{ Contact: { Name: 'Stripe Au' }, LineItems: [{ AccountCode: '440', LineAmount: 50 }] }` AND subscription_budgets has `{ vendor_name: 'Stripe Au', account_code: '440', monthly_amount: 50, ... }`. Response includes one Stripe vendor with `actual: 50, transaction_count: 1`.
    - Test 3 (mixed — budget-only + transacted): subscription_budgets has 2 rows (Lastpass on 415 with no tx, Stripe on 440 with tx). Response includes BOTH vendors.
    - Test 4 (unbudgeted Xero vendor still visible — no regression): Xero returns a SPEND tx for vendor 'Adobe' on account 440 but no subscription_budgets row for Adobe. Response includes Adobe (actual=50, transaction_count=1, budget=0 OR undefined).
  </behavior>
  <action>
    Create `src/__tests__/api/subscription-detail-budget-only.test.ts`. Mock Supabase + Xero token-manager + fetch (Xero API calls). Import `POST` from `@/app/api/monthly-report/subscription-detail/route`. Build mocks per the existing route's expected Supabase queries (subscription_budgets select, monthly_report_settings select if used, etc. — read route to identify).

    For Xero mock: `vi.stubGlobal('fetch', vi.fn().mockImplementation((url) => { ... }))` returning canned JSON per URL pattern (Bank Transactions endpoint).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/api/subscription-detail-budget-only.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    Test file exists with 4 tests. Test 1 + 3 FAIL (route filters out zero-actual rows). Tests 2 + 4 PASS (existing behavior). RED state for Task 2.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Stop filtering zero-actual budgeted vendors + add UI badge</name>
  <files>
    src/app/api/monthly-report/subscription-detail/route.ts
    src/app/finances/monthly-report/components/SubscriptionAnalysisTab.tsx
  </files>
  <read_first>
    - src/app/api/monthly-report/subscription-detail/route.ts (lines 380-450 — response construction)
    - src/app/finances/monthly-report/components/SubscriptionAnalysisTab.tsx (full file — locate vendor row render)
    - src/__tests__/api/subscription-detail-budget-only.test.ts (Task 1 output)
  </read_first>
  <action>
    1. Route changes (`subscription-detail/route.ts`):
       a. Before the response construction (~line 406), iterate over `subscription_budgets` rows for active accounts. For each budget row:
          - Compute `budgetKey = createVendorKey(row.vendor_name)`.
          - Look up `vendorData.get(row.account_code)?.get(budgetKey)`. If absent, insert a zero-actual entry: `{ vendor_name: row.vendor_name, actual: 0, prior_actual: null, transaction_count: 0 }`.
       b. Add `transaction_count` field to the vendor object — increment in `addLineItem` (currently only tracks `actual`/`prior_actual`).
       c. Add `budget` field on each vendor in the response — populate from the subscription_budgets row when matched, else 0/undefined.
       d. REMOVE the existing filter that drops zero-actual rows (whatever line 406-area is doing — preserve only rows with NEITHER actual NOR budget if any such filter is desirable).

    2. UI changes (`SubscriptionAnalysisTab.tsx`):
       - Locate the vendor row render block (where vendor.vendor_name + actual are displayed).
       - Add badge UI when `vendor.transaction_count === 0 && vendor.budget > 0`:
         ```tsx
         {vendor.transaction_count === 0 && vendor.budget > 0 && (
           <span className="ml-2 px-1.5 py-0.5 text-xs bg-amber-100 text-amber-800 rounded">
             not billed this month
           </span>
         )}
         ```
       - Update the row's CSS to dim slightly when zero-actual (e.g. `text-gray-500` on the amount cell) for visual differentiation.

    3. Type updates: extend the existing vendor response type in the route (or the shared `useSubscriptionDetail` hook return type) to include `transaction_count: number` and `budget?: number`.

    4. Run tests: `npx vitest run src/__tests__/api/subscription-detail-budget-only.test.ts` — all 4 pass.
    5. Typecheck: `npx tsc --noEmit`.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/api/subscription-detail-budget-only.test.ts</automated>
  </verify>
  <done>
    All 4 tests pass. `grep -c "transaction_count" src/app/api/monthly-report/subscription-detail/route.ts` ≥ 1. `grep -c "not billed this month" src/app/finances/monthly-report/components/SubscriptionAnalysisTab.tsx` returns 1. Typecheck clean.
  </done>
</task>

</tasks>

<verification>
- `npx vitest run src/__tests__/api/subscription-detail-budget-only.test.ts` — 4/4 pass.
- `grep -n "not billed this month" src/app/finances/monthly-report/components/SubscriptionAnalysisTab.tsx` returns ≥1 line.
- Typecheck clean on touched files.
</verification>

<success_criteria>
- Budget-only vendors visible in response with explicit zero markers.
- UI badge renders for those vendors.
- Transacted vendors unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/71-.../71-05-SUMMARY.md`.
</output>
