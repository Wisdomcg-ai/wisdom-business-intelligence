---
phase: 71-month-end-reporting-code-fixes-production-bugs-calxa-parity-scope-gaps-snapshot-serializer-remap
plan: 03
type: execute
wave: 2
depends_on: [71-01]
files_modified:
  - src/app/finances/monthly-report/page.tsx
  - src/app/finances/monthly-report/components/ReconciliationGate.tsx
  - src/app/finances/monthly-report/hooks/useAutoSaveReport.ts
  - src/__tests__/app/proceed-as-draft-persistence.test.ts
autonomous: true
requirements: [B3]
must_haves:
  truths:
    - "Clicking 'Generate Draft Report' creates a row in monthly_report_snapshots with status='draft' immediately (before any commentary is typed)"
    - "After the immediate save, auto-save UPDATES that row instead of creating a new one"
    - "User sees a 'Saved as draft' toast on the initial draft save"
    - "Closing tab + reopening finds snapshot at status='draft' (verified by integration test that hits /api/monthly-report/snapshot)"
    - "Existing reconciled-finalize happy path still works unchanged"
  artifacts:
    - path: src/app/finances/monthly-report/page.tsx
      provides: "handleGenerateReport(true) path that saves immediately"
      contains: "status: 'draft'"
    - path: src/__tests__/app/proceed-as-draft-persistence.test.ts
      provides: "Integration test simulating draft → reload → snapshot exists"
      contains: "proceed-as-draft"
  key_links:
    - from: src/app/finances/monthly-report/page.tsx
      to: src/app/api/monthly-report/snapshot/route.ts
      via: "saveSnapshot(report, { status: 'draft' }) called immediately after handleGenerateReport(true)"
      pattern: "saveSnapshot.*status.*draft"
---

<objective>
B3 — Proceed-as-Draft persistence.

Purpose: Today, clicking "Generate Draft Report" (ReconciliationGate.tsx:81) generates the report in memory only. Auto-save (useAutoSaveReport) watches commentary state — empty on fresh draft — so no save ever fires. Closing the tab loses the report and the coach has to regenerate from scratch.

Fix per CONTEXT D-B3: on Proceed-as-Draft click → immediately POST snapshot at status='draft' → show "Saved as draft" toast → subsequent auto-saves update that row.

Output: Modified handleGenerateReport path that saves immediately when forceDraft=true + integration test hitting the real API route + the snapshot table.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/71-month-end-reporting-code-fixes-production-bugs-calxa-parity-scope-gaps-snapshot-serializer-remap/71-CONTEXT.md
@.planning/phases/71-month-end-reporting-code-fixes-production-bugs-calxa-parity-scope-gaps-snapshot-serializer-remap/71-01-SUMMARY.md

<interfaces>
Current state (verified 2026-05-31):
- page.tsx:650 — `const handleGenerateReport = useCallback(async (forceDraft?: boolean) => { ... })`.
- page.tsx:1123 — `<ReconciliationGate ... onProceedDraft={() => handleGenerateReport(true)} />`.
- page.tsx:705 — `const handleSaveSnapshot = async (status: 'draft' | 'final' = 'draft') => { ... await saveSnapshot(report, { status, generatedBy: userId, commentary }) ... }`.
- useAutoSaveReport.ts:64 — `export function useAutoSaveReport(...)` — exposes `{ status, schedule, flushImmediately, retryNow }`.
- /api/monthly-report/snapshot/route.ts:74 — UPSERT semantics: `.upsert({ business_id, report_month, fiscal_year, status, ... }, { onConflict: 'business_id,report_month' })` (verify exact onConflict key during execution).
- ReconciliationGate.tsx:81 — `<button onClick={onProceedDraft}>Generate Draft Report</button>`.
- Toast: `import { toast } from 'sonner'` (already imported at page.tsx:13).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write integration test for proceed-as-draft persistence (RED)</name>
  <files>src/__tests__/app/proceed-as-draft-persistence.test.ts</files>
  <read_first>
    - src/app/finances/monthly-report/page.tsx (lines 650-735 — handleGenerateReport, handleSaveSnapshot, autoSave setup)
    - src/app/api/monthly-report/snapshot/route.ts (full file — POST upsert semantics)
    - src/app/finances/monthly-report/hooks/useAutoSaveReport.ts (full file — when schedule() fires saveSnapshot)
    - src/__tests__/api/ (browse 2-3 existing integration tests for vitest patterns hitting Next.js route handlers directly)
  </read_first>
  <behavior>
    - Test 1 (route-level integration, no React): Build a minimal POST request to the imported `POST` handler of `/api/monthly-report/snapshot/route.ts` with `{ business_id, report_month, fiscal_year, status: 'draft', is_draft: true, report_data: <stub>, summary: <stub> }`. Mock the Supabase client at the module level. Assert the upsert was called with `status: 'draft'` and the returned snapshot has `status === 'draft'`.
    - Test 2 (idempotency): Call the POST handler twice with the same `(business_id, report_month, fiscal_year)`. Assert upsert (not insert) — i.e., the second call produces a single row, not a duplicate. Use a Supabase mock that tracks call count.
    - Test 3 (status preservation): Call POST with `status: 'final'` after a previous `status: 'draft'`. Assert the row's status flips to 'final' (no constraint blocks the transition).
    Note: Pure E2E with browser would require Playwright infra. Per CONTEXT deep_work_rules: "an integration test that hits the API endpoint directly and verifies snapshot row exists is acceptable." Use this approach.
  </behavior>
  <action>
    Create `src/__tests__/app/proceed-as-draft-persistence.test.ts`. Mock `@/lib/supabase` (or whichever module the snapshot route imports) using `vi.mock(...)`. Import `POST` from `@/app/api/monthly-report/snapshot/route`. Construct a `Request` via `new Request('http://test/api/monthly-report/snapshot', { method: 'POST', body: JSON.stringify({ ... }) })` and call `POST(req as any)`.

    Use the existing snapshot tables Phase 42 patterns — look at any existing snapshot test for the Supabase-mock shape. If no test exists, mock minimum: `from().upsert().select().single()` chain returning `{ data: { id, status, report_month, ... }, error: null }`.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/app/proceed-as-draft-persistence.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    Test file exists with 3 tests. Tests verify the snapshot route correctly upserts at status='draft' and preserves idempotency. Tests should PASS even before Task 2 if the snapshot route already supports `status='draft'` upsert (it does — confirmed). The Task 2 change is on the CALLER side (page.tsx), not the route.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire immediate snapshot save on Proceed-as-Draft click + toast</name>
  <files>src/app/finances/monthly-report/page.tsx</files>
  <read_first>
    - src/app/finances/monthly-report/page.tsx (lines 650-735 — handleGenerateReport + handleSaveSnapshot)
    - src/app/finances/monthly-report/hooks/useMonthlyReport.ts (lines 340-400 — saveSnapshot wrapper)
  </read_first>
  <action>
    Modify `handleGenerateReport` (page.tsx:650) — after the existing `result = await generateReport(...)` + the snapshot/commentary load that follows. When `forceDraft === true`, IMMEDIATELY persist a draft snapshot:

    ```typescript
    // B3: Proceed-as-Draft persistence — save snapshot immediately so closing
    // the tab doesn't lose the report (auto-save can't fire on empty commentary).
    if (forceDraft && result && !('needsMappings' in result)) {
      try {
        await saveSnapshot(result, {
          status: 'draft',
          generatedBy: userId,
          commentary: persistedCommentary,  // may be undefined on fresh draft — that's fine
        })
        setLoadedSnapshotStatus('draft')
        toast.success('Saved as draft')
      } catch (err) {
        console.error('[MonthlyReport] B3 draft persistence failed', err)
        toast.error('Draft generated but not saved — refresh to retry')
      }
    }
    ```

    Wire-up notes:
    - `saveSnapshot` is already destructured from `useMonthlyReport` (page.tsx:172/428 reference).
    - `userId` is already in scope (used at line 708).
    - `persistedCommentary` is the variable used at line 668 — keep that exact ref.
    - DO NOT change the non-forceDraft path (the existing reconciled-and-finalise flow stays untouched per CONTEXT D-B3 "No regression to existing happy-path").
    - The `toast` import already exists at page.tsx:13.
    - Update `useCallback` deps array to include `saveSnapshot` + `userId`.

    Idempotency: the snapshot route uses upsert on `(business_id, report_month, fiscal_year)` so multiple consecutive `handleGenerateReport(true)` clicks just update the same row.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/app/proceed-as-draft-persistence.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>
    `grep -n "B3: Proceed-as-Draft" src/app/finances/monthly-report/page.tsx` returns 1 line. `grep -c "Saved as draft" src/app/finances/monthly-report/page.tsx` returns ≥1. `grep -c "saveSnapshot" src/app/finances/monthly-report/page.tsx` increased by 1 vs baseline. Typecheck clean. Manual smoke test path (for Matt during phase review): click "Generate Draft Report" before commentary entry → see "Saved as draft" toast → refresh page → snapshot loads.
  </done>
</task>

</tasks>

<verification>
- `npx vitest run src/__tests__/app/proceed-as-draft-persistence.test.ts` — all pass.
- `grep -n "B3" src/app/finances/monthly-report/page.tsx` shows the inline marker comment.
- `npx tsc --noEmit` clean.
- Manual smoke (in execute-phase review): generate draft → close tab → reopen → snapshot present.
</verification>

<success_criteria>
- Draft saves immediately on click, before any commentary entry.
- Toast confirms save to user.
- Idempotent upsert (multiple clicks don't create duplicates).
- Reconciled-finalize happy path unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/71-.../71-03-SUMMARY.md`.
</output>
