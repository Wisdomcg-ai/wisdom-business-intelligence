---
phase: 71-month-end-reporting-code-fixes-production-bugs-calxa-parity-scope-gaps-snapshot-serializer-remap
plan: 04
type: execute
wave: 3
depends_on: [71-01, 71-03]
files_modified:
  - src/app/finances/monthly-report/page.tsx
  - src/app/api/monthly-report/commentary/route.ts
  - src/__tests__/api/commentary-trigger-expansion.test.ts
autonomous: true
requirements: [S1]
must_haves:
  truths:
    - "Commentary fires for expense over-budget ≥$500 (existing — unchanged)"
    - "Commentary fires for revenue under-budget where shortfall ≥$500 OR ≥10% of budget"
    - "Commentary fires for large favourable expense variance ≥$500 under-budget AND ≥20% of budget"
    - "Commentary fires for BS movements where MoM change ≥ $5,000 OR ≥10% of opening balance"
    - "Each commentary row carries a trigger_reason field naming WHY it appeared"
  artifacts:
    - path: src/app/finances/monthly-report/page.tsx
      provides: "Expanded trigger collection (revenue + favourable expense + BS movements)"
      contains: "trigger_reason"
    - path: src/app/api/monthly-report/commentary/route.ts
      provides: "Accepts expanded line-set payload (expense_lines + revenue_lines + bs_lines + favourable_lines) and emits trigger_reason"
      contains: "trigger_reason"
    - path: src/__tests__/api/commentary-trigger-expansion.test.ts
      provides: "Regression tests for each trigger type"
      contains: "trigger_reason"
  key_links:
    - from: src/app/finances/monthly-report/page.tsx
      to: src/app/api/monthly-report/commentary/route.ts
      via: "POST /api/monthly-report/commentary with expanded payload"
      pattern: "expense_lines.*revenue_lines.*bs_lines"
---

<objective>
S1 — Commentary trigger expansion.

Purpose: Today commentary fires only on expense `variance_amount <= -500` (page.tsx:605). Calxa parity requires commentary on revenue shortfalls, large favourable expense swings, and significant balance-sheet movements. Each commentary row should also name WHY it appeared (`trigger_reason`).

Output: Page-side trigger collection extended to 4 trigger types + commentary route accepts the new line-sets and tags every commentary row with `trigger_reason` + regression tests for each trigger type.

**Wave note (post-checker B1 fix):** Moved to wave 3 with depends_on [71-01, 71-03] because both 71-03 (B3 handleGenerateReport edit ~L650) and this plan (S1 trigger collection ~L595-650) modify adjacent regions in `src/app/finances/monthly-report/page.tsx`. Sequencing avoids merge conflict.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/71-month-end-reporting-code-fixes-production-bugs-calxa-parity-scope-gaps-snapshot-serializer-remap/71-CONTEXT.md
@.planning/phases/71-month-end-reporting-code-fixes-production-bugs-calxa-parity-scope-gaps-snapshot-serializer-remap/71-01-SUMMARY.md
@.planning/phases/71-month-end-reporting-code-fixes-production-bugs-calxa-parity-scope-gaps-snapshot-serializer-remap/71-03-SUMMARY.md

<interfaces>
Current state (verified 2026-05-31):
- page.tsx:600-650 — collects `expense_lines: { account_name, xero_account_name }[]` where `variance_amount <= -500 && !is_budget_only && section.category in ['Operating Expenses', 'Cost of Sales', 'Other Expenses']`.
- page.tsx:617 — POSTs `{ business_id, report_month, expense_lines }` to `/api/monthly-report/commentary`.
- commentary/route.ts:91 — `export async function POST(request)` parses `{ business_id, report_month, expense_lines }`.
- commentary/route.ts:280 — `addToVendor(vendor, txn)` (modified in 71-01 to use createVendorKey).
- types.ts VarianceCommentary — `{ [accountName: string]: { vendor_summary, coach_note?, is_edited?, trigger_reason? } }` (extend type to add optional trigger_reason).
- Balance sheet data — comes via `useBalanceSheet` hook; report.sections doesn't include BS lines (BS is its own section). Need to fetch BS lines from the BS hook results when present.
- 71-03 will have added B3 immediate-draft-save block in handleGenerateReport (~L650). Read that file in current state before editing; do not touch the B3 marker block.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write regression tests for each S1 trigger type (RED)</name>
  <files>src/__tests__/api/commentary-trigger-expansion.test.ts</files>
  <read_first>
    - src/app/finances/monthly-report/page.tsx (lines 595-660 — trigger collection; note: 71-03's B3 block may have shifted line numbers — search for the trigger collection by content not line number)
    - src/app/api/monthly-report/commentary/route.ts (full file, especially the POST handler signature)
    - src/app/finances/monthly-report/types.ts (VarianceCommentary type definition)
  </read_first>
  <behavior>
    - Test 1 (trigger collector — pure helper from Task 2): `collectCommentaryTriggers(reportData, balanceSheet?)` returns:
      - expense_lines containing only expense rows with variance_amount ≤ -500 (existing trigger).
      - revenue_lines containing revenue rows where (budget - actual ≥ 500) OR ((budget - actual) / budget ≥ 0.10).
      - favourable_expense_lines containing expense rows where variance_amount ≥ 500 (i.e. actual < budget by ≥$500) AND variance_amount / budget ≥ 0.20.
      - bs_lines containing BS rows where abs(mom_change) ≥ 5000 OR abs(mom_change) / abs(opening_balance) ≥ 0.10.
    - Test 2 (existing trigger preserved): a report with one expense line at variance_amount = -600 still produces expense_lines with that one entry.
    - Test 3 (revenue shortfall — dollar trigger): revenue budget=10000, actual=9000 (shortfall=$1000, 10%) → revenue_lines includes it with reason='revenue_under_budget_dollar'.
    - Test 4 (revenue shortfall — percent trigger): revenue budget=2000, actual=1700 (shortfall=$300 < $500 BUT 15% > 10%) → revenue_lines includes it with reason='revenue_under_budget_percent'.
    - Test 5 (favourable expense): expense budget=10000, actual=7000 (variance=+3000, 30% favourable) → favourable_expense_lines includes it with reason='expense_favourable_significant'.
    - Test 6 (favourable but small %): expense budget=100000, actual=99000 (variance=+1000, 1%) → NOT included.
    - Test 7 (BS movement — dollar trigger): BS line MoM change = $7000 → bs_lines includes it with reason='bs_movement_dollar'.
    - Test 8 (BS movement — percent trigger): BS line opening=10000 closing=11200 (change=$1200 = 12%) → bs_lines includes with reason='bs_movement_percent'.
    - Test 9 (commentary route accepts new payload shape): POST `{ business_id, report_month, expense_lines, revenue_lines, favourable_expense_lines, bs_lines }` returns success — assert each commentary row in the response includes `trigger_reason`.
  </behavior>
  <action>
    Create test file with 9 vitest tests. Tests 1-8 are pure (no network — import `collectCommentaryTriggers` from a new pure helper module Task 2 will create at `src/app/finances/monthly-report/utils/commentary-triggers.ts`). Test 9 mocks Supabase + Xero fetch and calls the route's `POST` handler directly (same pattern as 71-03's integration test).

    Fixture: build a minimal `reportData` object matching `GeneratedReport` shape (sections: [{ category: 'Revenue', lines: [...] }, { category: 'Operating Expenses', lines: [...] }]) and a minimal `balanceSheet` array (`{ account_name, opening_balance, closing_balance }[]`).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/api/commentary-trigger-expansion.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    Test file exists with 9 tests, all FAIL (helper doesn't exist yet, route doesn't emit trigger_reason yet) — RED state for Task 2.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement collectCommentaryTriggers helper + update page + route (GREEN)</name>
  <files>
    src/app/finances/monthly-report/utils/commentary-triggers.ts
    src/app/finances/monthly-report/page.tsx
    src/app/api/monthly-report/commentary/route.ts
    src/app/finances/monthly-report/types.ts
  </files>
  <read_first>
    - src/app/finances/monthly-report/page.tsx (current state of trigger collection block — search for `expense_lines` and `variance_amount <= -500`; line numbers will have shifted post-71-03)
    - src/app/finances/monthly-report/types.ts (VarianceCommentary type)
    - src/app/finances/monthly-report/hooks/useBalanceSheet.ts (shape of BS data exposed to page)
    - src/__tests__/api/commentary-trigger-expansion.test.ts (Task 1 output)
  </read_first>
  <action>
    1. Create `src/app/finances/monthly-report/utils/commentary-triggers.ts` exporting `collectCommentaryTriggers(reportData: GeneratedReport, balanceSheet?: BSLine[]): TriggerPayload`:

       ```typescript
       export type TriggerReason =
         | 'expense_over_budget_dollar'
         | 'revenue_under_budget_dollar'
         | 'revenue_under_budget_percent'
         | 'expense_favourable_significant'
         | 'bs_movement_dollar'
         | 'bs_movement_percent'

       export interface TriggerLine {
         account_name: string
         xero_account_name: string
         trigger_reason: TriggerReason
       }

       export interface TriggerPayload {
         expense_lines: TriggerLine[]
         revenue_lines: TriggerLine[]
         favourable_expense_lines: TriggerLine[]
         bs_lines: TriggerLine[]
       }
       ```

       Thresholds (locked per CONTEXT D-S1):
       - Revenue: shortfall ≥ $500 OR shortfall / budget ≥ 0.10 (whichever fires).
       - Favourable expense: variance ≥ $500 AND variance / budget ≥ 0.20 (both required).
       - BS: |MoM change| ≥ $5000 OR |MoM change| / |opening_balance| ≥ 0.10.
       - Existing expense over-budget: variance_amount ≤ -500 (unchanged).
       - Skip `is_budget_only` rows.

    2. Update `src/app/finances/monthly-report/types.ts` `VarianceCommentary` entry type to include optional `trigger_reason?: TriggerReason`.

    3. Refactor page.tsx (locate by content — current trigger collection block):
       - Replace inline trigger collection with `const triggers = collectCommentaryTriggers(reportData, balanceSheetData)`.
       - Get `balanceSheetData` from the existing `useBalanceSheet` hook return (already destructured higher up in page.tsx — grep `useBalanceSheet` to find).
       - Send `{ business_id, report_month, ...triggers }` in the POST body.
       - Empty-state check now: `if (triggers.expense_lines.length === 0 && triggers.revenue_lines.length === 0 && triggers.favourable_expense_lines.length === 0 && triggers.bs_lines.length === 0)`.
       - **DO NOT touch the `// B3: Proceed-as-Draft persistence` block added by 71-03.** Edit only the trigger collection region.

    4. Refactor commentary/route.ts POST handler:
       - Accept `{ business_id, report_month, expense_lines, revenue_lines, favourable_expense_lines, bs_lines }` (all four arrays optional, defaulting to `[]`).
       - Build a single `allLines` map of `accountName → trigger_reason` (priority: expense_over_budget > revenue > favourable > bs — first wins on conflict but practically these don't collide).
       - For each commentary row produced, add `trigger_reason` field from `allLines[accountName]`.
       - Backward-compatible: if only `expense_lines` is sent (old client), still works — the other arrays default to `[]`.

    5. Run scoped tests:
       - `npx vitest run src/__tests__/api/commentary-trigger-expansion.test.ts` — 9/9 pass.
       - `npx tsc --noEmit` — clean.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/api/commentary-trigger-expansion.test.ts</automated>
  </verify>
  <done>
    All 9 tests pass. `grep -c "trigger_reason" src/app/api/monthly-report/commentary/route.ts` ≥ 1. `grep -c "collectCommentaryTriggers" src/app/finances/monthly-report/page.tsx` returns 1. Backward-compat: old `{ expense_lines }`-only payload still produces commentary (revenue/favourable/bs default to []). B3 marker comment `// B3: Proceed-as-Draft persistence` still present in page.tsx.
  </done>
</task>

</tasks>

<verification>
- `npx vitest run src/__tests__/api/commentary-trigger-expansion.test.ts` — 9/9 pass.
- `npx tsc --noEmit` clean.
- `grep -rn "trigger_reason" src/` returns hits in commentary-triggers.ts + types.ts + route.ts.
- `grep -c "B3: Proceed-as-Draft" src/app/finances/monthly-report/page.tsx` returns 1 (71-03 work preserved).
</verification>

<success_criteria>
- 4 trigger types fire correctly with documented thresholds.
- Each commentary row carries trigger_reason.
- Existing expense-over-$500 trigger preserved.
- Pure helper unit-testable + route backward-compatible.
- No regression to 71-03's B3 immediate-draft-save block.
</success_criteria>

<output>
After completion, create `.planning/phases/71-.../71-04-SUMMARY.md`.
</output>
