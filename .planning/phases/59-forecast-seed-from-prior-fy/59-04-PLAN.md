---
phase: 59-forecast-seed-from-prior-fy
plan: 04
type: execute
wave: 4
depends_on: ["59-01", "59-02", "59-03"]
files_modified:
  - src/lib/services/__tests__/forecast-seed-service.integration.test.ts
  - .planning/phases/59-forecast-seed-from-prior-fy/59-VERIFICATION.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "Integration test: given a fixture prior-FY assumptions JSONB, calling seedForecastFromPrior produces an object that, when passed through convertAssumptionsToPLLines with target FY's forecastStartMonth/forecastEndMonth, generates pl_lines whose forecast_months keys are all in the target FY range (no leakage of prior-FY months)"
    - "Integration test: round-trip through 59-01 then convertAssumptionsToPLLines preserves the SUM of revenue values across the 12 months (no values lost in transformation)"
    - "Integration test: idempotency — isForecastSeedable(seededAssumptions, generatedPlLines.length) === false after a seed (you can't reseed your own output)"
    - "Console-error budget check: `grep -rc 'console\\.error' src/app/api/forecast/` returns same value as pre-Phase-59 baseline = 5 (PHASE.md success criterion 6)"
    - "Build + typecheck + vitest + lint all green: `npm run build`, `npx tsc --noEmit`, `npx vitest run`, `npm run lint` each exit 0 (PHASE.md success criterion 7)"
    - "Wizard hydration design-verified: code tracing confirms `startFresh=true` clears localStorage (research Q1, useForecastWizard.ts:392-402) and ForecastWizardV4 loadData() then hydrates state from `forecast.assumptions` via `/api/forecast/{id}`. End-to-end UI hydration is verified by the manual smoke checklist in VERIFICATION.md §Manual Smoke (NOT by automated test in 59-04 — that would require React component testing infra not in scope)."
    - "VERIFICATION.md file exists in phase dir documenting: full success-criteria map (all 7 PHASE.md criteria checked), pl-lines round-trip evidence, console.error count, build/typecheck/test/lint statuses, and a manual smoke-test checklist for JDS preview deploy"
  artifacts:
    - path: src/lib/services/__tests__/forecast-seed-service.integration.test.ts
      provides: "Integration tests that exercise 59-01 service + convertAssumptionsToPLLines together (the actual seed pipeline 59-02 runs)"
      min_lines: 120
    - path: .planning/phases/59-forecast-seed-from-prior-fy/59-VERIFICATION.md
      provides: "PHASE.md success-criteria audit + manual smoke checklist for JDS preview deploy"
      min_lines: 80
  key_links:
    - from: "integration test"
      to: "src/lib/services/forecast-seed-service.ts"
      via: "import { seedForecastFromPrior }"
      pattern: "from.*forecast-seed-service"
    - from: "integration test"
      to: "src/app/finances/forecast/services/assumptions-to-pl-lines.ts"
      via: "import { convertAssumptionsToPLLines }"
      pattern: "convertAssumptionsToPLLines"
    - from: "59-VERIFICATION.md"
      to: "PHASE.md §Success criteria"
      via: "7-row table mapping each criterion to evidence"
      pattern: "Success criterion"
---

<objective>
Close the phase with integration tests that exercise the actual seed pipeline (59-01 transformer + `convertAssumptionsToPLLines` → the exact two-step chain 59-02's route runs), plus a verification document that maps every PHASE.md success criterion to concrete evidence and provides a manual smoke checklist for the JDS preview deploy.

This plan replaces the originally-planned standalone 59-04 (wizard hydration handshake) — research §Q1 confirmed the handshake is already solved by `startFresh=true` in `useForecastWizard.ts:392-402`. The wizard-hydration verification collapses to a single integration-test assertion + a manual smoke step.

Purpose: Catch the seam between 59-01's pure transformer and 59-02's RPC payload before production. If the transformer's shifted month keys don't match what `convertAssumptionsToPLLines` produces, pl_lines would still write but with mismatched month ranges. Unit tests in 59-01 can't catch this — it requires both modules running together.

Output: One integration test file + one VERIFICATION.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/59-forecast-seed-from-prior-fy/PHASE.md
@.planning/phases/59-forecast-seed-from-prior-fy/59-RESEARCH.md
@.planning/phases/59-forecast-seed-from-prior-fy/59-01-PLAN.md
@.planning/phases/59-forecast-seed-from-prior-fy/59-02-PLAN.md
@.planning/phases/59-forecast-seed-from-prior-fy/59-03-PLAN.md

<interfaces>
<!-- convertAssumptionsToPLLines ConvertContext (assumptions-to-pl-lines.ts:26-33) -->

```typescript
export interface ConvertContext {
  assumptions: ForecastAssumptions
  forecastStartMonth: string  // e.g. "2026-07"
  forecastEndMonth: string    // e.g. "2027-06"
  fiscalYear: number          // e.g. 2027
  forecastDuration: number    // 1, 2, or 3
  existingLines: PLLine[]
}
```

<!-- Wizard hydration chain (research §Q1, ForecastWizardV4.tsx:378-493) -->
1. page.tsx sets startFresh=true → wizard mounts
2. useForecastWizard's useState initializer (line 392-402) synchronously removes localStorage key
3. createInitialState produces empty wizard state
4. ForecastWizardV4 mount effect runs loadData() (lines 378-493)
5. loadData fetches /api/forecast/{forecastId} → reads forecast.assumptions (the seeded JSONB)
6. Wizard state populates from seeded assumptions
</interfaces>

@src/lib/services/forecast-seed-service.ts
@src/app/finances/forecast/services/assumptions-to-pl-lines.ts
@src/app/finances/forecast/components/wizard-v4/ForecastWizardV4.tsx
@src/app/finances/forecast/components/wizard-v4/useForecastWizard.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write integration test exercising seed transformer + pl-lines generator together</name>
  <files>src/lib/services/__tests__/forecast-seed-service.integration.test.ts</files>
  <read_first>
    - src/lib/services/forecast-seed-service.ts (the actual 59-01 implementation, not just the plan)
    - src/app/finances/forecast/services/assumptions-to-pl-lines.ts (full file — specifically the convertAssumptionsToPLLines entrypoint at line 626, plus generateMonthRange at line 39 for assertion utility)
    - src/lib/services/__tests__/forecast-seed-service.test.ts (the unit tests from 59-01 — reuse the makeFixture() helper pattern)
    - src/app/api/forecast/seed-from-prior/route.ts (from 59-02 — the integration test must mirror the exact transform → generate sequence the route performs)
    - .planning/phases/59-forecast-seed-from-prior-fy/59-RESEARCH.md (§"Common Pitfalls" #2 and #5)
  </read_first>
  <behavior>
    Test cases (all should pass once 59-01 + the existing convertAssumptionsToPLLines work together correctly):

    A. Month-range alignment (the critical seam):
    - Given prior FY26 assumptions (fiscalYear=2026, forecast_months in 2025-07..2026-06)
    - Seed to targetFY=2027 via seedForecastFromPrior
    - Pass result through convertAssumptionsToPLLines with { forecastStartMonth: '2026-07', forecastEndMonth: '2027-06', fiscalYear: 2027, forecastDuration: 1, existingLines: [] }
    - Assert: every line's `forecast_months` object has keys ONLY in the set generateMonthRange('2026-07', '2027-06') (i.e., 12 keys all between 2026-07 and 2027-06 inclusive)
    - Assert: ZERO keys in the 2025-07..2026-06 range (no leakage of prior-FY months into the target)

    B. Sum preservation:
    - Given a prior FY26 revenue line with year1Monthly summing to 600000 across 12 months
    - After seed + convertAssumptionsToPLLines for FY27 (duration=1)
    - Assert: the Revenue category line's forecast_months sum equals 600000 (within $1 tolerance for rounding)

    C. Multi-year forecast carry:
    - Given prior FY26 with forecast_duration=2 and year1Monthly + year2Monthly populated
    - Seed to targetFY=2027 (priorForecastDuration=2 passed in)
    - Assert: result.forecastDuration === 2
    - Pass through convertAssumptionsToPLLines with forecastDuration=2, forecastStartMonth='2026-07', forecastEndMonth='2028-06'
    - Assert: pl_lines have month keys spanning the full 24-month range (2026-07..2028-06), zero in 2025-07..2026-06

    D. Idempotency after seed (closes the loop with 59-01's isForecastSeedable):
    - After running A's pipeline, call isForecastSeedable(seededAssumptions, generatedPlLines.length)
    - Assert: returns false (because revenue.lines.length > 0 OR plLineCount > 0 — you can't reseed your own output)

    E. CapEx + Goals exclusion through full pipeline:
    - Prior assumptions have non-empty capex.items and goals
    - After seed + convertAssumptionsToPLLines, check that pl_lines contain NO category === 'CapEx' AND NO category === 'Goals' lines (research-confirmed exclusions)
    - Note: assumptions-to-pl-lines may not emit CapEx/Goals categories at all by default; this test verifies the integration produces a CapEx/Goals-free output regardless of which layer enforces it

    F. expectedMonths integration:
    - Prior opex includes an adhoc line with expectedMonths=['2025-10', '2026-03']
    - After seed, the seeded assumptions opex.lines[N].expectedMonths === ['2026-10', '2027-03']
    - When the seeded assumptions feed convertAssumptionsToPLLines, the resulting OpEx line for that adhoc has forecast_months populated at 2026-10 and 2027-03 (not 2025-10/2026-03)

    G. team.plannedHires cleared (decision 1 verified through full pipeline):
    - Prior team has plannedHires=[{ name: 'Bob', startMonth: '2025-10', ... }]
    - After seed, seededAssumptions.team.plannedHires === []
    - convertAssumptionsToPLLines output for the team/wages category does NOT include any line tracing back to "Bob" (the planned hire is gone, only existingTeam remains)
  </behavior>
  <action>
    Create `src/lib/services/__tests__/forecast-seed-service.integration.test.ts`.

    Imports:
    ```typescript
    import { describe, it, expect } from 'vitest'
    import { seedForecastFromPrior, isForecastSeedable } from '../forecast-seed-service'
    import { convertAssumptionsToPLLines, generateMonthRange } from '@/app/finances/forecast/services/assumptions-to-pl-lines'
    import type { ForecastAssumptions } from '@/app/finances/forecast/components/wizard-v4/types/assumptions'
    ```

    At the top of the file build a `makeFY26Fixture()` helper that returns a realistic prior-FY assumptions object. The fixture must include:
    - fiscalYearStart: '07'
    - revenue.lines: 2 lines
      - Line 1: year1Monthly with 12 keys 2025-07..2026-06 each set to 50000 (sum = 600000)
      - Line 2: year1Monthly with sparse 6 keys (every other month) totaling 120000
    - cogs.lines: 1 line with year1Monthly across the same 12 months
    - opex.lines: 3 lines — 1 normal, 1 percentage-of-revenue, 1 adhoc with expectedMonths=['2025-10', '2026-03']
    - team.existingTeam: 2 entries
    - team.plannedHires: 1 entry { name: 'Bob', startMonth: '2025-10', annualSalary: 80000 }
    - team.superannuationPct/workCoverPct/payrollTaxPct: realistic values
    - capex.items: 1 entry
    - goals: a populated goals object
    - version: 11

    Then write all tests in Groups A-G per the behavior block, using the fixture as input. For Group A, build the expected key set via `new Set(generateMonthRange('2026-07', '2027-06'))` and assert every line's forecast_months keys are subsets.

    The test file is integration-level — do not mock `convertAssumptionsToPLLines`. Run the real function. The 59-01 transformer is also called for real (no mock).

    Run the suite. If any test fails:
    - If failure reveals a real defect in 59-01: STOP and surface to the operator before patching (this is the integration seam — defects here mean unit tests missed something).
    - If failure reveals a fixture issue: fix the fixture, not the production code.

    Commit: `test(59-04): integration tests for seed transformer + pl-lines pipeline`.
  </action>
  <verify>
    <automated>npx vitest run src/lib/services/__tests__/forecast-seed-service.integration.test.ts</automated>
  </verify>
  <done>
    All Group A-G tests pass. The test file has at least 10 distinct `it(...)` cases. The fixture realistically represents a JDS-style portfolio business (multi-line revenue, mixed opex frequencies, planned hire, capex, goals). Commit created.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Run full quality gates and write 59-VERIFICATION.md mapping each PHASE.md success criterion to evidence</name>
  <files>.planning/phases/59-forecast-seed-from-prior-fy/59-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/59-forecast-seed-from-prior-fy/PHASE.md (§Success criteria — the 7 testable outcomes)
    - .planning/phases/59-forecast-seed-from-prior-fy/59-01-SUMMARY.md (if exists from 59-01 execution)
    - .planning/phases/59-forecast-seed-from-prior-fy/59-02-SUMMARY.md (if exists from 59-02 execution)
    - .planning/phases/59-forecast-seed-from-prior-fy/59-03-SUMMARY.md (if exists from 59-03 execution)
    - .planning/phases/53-xero-connection-durability/53-05-SUMMARY.md (reference for what a phase-closing summary looks like — STATE.md cites this verbose entry)
  </read_first>
  <action>
    Step 1 — Run quality gates and capture their output for the doc:

    ```bash
    # Each in its own command so failures are isolated
    npx tsc --noEmit > /tmp/59-tsc.log 2>&1 ; echo "tsc exit: $?"
    npx vitest run > /tmp/59-vitest.log 2>&1 ; echo "vitest exit: $?"
    npm run lint > /tmp/59-lint.log 2>&1 ; echo "lint exit: $?"
    npm run build > /tmp/59-build.log 2>&1 ; echo "build exit: $?"
    grep -rc "console\.error" src/app/api/forecast/ | awk -F: '{s+=$2} END {print "console.error count in src/app/api/forecast/: " s}'
    ```

    Capture each exit code and the console.error count. The console.error count MUST be 5 (the Phase 46 baseline, per PHASE.md success criterion 6). If it is >5, identify the new ones via:
    ```bash
    git diff main -- src/app/api/forecast/ | grep "+.*console\.error" | head -5
    ```
    And remove them before proceeding.

    Step 2 — Run the full vitest suite for the phase's added/modified test files explicitly:
    ```bash
    npx vitest run src/lib/services/__tests__/forecast-seed-service.test.ts src/lib/services/__tests__/forecast-seed-service.integration.test.ts src/app/api/forecast/seed-from-prior/__tests__/route.test.ts
    ```
    Capture the pass count.

    Step 3 — Write `.planning/phases/59-forecast-seed-from-prior-fy/59-VERIFICATION.md` with this structure (use exact section headings):

    ```markdown
    # Phase 59 Verification

    **Phase:** 59 — Forecast Seed from Prior FY
    **Verified:** {date}
    **Status:** {PASS|FAIL — be specific about which criteria failed}

    ## Success Criteria Audit

    | # | Criterion (from PHASE.md) | Evidence | Status |
    |---|---|---|---|
    | 1 | Empty state shows two CTAs (Start blank + Seed from FY{prior}) when prior-FY forecast exists | 59-03 ForecastEmptyState.tsx grep confirms onSeedForecast prop renders dual buttons | PASS |
    | 2 | Clicking "Seed from FY{prior}" creates a FY27 forecast pre-populated with revenue/COGS/OpEx/team/subscriptions; CapEx + Goals empty | Integration test §C + §E + §G in forecast-seed-service.integration.test.ts | PASS |
    | 3 | Wizard opens on FY27 with values visible in Step 3 (Revenue/COGS), Step 4 (Team), Step 5 (OpEx), Step 6 (Subscriptions) | Manual smoke checklist (below) — required on preview deploy before phase closes | PENDING-SMOKE |
    | 4 | Edit-and-save in wizard works normally; localStorage / DB sync not broken by seed | Research §Q1 confirms `startFresh=true` clears localStorage atomically; 59-03 wires this | PASS-by-design |
    | 5 | Re-running seed against a forecast that already has data is refused with a clear error | 59-02 route.test.ts §E (idempotency tests return 409); 59-01 isForecastSeedable unit tests | PASS |
    | 6 | console.error count in src/app/api/forecast/ does not regress from Phase 46 baseline (5) | `grep -rc console.error src/app/api/forecast/` = {captured value} | PASS if =5 |
    | 7 | Vercel build + typecheck + vitest + lint all green | tsc exit {N}, vitest exit {N}, lint exit {N}, build exit {N} | PASS if all 0 |

    ## Test Run Summary

    - **59-01 unit tests:** {N} passed in forecast-seed-service.test.ts
    - **59-02 route tests:** {N} passed in seed-from-prior/__tests__/route.test.ts
    - **59-04 integration tests:** {N} passed in forecast-seed-service.integration.test.ts
    - **Full vitest suite:** {N} files, {N} tests passed (from /tmp/59-vitest.log tail)

    ## Console.error Budget

    | Path | Baseline (Phase 46) | Current | Delta |
    |---|---|---|---|
    | src/app/api/forecast/ | 5 | {captured} | {delta} |

    {If delta > 0: list the new console.error sites and remove them before closing the phase.}

    ## Critical Decisions Audit (2026-05-11)

    | Decision | Plan | Evidence |
    |---|---|---|
    | D1: Clear plannedHires (don't shift forward) | 59-01 | Integration test §G; unit test Group B in forecast-seed-service.test.ts |
    | D2: Wizard starts on Step 1 (Goals), NOT Step 3 | 59-03 | `grep "setWizardStartStep(1)" src/app/finances/forecast/page.tsx` matches in handleSeedForecast |
    | D3: forecastDuration COPIED from prior | 59-01 + 59-02 | Unit test Group F in forecast-seed-service.test.ts; 59-02 route writes target row's forecast_duration column |

    ## Scope-Correction Audit (from research, applied during planning)

    | Scope correction | Applied where | Evidence |
    |---|---|---|
    | localStorage handshake already solved by startFresh=true (no new plan needed) | 59-03 reuses it | `grep -A2 "startFresh" src/app/finances/forecast/components/wizard-v4/useForecastWizard.ts` shows the synchronous removeItem block at line 392-402 |
    | subscription_budgets NOT year-scoped; no writes from seed endpoint | 59-02 | `grep -c subscription_budgets src/app/api/forecast/seed-from-prior/route.ts` returns 0 |
    | Reuse convertAssumptionsToPLLines + save_assumptions_and_materialize RPC | 59-02 | route.ts uses both — `grep -c "save_assumptions_and_materialize\|convertAssumptionsToPLLines" src/app/api/forecast/seed-from-prior/route.ts` returns ≥2 |

    ## Manual Smoke Checklist (Required on JDS Preview Deploy Before Phase Close)

    Run on a Vercel preview build, against JDS (or a JDS-fixture business) with FY26 forecast already populated:

    - [ ] Open `/finances/forecast` while logged in as the JDS coach
    - [ ] Verify the empty state shows BOTH buttons: "Seed from FY26 forecast" (primary, orange, Sparkles icon) AND "Start FY27 blank" (secondary, outline)
    - [ ] Click "Seed from FY26 forecast"
    - [ ] Wait for the seed POST to complete (button shows "Seeding…")
    - [ ] Verify the wizard opens on Step 1 (Goals) — NOT Step 3 (this is critical decision D2)
    - [ ] Verify Step 1 (Goals) is BLANK (goals were stripped from the seed)
    - [ ] Navigate to Step 3 (Revenue & COGS) — verify revenue lines and COGS lines are pre-populated with FY27 month keys (2026-07..2027-06)
    - [ ] Spot-check one revenue line's monthly values — they should equal the FY26 values shifted by 12 months
    - [ ] Navigate to Step 4 (Team) — existingTeam pre-populated, plannedHires empty (critical decision D1)
    - [ ] Navigate to Step 5 (OpEx) — opex lines pre-populated, adhoc expectedMonths shifted by 12 months
    - [ ] Navigate to Step 6 (Subscriptions) — subscriptions visible from subscription_budgets table (not from seed)
    - [ ] Click Save/Finish — wizard closes, return to `/finances/forecast`
    - [ ] Reload page — verify saved data persists (localStorage handshake didn't drop the seed)
    - [ ] Return to `/finances/forecast` → empty state is NO LONGER shown (because target FY now has data)
    - [ ] Re-invoke seed via direct POST to `/api/forecast/seed-from-prior` with curl → returns 409 (idempotency refusal)

    ## Sign-off

    - [ ] All success-criteria rows show PASS or PENDING-SMOKE
    - [ ] All quality gates exit 0
    - [ ] Manual smoke checklist completed on preview deploy
    - [ ] STATE.md updated with phase-59 entry
    - [ ] PR opened on `feat/59-forecast-seed-from-prior-fy`
    ```

    Step 4 — Fill in every {captured value} placeholder with real values from the logs captured in Step 1. Do NOT leave placeholder tokens in the final file.

    Step 5 — If any criterion is FAIL, do NOT close the phase. Surface the failure, fix it, and re-run the audit. If criterion 6 (console.error budget) fails, that's a code fix in whichever route added the new console.error.

    Step 6 — Commit: `docs(59-04): verification audit + manual smoke checklist`.
  </action>
  <verify>
    <automated>test -f .planning/phases/59-forecast-seed-from-prior-fy/59-VERIFICATION.md &amp;&amp; grep -c "PASS\|FAIL\|PENDING-SMOKE" .planning/phases/59-forecast-seed-from-prior-fy/59-VERIFICATION.md</automated>
  </verify>
  <done>
    `.planning/phases/59-forecast-seed-from-prior-fy/59-VERIFICATION.md` exists with all 7 success criteria audited. No `{captured value}` or `{date}` placeholder tokens remain. Console.error count = 5 (or documented deviation with rationale). Build/typecheck/vitest/lint exit codes all = 0 (recorded in the doc). Manual smoke checklist is complete (10+ items). Commit created.
  </done>
</task>

</tasks>

<verification>
1. `npx vitest run src/lib/services/__tests__/forecast-seed-service.integration.test.ts` — all pass.
2. `npx vitest run` (full suite) — exit 0.
3. `npx tsc --noEmit` — exit 0.
4. `npm run lint` — exit 0.
5. `npm run build` — exit 0.
6. `grep -rc "console\.error" src/app/api/forecast/ | awk -F: '{s+=$2} END {print s}'` returns 5 (no regression).
7. `test -f .planning/phases/59-forecast-seed-from-prior-fy/59-VERIFICATION.md` — file exists.
8. `grep -c "{captured value}\|{date}\|{N}" .planning/phases/59-forecast-seed-from-prior-fy/59-VERIFICATION.md` — returns 0 (no unfilled placeholders).
</verification>

<success_criteria>
- Integration test confirms 59-01 transformer + convertAssumptionsToPLLines produce coherent output (the actual pipeline 59-02 runs)
- All 7 PHASE.md success criteria audited against concrete evidence in VERIFICATION.md
- console.error baseline of 5 in src/app/api/forecast/ preserved (PHASE criterion 6)
- All quality gates (tsc/vitest/lint/build) green (PHASE criterion 7)
- Manual smoke checklist exists for the operator to run on JDS preview before closing
- The three critical decisions (D1, D2, D3) and the three scope corrections from research are explicitly audited
</success_criteria>

<output>
After completion, create `.planning/phases/59-forecast-seed-from-prior-fy/59-04-SUMMARY.md`. Record:
- Final test counts (unit + route + integration)
- Build/typecheck/vitest/lint exit codes
- Console.error budget evidence
- Manual smoke checklist completion status (PENDING until operator runs on preview)
- Any deviations from plan (e.g. tests that had to be relaxed, criteria that hit PENDING-SMOKE)
- Recommendation to operator: open PR on `feat/59-forecast-seed-from-prior-fy` once smoke is complete
</output>
</output>
