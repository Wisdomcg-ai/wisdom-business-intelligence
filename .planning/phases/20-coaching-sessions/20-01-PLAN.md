---
phase: 20-coaching-sessions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260407_fix_coaching_sessions_schema.sql
  - src/app/api/sessions/route.ts
  - src/app/api/sessions/[id]/route.ts
  - src/app/api/sessions/[id]/actions/route.ts
  - src/app/api/sessions/[id]/analyze-transcript/route.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "POST /api/sessions creates a coaching_sessions row without 400 error"
    - "POST /api/sessions/[id]/actions creates a session_actions row with correct column names"
    - "POST /api/sessions/[id]/analyze-transcript inserts action items using correct session_actions columns"
    - "GET /api/sessions/[id] returns session data without FK join errors"
    - "Coach schedule page loads coaching_sessions with session_type and prep_completed columns"
  artifacts:
    - path: "supabase/migrations/20260407_fix_coaching_sessions_schema.sql"
      provides: "Missing columns on coaching_sessions + strategic_initiative_id on session_actions"
      contains: "session_type"
    - path: "src/app/api/sessions/[id]/actions/route.ts"
      provides: "Fixed action creation API"
      contains: "session_note_id"
    - path: "src/app/api/sessions/[id]/analyze-transcript/route.ts"
      provides: "Fixed transcript analysis API"
      contains: "session_note_id"
    - path: "src/app/api/sessions/route.ts"
      provides: "Fixed session CRUD API"
      contains: "coaching_sessions"
  key_links:
    - from: "src/app/api/sessions/[id]/actions/route.ts"
      to: "session_actions table"
      via: "supabase insert with correct columns"
      pattern: "session_note_id.*description.*status.*pending.*action_number.*created_by"
    - from: "src/app/api/sessions/[id]/analyze-transcript/route.ts"
      to: "session_actions table"
      via: "supabase insert with correct columns"
      pattern: "session_note_id"
---

<objective>
Fix the coaching_sessions 400 error and reconcile all /api/sessions/ routes with the actual database schema.

Purpose: The /api/sessions/ routes are completely broken — they insert columns that do not exist (agenda, summary, session_metadata), use wrong column names (action_text instead of description, session_id instead of session_note_id), insert invalid enum values (open instead of pending), and omit required NOT NULL fields (action_number, created_by). The coaching_sessions table is also missing columns (session_type, prep_completed, session_metadata) that the schedule page expects. This plan fixes all of it.

Output: Working migration file + 4 fixed API route files that correctly interact with the database.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-coaching-sessions/20-RESEARCH.md

<interfaces>
<!-- coaching_sessions table (from 20251209_create_missing_tables.sql) — CURRENT schema -->
CREATE TABLE coaching_sessions (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  coach_id UUID NOT NULL REFERENCES auth.users(id),
  client_id UUID REFERENCES auth.users(id),
  business_id UUID REFERENCES businesses(id) ON DELETE CASCADE,
  title TEXT,
  description TEXT,
  scheduled_at TIMESTAMPTZ NOT NULL,
  duration_minutes INTEGER DEFAULT 60,
  status TEXT DEFAULT 'scheduled',  -- 'scheduled', 'completed', 'cancelled', 'no_show'
  meeting_url TEXT,
  location TEXT,
  notes TEXT,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);
-- MISSING: session_type, prep_completed, session_metadata, agenda, summary

<!-- session_actions table (from 20251129_session_actions.sql) — CURRENT schema -->
CREATE TABLE session_actions (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  session_note_id UUID REFERENCES session_notes(id) ON DELETE SET NULL,
  business_id UUID NOT NULL REFERENCES businesses(id) ON DELETE CASCADE,
  action_number INTEGER NOT NULL CHECK (action_number >= 1),
  description TEXT NOT NULL,
  due_date DATE,
  status TEXT NOT NULL DEFAULT 'pending' CHECK (status IN ('pending', 'completed', 'missed', 'carried_over')),
  completed_at TIMESTAMPTZ,
  follow_up_notes TEXT,
  reviewed_at TIMESTAMPTZ,
  reviewed_in_session_id UUID REFERENCES session_notes(id) ON DELETE SET NULL,
  carried_over_to_id UUID REFERENCES session_actions(id) ON DELETE SET NULL,
  carried_over_from_id UUID REFERENCES session_actions(id) ON DELETE SET NULL,
  created_by UUID NOT NULL REFERENCES auth.users(id),
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);
-- MISSING: strategic_initiative_id

<!-- Correct insert pattern from session detail page (src/app/coach/sessions/[id]/page.tsx) -->
await supabase
  .from('session_actions')
  .insert({
    session_note_id: sessionId,
    business_id: session.business_id,
    action_number: startNumber + index,
    description: action.description.trim(),
    due_date: action.due_date || null,
    status: 'pending',
    created_by: currentUserId
  })
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema migration — add missing columns to coaching_sessions and session_actions</name>
  <files>supabase/migrations/20260407_fix_coaching_sessions_schema.sql</files>
  <read_first>
    - supabase/migrations/20251209_create_missing_tables.sql (lines 575-614, coaching_sessions definition)
    - supabase/migrations/20251129_session_actions.sql (full file, session_actions definition)
    - src/app/coach/schedule/page.tsx (lines 57-73, see what columns the schedule page selects)
  </read_first>
  <action>
    Create a migration file at supabase/migrations/20260407_fix_coaching_sessions_schema.sql that:

    1. Adds missing columns to coaching_sessions:
       - session_type TEXT DEFAULT 'video' CHECK (session_type IN ('video', 'phone', 'in-person'))
       - prep_completed BOOLEAN DEFAULT FALSE
       - session_metadata JSONB DEFAULT '{}'::jsonb
       - agenda JSONB DEFAULT '[]'::jsonb
       - summary TEXT

    2. Adds strategic_initiative_id to session_actions:
       - strategic_initiative_id UUID REFERENCES strategic_initiatives(id) ON DELETE SET NULL
       - CREATE INDEX idx_session_actions_initiative ON session_actions(strategic_initiative_id)

    Use ADD COLUMN IF NOT EXISTS for all columns (idempotent).
    Use CREATE INDEX IF NOT EXISTS for the index.
    Add a comment header explaining this is for Phase 20 coaching sessions reconciliation.
  </action>
  <verify>
    <automated>grep -c "ADD COLUMN IF NOT EXISTS" supabase/migrations/20260407_fix_coaching_sessions_schema.sql</automated>
  </verify>
  <acceptance_criteria>
    - File contains exactly 6 ADD COLUMN IF NOT EXISTS statements (5 for coaching_sessions + 1 for session_actions)
    - session_type has CHECK constraint with video, phone, in-person
    - session_metadata defaults to empty JSONB object
    - agenda defaults to empty JSONB array
    - strategic_initiative_id references strategic_initiatives(id) ON DELETE SET NULL
    - Index created on session_actions(strategic_initiative_id)
  </acceptance_criteria>
  <done>Migration file exists with all 6 column additions and 1 index, all idempotent</done>
</task>

<task type="auto">
  <name>Task 2: Fix all 4 /api/sessions/ route files to match actual database schema</name>
  <files>
    src/app/api/sessions/route.ts
    src/app/api/sessions/[id]/route.ts
    src/app/api/sessions/[id]/actions/route.ts
    src/app/api/sessions/[id]/analyze-transcript/route.ts
  </files>
  <read_first>
    - src/app/api/sessions/route.ts (full file — POST inserts agenda which is now JSONB, verify it works)
    - src/app/api/sessions/[id]/route.ts (full file — GET joins session_actions via wrong FK, PUT uses summary)
    - src/app/api/sessions/[id]/actions/route.ts (full file — every column name is wrong)
    - src/app/api/sessions/[id]/analyze-transcript/route.ts (full file — inserts coaching_session_id, action_text, status open)
    - src/app/coach/sessions/[id]/page.tsx (lines 424-460 — reference correct insert pattern for session_actions)
  </read_first>
  <action>
    Fix each route file against the actual database schema:

    **A) /api/sessions/route.ts (POST handler):**
    The POST handler creates coaching_sessions rows. After the migration adds the agenda column (JSONB), the insert is mostly correct. But verify:
    - agenda is now JSONB so `agenda: agenda || []` is valid after migration
    - No other mismatched columns
    - The route is functional as-is after the migration adds the missing columns

    **B) /api/sessions/[id]/route.ts (GET handler):**
    Line 21-25: The GET handler selects `coaching_sessions` with `session_actions (*)` join. But `session_actions` has NO foreign key to `coaching_sessions` — the FK is `session_note_id` pointing to `session_notes`, not `coaching_sessions`. This join will silently return empty arrays or error.

    Fix: Remove the `session_actions (*)` join from the GET query entirely. coaching_sessions is for scheduling — it does not own session_actions. The query should just be:
    ```typescript
    .from('coaching_sessions')
    .select('*')
    .eq('id', sessionId)
    .single()
    ```

    Line 69 (PUT handler): The PUT handler accepts `summary` in the body and writes it to `coaching_sessions`. After the migration adds the `summary` column, this is valid. No change needed.

    **C) /api/sessions/[id]/actions/route.ts (POST handler) — CRITICAL FIXES:**
    Every column name is wrong. Rewrite the insert to match the actual session_actions schema:

    1. Change `action_text` to `description` in the request body destructuring (line 30): `const { description, due_date } = body`
    2. Change validation (line 32): `if (!description)` with error message `'description is required'`
    3. Fix the insert object (lines 38-46):
       - `session_id: sessionId` -> `session_note_id: sessionId` (the param ID should be a session_notes ID, not coaching_sessions ID — the route is used by session_notes detail page)
       - `action_text` -> `description`
       - Remove `assigned_to` (column does not exist on session_actions)
       - `status: 'open'` -> `status: 'pending'` (CHECK constraint only allows pending/completed/missed/carried_over)
       - Add `action_number`: compute as `(await supabase.from('session_actions').select('id', { count: 'exact', head: true }).eq('session_note_id', sessionId)).count + 1` or simpler: query max action_number for this session and add 1
       - Add `created_by: user.id` (NOT NULL required field)

    4. Also fix the session access check (lines 19-25): Currently queries `coaching_sessions` to get business_id and verify coach_id. Since this route is called with a session_notes ID, change to query `session_notes` instead:
       ```typescript
       const { data: session } = await supabase
         .from('session_notes')
         .select('business_id, coach_id')
         .eq('id', sessionId)
         .single()
       ```

    **D) /api/sessions/[id]/analyze-transcript/route.ts — CRITICAL FIXES:**

    1. Line 52-54: Session access check queries `coaching_sessions`. This route receives a coaching_sessions ID from the schedule page, so the access check is correct for its use case. Keep querying coaching_sessions.

    2. Lines 151-167: The session update writes `session_metadata` to `coaching_sessions`. After migration adds the column, this is valid. No change needed.

    3. Lines 179-190: The action items insert has wrong columns. Fix:
       - `coaching_session_id: sessionId` -> `session_note_id: null` (these AI-extracted actions are NOT linked to a session_notes row — they come from a coaching_sessions transcript analysis. Set to null since the FK is nullable.)
       - `action_text: item.action_text` -> `description: item.action_text` (keep the AI response field name but map to correct DB column)
       - `status: 'open'` -> `status: 'pending'`
       - Remove `priority: item.priority || 'medium'` (column does not exist on session_actions)
       - Add `action_number: index + 1` (loop index-based, since these are fresh AI-extracted items)
       - Add `created_by: user.id`
       - `business_id: session.business_id` stays correct

    The full fixed insert should be:
    ```typescript
    const { data: action, error: actionError } = await supabase
      .from('session_actions')
      .insert({
        session_note_id: null,
        business_id: session.business_id,
        action_number: index + 1,
        description: item.action_text,
        due_date: dueDate.toISOString(),
        status: 'pending',
        created_by: user.id
      })
      .select()
      .single()
    ```

    Change the for-loop to for...of with index or use forEach with index, since `index` is needed for action_number. Use `analysis.action_items.forEach((item, index) => {...})` or convert to a standard for loop with index.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "session_note_id" src/app/api/sessions/[id]/actions/route.ts returns >= 1
    - grep -c "description" src/app/api/sessions/[id]/actions/route.ts returns >= 2
    - grep -c "'pending'" src/app/api/sessions/[id]/actions/route.ts returns >= 1
    - grep -c "created_by" src/app/api/sessions/[id]/actions/route.ts returns >= 1
    - grep -c "action_number" src/app/api/sessions/[id]/actions/route.ts returns >= 1
    - grep -c "action_text" src/app/api/sessions/[id]/actions/route.ts returns 0 (removed)
    - grep -c "assigned_to" src/app/api/sessions/[id]/actions/route.ts returns 0 (removed)
    - grep -c "'open'" src/app/api/sessions/[id]/actions/route.ts returns 0 (removed)
    - grep -c "session_note_id" src/app/api/sessions/[id]/analyze-transcript/route.ts returns >= 1
    - grep -c "coaching_session_id" src/app/api/sessions/[id]/analyze-transcript/route.ts returns 0 (removed)
    - grep -c "created_by" src/app/api/sessions/[id]/analyze-transcript/route.ts returns >= 1
    - grep -c "session_actions" src/app/api/sessions/[id]/route.ts returns 0 (join removed from GET)
    - npx tsc --noEmit completes without errors in these 4 files
  </acceptance_criteria>
  <done>All 4 API routes use correct column names matching the actual session_actions and coaching_sessions schema. No action_text, no coaching_session_id, no status open, no assigned_to. TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
1. `grep -rn "action_text\|coaching_session_id\|'open'" src/app/api/sessions/` returns zero matches (all wrong column names removed)
2. `grep -rn "session_note_id\|created_by\|action_number\|'pending'" src/app/api/sessions/` confirms correct columns are used
3. `npx tsc --noEmit` passes with no errors in session route files
4. Migration file has all 6 ADD COLUMN statements
</verification>

<success_criteria>
- Migration file exists with all missing columns added idempotently
- All 4 API route files corrected to match actual database schema
- No references to non-existent columns (action_text, assigned_to, coaching_session_id)
- No invalid enum values (open)
- All required NOT NULL fields included (action_number, created_by)
- TypeScript compilation passes
</success_criteria>

<output>
After completion, create `.planning/phases/20-coaching-sessions/20-01-SUMMARY.md`
</output>
