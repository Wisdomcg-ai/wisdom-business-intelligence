---
phase: 44-test-gate-ci-hardening
plan: 02
type: execute
wave: 2
depends_on: ["44-01"]
files_modified:
  - next.config.js
autonomous: true
requirements_addressed: [TEST-02]

must_haves:
  truths:
    - "`next.config.js` no longer disables ESLint during builds"
    - "`npm run lint` exits 0 on the codebase"
    - "`npm run build` succeeds end-to-end (lint + tsc + bundle)"
    - "The two existing rule overrides in `.eslintrc.json` are preserved"
    - "The `src/middleware.ts` Edge-runtime import restriction still applies"
  artifacts:
    - path: "next.config.js"
      provides: "Next config WITHOUT eslint.ignoreDuringBuilds"
      min_lines: 100
  key_links:
    - from: "next build"
      to: ".eslintrc.json"
      via: "eslint-config-next runs lint as part of build (no longer suppressed)"
      pattern: "ignoreDuringBuilds"
---

<objective>
Remove the `eslint.ignoreDuringBuilds: true` flag from `next.config.js:4-6` so
that `next build` fails on lint violations. Then run `next lint` once and fix
whatever ESLint surfaces — likely a small set of `react/no-unescaped-entities`
warnings (already turned off) and possibly some `@next/next/no-img-element`
or unused-imports issues.

Purpose: TEST-02 — make ESLint a real merge gate. Today, lint errors silently
pass through `next build`. After this plan, a lint violation = failing build.

Output: `next.config.js` with no eslint suppression; `npm run lint` exits 0;
`npm run build` succeeds. Any source-file lint fixes are minimal and surgical.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/44-test-gate-ci-hardening/PHASE.md
@.planning/phases/44-test-gate-ci-hardening/44-01-SUMMARY.md
@CODEBASE-AUDIT.md
@next.config.js
@.eslintrc.json
@package.json

<interfaces>
<!-- Existing rules and overrides that MUST be preserved -->

From .eslintrc.json (must remain intact):
```json
{
  "extends": ["next/core-web-vitals"],
  "rules": {
    "react/no-unescaped-entities": "off",
    "@next/next/no-assign-module-variable": "off"
  },
  "overrides": [
    {
      "files": ["src/middleware.ts"],
      "rules": {
        "no-restricted-imports": [
          "error",
          {
            "paths": [
              { "name": "crypto", "message": "Edge runtime — use Web Crypto API" },
              { "name": "fs", "message": "Edge runtime — fs not available" },
              { "name": "path", "message": "Edge runtime — path not available" },
              { "name": "child_process", "message": "Edge runtime — not available" }
            ]
          }
        ]
      }
    }
  ]
}
```

From next.config.js (lines 1-7, the bit to delete):
```javascript
/** @type {import('next').NextConfig} */
const nextConfig = {
  // Suppress ESLint warnings during builds (they're shown during development)
  eslint: {
    ignoreDuringBuilds: true,
  },

  // Image optimization
  ...
}
```

From package.json scripts:
```json
"lint": "next lint",
"build": "next build"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Run baseline lint to inventory violations</name>
  <read_first>
    - next.config.js lines 1-10 (confirm the eslint block is at lines 4-6 as documented)
    - .eslintrc.json (full file — confirm rule overrides exist)
    - package.json scripts (confirm `lint: next lint`)
  </read_first>
  <files>(none — read-only inventory step; output captured in SUMMARY)</files>
  <action>
Run `npm run lint` BEFORE editing `next.config.js`. Capture the full output.

```bash
npm run lint 2>&amp;1 | tee /tmp/lint-baseline.txt
```

Read the output. Categorise each violation by rule (`@next/next/no-img-element`,
`react-hooks/exhaustive-deps`, `react/jsx-key`, etc.). Count total errors and
warnings.

If `npm run lint` reports zero errors AND zero warnings, proceed to Task 2 with
a note in the SUMMARY: "Baseline lint was already clean — no fixes needed."

If `npm run lint` reports errors: list each with file path and rule. Decide for
each whether to (a) fix the code, (b) add a focused `// eslint-disable-next-line
&lt;rule&gt;` with a one-line justification comment, or (c) escalate by adding the
rule to `.eslintrc.json`'s `rules` block as `"warn"` instead of `"error"`.

Prefer (a) — fix the code. Use (b) only when the violation is intentional
(e.g. a deliberate `&lt;img&gt;` for performance reasons in one specific component).
Use (c) only as a last resort, and ONLY for rules that are clearly noise
project-wide (e.g. `react-hooks/exhaustive-deps` if it floods the output with
50+ false positives across the whole forecast wizard — but try (a) first).

DO NOT remove or weaken the two existing overrides in `.eslintrc.json`:
- `react/no-unescaped-entities: off` (deliberate)
- `@next/next/no-assign-module-variable: off` (deliberate)

DO NOT touch the `src/middleware.ts` `no-restricted-imports` override — it
guards Edge-runtime safety.

DO NOT run `next lint --fix` blindly — review every auto-fix before staging.
The `--fix` flag can rewrite imports in ways that break tree-shaking.
  </action>
  <verify>
    <automated>test -s /tmp/lint-baseline.txt</automated>
  </verify>
  <acceptance_criteria>
    - `/tmp/lint-baseline.txt` exists and contains the full output of `npm run lint`
    - The SUMMARY records the count of errors and warnings, broken down by rule
    - No source files modified yet (this task is inventory-only — fixes happen in Task 2)
    - `.eslintrc.json` is unchanged
  </acceptance_criteria>
  <done>
    Lint baseline captured. Plan for fixes is decided per rule and recorded.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Remove ignoreDuringBuilds and fix lint fallout</name>
  <read_first>
    - /tmp/lint-baseline.txt (from Task 1)
    - next.config.js (the file being edited)
    - .eslintrc.json (the rules being respected)
    - Each source file flagged by the baseline (read before editing)
  </read_first>
  <files>next.config.js, &lt;source files flagged by Task 1 lint baseline&gt;</files>
  <action>
**Step A — Edit `next.config.js`:**

Delete lines 3-6 (the comment + the eslint block). The file should go from:

```javascript
/** @type {import('next').NextConfig} */
const nextConfig = {
  // Suppress ESLint warnings during builds (they're shown during development)
  eslint: {
    ignoreDuringBuilds: true,
  },

  // Image optimization
  images: {
```

to:

```javascript
/** @type {import('next').NextConfig} */
const nextConfig = {
  // Image optimization
  images: {
```

Leave every other section untouched: images, compress, poweredByHeader,
reactStrictMode, experimental, webpack, headers, the `withSentryConfig` wrapper,
all of it. Only the 4-line `eslint: { ignoreDuringBuilds: true }` block goes.

**Step B — Apply each fix decided in Task 1.**

For each violation in `/tmp/lint-baseline.txt`:

- If the fix is a code change: edit the source file. Use Edit tool. Verify
  the edit is minimal and preserves behaviour. Re-run `npm run lint` after
  every 5-10 fixes to confirm the count is decreasing.

- If the fix is a focused `// eslint-disable-next-line`: add it on the line
  ABOVE the violation, with a one-line comment explaining WHY the rule is
  being suppressed. Format:
  ```
  // eslint-disable-next-line @next/next/no-img-element -- Sentry-loaded preview, no Next/Image optimization needed
  &lt;img src={previewUrl} alt="Sentry preview" /&gt;
  ```

- If the fix is a `.eslintrc.json` rule downgrade: add to the `rules` block
  with a comment in the JSON5 style only if absolutely necessary. Document
  the rationale in the plan SUMMARY.

**Step C — Verify lint is clean:**

```bash
npm run lint            # must exit 0
```

**Step D — Verify build is clean:**

```bash
npm run build           # must exit 0; lint runs as part of build now
```

If `npm run build` fails on a lint error that `npm run lint` did not surface,
that's an ESLint config drift between the two — fix the source error and
investigate which preset is firing in build but not in `next lint` (likely a
Next.js core-web-vitals rule that's only enabled at build time).

DO NOT touch:
- `.eslintrc.json` rule overrides (the two `off` entries and the middleware override)
- `vitest.config.ts`
- Any test file (`*.test.ts`, `*.test.tsx`)
- Any source file NOT flagged by the lint baseline
- The Sentry config wrapper at the bottom of `next.config.js`
  </action>
  <verify>
    <automated>npm run lint &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ignoreDuringBuilds" next.config.js` returns 0
    - `grep -c "eslint:" next.config.js` returns 0 (the entire `eslint:` config block is gone)
    - `npm run lint` exits 0 with zero errors
    - `npm run build` exits 0 (a successful Next build prints `Compiled successfully` and does NOT print `Skipping linting`)
    - `npm test` still exits 0 (no test regression from any source-file edits)
    - `.eslintrc.json` rules block STILL contains both `"react/no-unescaped-entities": "off"` and `"@next/next/no-assign-module-variable": "off"` (verified by grep)
    - `.eslintrc.json` overrides block STILL contains the `src/middleware.ts` `no-restricted-imports` rule (verified by grep)
    - The Sentry `withSentryConfig` wrapper at the bottom of `next.config.js` is untouched (`grep -c "withSentryConfig" next.config.js` returns ≥ 1)
  </acceptance_criteria>
  <done>
    `next.config.js` no longer suppresses ESLint. `npm run lint` and `npm run
    build` both exit 0 on the current `main` codebase. Source-file edits are
    minimal and listed in the SUMMARY with rule + file + before/after.
  </done>
</task>

</tasks>

<verification>
End-of-plan verification (run all):

```bash
grep -c "ignoreDuringBuilds" next.config.js   # must return 0
grep -c "eslint:" next.config.js              # must return 0
npm run lint                                  # must exit 0
npm run build                                 # must exit 0
npm test                                      # must still exit 0 (no regressions)
grep -c "react/no-unescaped-entities" .eslintrc.json   # must return ≥ 1
grep -c "no-restricted-imports" .eslintrc.json         # must return ≥ 1
```
</verification>

<success_criteria>
TEST-02 satisfied: `next build` will fail on a lint violation, because the
`ignoreDuringBuilds` escape hatch is gone. Once Plan 44-03 wires `next lint` as
its own CI job, ESLint becomes a required merge gate.

Sentry, middleware Edge-runtime restrictions, and intentional rule overrides all preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/44-test-gate-ci-hardening/44-02-SUMMARY.md`
documenting:
- The lint baseline output (count of errors/warnings by rule)
- Every source-file fix applied (file:line + rule + before/after)
- Any `eslint-disable-next-line` additions and their justifications
- Any `.eslintrc.json` rule changes (should be none ideally)
- Confirmation that `npm run build` now lints as part of the build
</output>
