---
phase: 49-database-integrity-hygiene
plan: 03
type: execute
wave: 1
depends_on:
  - 44-05
files_modified:
  - supabase/migrations/20260504000001_db06_rls_policy_intent_documentation.sql
  - src/__tests__/migrations/db-06-rls-comments.test.ts
autonomous: true
requirements:
  - DB-06
gap_closure: false

must_haves:
  truths:
    - "All 3 over-permissive RLS policies (`swot_templates` 'Authenticated users can view swot templates', `kpi_benchmarks` 'kpi_benchmarks_select_consolidated', `kpi_definitions` 'kpi_definitions_select_consolidated') carry an explicit `COMMENT ON POLICY` recording intent."
    - "Each COMMENT begins with the sentinel string 'INTENT:' so future migrations / introspection scripts can detect the convention."
    - "No policy is narrowed in this plan — all 3 tables structurally have no tenant column (no business_id, no user_id, no creator column), so they are legitimately system-wide reference data per RESEARCH.md DB-06 schema inspection."
    - "If the operator decides during execution that a policy SHOULD be narrowed (i.e., reverses RESEARCH.md's recommendation), the plan halts and surfaces the schema-change requirement (adding a tenant column) as out-of-scope for this plan."
    - "Migration applies cleanly to a fresh local DB / preview branch."
  artifacts:
    - path: "supabase/migrations/20260504000001_db06_rls_policy_intent_documentation.sql"
      provides: "COMMENT ON POLICY statements for the 3 reference-data RLS policies, plus a migration-header explanation of why USING (true) is intentional and not a vulnerability."
      contains: "COMMENT ON POLICY"
    - path: "src/__tests__/migrations/db-06-rls-comments.test.ts"
      provides: "Vitest assertion that all 3 policies have a non-NULL intent comment containing the 'INTENT:' sentinel."
      contains: "INTENT:"
  key_links:
    - from: "supabase/migrations/20260504000001_db06_rls_policy_intent_documentation.sql"
      to: "pg_policy.polname (swot_templates, kpi_benchmarks, kpi_definitions)"
      via: "COMMENT ON POLICY <name> ON public.<table>"
      pattern: "COMMENT ON POLICY"
---

<objective>
DB-06 in one short PR. Independent of all other Phase 49 plans — can ship in Wave 1 alongside 49-01.

PHASE.md success criterion #5: *"The 3 `USING (true)` RLS policies (`swot_templates`, `kpi_benchmarks`, `kpi_definitions`) carry an explicit migration comment recording the intent (system reference data — open by design — vs per-business — narrowed). Any policy narrowed has a regression test confirming a non-owner cannot read another tenant's row."*

RESEARCH.md DB-06 schema inspection confirmed all 3 tables structurally have **no tenant column** (no `business_id`, no `user_id`, no `creator_id`). They are genuinely system-wide reference catalogues:
- `swot_templates`: industry-tagged SWOT prompt seeds (`industry`, `business_stage`, `category` columns)
- `kpi_benchmarks`: industry benchmark percentiles (`industry`, `revenue_stage` columns)
- `kpi_definitions`: universal KPI catalogue (text PKs like `gross_margin`, `cac`)

**Recommendation: comment-only migration.** Narrowing any of these to per-business would require first adding a `business_id` (or `creator_id`) column — that's a schema change PHASE.md says is out of scope. If the operator later wants per-business overrides (e.g., a coach uploads a custom SWOT template for their client), that's a future Phase, not this one.

No regression test required (per PHASE.md success criterion #5 wording — only narrowed policies need regression tests). The vitest test in this plan only asserts the COMMENTs exist with the `INTENT:` sentinel.

Output: 1 small SQL migration (3 COMMENT ON POLICY statements + a header explanation) + 1 introspection vitest.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/49-database-integrity-hygiene/PHASE.md
@.planning/phases/49-database-integrity-hygiene/RESEARCH.md
@supabase/migrations/00000000000000_baseline_schema.sql
@src/__tests__/migrations/06C-bs-schema-migration.test.ts

<interfaces>
**Three target policies (RESEARCH.md DB-06 lines 510-515):**

| Table | Policy name | File:line | Current `USING` |
|-------|-------------|-----------|-----------------|
| `swot_templates` | `Authenticated users can view swot templates` | baseline:9695 | `USING (true)` (SELECT, authenticated) |
| `kpi_benchmarks` | `kpi_benchmarks_select_consolidated` | baseline:12164 | `USING (true)` (SELECT, authenticated) |
| `kpi_definitions` | `kpi_definitions_select_consolidated` | baseline:12174 | `USING (true)` (SELECT, authenticated) |

**Schema confirmation that none of the 3 tables has a tenant column (RESEARCH.md DB-06 lines 522-577):**

`swot_templates` columns: `id, name, industry, business_stage, category, prompt_text, example_items, is_active, …` — no `business_id`, no `user_id`.

`kpi_benchmarks` columns: `id, kpi_id, industry, revenue_stage, benchmark_value, percentile, source, notes, business_size` — `industry` is a text label, not a tenant FK.

`kpi_definitions` columns: `id (text PK like 'gross_margin'), name, description, why_it_matters, what_to_do, category, business_function, industries (jsonb), unit, frequency, formula, is_universal, …` — `is_universal` is a flag, not a tenant.

**Sentinel SQL for verification (RESEARCH.md cross-cutting completeness section, Sentinel 4):**

```sql
SELECT n.nspname, c.relname, p.polname,
       obj_description(p.oid, 'pg_policy') AS intent_comment
FROM   pg_policy p
JOIN   pg_class c ON c.oid = p.polrelid
JOIN   pg_namespace n ON n.oid = c.relnamespace
WHERE  n.nspname = 'public'
  AND  c.relname IN ('swot_templates','kpi_benchmarks','kpi_definitions');
-- Expected: 3 rows, each with a non-NULL intent_comment containing 'INTENT:'.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD — write failing introspection test for the 3 policy intent COMMENTs</name>
  <files>src/__tests__/migrations/db-06-rls-comments.test.ts</files>
  <read_first>
    - src/__tests__/migrations/06C-bs-schema-migration.test.ts (skip-on-placeholder pattern, service-role client setup)
    - .planning/phases/49-database-integrity-hygiene/RESEARCH.md DB-06 sentinel SQL (lines 711-766)
  </read_first>
  <behavior>
    - Test 1: `swot_templates` policy `Authenticated users can view swot templates` has a non-NULL `obj_description(pg_policy.oid, 'pg_policy')` containing 'INTENT:'.
    - Test 2: `kpi_benchmarks` policy `kpi_benchmarks_select_consolidated` has the same.
    - Test 3: `kpi_definitions` policy `kpi_definitions_select_consolidated` has the same.
    - All 3 tests skip when `SUPABASE_URL` is unset or placeholder (CI default state).
  </behavior>
  <action>
    Create `src/__tests__/migrations/db-06-rls-comments.test.ts` following the 06C skip-on-placeholder pattern. The test iterates over the 3 (table, policy name) pairs and asserts the sentinel SQL above returns a non-NULL `intent_comment` containing the 'INTENT:' sentinel for each.

    Use whatever SQL-execution helper the project already exposes (matching `06C-bs-schema-migration.test.ts` style — do NOT invent a new helper). If `06C-bs-schema-migration.test.ts` uses raw `supabase.from('pg_policy')` style calls or an `execute_sql` RPC, reuse the same approach.

    Run the test against a Supabase preview branch with the migration NOT yet applied — the 3 tests should be RED (no COMMENTs exist on the baseline policies). In CI placeholder mode, the suite skips.
  </action>
  <verify>
    <automated>test -f src/__tests__/migrations/db-06-rls-comments.test.ts && grep -q "INTENT:" src/__tests__/migrations/db-06-rls-comments.test.ts && grep -q "swot_templates" src/__tests__/migrations/db-06-rls-comments.test.ts && grep -q "kpi_benchmarks" src/__tests__/migrations/db-06-rls-comments.test.ts && grep -q "kpi_definitions" src/__tests__/migrations/db-06-rls-comments.test.ts && npx vitest run src/__tests__/migrations/db-06-rls-comments.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test file exists with 3 assertions (one per policy).
    - Skip-on-placeholder pattern matches 06C convention.
    - Tests RED against an unmigrated preview branch; SKIP cleanly in CI placeholder mode.
  </acceptance_criteria>
  <done>RED: regression tests committed; failing as expected against unmigrated DB.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — author the DB-06 COMMENT-only migration</name>
  <files>supabase/migrations/20260504000001_db06_rls_policy_intent_documentation.sql</files>
  <read_first>
    - supabase/migrations/00000000000000_baseline_schema.sql lines 9695, 12164, 12174 (confirm policy names match exactly — case-sensitive, including the long policy name with spaces in `swot_templates`)
    - .planning/phases/49-database-integrity-hygiene/RESEARCH.md DB-06 sketch migration (lines 583-603)
  </read_first>
  <action>
    Author `supabase/migrations/20260504000001_db06_rls_policy_intent_documentation.sql`. The timestamp suffix is `000001` to sort AFTER 49-01's `20260504000000_db01_db02_db05_audit_columns_and_renames.sql` so the two Wave 1 migrations apply in deterministic order.

    The migration consists of:

    1. A long header COMMENT explaining the audit context: the 2026-04-28 audit (Section D #5) flagged 3 RLS policies as over-permissive because they use `USING (true)`. This migration documents that the open SELECT is INTENTIONAL — all 3 tables are system-wide reference catalogues with no tenant column. Narrowing them would require a schema change (adding a tenant column), which is out of scope for Phase 49 (additive-only). RLS is still ENABLED on each table — this gates future INSERT/UPDATE/DELETE policies. The 'INTENT:' sentinel string in each COMMENT is the convention the introspection test and future grep-based audits look for.

    2. Three `COMMENT ON POLICY` statements (verbatim from RESEARCH.md DB-06 sketch lines 592-602):

       - `COMMENT ON POLICY "Authenticated users can view swot templates" ON "public"."swot_templates" IS 'INTENT: system-wide reference data (SWOT prompt catalogue, indexed by industry + business_stage). No business_id column — open SELECT is intentional. Confirmed Phase 49 DB-06.';`
       - `COMMENT ON POLICY "kpi_benchmarks_select_consolidated" ON "public"."kpi_benchmarks" IS 'INTENT: system-wide industry benchmark reference data. No business_id — open SELECT is intentional. Confirmed Phase 49 DB-06.';`
       - `COMMENT ON POLICY "kpi_definitions_select_consolidated" ON "public"."kpi_definitions" IS 'INTENT: system-wide KPI catalogue (universal financial/operational metrics). No business_id — open SELECT is intentional. Confirmed Phase 49 DB-06.';`

    **Re-confirm the policy names match the baseline schema EXACTLY** before saving — the `swot_templates` policy name has spaces in it and is case-sensitive. Mistyping it produces a silent no-op (`COMMENT ON POLICY` against a non-existent name does NOT raise on all PostgreSQL versions). Re-check by running the migration locally and verifying the test goes GREEN.

    Apply the migration to a fresh local DB / preview branch. Re-run the DB-06 vitest — should be GREEN.

    **If during execution Matt reverses RESEARCH.md's recommendation** and decides one of the 3 policies should be narrowed (per-business): HALT, surface the conflict. Narrowing requires adding a `business_id` column first, which is a schema change explicitly out of scope for Phase 49. The right path is to ship the COMMENT-only migration in this plan and open a separate phase for the schema change.
  </action>
  <verify>
    <automated>test -f supabase/migrations/20260504000001_db06_rls_policy_intent_documentation.sql && [ "$(grep -c 'COMMENT ON POLICY' supabase/migrations/20260504000001_db06_rls_policy_intent_documentation.sql)" = "3" ] && [ "$(grep -c 'INTENT:' supabase/migrations/20260504000001_db06_rls_policy_intent_documentation.sql)" -ge 3 ] && grep -q "Authenticated users can view swot templates" supabase/migrations/20260504000001_db06_rls_policy_intent_documentation.sql && npx vitest run src/__tests__/migrations/db-06-rls-comments.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Migration file exists at the listed path.
    - Contains exactly 3 `COMMENT ON POLICY` statements.
    - Each COMMENT body starts with `INTENT:`.
    - Policy names match the baseline schema verbatim (no typos in the spaces-containing `swot_templates` policy name).
    - Migration applies cleanly to a fresh local DB / preview branch.
    - DB-06 vitest test passes (GREEN) when run against the migrated DB.
  </acceptance_criteria>
  <done>GREEN: 3 RLS policies carry intent COMMENTs; RESEARCH.md sentinel SQL returns 3 rows with non-NULL intent_comment values containing 'INTENT:'.</done>
</task>

</tasks>

<verification>
- [ ] DB-06: 3 RLS policies have `COMMENT ON POLICY` statements with the 'INTENT:' sentinel.
- [ ] Migration applies cleanly to a fresh local DB / preview branch.
- [ ] DB-06 vitest test passes against a migrated DB; skips cleanly in CI placeholder mode.
- [ ] RESEARCH.md Sentinel 4 SQL returns 3 rows with non-NULL `intent_comment` values.
- [ ] No policy was narrowed (per RESEARCH.md DB-06 recommendation — all 3 tables lack a tenant column to narrow on).
- [ ] CI green (typecheck + lint + vitest + build + migration-check).
</verification>

<success_criteria>
- The 3 `USING (true)` RLS policies (`swot_templates`, `kpi_benchmarks`, `kpi_definitions`) carry an explicit migration comment recording the intent (PHASE.md success criterion #5).
- No regression test required because no policy was narrowed (per PHASE.md success criterion #5 wording).
- Future code reviewers / schema authors who grep for `INTENT:` find documented justification for the open SELECT, preventing future "this looks over-permissive" audit confusion.
</success_criteria>

<output>
After completion, create `.planning/phases/49-database-integrity-hygiene/49-03-SUMMARY.md`
</output>
