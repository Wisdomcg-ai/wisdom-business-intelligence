---
phase: 49-database-integrity-hygiene
plan: 05
type: execute
wave: 3
depends_on:
  - 44-05
  - 49-02
  - 49-04
files_modified:
  - supabase/migrations/20260506000000_db04_set_null_fks_batch_2.sql
  - src/__tests__/migrations/db-04-set-null-batch-2.test.ts
  - docs/db/fk-policy.md
autonomous: true
requirements:
  - DB-04
gap_closure: false

must_haves:
  truths:
    - "Second batch of ~24 SET NULL FKs (Bucket A part 2, per docs/db/fk-policy.md) is converted from NO ACTION to ON DELETE SET NULL via DROP CONSTRAINT + ADD CONSTRAINT."
    - "Each FK in the batch has a paired test in src/__tests__/migrations/db-04-set-null-batch-2.test.ts following the same pattern as batch 1: createTestUser → insert dependent → deleteTestUser → assertOrphans('null')."
    - "Test file imports from src/__tests__/migrations/_helpers.ts (created in 49-04). NO duplication of helpers."
    - "Migration applies cleanly to a Supabase preview branch; operator runs the test suite against the preview branch URL and pastes the (non-skipped) output into the PR description."
    - "docs/db/fk-policy.md is updated with `applied:` annotations + a migration-history entry for this batch."
    - "After 49-05 merges, all ~48 Bucket A SET NULL FKs are covered. Sentinel 1 SQL count of NO ACTION FKs has dropped by ~48 from the pre-Phase-49 baseline."
    - "Migration header COMMENT cites the fk-policy.md row(s) it implements (`-- Per docs/db/fk-policy.md Bucket A rows 25-48`)."
    - "Plan reuses lessons learned from 49-04 — any test pattern adjustment, any helper extension, any per-FK insert-template tweak is documented in the plan SUMMARY."
  artifacts:
    - path: "supabase/migrations/20260506000000_db04_set_null_fks_batch_2.sql"
      provides: "Second half of Bucket A SET NULL conversions (~24 FKs across ~12 tables) — completes the SET NULL coverage."
      contains: "ON DELETE SET NULL"
    - path: "src/__tests__/migrations/db-04-set-null-batch-2.test.ts"
      provides: "Per-FK test cases for the batch-2 FKs — 1 test per FK following the create-user → insert-dependent → delete-user → assert-NULL pattern."
      contains: "assertOrphans"
    - path: "docs/db/fk-policy.md"
      provides: "Updated to mark each batch-2 FK as 'applied: 20260506000000_db04_set_null_fks_batch_2.sql'; migration history entry added."
      contains: "20260506000000"
  key_links:
    - from: "supabase/migrations/20260506000000_db04_set_null_fks_batch_2.sql"
      to: "auth.users(id)"
      via: "DROP CONSTRAINT … ADD CONSTRAINT … REFERENCES auth.users(id) ON DELETE SET NULL"
      pattern: "ON DELETE SET NULL"
    - from: "src/__tests__/migrations/db-04-set-null-batch-2.test.ts"
      to: "src/__tests__/migrations/_helpers.ts"
      via: "import { createTestUser, deleteTestUser, assertOrphans, seedTestBusiness } from './_helpers'"
      pattern: "from './_helpers'"
---

<objective>
Second half of the DB-04 SET NULL conversions. Same pattern as plan 49-04, applied to the second ~24 of the ~48 Bucket A FKs.

**Why a separate plan rather than a single bigger PR:** RESEARCH.md DB-04 lines 414-415 — "PRs of 12+ migration files are still reviewable; 24 is too many. Half lets the second batch incorporate any lessons from the first." The split makes the review surface manageable AND lets any helper-API issues found in 49-04 testing be fixed before the second batch lands.

**Why depends on 49-04:** the `_helpers.ts` file is built in 49-04. Also, any lessons (test pattern adjustments, helper API gaps, surprises in actual NO ACTION behavior on the live DB) must propagate before this plan ships.

**Per-FK selection:** the batch-2 FKs are the second half of `docs/db/fk-policy.md` Bucket A — roughly the alphabetically-second half from RESEARCH.md DB-03 Bucket A lines 261-279, plus any rebucketed entries from RESEARCH.md DB-03 Bucket B re-bucketing (lines 287-301). The signed-off `fk-policy.md` after plan 49-04's `applied:` annotations is the authoritative source — anything still WITHOUT an `applied:` annotation in Bucket A goes into this batch.

Output: 1 migration (~24 FKs) + 1 test file (~24 cases, importing helpers from 49-04) + fk-policy.md update.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/49-database-integrity-hygiene/PHASE.md
@.planning/phases/49-database-integrity-hygiene/RESEARCH.md
@.planning/phases/49-database-integrity-hygiene/49-04-SUMMARY.md
@docs/db/fk-policy.md
@supabase/migrations/00000000000000_baseline_schema.sql
@supabase/migrations/20260505000000_db04_set_null_fks_batch_1.sql
@src/__tests__/migrations/_helpers.ts
@src/__tests__/migrations/db-04-set-null-batch-1.test.ts

<interfaces>
**Bucket A FK selection for batch 2 (the FKs without an `applied:` annotation in fk-policy.md after plan 49-04 lands):**

Per RESEARCH.md DB-03 Bucket A second half (lines 261-279) plus the re-bucketed entries from lines 287-301:

- `process_comments` columns covered in batch 1, NOT here
- `roadmap_completions.user_id → public.profiles.id` (baseline:9230)
- `session_actions.created_by → auth.users.id` (baseline:9255)
- `session_actions.strategic_initiative_id → strategic_initiatives.id` (baseline:9270 — re-bucketed to A)
- `session_attendees.added_by → auth.users.id` (baseline:9275)
- `session_notes.coach_id → auth.users.id` (baseline:9295)
- `session_prep.client_id → auth.users.id` (baseline:9305)
- `sessions.coach_id → auth.users.id` (baseline:9325)
- `shared_documents.uploaded_by → auth.users.id` (baseline:9335)
- `sprint_actions.user_id → auth.users.id` (baseline:9340)
- `sprint_key_actions.user_id → auth.users.id` (baseline:9345)
- `strategic_initiatives.user_id → auth.users.id` (baseline:9415)
- `strategic_todos.created_by → auth.users.id` (baseline:9440)
- `strategic_todos.owner_id → auth.users.id` (baseline:9445)
- `system_roles.created_by → auth.users.id` (baseline:9550)
- `team_invites.accepted_by → auth.users.id` (baseline:9570)
- `team_invites.invited_by → auth.users.id` (baseline:9580)
- `todo_items.created_by → auth.users.id` (baseline:9590)
- `todo_items.parent_task_id → todo_items.id` (baseline:9595 — re-bucketed to A, self-FK)
- `user_roles.granted_by → auth.users.id` (baseline:9640)
- `weekly_checkins.created_by → auth.users.id` (baseline:9675)
- `annual_snapshots.q1_snapshot_id → quarterly_snapshots.id` (baseline:8495 — re-bucketed to A)
- `annual_snapshots.q2_snapshot_id → quarterly_snapshots.id` (baseline:8500 — re-bucketed to A)
- `annual_snapshots.q3_snapshot_id → quarterly_snapshots.id` (baseline:8505 — re-bucketed to A)
- `annual_snapshots.q4_snapshot_id → quarterly_snapshots.id` (baseline:8510 — re-bucketed to A)
- `swot_items.carried_from_item_id → swot_items.id` (baseline:9540 — re-bucketed to A, self-FK)

**The signed-off `docs/db/fk-policy.md` (after 49-04's `applied:` annotations) is authoritative.** Whatever Bucket A rows are still without an `applied:` annotation IS this batch.

**Migration shape:** identical to plan 49-04 Task 3 — DROP CONSTRAINT IF EXISTS + ADD CONSTRAINT pattern. See plan 49-04 for the template.

**Test pattern:** identical to plan 49-04 Task 2 — import helpers from `./_helpers`, one `it()` per FK, follow the 6-step pattern.

**Self-FK and non-auth.users tests (the 6 entries that don't reference auth.users):**
- `todo_items.parent_task_id → todo_items.id`: test creates a parent todo_items row, then a child referencing it via parent_task_id, deletes the parent, asserts the child still exists with parent_task_id = NULL.
- `swot_items.carried_from_item_id → swot_items.id`: same pattern — create source SWOT item, child carrying from it, delete source, assert child's carried_from_item_id = NULL.
- `annual_snapshots.q[1-4]_snapshot_id → quarterly_snapshots.id`: create 4 quarterly_snapshots, an annual_snapshots referencing them, delete one quarterly, assert annual still exists with the relevant qN_snapshot_id = NULL.
- `roadmap_completions.user_id → public.profiles.id`: create profiles row, completions row, delete profiles, assert completion still exists with user_id = NULL.
- `session_actions.strategic_initiative_id → strategic_initiatives.id`: create initiative, action referencing it, delete initiative, assert action still exists with strategic_initiative_id = NULL.

These broaden the test pattern beyond the auth.users-only one in batch 1; the `_helpers.ts` `createTestUser`/`deleteTestUser` is replaced with table-specific create/delete pairs. If the helper file needs to be extended (e.g., adding a generic `createParentRow(table, columns)` helper), do so as part of Task 1 below.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Review 49-04 lessons and extend _helpers.ts if needed</name>
  <files>src/__tests__/migrations/_helpers.ts</files>
  <read_first>
    - .planning/phases/49-database-integrity-hygiene/49-04-SUMMARY.md (lessons learned, surprises, helper-API gaps)
    - src/__tests__/migrations/_helpers.ts (current state)
    - src/__tests__/migrations/db-04-set-null-batch-1.test.ts (patterns that worked vs. patterns that needed workarounds)
  </read_first>
  <action>
    Read the 49-04 SUMMARY for any documented helper-API gaps. Common items expected to surface:
    - The 6 non-auth.users self-FKs / cross-table FKs (`todo_items.parent_task_id`, `swot_items.carried_from_item_id`, `annual_snapshots.q[1-4]_snapshot_id`, `roadmap_completions.user_id → profiles`, `session_actions.strategic_initiative_id`) need a more generic "create dependent parent row" helper. If 49-04's batch happened to skip these (because they're alphabetically-later), the helper API may need extending here.
    - Test runtime concerns (preview-branch deletion of users may be slow if the auth.users table has cron-job triggers; if 49-04 ran slow, batch 2 may need to use a `beforeAll` to seed shared fixtures).

    If extension is needed, add the new helpers to `_helpers.ts` with JSDoc. Examples:

    ```ts
    /** Create a row in an arbitrary table for FK-test purposes; returns the row's id. */
    export async function createParentRow(
      supabase: SupabaseClient,
      table: string,
      columns: Record<string, unknown>
    ): Promise<string> { /* ... */ }

    /** Delete a row by id. Used for testing non-user-deletion FK behavior. */
    export async function deleteRow(
      supabase: SupabaseClient,
      table: string,
      id: string
    ): Promise<void> { /* ... */ }
    ```

    If no extension is needed, this task is a no-op — record that in the plan SUMMARY ("49-04 helpers were sufficient; no extension required").

    **Do NOT change the existing helper signatures** — that would break batch-1's tests that 49-04 just made GREEN. Only ADD.
  </action>
  <verify>
    <automated>test -f src/__tests__/migrations/_helpers.ts && grep -q "createTestUser" src/__tests__/migrations/_helpers.ts && grep -q "assertOrphans" src/__tests__/migrations/_helpers.ts && npx tsc --noEmit && npx vitest run src/__tests__/migrations/db-04-set-null-batch-1.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `_helpers.ts` exists with original 49-04 exports intact.
    - Any new exports added carry JSDoc.
    - tsc clean.
    - 49-04 batch-1 tests still pass (helpers backward-compatible).
  </acceptance_criteria>
  <done>Helper file is ready for batch-2 tests. Backward-compatible with 49-04.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TDD — write failing per-FK tests for the ~24 batch-2 FKs</name>
  <files>src/__tests__/migrations/db-04-set-null-batch-2.test.ts</files>
  <read_first>
    - docs/db/fk-policy.md (Bucket A rows still without `applied:` annotation = this batch)
    - src/__tests__/migrations/_helpers.ts
    - src/__tests__/migrations/db-04-set-null-batch-1.test.ts (style reference)
  </read_first>
  <behavior>
    For each of the ~24 batch-2 FKs from `<interfaces>` above, one test:
    1. Skip if no live DB.
    2. Create the parent row (test user OR specific table row depending on FK target).
    3. Insert the dependent row referencing the parent via the FK column under test.
    4. Delete the parent.
    5. Assert the dependent row still exists and the FK column is NULL ('null' mode).
    6. Cleanup the dependent row.

    For self-FKs, the parent and child are in the same table — the test inserts both in setup.
  </behavior>
  <action>
    Create `src/__tests__/migrations/db-04-set-null-batch-2.test.ts`. Mirror the structure of batch-1's test file. Import all helpers from `./_helpers`.

    For the 6 non-auth.users FKs, use the table-specific create/delete pattern:

    ```ts
    it('todo_items.parent_task_id becomes NULL when parent task is deleted', async () => {
      if (skipIfNoLiveDb()) return
      const supabase = getTestSupabase()
      await seedTestBusiness(supabase)
      const { data: parent } = await supabase.from('todo_items').insert({
        business_id: TEST_BUSINESS_ID, title: 'parent', /* ...required cols... */
      }).select('id').single()
      const { data: child } = await supabase.from('todo_items').insert({
        business_id: TEST_BUSINESS_ID, title: 'child', parent_task_id: parent.id,
      }).select('id').single()
      await supabase.from('todo_items').delete().eq('id', parent.id)
      const { data: after } = await supabase.from('todo_items').select('parent_task_id').eq('id', child.id).single()
      expect(after.parent_task_id).toBeNull()
    })
    ```

    For the `annual_snapshots.q[1-4]_snapshot_id` FKs, one test per quarter (4 tests). Insert 4 quarterly_snapshots, an annual_snapshots referencing them, delete one quarterly, assert the corresponding qN_snapshot_id is NULL.

    Run the suite against a preview branch with the migration NOT yet applied — RED state expected (deletes blocked, or tests fail on the assertion).
  </action>
  <verify>
    <automated>test -f src/__tests__/migrations/db-04-set-null-batch-2.test.ts && grep -q "from './_helpers'" src/__tests__/migrations/db-04-set-null-batch-2.test.ts && [ "$(grep -c '^  it(' src/__tests__/migrations/db-04-set-null-batch-2.test.ts)" -ge 20 ] && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Test file exists, imports from `./_helpers`.
    - At least 20 `it()` blocks (1 per FK; the 4 annual_snapshots q1..q4 tests count as 4).
    - Each test follows the 6-step pattern (or self-FK / non-user variant).
    - tsc clean.
    - Tests RED against an unmigrated preview branch.
  </acceptance_criteria>
  <done>RED: ~24 batch-2 per-FK regression tests committed; failing as expected.</done>
</task>

<task type="auto">
  <name>Task 3: GREEN — author the SET NULL migration for batch 2 FKs</name>
  <files>supabase/migrations/20260506000000_db04_set_null_fks_batch_2.sql</files>
  <read_first>
    - docs/db/fk-policy.md (Bucket A rows still pending after 49-04)
    - supabase/migrations/00000000000000_baseline_schema.sql (verify each FK's actual constraint name)
    - supabase/migrations/20260505000000_db04_set_null_fks_batch_1.sql (style reference — same pattern, different FKs)
  </read_first>
  <action>
    Author `supabase/migrations/20260506000000_db04_set_null_fks_batch_2.sql`. Same structure as batch 1 (DROP CONSTRAINT IF EXISTS + ADD CONSTRAINT, header COMMENT citing fk-policy.md rows 25-48).

    Header COMMENT specifically calls out:
    - This is batch 2 of 2 for Bucket A.
    - After this migration, all ~48 Bucket A FKs carry ON DELETE SET NULL.
    - Sentinel 1 SQL count of NO ACTION FKs should drop by ~24 from post-49-04 state, and ~48 from the pre-Phase-49 baseline.
    - Includes the 6 non-auth.users FKs (self-FKs and cross-table) — these reference parent tables other than auth.users.

    Apply to a Supabase preview branch. Re-run the batch-2 vitest — all ~24 tests should be GREEN.

    Capture preview-branch test output for the PR description.
  </action>
  <verify>
    <automated>test -f supabase/migrations/20260506000000_db04_set_null_fks_batch_2.sql && [ "$(grep -c 'ON DELETE SET NULL' supabase/migrations/20260506000000_db04_set_null_fks_batch_2.sql)" -ge 20 ] && [ "$(grep -c 'DROP CONSTRAINT IF EXISTS' supabase/migrations/20260506000000_db04_set_null_fks_batch_2.sql)" -ge 20 ] && grep -q "fk-policy.md Bucket A" supabase/migrations/20260506000000_db04_set_null_fks_batch_2.sql</automated>
  </verify>
  <acceptance_criteria>
    - Migration file exists with header COMMENT citing fk-policy.md rows 25-48.
    - At least 20 `ON DELETE SET NULL` clauses.
    - At least 20 `DROP CONSTRAINT IF EXISTS` statements.
    - Migration applies cleanly to a preview branch.
    - All ~24 batch-2 vitest tests pass GREEN against the migrated preview branch.
    - PR description includes preview-branch test output.
    - All ~48 Bucket A FKs are now covered (cross-check fk-policy.md after Task 4).
  </acceptance_criteria>
  <done>GREEN: batch-2 SET NULL conversions applied; all of Bucket A is now covered.</done>
</task>

<task type="auto">
  <name>Task 4: Update docs/db/fk-policy.md to mark batch-2 FKs as applied</name>
  <files>docs/db/fk-policy.md</files>
  <read_first>
    - docs/db/fk-policy.md (post-49-04 state)
  </read_first>
  <action>
    For each FK touched by `20260506000000_db04_set_null_fks_batch_2.sql`, add an `applied: 20260506000000_db04_set_null_fks_batch_2.sql` annotation in the Bucket A table row.

    Append to the Migration history section:
    ```
    - 2026-05-06 — `20260506000000_db04_set_null_fks_batch_2.sql` — applied SET NULL to Bucket A rows 25-48 (plan 49-05). All Bucket A coverage complete.
    ```

    Verify Bucket A is now 100% applied — every row in the Bucket A table should carry an `applied:` annotation. If any row is missing one, surface the discrepancy.
  </action>
  <verify>
    <automated>grep -q "20260506000000_db04_set_null_fks_batch_2.sql" docs/db/fk-policy.md</automated>
  </verify>
  <acceptance_criteria>
    - fk-policy.md Bucket A is 100% annotated `applied:` (every row covered by either 49-04 or 49-05).
    - Migration history has entries for both 49-04 and 49-05.
    - Bucket counts in TL;DR remain consistent.
  </acceptance_criteria>
  <done>fk-policy.md shows all of Bucket A is shipped. Plans 49-06 and 49-07 cover the remaining ~8 FKs (Bucket B + C).</done>
</task>

</tasks>

<verification>
- [ ] DB-04 (batch 2): ~24 SET NULL FKs converted via DROP CONSTRAINT + ADD CONSTRAINT.
- [ ] All ~48 Bucket A FKs are now SET NULL (across 49-04 + 49-05).
- [ ] Per-FK preview-branch tests pass GREEN.
- [ ] No regression in 49-04 batch-1 tests (helpers backward-compatible).
- [ ] `docs/db/fk-policy.md` Bucket A is fully annotated.
- [ ] CI green.
</verification>

<success_criteria>
- All ~48 Bucket A FKs have ON DELETE SET NULL applied — verified by per-FK preview-branch tests across 49-04 + 49-05 (PHASE.md success criterion #3 — 48 of 56 FKs covered).
- RESEARCH.md Sentinel 1 (`information_schema.referential_constraints WHERE delete_rule = 'NO ACTION'`) returns ~48 fewer rows than the pre-Phase-49 baseline.
- Operator can delete a test user against a preview branch and observe SET NULL behavior across all 48 dependent tables.
</success_criteria>

<output>
After completion, create `.planning/phases/49-database-integrity-hygiene/49-05-SUMMARY.md`. Capture: which FKs were in batch 2, the preview-branch URL, and the test output. Note any helper-API extensions and any FKs that needed reclassification mid-execution (these would propagate to 49-06/07).
</output>
