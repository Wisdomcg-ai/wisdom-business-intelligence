---
phase: 49-database-integrity-hygiene
plan: 02
type: execute
wave: 2
depends_on:
  - 44-05
files_modified:
  - docs/db/fk-policy.md
autonomous: false
requirements:
  - DB-03
gap_closure: false

must_haves:
  truths:
    - "docs/db/fk-policy.md exists and documents the ON DELETE behavior recommendation for each of the 56 orphan-prone FKs identified in RESEARCH.md."
    - "Each of the 56 FKs is listed with: source table, source column, referenced table, referenced column, recommended ON DELETE behavior, justification."
    - "The doc reframes the audit's misleading 'businesses.id' framing — 41+ of the 56 FKs reference auth.users.id (~77%), NOT businesses.id. Every existing business_id FK already CASCADEs."
    - "The doc records a final operator-signed-off product decision for businesses.owner_id ON DELETE behavior (CASCADE / SET NULL / RESTRICT) with reasoning."
    - "The doc captures the operator's product call on each of the other Bucket C / RESTRICT-or-manual-review FKs (custom_kpis_library.business_id, session_attendees.user_id, plus any held-back items)."
    - "The doc establishes the project-wide policy principles (business_id → CASCADE; *_by → auth.users → SET NULL; tightly-coupled children → CASCADE; self-FKs → SET NULL) that future schema authors must follow."
    - "The doc is the authoritative source for plans 49-04, 49-05, 49-06, 49-07 — every DB-04 migration in those plans MUST cite the fk-policy.md row it implements."
    - "Operator-blocking checkpoint pauses execution before final commit so Matt reviews + signs off every FK + makes the businesses.owner_id product call."
  artifacts:
    - path: "docs/db/fk-policy.md"
      provides: "Authoritative ON DELETE policy doc for the 56 orphan-prone FKs + the project's go-forward FK convention."
      contains: "businesses.owner_id"
  key_links:
    - from: "docs/db/fk-policy.md"
      to: ".planning/phases/49-database-integrity-hygiene/RESEARCH.md DB-03 section"
      via: "verbatim FK enumeration carried forward into the doc"
      pattern: "auth\\.users"
    - from: "docs/db/fk-policy.md"
      to: "supabase/migrations/00000000000000_baseline_schema.sql"
      via: "every FK cites the baseline line number RESEARCH.md identified"
      pattern: "baseline:"
---

<objective>
Author the gating decision document for Phase 49. This plan ships **only one artifact** — `docs/db/fk-policy.md` — and pauses for operator sign-off before final commit.

Why this is its own plan: PHASE.md success criterion #2 makes this an explicit gate ("`docs/db/fk-policy.md` exists and documents the CASCADE vs SET NULL decision for each of the 56 FKs, signed off by the developer (Matt) before any DB-04 migration ships"). Plans 49-04, 49-05, 49-06, 49-07 cannot start until the doc is signed.

**Critical reframing the doc must surface front-and-centre:** RESEARCH.md DB-03 confirmed the audit was wrong about the 56 FKs being on `businesses.id`. **They are not.** Every existing `business_id` FK already has `ON DELETE CASCADE`. Of the 56 orphan-prone FKs, **~77% reference `auth.users.id`** (audit attribution: who created/assigned/approved/sent X). The right policy for those is **SET NULL**, preserving the historical record when a coach or user leaves the org. The right *test* is deleting a test **user**, not a test business.

**The single FK that requires a product judgment from the operator:** `businesses.owner_id → auth.users.id`. Researcher recommends RESTRICT (force manual reassignment before user deletion) but it's a CASCADE (delete the business when owner deleted — destructive) vs SET NULL (orphan business, no owner) vs RESTRICT (block) call. The operator-blocking checkpoint in this plan captures Matt's decision in writing.

Output: 1 markdown doc + Matt's sign-off recorded in the plan SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/49-database-integrity-hygiene/PHASE.md
@.planning/phases/49-database-integrity-hygiene/RESEARCH.md
@supabase/migrations/00000000000000_baseline_schema.sql

<interfaces>
**RESEARCH.md DB-03 final bucket counts (must sum to 56):**

| Bucket | Count | Behaviour |
|--------|-------|-----------|
| A — SET NULL | ~48 | Audit / attribution FKs; user deletion preserves the record |
| B — CASCADE | ~5 | `process_flows.from_step_id`, `process_flows.to_step_id`, `process_flows.process_id`, `process_phases.process_id`, `session_attendees.user_id` |
| C — RESTRICT / manual | ~3 | `businesses.owner_id` (RESTRICT recommended — Matt decides), `custom_kpis_library.business_id` (CASCADE per dual-id reconciliation, flag for verification), one held back for Matt's review |

**Total: 56.** The ~ symbol indicates the planner / operator may shift 1-2 between buckets at sign-off; the doc must justify each placement.

**Project FK convention principles (RESEARCH.md DB-03 draft, lines 339-356):**

1. **`business_id` → `businesses.id` always CASCADE.** Established convention; every existing business_id FK in baseline_schema.sql already follows this.
2. **`*_by`, `*_id` → `auth.users.id` (audit attribution) → SET NULL.** Records survive user deletion; attribution is nulled. Protects audit trails.
3. **Owner / sole-relationship FKs → RESTRICT.** When a user IS the primary owner of a record (e.g., `businesses.owner_id`), block the deletion until ownership is transferred or the dependent is archived.
4. **Tightly-coupled child tables → CASCADE.** Tables that have NO meaning without their parent (process_flows without process_diagrams) cascade. Test: "would a SELECT on this child table without the parent make any sense?"
5. **Self-references → SET NULL.** Self-FKs (parent_task_id, carried_from_item_id) preserve the child while losing the ancestry pointer.

**Source data (full FK enumeration):** RESEARCH.md DB-03 section, lines 229-323. The doc lifts these tables verbatim, then adds Matt's per-FK sign-off marker (e.g., a `[x]` checkbox, or a "Decision: …" line under each).

**businesses.owner_id options for the operator checkpoint (RESEARCH.md DB-03 Bucket C, line 307):**

| Option | What happens when owner is deleted | Pros | Cons |
|--------|-----------------------------------|------|------|
| **CASCADE** | Business is deleted | Clean — no orphans | **Destructive.** A user-deletion event silently destroys a business and (via the existing business_id CASCADE chain) all 26 child tables of business data — forecasts, monthly reports, Xero sync state, everything. |
| **SET NULL** | Business survives with owner_id = NULL | Preserves audit trail | Business is orphaned; no clear UI/access semantics for an owner-less business. May confuse RLS policies that key off owner_id. |
| **RESTRICT** | User deletion is blocked | Forces manual ownership transfer or business archival before deletion | Adds a manual step to user deactivation; coach offboarding becomes a 2-step process. **Researcher recommendation.** |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Draft docs/db/fk-policy.md from the RESEARCH.md DB-03 enumeration</name>
  <files>docs/db/fk-policy.md</files>
  <read_first>
    - .planning/phases/49-database-integrity-hygiene/RESEARCH.md DB-03 section (lines 227-374 — the full FK enumeration + the 5 policy principles)
    - supabase/migrations/00000000000000_baseline_schema.sql (spot-check 5-10 of the cited line numbers to confirm RESEARCH.md's enumeration matches the live baseline)
  </read_first>
  <action>
    Create `docs/db/` directory if it does not exist. Author `docs/db/fk-policy.md` with the structure below. The doc IS the authoritative reference — write it for future schema authors who have never read RESEARCH.md.

    **Structure:**

    ```markdown
    # Foreign Key ON DELETE Policy

    **Status:** DRAFT — pending operator sign-off (Matt Malouf, target 2026-MM-DD)
    **Source:** Phase 49 Database Integrity Hygiene — research output (.planning/phases/49-database-integrity-hygiene/RESEARCH.md DB-03 section)
    **Audience:** future schema authors and migration reviewers
    **Authoritative for:** plans 49-04, 49-05, 49-06, 49-07 (every DB-04 migration cites the fk-policy.md row it implements)

    ## TL;DR

    The 56 orphan-prone FKs surfaced by the 2026-04-28 codebase audit are NOT predominantly on `businesses.id` as the audit summary suggested. Every existing `business_id` FK already has `ON DELETE CASCADE`. The actual breakdown:

    - **~48 FKs reference `auth.users.id`** for audit attribution (who created / assigned / approved / sent / etc.). Default policy: **SET NULL** — preserve the record, lose the attribution.
    - **~5 FKs** are tightly-coupled children (process_flows / process_phases / session_attendees). Default policy: **CASCADE** — child has no meaning without parent.
    - **~3 FKs** require manual / product judgement, including `businesses.owner_id`. Default policy varies (see Bucket C).

    The implication for testing: **delete a test user, not a test business.** The DB-04 preview-branch tests (49-04 onward) seed an `auth.users` row, populate dependent records across multiple tables, then call `supabase.auth.admin.deleteUser` and assert each FK column is NULL (Bucket A), the row is gone (Bucket B), or the deletion was blocked (Bucket C — RESTRICT only).

    ## Principles

    [carry forward the 5 principles from RESEARCH.md DB-03 lines 339-356, verbatim]

    ## Bucket A — SET NULL (~48 FKs)

    Audit / attribution FKs. When the user is deleted, the dependent record is preserved with the FK column nulled.

    | # | Source table.column | Referenced | Baseline line | Justification | Sign-off |
    |---|---------------------|------------|---------------|---------------|----------|
    | 1 | action_items.assigned_to | auth.users.id | 8445 | Action item survives user deletion; assignee becomes "unassigned" | [ ] |
    | 2 | action_items.created_by | auth.users.id | 8455 | Same | [ ] |
    [...continue for all ~48 FKs from RESEARCH.md DB-03 Bucket A table, lines 237-279...]

    ## Bucket B — CASCADE (~5 FKs)

    Tightly-coupled children that have no meaning without their parent. **CASCADE is irreversible if a delete fires in production** — these get the most thorough preview-branch testing in plan 49-06.

    | # | Source table.column | Referenced | Baseline line | Justification | Sign-off |
    |---|---------------------|------------|---------------|---------------|----------|
    | 1 | process_flows.from_step_id | process_steps.id | 9130 | If a step is deleted, its inbound/outbound flows are nonsensical | [ ] |
    | 2 | process_flows.to_step_id | process_steps.id | 9140 | Same | [ ] |
    | 3 | process_flows.process_id | process_diagrams.id | 9135 | Diagram deleted → all flows gone | [ ] |
    | 4 | process_phases.process_id | process_diagrams.id | 9145 | Same | [ ] |
    | 5 | session_attendees.user_id | auth.users.id | 9285 | An attendance record without a user is meaningless | [ ] |

    ## Bucket C — RESTRICT or manual review (~3 FKs)

    These require explicit product judgement. **Operator must decide each before plan 49-07 ships.**

    ### C-1: businesses.owner_id → auth.users.id (baseline:8610)

    **The single highest-stakes FK in this phase.** Cascading existing CASCADE chains via `business_id` means deleting an owner could trigger destruction of forecasts, Xero sync state, monthly reports across 26+ child tables in one statement.

    **Options** (verbatim from RESEARCH.md interface):
    - CASCADE — destructive
    - SET NULL — orphan business
    - RESTRICT — block user deletion until ownership transferred (researcher recommendation)

    **Operator decision:** ___________________
    **Reasoning:** ___________________
    **Sign-off date:** ___________________

    ### C-2: custom_kpis_library.business_id → business_profiles.id (baseline:8715)

    Note: this FK references `business_profiles` (the dual-id ambiguity from MEMORY.md `project_dual_id`), NOT `businesses`. Researcher recommends CASCADE to mirror other business_id FKs but flags for verification given the dual-id wrinkle.

    **Operator decision:** ___________________
    **Reasoning:** ___________________

    ### C-3: [held back for Matt's review — see RESEARCH.md DB-03 Bucket C]

    **Operator decision:** ___________________
    **Reasoning:** ___________________

    ## Process for new FKs (going forward)

    Every new FK in a future migration MUST include an explicit `ON DELETE` clause. The CI migration-check step in `.github/workflows/supabase-preview.yml` will be tightened (separate follow-up phase) to reject FKs without an explicit `ON DELETE` clause for newly-added constraints.

    ## How DB-04 plans cite this doc

    Each migration in plans 49-04 / 49-05 / 49-06 / 49-07 must include a header COMMENT pointing to the row(s) of this doc it implements. Example:

    ```sql
    -- Phase 49 DB-04: SET NULL on action_items.assigned_to FK.
    -- Per docs/db/fk-policy.md Bucket A row #1.
    -- Tested via src/__tests__/migrations/db-04-set-null-batch-1.test.ts.
    ```

    ## Sign-off

    - [ ] Matt confirms every Bucket A row's SET NULL recommendation is correct
    - [ ] Matt confirms every Bucket B row's CASCADE recommendation is correct
    - [ ] Matt records a decision for each Bucket C row (especially businesses.owner_id)
    - [ ] Doc is committed to main; plans 49-04..49-07 cleared to start
    ```

    Lift the full FK lists from RESEARCH.md DB-03 lines 237-323 verbatim. Do not invent FKs the research did not enumerate. Do not skip rows to save space.

    Spot-check 5-10 of the RESEARCH.md cited baseline line numbers against the actual `00000000000000_baseline_schema.sql` to confirm accuracy. If any line number is off by more than ±5 lines, flag in a note at the top of the doc and re-cite the correct line.

    Save the file. Do NOT yet commit — Task 2 is the operator checkpoint.
  </action>
  <verify>
    <automated>test -f docs/db/fk-policy.md && grep -q "businesses.owner_id" docs/db/fk-policy.md && grep -q "Bucket A" docs/db/fk-policy.md && grep -q "Bucket B" docs/db/fk-policy.md && grep -q "Bucket C" docs/db/fk-policy.md && grep -c "auth.users.id" docs/db/fk-policy.md | awk '{ if ($1 < 30) exit 1; else exit 0 }'</automated>
  </verify>
  <acceptance_criteria>
    - `docs/db/fk-policy.md` exists.
    - Lists all 56 FKs across the three buckets (A + B + C count to 56).
    - Each row cites the baseline schema line number from RESEARCH.md.
    - businesses.owner_id has its own subsection with the 3 options spelled out.
    - Sign-off checklist at the bottom.
    - Status line says "DRAFT — pending operator sign-off".
  </acceptance_criteria>
  <done>Draft policy doc authored; ready for operator review.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator checkpoint — Matt reviews fk-policy.md and signs off every FK + makes the businesses.owner_id product call</name>
  <what-built>docs/db/fk-policy.md — draft FK ON DELETE policy doc covering all 56 orphan-prone FKs, with explicit decision slots for businesses.owner_id and the other Bucket C items.</what-built>
  <how-to-verify>
    Step 1 — Open `docs/db/fk-policy.md` in your editor or on the feature branch.

    Step 2 — Skim the TL;DR section. Confirm you accept the reframing (the audit was wrong; FKs are predominantly on `auth.users.id`, not `businesses.id`). If you reject the reframing, surface that to the planner — the bucketing changes downstream.

    Step 3 — **Bucket A (~48 SET NULL FKs):** for each row, confirm the recommendation matches your intent. Tick the [ ] sign-off checkbox or annotate "REJECT: should be CASCADE/RESTRICT because …". Most should be obvious yeses (audit attribution preserved).

    Step 4 — **Bucket B (~5 CASCADE FKs):** for each row, confirm CASCADE is correct. The 4 process_* FKs should be obvious yeses. The `session_attendees.user_id` is the one to scrutinise — researcher rated MEDIUM confidence (defensible either way).

    Step 5 — **Bucket C (~3 RESTRICT / manual review FKs) — the load-bearing decision:**
      - **C-1 `businesses.owner_id`:** make the product call. CASCADE (destroys business + 26 downstream tables when owner deleted), SET NULL (orphan business — no clear UI semantics), or RESTRICT (block user deletion until ownership transferred). Researcher recommends RESTRICT. Fill in the "Operator decision" + "Reasoning" + "Sign-off date" lines.
      - **C-2 `custom_kpis_library.business_id`:** confirm CASCADE matches your intent given this is the dual-id wrinkle (FK references `business_profiles`, not `businesses`). Recall the `project_dual_id` MEMORY.md note about businesses.id vs business_profiles.id divergence.
      - **C-3** (held-back item): make the call.

    Step 6 — Decide whether any Bucket A row should move to Bucket B or C, or vice versa. The bucket counts may shift by 1-2 — that's fine, just document the move.

    Step 7 — When all sign-off checkboxes are ticked and the businesses.owner_id decision is recorded, type the resume-signal.

    **What to look for that should make you reject:**
    - Any Bucket A row that's actually a sole-relationship FK (the user IS the primary owner, not just an audit reference) — those belong in Bucket C with RESTRICT.
    - Any Bucket B row where deleting the parent shouldn't actually delete the child (e.g., a misclassified self-FK).
    - Any FK in the 56-list that you recognise as already having an `ON DELETE` clause (RESEARCH.md verified the enumeration via grep, but a manual eye is the last line of defence).
  </how-to-verify>
  <resume-signal>Type "policy approved" and include the businesses.owner_id decision (e.g., "policy approved; owner_id = RESTRICT"). If you moved any FKs between buckets, list the changes. If anything was rejected, describe what to change in fk-policy.md.</resume-signal>
  <files>(checkpoint task — Claude makes no file edits during this step)</files>
  <action>Pause execution. Wait for the resume-signal from the operator.</action>
  <verify>(human checkpoint — verify per how-to-verify; resume-signal acts as the verification gate)</verify>
  <done>Operator has signed off every FK and recorded a final product decision for businesses.owner_id and the other Bucket C items.</done>
</task>

<task type="auto">
  <name>Task 3: Update fk-policy.md with operator decisions and finalise the doc</name>
  <files>docs/db/fk-policy.md</files>
  <read_first>
    - The resume-signal text from Task 2 (capture verbatim into the plan SUMMARY)
    - docs/db/fk-policy.md (current draft state)
  </read_first>
  <action>
    Apply the operator's decisions from Task 2's resume-signal:

    1. Update the businesses.owner_id "Operator decision" + "Reasoning" + "Sign-off date" lines with Matt's call.
    2. Update the other Bucket C "Operator decision" lines.
    3. Apply any inter-bucket moves the operator requested (rare).
    4. Tick the bottom-of-doc sign-off checkboxes:
       - [x] Matt confirms every Bucket A row's SET NULL recommendation is correct
       - [x] Matt confirms every Bucket B row's CASCADE recommendation is correct
       - [x] Matt records a decision for each Bucket C row
       - [x] Doc is committed to main; plans 49-04..49-07 cleared to start
    5. Update the status line at the top: `**Status:** DRAFT — pending operator sign-off` → `**Status:** ACTIVE (signed off: Matt Malouf, YYYY-MM-DD)`
    6. Update bucket counts in the TL;DR if any FKs moved buckets.

    Do NOT change any FK rationale that the operator did not explicitly reject. The doc is the contract.

    Stage the file for commit. The plan SUMMARY (Task 4 of execute-plan workflow) records the operator's decision verbatim for audit trail.
  </action>
  <verify>
    <automated>test -f docs/db/fk-policy.md && grep -q "Status.*ACTIVE" docs/db/fk-policy.md && grep -E "Operator decision: (CASCADE|SET NULL|RESTRICT)" docs/db/fk-policy.md | wc -l | awk '{ if ($1 < 1) exit 1; else exit 0 }'</automated>
  </verify>
  <acceptance_criteria>
    - `docs/db/fk-policy.md` status line says "ACTIVE (signed off: …)" with a date.
    - businesses.owner_id "Operator decision" line is filled in (not blank).
    - All bottom-of-doc sign-off checkboxes are ticked.
    - Bucket counts in TL;DR match the (possibly adjusted) bucket tables.
  </acceptance_criteria>
  <done>fk-policy.md is signed off and committed. Plans 49-04, 49-05, 49-06, 49-07 are cleared to start.</done>
</task>

</tasks>

<verification>
- [ ] DB-03: `docs/db/fk-policy.md` exists in the repo.
- [ ] All 56 orphan-prone FKs from RESEARCH.md DB-03 are enumerated in the doc.
- [ ] businesses.owner_id has an explicit operator decision recorded.
- [ ] Each Bucket C FK has an explicit operator decision recorded.
- [ ] Status line is ACTIVE (not DRAFT) — operator has signed off.
- [ ] Plan SUMMARY captures the operator's resume-signal verbatim for audit trail.
</verification>

<success_criteria>
- `docs/db/fk-policy.md` exists, documents all 56 FKs, and is signed off by Matt — satisfies PHASE.md success criterion #2.
- Plans 49-04, 49-05, 49-06, 49-07 can now start with explicit per-FK recommendations to implement.
- Future schema authors have a one-stop reference for the project's FK ON DELETE policy.
</success_criteria>

<output>
After completion, create `.planning/phases/49-database-integrity-hygiene/49-02-SUMMARY.md`. Capture the operator's resume-signal text verbatim — this is the audit trail for which businesses.owner_id behavior was chosen and why.
</output>
