---
phase: 51-forecast-wizard-ux
plan: 06
type: execute
wave: 2
depends_on: []
files_modified:
  - src/app/finances/forecast/components/wizard-v4/steps/Step6Subscriptions.tsx
  - src/__tests__/forecast/phase-51-step6-sidebar.test.tsx
  - src/__tests__/forecast/phase-51-step6-re-analyze.test.tsx
  - src/__tests__/forecast/phase-51-step6-manual-entry.test.tsx
autonomous: true
requirements:
  - UX-S6-01
  - UX-S6-02
  - UX-S6-03
must_haves:
  truths:
    - "When phase === 'review' (vendor view), a persistent sidebar (or collapsible side panel) shows the names of selected Xero accounts AND a per-account total computed from active vendors mapped to that account"
    - "Sidebar updates reactively when operator toggles vendors on/off"
    - "Clicking 'Change selected accounts' returns to the select-accounts phase WITHOUT losing operator's existing vendor isActive / monthlyBudget state — re-analyzing merges by vendorKey rather than replacing the array"
    - "'+ Add Subscription' button is visible in xero mode (phase === 'review'), not just isManualMode — but isManualMode branches are PRESERVED for the no-Xero fallback (per RESEARCH.md anti-pattern)"
    - "Manual subscriptions added via the button appear in the same vendor list with vendor name, monthly amount, frequency, start month, and category (matching existing categories)"
    - "Existing vendor analysis math is unchanged — only the merge-on-replace behavior of analyzeSubscriptions changes (UX-S6-02 fix)"
    - "Loading an existing forecast with no manual subscriptions added behaves identically to pre-Phase-51 (back-compat)"
    - "WIZARD_VERSION stays at 10"
  artifacts:
    - path: src/app/finances/forecast/components/wizard-v4/steps/Step6Subscriptions.tsx
      provides: "Sidebar component + merge-by-vendorKey logic in analyzeSubscriptions + always-visible Add Subscription button"
      contains: "Selected Accounts"
    - path: src/__tests__/forecast/phase-51-step6-sidebar.test.tsx
      provides: "Sidebar rendering + per-account totals tests (UX-S6-01)"
      contains: "describe('UX-S6-01"
    - path: src/__tests__/forecast/phase-51-step6-re-analyze.test.tsx
      provides: "Re-analyze preserves vendor toggles (UX-S6-02)"
      contains: "describe('UX-S6-02"
    - path: src/__tests__/forecast/phase-51-step6-manual-entry.test.tsx
      provides: "Add Subscription button visible in xero mode + manual entry flow (UX-S6-03)"
      contains: "describe('UX-S6-03"
  key_links:
    - from: Sidebar list of selected accounts
      to: accounts.filter(a => a.isSelected) AND vendors.filter(v => v.isActive && v.account_codes.includes(a.code))
      via: "Reactive derived state computed from accounts + vendors"
      pattern: "accounts.filter"
    - from: "'Change selected accounts' button (existing 'Re-analyze' at ~line 870)"
      to: setPhase('select-accounts')
      via: "Existing button text/behavior; vendor state preserved by NOT calling setVendors([])"
      pattern: "setPhase"
    - from: analyzeSubscriptions vendor merge
      to: setVendors(prev => mergeByVendorKey(prev, vendorBudgets))
      via: "New merge function preserves existing isActive + monthlyBudget by vendorKey"
      pattern: "mergeByVendorKey"
    - from: "'+ Add Subscription' button"
      to: setShowAddVendor(true)
      via: "Removed isManualMode gate (per RESEARCH.md UX-S6-03)"
      pattern: "setShowAddVendor"
---

<objective>
Bundle UX-S6-01 (sidebar with selected accounts) + UX-S6-02 (preserve vendor toggles on re-analyze) + UX-S6-03 (manual subscription entry button always visible) into one PR. All three changes are localized to `Step6Subscriptions.tsx` and don't share state or logic with the rest of the wizard.

Per RESEARCH.md: bundle Step 6 changes into one plan because splitting them would mean 3 PRs for ~80 lines of changes total. UX-S6-02 fix is small (merge instead of replace in analyzeSubscriptions). UX-S6-03 is a single-line ungating. UX-S6-01 sidebar is the main visual addition.

Output: 1 source file modified + 3 new test files (one per requirement for clear traceability). Existing isManualMode branches preserved for no-Xero fallback (per RESEARCH.md anti-pattern).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/51-forecast-wizard-ux/PHASE.md
@.planning/phases/51-forecast-wizard-ux/RESEARCH.md
@src/app/finances/forecast/components/wizard-v4/steps/Step6Subscriptions.tsx
</context>

<interfaces>
Sidebar component (new — inline within Step6Subscriptions.tsx, rendered when phase === 'review'):

```jsx
{phase === 'review' && (
  <aside className="w-64 border-r border-gray-200 p-4 bg-gray-50">
    <h3 className="text-sm font-semibold mb-3">Selected Accounts</h3>
    <ul className="space-y-2">
      {accounts.filter(a => a.isSelected).map(account => {
        const total = vendors
          .filter(v => v.isActive && v.account_codes?.includes(account.code))
          .reduce((sum, v) => sum + (v.monthlyBudget || 0), 0);
        return (
          <li key={account.accountId} className="flex justify-between text-sm">
            <span className="truncate" title={account.name}>{account.name}</span>
            <span className="font-medium">${total.toLocaleString()}</span>
          </li>
        );
      })}
    </ul>
    {accounts.filter(a => a.isSelected).length === 0 && (
      <p className="text-xs text-gray-500 italic">No accounts selected.</p>
    )}
  </aside>
)}
```

mergeByVendorKey helper (new — inline or near top of file):

```typescript
function mergeByVendorKey(
  prev: VendorBudget[],
  incoming: VendorBudget[],
): VendorBudget[] {
  const prevByKey = new Map(prev.map(v => [v.vendorKey, v]));
  return incoming.map(newV => {
    const existing = prevByKey.get(newV.vendorKey);
    if (!existing) return newV;
    return { ...newV, isActive: existing.isActive, monthlyBudget: existing.monthlyBudget };
  });
}
```

Existing analyzeSubscriptions change (Step6Subscriptions.tsx around line 367):
```typescript
// BEFORE
setVendors(vendorBudgets);
// AFTER
setVendors(prev => mergeByVendorKey(prev, vendorBudgets));
```

Add Subscription button — change the existing gate:
```jsx
// BEFORE (around line 862)
{isManualMode && (
  <button onClick={() => setShowAddVendor(true)}>+ Add Subscription</button>
)}
// AFTER
{phase === 'review' && (
  <button onClick={() => setShowAddVendor(true)}>+ Add Subscription</button>
)}
```
(Note: do NOT delete the isManualMode-specific JSX elsewhere in the file — that's still the no-Xero render path.)
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — sidebar tests (UX-S6-01)</name>
  <files>src/__tests__/forecast/phase-51-step6-sidebar.test.tsx</files>
  <read_first>
    - src/__tests__/forecast/wizard-v4-bug-fixes.test.tsx (test infra)
    - src/app/finances/forecast/components/wizard-v4/steps/Step6Subscriptions.tsx lines 88-140 (phase machine + accounts/vendors state shape)
  </read_first>
  <behavior>
    UX-S6-01 (4 tests):
      - Test 1: render Step6 with phase forced to 'review' AND accounts (3 with isSelected mixed) AND vendors (2 active mapped to selected accounts) → sidebar lists names of the 2 selected accounts with their per-account vendor sums; the unselected account is NOT listed
      - Test 2: with the same setup, toggle a vendor to isActive=false → sidebar account total updates to $0 (reactive)
      - Test 3: when no accounts selected, sidebar shows "No accounts selected." italic empty state
      - Test 4: in phase === 'select-accounts', sidebar is NOT rendered (queryByRole('complementary') returns null)
  </behavior>
  <action>
    Build Step6Harness similar to Step3Harness — render Step6Subscriptions with seeded state. May need to mock the analyzeSubscriptions network call OR provide enough state to skip it (set phase to 'review' directly via test helper if exposed; otherwise need to investigate the component's internal state-setter exposure).

    Find sidebar via screen.getByRole('complementary') (the aside tag) or by text "Selected Accounts".

    Run vitest — RED.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/forecast/phase-51-step6-sidebar.test.tsx 2>&amp;1 | grep -E "FAIL|TestingLibraryElementError"</automated>
  </verify>
  <acceptance_criteria>
    - 4 tests in describe('UX-S6-01 — Step 6 sidebar with selected accounts')
    - All 4 RED
  </acceptance_criteria>
  <done>RED tests committed as test(51-06): RED tests for Step 6 sidebar</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED — re-analyze preserves vendor toggles (UX-S6-02)</name>
  <files>src/__tests__/forecast/phase-51-step6-re-analyze.test.tsx</files>
  <read_first>
    - src/app/finances/forecast/components/wizard-v4/steps/Step6Subscriptions.tsx lines 343-385 (analyzeSubscriptions function)
  </read_first>
  <behavior>
    UX-S6-02 (3 tests):
      - Test 1: render Step6 in phase='review' with 2 vendors; toggle v1 isActive=false; click "Change selected accounts" → phase becomes 'select-accounts'; vendors state UNCHANGED (v1 still isActive=false)
      - Test 2: simulate re-analyze returning the same 2 vendors + 1 NEW vendor v3 → after merge, v1.isActive=false (preserved), v2 unchanged, v3 newly added with isActive=true
      - Test 3: simulate re-analyze returning ONLY v1 (v2 dropped because account no longer selected) → resulting vendors list contains only v1 with v1.isActive=false preserved
  </behavior>
  <action>
    Create the test file. Tests 2 and 3 will require either:
      (a) extracting `mergeByVendorKey` as a top-level export and unit-testing it directly (recommended — easier to test the logic in isolation), OR
      (b) mocking the network call inside analyzeSubscriptions and asserting the resulting vendors state.

    Recommend (a): test the helper directly. Then for the integration confidence, Test 1 covers the simple case via UI interaction (toggle + click button + assert state).

    Run vitest — RED on Tests 2 & 3 (mergeByVendorKey doesn't exist); Test 1 may PASS today (vendor state already preserved across phase changes per RESEARCH.md analysis) — INCLUDE it as a regression lock.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/forecast/phase-51-step6-re-analyze.test.tsx 2>&amp;1 | grep -E "FAIL|PASS"</automated>
  </verify>
  <acceptance_criteria>
    - 3 tests in describe('UX-S6-02 — Step 6 re-analyze preserves vendor toggles')
    - At least 2 RED
  </acceptance_criteria>
  <done>RED tests committed as test(51-06): RED tests for Step 6 re-analyze merge</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: RED — manual entry button always visible (UX-S6-03)</name>
  <files>src/__tests__/forecast/phase-51-step6-manual-entry.test.tsx</files>
  <read_first>
    - src/app/finances/forecast/components/wizard-v4/steps/Step6Subscriptions.tsx lines 433-448, 860-870 (addManualVendor + button)
  </read_first>
  <behavior>
    UX-S6-03 (3 tests):
      - Test 1: render Step6 in phase='review' with isManualMode=false (xero mode) → "+ Add Subscription" button is visible
      - Test 2: click the button → form/modal opens; fill vendor name "Stripe", monthly $50, frequency "monthly", start month "2026-08", category "Software"; submit → vendors state contains a new entry matching those values
      - Test 3: in phase='select-accounts' → "+ Add Subscription" button is NOT visible (gated by phase === 'review')
  </behavior>
  <action>
    Create the test file. Find the button via screen.getByRole('button', { name: /add subscription/i }). Find the modal form fields via accessible labels.

    Run vitest — RED (button currently isManualMode-gated; not visible in xero mode by default).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/forecast/phase-51-step6-manual-entry.test.tsx 2>&amp;1 | grep -E "FAIL|PASS"</automated>
  </verify>
  <acceptance_criteria>
    - 3 tests in describe('UX-S6-03 — Step 6 manual subscription entry')
    - At least 1 RED (Test 1 — button hidden in xero mode today)
  </acceptance_criteria>
  <done>RED tests committed as test(51-06): RED tests for Step 6 manual entry</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: GREEN — sidebar + mergeByVendorKey + always-visible button</name>
  <files>src/app/finances/forecast/components/wizard-v4/steps/Step6Subscriptions.tsx</files>
  <read_first>
    - src/app/finances/forecast/components/wizard-v4/steps/Step6Subscriptions.tsx (full file)
  </read_first>
  <action>
    1. UX-S6-01 (sidebar): wrap the existing review-phase content in a flex container with the sidebar on the left. Inside the sidebar, render the JSX from interfaces section (filter accounts by isSelected, sum vendor totals by account_codes). Confirm the sidebar appears only when phase === 'review'.

    2. UX-S6-02 (merge-on-replace): add the mergeByVendorKey helper at file scope (top of the file, after imports). Replace setVendors(vendorBudgets) at line ~367 with setVendors(prev => mergeByVendorKey(prev, vendorBudgets)). Verify by grep:
       grep -n "setVendors(vendorBudgets)" src/app/finances/forecast/components/wizard-v4/steps/Step6Subscriptions.tsx
       Should return 0 hits after the change. (Note: there are 2 setVendors call sites with different arg shapes — ONLY the one in analyzeSubscriptions that overwrites needs the merge wrapper. The setVendors at line 256/303 sets initial existingVendors from saved state — those already preserve operator state by definition. Be surgical.)

    3. UX-S6-03 (always-visible Add Subscription button): change the {isManualMode && ( gate at line ~862 to {phase === 'review' && (. Do NOT delete any other isManualMode branches in the file (research warned against this — they handle the no-Xero fallback render path).

    4. Run all tests. All 10 new tests + 13 baseline must pass.

    5. Self-check before commit:
       - grep -n "isManualMode" src/app/finances/forecast/components/wizard-v4/steps/Step6Subscriptions.tsx — still has hits (no-Xero branches preserved)
       - grep -n "mergeByVendorKey" Step6Subscriptions.tsx — at least 2 hits (helper definition + call site)
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/forecast/phase-51-step6-sidebar.test.tsx src/__tests__/forecast/phase-51-step6-re-analyze.test.tsx src/__tests__/forecast/phase-51-step6-manual-entry.test.tsx src/__tests__/forecast/wizard-v4-bug-fixes.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - 4 sidebar tests pass
    - 3 re-analyze tests pass
    - 3 manual-entry tests pass
    - 13 Phase 50 baseline tests still pass
    - tsc clean; lint clean
    - isManualMode branches still present (verified via grep)
    - mergeByVendorKey defined + called
  </acceptance_criteria>
  <done>Step 6 changes committed as feat(51-06): Step 6 sidebar + merge-on-re-analyze + always-visible Add Subscription (UX-S6-01/02/03)</done>
</task>

</tasks>

<verification>
- [ ] npx vitest run src/__tests__/forecast/phase-51-step6-sidebar.test.tsx — 4/4
- [ ] npx vitest run src/__tests__/forecast/phase-51-step6-re-analyze.test.tsx — 3/3
- [ ] npx vitest run src/__tests__/forecast/phase-51-step6-manual-entry.test.tsx — 3/3
- [ ] npx vitest run src/__tests__/forecast/wizard-v4-bug-fixes.test.tsx — 13/13
- [ ] npm run lint clean; npm run typecheck clean; npm run build green
- [ ] Sentinel (operator preview): open JDS forecast → Step 6 → analyze 5 accounts → review 20 vendors → toggle 3 off → click Change selected accounts → add a 6th account → re-analyze → 3 originally-toggled vendors STILL off → click + Add Subscription → fill modal → vendor appears in list
</verification>

<success_criteria>
PHASE.md success criteria #9 (sidebar lists selected accounts + totals), #10 (change selected accounts preserves vendor toggles), #11 (manual subscription entry).
</success_criteria>

<output>
After completion, write .planning/phases/51-forecast-wizard-ux/51-06-SUMMARY.md documenting:
- 4 task commits
- LOC delta on Step6Subscriptions.tsx
- Test counts: 10 new + 13 baseline = 23 green
- Confirmation: isManualMode branches preserved (no-Xero fallback intact)
- Confirmation: mergeByVendorKey replaced ONLY the analyzeSubscriptions setVendors call (not the saved-state restoration calls)
</output>
