---
phase: 68-step-4-wizard-mvp-improvements-armstrong-co-plan-data-refresh
plan: 10
type: execute
wave: 3
depends_on: ["68-09"]
files_modified:
  - src/app/goals/utils/quarters.ts
  - src/app/goals/components/Step4AnnualPlan.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "deriveCurrentRemainderColumn accepts optional isExtendedPeriod + planStartDate params; existing call sites with the 3-arg signature still work unchanged"
    - "When isExtendedPeriod=true AND planStartDate < fyEnd, the returned remainder column.endDate = planStartDate - 1 day"
    - "autoSplitEvenly distributes the annual amount across 5 periods (remainder + q1-q4) when isExtendedPeriod=true AND remainder column is visible"
    - "autoSplitEvenly preserves current 4-period behaviour when isExtendedPeriod=false OR remainder column is hidden"
    - "tsc + lint + build green"
  artifacts:
    - path: src/app/goals/utils/quarters.ts
      provides: B15 — extended-period-aware remainder column derivation
    - path: src/app/goals/components/Step4AnnualPlan.tsx
      provides: B16 — autoSplitEvenly distributes across remainder + 4 quarters when extended
  key_links:
    - from: Step4AnnualPlan props (isExtendedPeriod, planStartDate)
      to: deriveCurrentRemainderColumn(today, planYear, fiscalYearStart, thresholdMonths, isExtendedPeriod, planStartDate)
      via: "pass new optional args through useMemo"
      pattern: "deriveCurrentRemainderColumn"
    - from: autoSplitEvenly
      to: 5-period distribution
      via: "include current_remainder in the period array when isExtendedPeriod && currentRemainderInfo"
      pattern: "includeRemainder"
---

<objective>
Fix the extended-period bug pair. Both touch the same dataflow, so they ship together.

B15: deriveCurrentRemainderColumn at quarters.ts:259 computes fyEnd from fiscalYearStart alone. For extended-period plans (Phase 14), the remainder column overlaps with planned Year 1. Fix: when caller passes isExtendedPeriod=true AND planStartDate is before fyEnd, set the returned column's endDate to planStartDate - 1 day and trim the months label.

B16: autoSplitEvenly at Step4AnnualPlan.tsx:662 explicitly skips current_remainder. Wrong for extended plans where Y1 includes the remainder months. Fix: when isExtendedPeriod=true AND currentRemainderInfo exists, distribute annual / 5; Q4 absorbs rounding. Non-extended behaviour unchanged.

Purpose: Removes data-incorrectness for Phase 14 clients.
Output: Two file edits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/68-step-4-wizard-mvp-improvements-armstrong-co-plan-data-refresh/68-CONTEXT.md
@src/app/goals/utils/quarters.ts
@src/app/goals/components/Step4AnnualPlan.tsx

INTERFACES:

Current deriveCurrentRemainderColumn signature (quarters.ts:238-292):
  export function deriveCurrentRemainderColumn(today, planYear, fiscalYearStart, thresholdMonths=3): QuarterInfo | null

Target signature (additive — default values preserve back-compat):
  export function deriveCurrentRemainderColumn(today, planYear, fiscalYearStart, thresholdMonths=3, isExtendedPeriod=false, planStartDate: Date|null=null): QuarterInfo | null

Current autoSplitEvenly distributes across q1..q4 only (line 662). The CURRENT
implementation at Step4AnnualPlan.tsx:662-681 ends the per-metric write with the
explicit cast `as { q1: string; q2: string; q3: string; q4: string; current_remainder?: string }`
to satisfy the strict `quarterlyTargets` index signature declared at
Step4AnnualPlan.tsx:21-22 as
  Record<string, { q1: string; q2: string; q3: string; q4: string; current_remainder?: string }>.
That cast MUST be preserved in the rewrite — relaxing the index signature is
out of scope for B16 and would silently weaken types on every consumer.

Current Step4Props (lines 28-36) marks isExtendedPeriod as legacy. We un-deprecate it and ADD planStartDate prop.
</context>

<tasks>

<task type="auto">
  <name>Task 1: B15 — Extend deriveCurrentRemainderColumn with isExtendedPeriod + planStartDate</name>
  <files>src/app/goals/utils/quarters.ts</files>
  <read_first>
    - src/app/goals/utils/quarters.ts (full file — confirm current signature and lines 238-292)
    - .planning/phases/68-step-4-wizard-mvp-improvements-armstrong-co-plan-data-refresh/68-CONTEXT.md (B15 decision)
  </read_first>
  <action>
    Edit src/app/goals/utils/quarters.ts:

    1. Update the deriveCurrentRemainderColumn JSDoc (lines 220-237). Append a paragraph: "When isExtendedPeriod=true and planStartDate is provided AND planStartDate is before fyEnd, the returned column's endDate is trimmed to one day before planned Year 1 starts so the remainder does not overlap with planned Year 1. This handles Phase 14 first-time-extended-period plans where Year 1 begins mid-FY."

    2. Change the function signature to:
       export function deriveCurrentRemainderColumn(
         today: Date,
         planYear: number,
         fiscalYearStart: number,
         thresholdMonths: number = 3,
         isExtendedPeriod: boolean = false,
         planStartDate: Date | null = null,
       ): QuarterInfo | null {

    3. Change "const fyEnd = new Date(fyEndYear, fyEndMonth, 0)" to "let fyEnd = new Date(fyEndYear, fyEndMonth, 0)" so it can be reassigned.

    4. Immediately after the existing fyEnd.setHours(23, 59, 59, 999) line, insert the override block:

       // B15: extended-period override — trim remainder to end the day before
       // planned Year 1 starts, so the remainder does not overlap planned months.
       if (isExtendedPeriod && planStartDate instanceof Date && planStartDate < fyEnd) {
         fyEnd = new Date(planStartDate.getTime() - 24 * 60 * 60 * 1000)
         fyEnd.setHours(23, 59, 59, 999)
       }

    5. Replace usages of the constant fyEndMonth in the label/range calculation with a dynamic endMonth read from fyEnd AFTER the override block. Specifically: change "const endMonth = fyEndMonth" to "const endMonth = fyEnd.getMonth() + 1". The existing arithmetic on monthsUntilEnd already reads fyEnd.getFullYear()/getMonth() so it picks up the override automatically.

    6. Returned object's endDate field continues to use (the possibly-overridden) fyEnd.

    Do NOT change calculateQuarters, determinePlanYear, or getAvailableQuarters. Default values on the new params preserve back-compat for the existing 3-arg call site in Step4AnnualPlan.tsx:76.
  </action>
  <verify>
    <automated>grep -n "isExtendedPeriod: boolean = false" src/app/goals/utils/quarters.ts &amp;&amp; grep -n "planStartDate: Date | null = null" src/app/goals/utils/quarters.ts &amp;&amp; grep -n "planStartDate instanceof Date && planStartDate < fyEnd" src/app/goals/utils/quarters.ts &amp;&amp; grep -n "let fyEnd" src/app/goals/utils/quarters.ts &amp;&amp; npx tsc --noEmit 2&gt;&amp;1 | grep -E "quarters\.ts|error TS" | head -10 || echo "tsc clean for quarters.ts"</automated>
  </verify>
  <acceptance_criteria>
    - File contains literal substring "isExtendedPeriod: boolean = false".
    - File contains literal substring "planStartDate: Date | null = null".
    - File contains literal substring "planStartDate instanceof Date && planStartDate < fyEnd".
    - File contains literal substring "planStartDate.getTime() - 24 * 60 * 60 * 1000".
    - File contains "let fyEnd" (not just "const fyEnd").
    - Any existing test files matching quarters*.test.ts still pass: "npx vitest run --include '**/quarters*.test.*'" exits 0 or reports no tests.
    - "npx tsc --noEmit" reports no errors for src/app/goals/utils/quarters.ts.
  </acceptance_criteria>
  <done>Function signature extended; existing 3-arg call site keeps working; tsc green.</done>
</task>

<task type="auto">
  <name>Task 2: B16 — autoSplitEvenly distributes across 5 periods when extended; wire props through</name>
  <files>src/app/goals/components/Step4AnnualPlan.tsx</files>
  <read_first>
    - src/app/goals/components/Step4AnnualPlan.tsx (Step4Props lines 11-44 — confirms the
      `quarterlyTargets` index signature is `Record<string, { q1: string; q2: string; q3: string; q4: string; current_remainder?: string }>`,
      which is WHY the existing autoSplitEvenly at line 675 ends with the explicit cast
      `as { q1: string; q2: string; q3: string; q4: string; current_remainder?: string }`.
      The rewrite below MUST keep that cast verbatim — without it, `tsc --noEmit` fails
      because the spread `{...(newTargets[metricKey] || {}), ...distributed}` widens the
      shape and TS can't prove it matches the index signature. Relaxing the index signature
      is explicitly out of scope for B16.)
    - src/app/goals/components/Step4AnnualPlan.tsx (useMemo at line 75-78; autoSplitEvenly at 662-681)
    - src/app/goals/utils/quarters.ts (post-Task-1 — confirm new signature)
    - .planning/phases/68-step-4-wizard-mvp-improvements-armstrong-co-plan-data-refresh/68-CONTEXT.md (B16 decision)
  </read_first>
  <action>
    Edit src/app/goals/components/Step4AnnualPlan.tsx:

    1. Un-deprecate isExtendedPeriod prop and add planStartDate prop. In Step4Props (around lines 28-36):
       - Replace the JSDoc above isExtendedPeriod with: "Phase 14 extended-period support — when true, the wizard is planning a 13-15 month first plan whose Year 1 begins mid-FY. Combined with planStartDate, this trims the remainder column to end before Y1 begins AND distributes auto-split across 5 periods instead of 4."
       - Keep the lines: isExtendedPeriod?: boolean; currentYearRemainingMonths?: number; fiscalYearStart?: number;
       - ADD a new line right below them: planStartDate?: Date | string | null

    2. Update the destructure (lines 49-65):
       - Change "isExtendedPeriod: _isExtendedPeriod" to "isExtendedPeriod" (remove the underscore prefix and rename).
       - ADD "planStartDate: planStartDateProp" to the destructure list.

    3. Right after the destructure (just before "const planYear = planYearProp ?? ..."), add:

       const planStartDate = useMemo<Date | null>(() => {
         if (!planStartDateProp) return null
         if (planStartDateProp instanceof Date) return planStartDateProp
         const d = new Date(planStartDateProp)
         return isNaN(d.getTime()) ? null : d
       }, [planStartDateProp])

    4. Update the deriveCurrentRemainderColumn call (line 76). Replace:
       () => deriveCurrentRemainderColumn(new Date(), planYear, fiscalYearStart ?? 7),
       with:
       () => deriveCurrentRemainderColumn(new Date(), planYear, fiscalYearStart ?? 7, 3, !!isExtendedPeriod, planStartDate),

       Also update the useMemo dependency array to [planYear, fiscalYearStart, isExtendedPeriod, planStartDate].

    5. Replace autoSplitEvenly (lines 662-681) with the block below. IMPORTANT: the final
       assignment to `newTargets[metricKey]` MUST keep the explicit cast
       `as { q1: string; q2: string; q3: string; q4: string; current_remainder?: string }`.
       The cast is needed because `quarterlyTargets` is declared with the strict index
       signature at Step4Props lines 21-22, and TS cannot prove a spread of
       `newTargets[metricKey] || {}` matches that signature. The current code at line 675
       has this exact cast — DO NOT remove it during the rewrite. (Relaxing the index
       signature on Step4Props.quarterlyTargets is out of scope for B16.)

       const autoSplitEvenly = () => {
         if (!financialData) return
         const includeRemainder = !!(isExtendedPeriod && currentRemainderInfo)
         const periodCount = includeRemainder ? 5 : 4
         const newTargets = { ...quarterlyTargets }
         const updateMetric = (metricKey: string, annual: number) => {
           if (annual <= 0) return
           const each = Math.round(annual / periodCount)
           const distributed: { q1: string; q2: string; q3: string; q4: string; current_remainder?: string } = {
             ...(newTargets[metricKey] || { q1: '', q2: '', q3: '', q4: '' }),
             q1: each.toString(),
             q2: each.toString(),
             q3: each.toString(),
             // Q4 absorbs rounding remainder so the sum equals annual exactly.
             q4: (annual - each * (periodCount - 1)).toString(),
           }
           if (includeRemainder) {
             distributed.current_remainder = each.toString()
           }
           // Preserve the explicit cast from the original code (line 675) so the
           // assignment satisfies the strict `quarterlyTargets` index signature
           // declared at Step4Props:21-22. Without this cast, tsc --noEmit fails.
           newTargets[metricKey] = {
             ...(newTargets[metricKey] || {}),
             ...distributed,
           } as { q1: string; q2: string; q3: string; q4: string; current_remainder?: string }
         }
         updateMetric('revenue', financialData.revenue.year1)
         updateMetric('grossProfit', financialData.grossProfit.year1)
         updateMetric('netProfit', financialData.netProfit.year1)
         setQuarterlyTargets(newTargets)
       }

    The Q4 calculation annual - each*(periodCount-1):
       - periodCount=4 -> annual - each*3 (same as current behaviour, unchanged for non-extended)
       - periodCount=5 -> annual - each*4 (5 periods x each = annual ± rounding; Q4 absorbs)

    6. Do NOT change handleStaggerByPriority. Leave the underscore-prefixed currentYearRemainingMonths param alone (we don't need it).
  </action>
  <verify>
    <automated>grep -n "isExtendedPeriod && currentRemainderInfo" src/app/goals/components/Step4AnnualPlan.tsx &amp;&amp; grep -n "includeRemainder ? 5 : 4" src/app/goals/components/Step4AnnualPlan.tsx &amp;&amp; grep -n "deriveCurrentRemainderColumn(new Date(), planYear, fiscalYearStart ?? 7, 3, !!isExtendedPeriod, planStartDate)" src/app/goals/components/Step4AnnualPlan.tsx &amp;&amp; grep -n "as { q1: string; q2: string; q3: string; q4: string; current_remainder?: string }" src/app/goals/components/Step4AnnualPlan.tsx &amp;&amp; npx tsc --noEmit 2&gt;&amp;1 | grep -E "Step4AnnualPlan|error TS" | head -10 || echo "tsc clean" &amp;&amp; npm run lint -- src/app/goals/components/Step4AnnualPlan.tsx 2&gt;&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File contains literal "isExtendedPeriod && currentRemainderInfo".
    - File contains literal "includeRemainder ? 5 : 4".
    - File contains literal "deriveCurrentRemainderColumn(new Date(), planYear, fiscalYearStart ?? 7, 3, !!isExtendedPeriod, planStartDate)".
    - File contains literal "planStartDate?: Date | string | null" in Step4Props.
    - File contains literal "planStartDate: planStartDateProp" in the destructure.
    - File contains the literal cast `as { q1: string; q2: string; q3: string; q4: string; current_remainder?: string }` inside autoSplitEvenly (preserved from the pre-rewrite implementation).
    - "npx tsc --noEmit" reports no errors in this file.
    - "npm run lint -- src/app/goals/components/Step4AnnualPlan.tsx" exits 0.
    - "npm run build" exits 0.
  </acceptance_criteria>
  <done>5-period distribution active for extended-period plans; non-extended behaviour unchanged; tsc + lint + build green.</done>
</task>

</tasks>

<verification>
- Simulated mental test: for an extended plan with annual revenue = 1,000,000, autoSplitEvenly produces { current_remainder: 200000, q1: 200000, q2: 200000, q3: 200000, q4: 200000 }. Sum equals 1,000,000.
- For a non-extended plan with annual = 1,000,000, autoSplitEvenly produces { q1: 250000, q2: 250000, q3: 250000, q4: 250000 } (unchanged from today).
- For an extended plan with annual = 1,000,003 (rounding case), each = 200001, Q4 = 1000003 - 200001*4 = 199999. Sum = 200001 + 200001 + 200001 + 199999 + 200001 = 1,000,003. Verify Q4 absorbs.
- npm run build exits 0.
</verification>

<success_criteria>
- B15 fix: remainder column properly bounded for extended plans.
- B16 fix: auto-split is correct for extended plans without breaking non-extended.
- No regression in deriveCurrentRemainderColumn back-compat.
</success_criteria>

<output>
After completion, create .planning/phases/68-step-4-wizard-mvp-improvements-armstrong-co-plan-data-refresh/68-10-SUMMARY.md documenting both diffs and the arithmetic verification examples.
</output>
</content>
</invoke>