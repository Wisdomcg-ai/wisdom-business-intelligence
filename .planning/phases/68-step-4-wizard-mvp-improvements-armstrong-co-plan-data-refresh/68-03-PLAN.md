---
phase: 68-step-4-wizard-mvp-improvements-armstrong-co-plan-data-refresh
plan: 03
type: execute
wave: 1
depends_on: ["68-01", "68-02"]
files_modified:
  - scripts/68-03-armstrong-diversification-ideas.mjs
autonomous: true
requirements: []
must_haves:
  truths:
    - "All 13 A3 diversification ideas exist in strategic_initiatives for Armstrong as step_type='strategic_ideas', idea_type='exploratory', selected=false"
    - "Re-running the script with --apply does NOT create duplicate rows"
    - "Dry-run prints the 13 insert payloads and writes nothing"
  artifacts:
    - path: scripts/68-03-armstrong-diversification-ideas.mjs
      provides: A3 insert of 13 diversification ideas as exploratory strategic_ideas rows
  key_links:
    - from: scripts/68-03-armstrong-diversification-ideas.mjs
      to: strategic_initiatives table
      via: "POST with conflict-target on (business_id, title, step_type) via pre-check existence"
      pattern: "idea_type: 'exploratory'"
---

<objective>
A3: Add 13 diversification ideas to Armstrong's `strategic_initiatives` as parking-lot exploratory items. Each row gets `step_type='strategic_ideas'`, `idea_type='exploratory'`, `selected=false`, `quarter_assigned=null`, `category='diversification'`.

Purpose: Captures the 2026-05-12 session brainstorm so they don't get lost.
Output: One Node script, dry-run + `--apply`, idempotent via title-based existence check.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/68-step-4-wizard-mvp-improvements-armstrong-co-plan-data-refresh/68-CONTEXT.md
@scripts/onboard-fit2shine.mjs

<interfaces>
Armstrong tenant:
- BUSINESS_PROFILES_ID = '678ae542-7f0b-43d1-8784-e7341767c250'
- USER_ID              = 'f4702002-69a6-44f1-b963-ada2a95c843b'

13 diversification idea titles (insert verbatim):
1.  "Australian Housing partnership (Jordan Ricketts)"
2.  "NSW affordable housing builder panel"
3.  "Strata maintenance / repair work"
4.  "In-house electrical + plumbing"
5.  "Insurance remediation work (flood/storm/roof)"
6.  "Duplex defect remediation"
7.  "School works / demountables"
8.  "Government tenders"
9.  "University maintenance contracts"
10. "Waterfront / barge + maritime partner (Class 2 licence, Evolve FM panel)"
11. "Prefab passive homes / kit homes"
12. "Pontoon innovation (Kevlar/fibreglass)"
13. "Experiences subscription business (Portelli-style)"

Per-row payload (all 13 identical except title):
{
  business_id: BUSINESS_PROFILES_ID,
  user_id: USER_ID,
  title: <one of 13>,
  step_type: 'strategic_ideas',
  idea_type: 'exploratory',
  selected: false,
  category: 'diversification',
  quarter_assigned: null,
  year_assigned: null,
  fiscal_year: null,
  priority: null,
  source: 'manual'
}
</interfaces>
</context>

<verification_policy>
ENV-GATING (mirrors plan 68-01's policy across the A workstream):

Acceptance criteria split into TWO groups:

1. **Static** — always run, no env required: file existence, `node --check`,
   and `grep` of literal constants in the script source.

2. **Live-execution** — requires `.env.local` with Supabase credentials.
   Invoked via the tolerant pattern
       test -f .env.local && (<live cmd>) || echo "SKIP: live verify requires .env.local"
   so the verify step does NOT fail when `.env.local` is absent.

Static acceptance criteria block on every CI/agent environment; live-execution
criteria are operator-run and gated by `.env.local`. If `.env.local` is absent
in the executor environment, the operator MUST run the live block manually
before merge and paste the output into the SUMMARY.

Pattern mirrors `scripts/onboard-fit2shine.mjs`, which prints payloads in
dry-run mode without requiring write access.
</verification_policy>

<tasks>

<task type="auto">
  <name>Task 1: Build diversification ideas insert script</name>
  <files>scripts/68-03-armstrong-diversification-ideas.mjs</files>
  <read_first>
    - scripts/onboard-fit2shine.mjs (env loading, --apply pattern, headers, POST helper)
    - .planning/phases/68-step-4-wizard-mvp-improvements-armstrong-co-plan-data-refresh/68-CONTEXT.md (A3 list)
    - scripts/68-02-armstrong-initiatives-dedupe-and-assign.mjs (consistency of style with sibling A scripts — read after Plan 68-02 lands)
  </read_first>
  <action>
    Create `scripts/68-03-armstrong-diversification-ideas.mjs`:

    1. Header JSDoc: A3 description, idempotency note, run instructions.
    2. Env loading + `APPLY` flag — same as onboard-fit2shine.mjs lines 16-24.
    3. Hardcode `BUSINESS_PROFILES_ID`, `USER_ID`.
    4. Define `const DIVERSIFICATION_TITLES = [ ... 13 strings ... ]` — literal verbatim from <interfaces>.
    5. Define `buildPayload(title)` returning the exact object shape in <interfaces>.
    6. Headers + `apiGet`, `apiInsert` helpers.
    7. Main flow:
       a. Fetch existing titles: `GET strategic_initiatives?business_id=eq.${BPID}&step_type=eq.strategic_ideas&select=title`.
       b. Build `existingTitles = new Set(rows.map(r => (r.title||'').trim().toLowerCase()))`.
       c. For each of the 13 titles, check `existingTitles.has(title.trim().toLowerCase())`. If yes → skip + log "SKIP (already exists)". If no → add to inserts queue.
       d. Print "INSERT PLAN (N new ideas)" with the queue.
       e. If `!APPLY`, log "DRY RUN" and exit.
       f. If `APPLY`, POST each payload sequentially. Stop on first non-2xx response with a clear error.
    8. Final summary: "Inserted N rows. Skipped M rows (already existed)."
  </action>
  <verify>
    <!-- STATIC verify — runs unconditionally, no env required -->
    <automated>test -f scripts/68-03-armstrong-diversification-ideas.mjs && node --check scripts/68-03-armstrong-diversification-ideas.mjs && grep -q "'678ae542-7f0b-43d1-8784-e7341767c250'" scripts/68-03-armstrong-diversification-ideas.mjs && grep -q "Australian Housing partnership" scripts/68-03-armstrong-diversification-ideas.mjs</automated>
    <!-- LIVE verify — requires .env.local with NEXT_PUBLIC_SUPABASE_URL + SUPABASE_SERVICE_KEY -->
    <live_automated>test -f .env.local && (node scripts/68-03-armstrong-diversification-ideas.mjs | tee /tmp/68-03-dryrun.log && grep -E "INSERT PLAN|DRY RUN" /tmp/68-03-dryrun.log && grep -c "Australian Housing partnership\|Pontoon innovation\|Experiences subscription" /tmp/68-03-dryrun.log) || echo "SKIP: live verify requires .env.local"</live_automated>
  </verify>
  <acceptance_criteria>
    Static (always run, no env required):
    - File `scripts/68-03-armstrong-diversification-ideas.mjs` exists.
    - Contains literal `'678ae542-7f0b-43d1-8784-e7341767c250'`.
    - Contains literal `APPLY = process.argv.includes('--apply')`.
    - Contains literal title `"Australian Housing partnership (Jordan Ricketts)"`.
    - Contains literal title `"Pontoon innovation (Kevlar/fibreglass)"`.
    - Contains literal title `"Experiences subscription business (Portelli-style)"`.
    - Contains literal `'strategic_ideas'`, `'exploratory'`, `'diversification'`.
    - Contains literal `selected: false`.
    - `grep -c "^\\s*\"" scripts/68-03-armstrong-diversification-ideas.mjs` shows at least 13 string literals (one per title).

    Live (requires `.env.local` with live Supabase credentials; operator-run, gated on env presence):
    - Dry-run output prints `INSERT PLAN` and `DRY RUN`.
    - Running `--apply` once then again produces "Inserted 0 rows. Skipped 13 rows" (idempotent).
  </acceptance_criteria>
  <done>Script committed; dry-run shows 13 inserts pending (assuming none exist yet); idempotency verified.</done>
</task>

</tasks>

<verification>
- Post-apply re-query: `SELECT COUNT(*) FROM strategic_initiatives WHERE business_id='678ae542-7f0b-43d1-8784-e7341767c250' AND step_type='strategic_ideas' AND category='diversification'` returns ≥ 13.
- Re-running with `--apply` is a no-op.
</verification>

<success_criteria>
- Armstrong has 13 new exploratory diversification ideas in strategic_initiatives.
- Idempotent — re-runs do not duplicate.
</success_criteria>

<output>
After completion, create `.planning/phases/68-step-4-wizard-mvp-improvements-armstrong-co-plan-data-refresh/68-03-SUMMARY.md` with dry-run + apply output and idempotency confirmation.
</output>
