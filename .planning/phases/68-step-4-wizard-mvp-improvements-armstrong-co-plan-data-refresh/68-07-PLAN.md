---
phase: 68-step-4-wizard-mvp-improvements-armstrong-co-plan-data-refresh
plan: 07
type: execute
wave: 1
depends_on: ["68-01", "68-02"]
files_modified:
  - scripts/68-07-armstrong-sales-process-note.mjs
autonomous: true
requirements: []
must_haves:
  truths:
    - "The 'Unpack sales process' strategic_initiative for Armstrong has a notes JSONB containing currentProcess + proposedAdditions arrays"
    - "Re-running the script does NOT overwrite an already-correct notes payload"
    - "Dry-run prints the patch payload and writes nothing"
  artifacts:
    - path: scripts/68-07-armstrong-sales-process-note.mjs
      provides: A11 sales-process documentation written to initiative notes JSONB
  key_links:
    - from: scripts/68-07-armstrong-sales-process-note.mjs
      to: strategic_initiatives.notes (JSONB)
      via: "PATCH on row where business_id=BPID AND step_type='twelve_month' AND lower(trim(title))='unpack sales process'"
      pattern: "currentProcess"
---

<objective>
A11: Document Armstrong's current sales process and proposed additions as a structured JSONB note on the existing "Unpack sales process" strategic_initiative row (Q1 FY27 — assigned in plan 68-02). No new schema.

Purpose: Captures session content so Luke + Matt can refer back when running the sales-process unpack initiative.
Output: One Node script, dry-run + `--apply`, idempotent via deep-equality on notes payload.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/68-step-4-wizard-mvp-improvements-armstrong-co-plan-data-refresh/68-CONTEXT.md
@scripts/onboard-fit2shine.mjs

<interfaces>
Armstrong:
- BUSINESS_PROFILES_ID = '678ae542-7f0b-43d1-8784-e7341767c250'

Target initiative match:
- step_type = 'twelve_month'
- lower(trim(title)) = 'unpack sales process'

Target notes payload (write to strategic_initiatives.notes JSONB):
{
  capturedFrom: '2026-05-12 Armstrong session',
  currentProcess: [
    'Architect produces plans',
    'Client takes plans to builder for price',
    'Initial meeting with client',
    'Site walk',
    'Reference check / references provided',
    'Formal quote',
    'Follow-up'
  ],
  proposedAdditions: [
    'Discovery questions block before pricing — surface budget, decision-makers, timeline, decision criteria',
    'Choreographed indecision period — make space for the client to slow down and feel the decision',
    '2-beer fit-test — informal social meeting to confirm relationship-fit before signing'
  ]
}

REST:
- GET   `strategic_initiatives?business_id=eq.${BPID}&step_type=eq.twelve_month&select=id,title,notes`
- PATCH `strategic_initiatives?id=eq.${rowId}` body { notes: <payload> }
</interfaces>
</context>

<verification_policy>
ENV-GATING (mirrors plan 68-01's policy across the A workstream):

Acceptance criteria split into TWO groups:

1. **Static** — always run, no env required: file existence, `node --check`,
   and `grep` of literal constants in the script source.

2. **Live-execution** — requires `.env.local` with Supabase credentials.
   Invoked via the tolerant pattern
       test -f .env.local && (<live cmd>) || echo "SKIP: live verify requires .env.local"
   so the verify step does NOT fail when `.env.local` is absent.

Static acceptance criteria block on every CI/agent environment; live-execution
criteria are operator-run and gated by `.env.local`. If `.env.local` is absent
in the executor environment, the operator MUST run the live block manually
before merge and paste the output into the SUMMARY.

Pattern mirrors `scripts/onboard-fit2shine.mjs`, which prints payloads in
dry-run mode without requiring write access.
</verification_policy>

<tasks>

<task type="auto">
  <name>Task 1: Build sales-process note patch script</name>
  <files>scripts/68-07-armstrong-sales-process-note.mjs</files>
  <read_first>
    - scripts/onboard-fit2shine.mjs (env + APPLY + headers + patch pattern)
    - .planning/phases/68-step-4-wizard-mvp-improvements-armstrong-co-plan-data-refresh/68-CONTEXT.md (A11 decision)
    - scripts/68-02-armstrong-initiatives-dedupe-and-assign.mjs (the script that creates/assigns the "Unpack sales process" row; this plan depends on 68-02 having run so the canonical row exists)
  </read_first>
  <action>
    Create `scripts/68-07-armstrong-sales-process-note.mjs`:

    1. Header JSDoc — note dependency on 68-02 (the "Unpack sales process" row must exist with step_type='twelve_month').
    2. Env + `APPLY` flag.
    3. Hardcode `BUSINESS_PROFILES_ID`.
    4. Define `const NOTES_PAYLOAD = { ... }` — literal verbatim from <interfaces>.
    5. Helpers: apiGet, apiPatch.
    6. Main flow:
       a. GET strategic_initiatives for BPID with step_type=twelve_month.
       b. Find row where `lower(trim(row.title || '')) === 'unpack sales process'`.
       c. If not found → log "ERROR: 'Unpack sales process' initiative not found — has plan 68-02 run?" and exit non-zero.
       d. Compare existing row.notes to NOTES_PAYLOAD via `JSON.stringify(a) === JSON.stringify(b)`. If equal → log "notes already match — skipping" and exit 0.
       e. Print PATCH plan with payload.
       f. If `!APPLY`, exit (DRY RUN).
       g. Else PATCH row.notes.
    7. Final summary.
  </action>
  <verify>
    <!-- STATIC verify — runs unconditionally, no env required -->
    <automated>test -f scripts/68-07-armstrong-sales-process-note.mjs && node --check scripts/68-07-armstrong-sales-process-note.mjs && grep -q "'678ae542-7f0b-43d1-8784-e7341767c250'" scripts/68-07-armstrong-sales-process-note.mjs && grep -q "unpack sales process" scripts/68-07-armstrong-sales-process-note.mjs</automated>
    <!-- LIVE verify — requires .env.local with NEXT_PUBLIC_SUPABASE_URL + SUPABASE_SERVICE_KEY -->
    <live_automated>test -f .env.local && (node scripts/68-07-armstrong-sales-process-note.mjs | tee /tmp/68-07-dryrun.log && grep -E "DRY RUN|PATCH plan|already match|ERROR" /tmp/68-07-dryrun.log) || echo "SKIP: live verify requires .env.local"</live_automated>
  </verify>
  <acceptance_criteria>
    Static (always run, no env required):
    - File `scripts/68-07-armstrong-sales-process-note.mjs` exists.
    - Contains literal `'678ae542-7f0b-43d1-8784-e7341767c250'`.
    - Contains literal `'unpack sales process'` (lowercase, for match).
    - Contains literal `'currentProcess'` and `'proposedAdditions'` (JSONB keys).
    - Contains literal `'2-beer fit-test'` (proposed addition).
    - Contains literal `'Choreographed indecision period'`.
    - Contains literal `'Architect produces plans'` (current process step 1).
    - Contains literal `'capturedFrom: \'2026-05-12 Armstrong session\''` (or equivalent escaping).

    Live (requires `.env.local` with live Supabase credentials; operator-run, gated on env presence):
    - Dry-run output contains `DRY RUN` (assuming the row exists and notes don't yet match).
    - Running `--apply` once then again prints "notes already match — skipping" (idempotent).
  </acceptance_criteria>
  <done>Script committed; dry-run shows the patch payload; idempotency verified.</done>
</task>

</tasks>

<verification>
- Post-apply re-query: the "Unpack sales process" row's `notes` JSONB has top-level keys `capturedFrom`, `currentProcess` (7 items), `proposedAdditions` (3 items).
</verification>

<success_criteria>
- Sales-process knowledge captured in-app where coach + client can reference it.
- Idempotent.
</success_criteria>

<output>
After completion, create `.planning/phases/68-step-4-wizard-mvp-improvements-armstrong-co-plan-data-refresh/68-07-SUMMARY.md` with dry-run + apply output.
</output>
