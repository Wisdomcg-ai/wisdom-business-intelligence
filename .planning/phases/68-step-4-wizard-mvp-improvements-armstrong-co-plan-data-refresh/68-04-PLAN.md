---
phase: 68-step-4-wizard-mvp-improvements-armstrong-co-plan-data-refresh
plan: 04
type: execute
wave: 1
depends_on: ["68-01"]
files_modified:
  - scripts/68-04-armstrong-team-roster-and-owner-hours.mjs
autonomous: true
requirements: []
must_haves:
  truths:
    - "business_profiles.key_roles for Armstrong contains entries for Pablo (Foreman Lockup→Finish), Kye (Foreman Setup→Lockup), Carly, Cooper, Chris"
    - "Pablo's role entry includes a notes/status indicating foreman-promotion candidate target Jan-Jun 2028"
    - "business_profiles.owner_info.desired_hours = 10 (was 0)"
    - "business_financial_goals.owner_hours_per_week_current=50, year1=40, year2=25, year3=10"
    - "Re-running the script with --apply does NOT overwrite already-correct fields"
  artifacts:
    - path: scripts/68-04-armstrong-team-roster-and-owner-hours.mjs
      provides: A4 team roster + A6 owner-hours backfill for Armstrong
  key_links:
    - from: scripts/68-04-armstrong-team-roster-and-owner-hours.mjs
      to: business_profiles + business_financial_goals
      via: "PATCH with JSONB merge for key_roles + owner_info; PATCH for owner_hours_per_week_*"
      pattern: "owner_hours_per_week_current"
---

<objective>
A4: Update `business_profiles.key_roles` to include Pablo (Foreman Lockup→Finish), Kye (Foreman Setup→Lockup), Carly (Subcontract Carpenter — first apprentice), Cooper (Subcontract Carpenter), Chris (Carpenter — just started). Annotate Pablo as foreman-promotion candidate, target Jan-Jun 2028.

A6: Set `business_financial_goals.owner_hours_per_week_current=50`, `_year1=40`, `_year2=25`, `_year3=10`. Also fix `business_profiles.owner_info.desired_hours` from 0 to 10.

Note on A4 "Pablo == Pubs" reconciliation: per CONTEXT.md, this is pending Matt's answer. Script documents both names as same person via the `notes` field on Pablo's role entry, but does NOT delete any "Pubs" row if it exists separately — flags it for review.

Purpose: Reflects 2026-05-12 session — Luke's true current hours (50), realistic glide-path, full team roster.
Output: One Node script, dry-run + `--apply`, idempotent via field-level equality check.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/68-step-4-wizard-mvp-improvements-armstrong-co-plan-data-refresh/68-CONTEXT.md
@scripts/onboard-fit2shine.mjs
@docs/BUSINESS_ID_PATTERNS.md

<interfaces>
Armstrong:
- BUSINESS_PROFILES_ID = '678ae542-7f0b-43d1-8784-e7341767c250'
- USER_ID              = 'f4702002-69a6-44f1-b963-ada2a95c843b'

Target key_roles entries (full array — script REPLACES key_roles since it's a managed JSONB list):
[
  { name: 'Pablo', role: 'Foreman (Lockup → Finish)', notes: 'Foreman-promotion candidate — target ready Jan-Jun 2028. Possibly same person as "Pubs" referenced in earlier notes; confirm with Matt.', status: 'promotion_candidate' },
  { name: 'Kye',   role: 'Foreman (Setup → Lockup)', notes: '' },
  { name: 'Carly', role: 'Subcontract Carpenter',    notes: 'First apprentice — "huge part of the cocktail"' },
  { name: 'Cooper', role: 'Subcontract Carpenter',   notes: '' },
  { name: 'Chris', role: 'Carpenter',                 notes: 'Just started' }
]

Note: if current key_roles contains any names NOT in this list, script PRINTS them as "PRESERVING existing roles" and APPENDS them so we don't accidentally drop coach data. The final write is the union: (existing rows whose name isn't in the target list) + (the 5 target rows above).

Target owner_info merge (PATCH-merge — preserves all other owner_info keys):
{
  ...existing_owner_info,
  desired_hours: 10
}

Target business_financial_goals row (PATCH on business_id=BUSINESS_PROFILES_ID):
{
  owner_hours_per_week_current: 50,
  owner_hours_per_week_year1: 40,
  owner_hours_per_week_year2: 25,
  owner_hours_per_week_year3: 10
}

REST endpoints:
- GET   `business_profiles?id=eq.${BPID}&select=key_roles,owner_info`
- PATCH `business_profiles?id=eq.${BPID}` body { key_roles, owner_info }
- GET   `business_financial_goals?business_id=eq.${BPID}&select=owner_hours_per_week_current,owner_hours_per_week_year1,owner_hours_per_week_year2,owner_hours_per_week_year3`
- PATCH `business_financial_goals?business_id=eq.${BPID}` body { owner_hours_per_week_current, owner_hours_per_week_year1, owner_hours_per_week_year2, owner_hours_per_week_year3 }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build team-roster + owner-hours patch script</name>
  <files>scripts/68-04-armstrong-team-roster-and-owner-hours.mjs</files>
  <read_first>
    - scripts/onboard-fit2shine.mjs (lines 210-225 owner_info shape; lines 248-272 business_profiles patch pattern; lines 275-285 business_financial_goals insert pattern)
    - .planning/phases/68-step-4-wizard-mvp-improvements-armstrong-co-plan-data-refresh/68-CONTEXT.md (A4 + A6 decisions)
    - src/app/goals/components/Step4AnnualPlan.tsx (lines 134-189 — confirms key_roles + owner_info.partners shapes the wizard reads; our writes must keep those shapes intact)
  </read_first>
  <action>
    Create `scripts/68-04-armstrong-team-roster-and-owner-hours.mjs`:

    1. Header JSDoc: A4 + A6 description, idempotency note, run instructions.
    2. Env + `APPLY` flag.
    3. Hardcode `BUSINESS_PROFILES_ID`, `USER_ID`.
    4. Define `const TARGET_ROLES = [ ... 5 objects ... ]` — exactly as in <interfaces>.
    5. Define `const TARGET_OWNER_HOURS = { owner_hours_per_week_current: 50, owner_hours_per_week_year1: 40, owner_hours_per_week_year2: 25, owner_hours_per_week_year3: 10 };`
    6. Define `const TARGET_DESIRED_HOURS = 10;`
    7. Headers + helpers (apiGet, apiPatch).
    8. Main flow:
       a. GET current business_profiles row.
       b. Merge key_roles: build `existingByName = new Map(existing.key_roles.filter(r=>r?.name).map(r=>[r.name.toLowerCase(), r]))`. Preserve any existing role whose name isn't in TARGET_ROLES (case-insensitive). Final write = `[...preserved, ...TARGET_ROLES]`. Log "PRESERVING existing roles: [names]" if any.
       c. Merge owner_info: `{...existing.owner_info, desired_hours: TARGET_DESIRED_HOURS}`. Skip write if `existing.owner_info.desired_hours === 10`.
       d. Determine if key_roles or owner_info actually changes. If neither changes, log "business_profiles: no changes needed".
       e. If at least one changes, build PATCH body containing only the changed key(s).
       f. GET current business_financial_goals row. Build PATCH body containing only the owner_hours_per_week_* fields whose current value differs from target. If all four already match, log "business_financial_goals: no changes needed".
       g. Print full PATCH plan to console.
       h. If `!APPLY`, exit (DRY RUN).
       i. If `APPLY`, execute PATCH on business_profiles (if changes) then business_financial_goals (if changes).
    9. Final summary.

    DO NOT call this script with a single bulk replacement of key_roles or owner_info — always merge to preserve unrelated keys (CLAUDE.md additive-JSONB principle).
  </action>
  <verify>
    <automated>node scripts/68-04-armstrong-team-roster-and-owner-hours.mjs | tee /tmp/68-04-dryrun.log &amp;&amp; grep -E "PATCH plan|DRY RUN|no changes" /tmp/68-04-dryrun.log</automated>
  </verify>
  <acceptance_criteria>
    - File `scripts/68-04-armstrong-team-roster-and-owner-hours.mjs` exists.
    - Contains literal `'678ae542-7f0b-43d1-8784-e7341767c250'`.
    - Contains literal `owner_hours_per_week_current: 50`.
    - Contains literal `owner_hours_per_week_year1: 40`.
    - Contains literal `owner_hours_per_week_year2: 25`.
    - Contains literal `owner_hours_per_week_year3: 10`.
    - Contains literal `desired_hours: 10`.
    - Contains literal `'Foreman (Lockup → Finish)'` (Pablo).
    - Contains literal `'Foreman (Setup → Lockup)'` (Kye).
    - Contains literal `'Carly'`, `'Cooper'`, `'Chris'`.
    - Contains literal `'promotion_candidate'` (Pablo's status field).
    - Contains the string `'Pubs'` (in Pablo's notes for reconciliation flag).
    - Dry-run output contains `PATCH plan` and `DRY RUN`.
    - Running `--apply` once then again logs `business_profiles: no changes needed` AND `business_financial_goals: no changes needed`.
  </acceptance_criteria>
  <done>Script committed; dry-run shows expected patches; idempotency verified.</done>
</task>

</tasks>

<verification>
- Post-apply re-query confirms business_profiles.key_roles contains all 5 target names with correct role strings.
- Post-apply re-query confirms business_financial_goals owner_hours_per_week_current=50, year1=40, year2=25, year3=10.
- Post-apply re-query confirms business_profiles.owner_info.desired_hours=10.
</verification>

<success_criteria>
- Armstrong team roster reflects 2026-05-12 session reality.
- Owner-hours fields show realistic glide-path on the goals wizard.
- Idempotent.
</success_criteria>

<output>
After completion, create `.planning/phases/68-step-4-wizard-mvp-improvements-armstrong-co-plan-data-refresh/68-04-SUMMARY.md` with dry-run + apply output and the Pubs reconciliation flag (yes/no answer when Matt provides it).
</output>
