---
phase: 68-step-4-wizard-mvp-improvements-armstrong-co-plan-data-refresh
plan: 08
type: execute
wave: 1
depends_on: ["68-01", "68-02", "68-03", "68-04", "68-05", "68-06", "68-07"]
files_modified:
  - scripts/68-08-armstrong-plan-snapshot-baseline.mjs
autonomous: true
requirements: []
must_haves:
  truths:
    - "A new plan_snapshots row exists for Armstrong with snapshot_type='goals_wizard_complete', label='Post 2026-05-12 session refresh'"
    - "version_number = max(existing for tenant) + 1"
    - "plan_data is a fully-assembled OnePagePlanData payload reflecting the post-A1..A9+A11 DB state"
    - "Re-running the script with --apply does NOT create a duplicate row with the same label"
    - "All 5 coreMetrics.year{1,2,3} fields (leadsPerMonth, conversionRate, avgTransactionValue, teamHeadcount, ownerHoursPerWeek) are sourced from business_financial_goals — no nullable, no zero-default. coreMetrics.quarter uses the *_current column variants where they exist, else explicit null (not 0)."
  artifacts:
    - path: scripts/68-08-armstrong-plan-snapshot-baseline.mjs
      provides: A10 plan_snapshots baseline insert
  key_links:
    - from: scripts/68-08-armstrong-plan-snapshot-baseline.mjs
      to: plan_snapshots table
      via: "POST after composing OnePagePlanData from strategy_data + business_financial_goals + strategic_initiatives + swot_items + business_kpis"
      pattern: "snapshot_type: 'goals_wizard_complete'"
---

<objective>
A10: After plans 68-02 through 68-07 have applied, capture a single `plan_snapshots` row for Armstrong as the post-refresh baseline. `snapshot_type='goals_wizard_complete'`, `version_number = max+1`, `label="Post 2026-05-12 session refresh"`, `plan_data` = full `OnePagePlanData` payload assembled from current DB state.

Purpose: Frozen baseline of the refreshed plan. Subsequent edits can be diff'd against it. Also the source of truth Workstream B's B8 button (plan 68-15) will use as its write-model.

Output: One Node script, dry-run + `--apply`. Idempotency: refuse to write a second row with the same label for the same tenant.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/68-step-4-wizard-mvp-improvements-armstrong-co-plan-data-refresh/68-CONTEXT.md
@scripts/onboard-fit2shine.mjs
@src/app/one-page-plan/types.ts

<interfaces>
Armstrong:
- BUSINESS_PROFILES_ID = '678ae542-7f0b-43d1-8784-e7341767c250'  // plan_snapshots.business_id
- USER_ID              = 'f4702002-69a6-44f1-b963-ada2a95c843b'

OnePagePlanData shape (from src/app/one-page-plan/types.ts:3-66) — keys to populate:
- vision               (string) — from strategy_data.vision_mission.vision_statement
- mission              (string) — from strategy_data.vision_mission.mission_statement
- coreValues           (string[]) — from strategy_data.vision_mission.core_values
- strengths/weaknesses/opportunities/threats (string[]) — from swot_items grouped by category, .content values
- financialGoals.{year3,year2,year1,quarter}.{revenue,grossProfit,netProfit} — from business_financial_goals
- coreMetrics.{year3,year2,year1,quarter}.{leadsPerMonth, conversionRate, avgTransactionValue, teamHeadcount, ownerHoursPerWeek}
   - ownerHoursPerWeek from business_financial_goals.owner_hours_per_week_*
   - others from business_financial_goals or business_metrics (use null if not present)
- kpis (array of {name, category, year3Target, year1Target, quarterTarget}) — from business_kpis where is_active=true; quarterTarget = null if not present
- strategicInitiatives (array of {title, quarters[], owner?}) — from strategic_initiatives where step_type='twelve_month'; quarters=[quarter_assigned] when assigned, [] when not
- quarterlyRocks ([]) — empty for baseline (Step 5 not yet completed)
- currentQuarter, currentQuarterLabel — derive from today's date relative to FY27 (e.g., 'q4' for 2026-05-29 = pre-FY27, label "Pre-FY27 (May 2026)")
- yearType ('FY')
- planYear (2027)
- companyName — from businesses.name or business_profiles.company_name
- ownerGoals.{desiredHoursPerWeek, currentHoursPerWeek, primaryGoal, timeHorizon, exitStrategy} — from business_profiles.owner_info

REST:
- GET   `strategy_data?business_id=eq.${BPID}&select=vision_mission`
- GET   `business_financial_goals?business_id=eq.${BPID}&select=*` (select=* so the script captures every coreMetrics column without naming each one; the assembled payload still references each by name)
- GET   `business_kpis?business_id=eq.${BPID}&is_active=eq.true&select=name,category,year1_target,year2_target,year3_target`
- GET   `strategic_initiatives?business_id=eq.${BPID}&step_type=eq.twelve_month&select=title,quarter_assigned`
- GET   `swot_items?swot_analysis_id=eq.cb6d1358-a0ec-48b8-878c-159df6b3a576&select=category,content`
- GET   `business_profiles?id=eq.${BPID}&select=company_name,owner_info`
- GET   `businesses?id=eq.a0bf1b0a-663e-4636-8c0d-eef62972dcbc&select=name`
- GET   `plan_snapshots?business_id=eq.${BPID}&select=id,version_number,label&order=version_number.desc`
- POST  `plan_snapshots` body { business_id, user_id, snapshot_type, version_number, plan_data, label }
</interfaces>
</context>

<verification_policy>
ENV-GATING (mirrors plan 68-01's policy across the A workstream):

Acceptance criteria split into TWO groups:

1. **Static** — always run, no env required: file existence, `node --check`,
   and `grep` of literal constants in the script source.

2. **Live-execution** — requires `.env.local` with Supabase credentials.
   Invoked via the tolerant pattern
       test -f .env.local && (<live cmd>) || echo "SKIP: live verify requires .env.local"
   so the verify step does NOT fail when `.env.local` is absent.

Static acceptance criteria block on every CI/agent environment; live-execution
criteria are operator-run and gated by `.env.local`. If `.env.local` is absent
in the executor environment, the operator MUST run the live block manually
before merge and paste the output into the SUMMARY.

Pattern mirrors `scripts/onboard-fit2shine.mjs`, which prints payloads in
dry-run mode without requiring write access.
</verification_policy>

<tasks>

<task type="auto">
  <name>Task 1: Build plan_snapshots baseline insert script</name>
  <files>scripts/68-08-armstrong-plan-snapshot-baseline.mjs</files>
  <read_first>
    - src/app/one-page-plan/types.ts (full file — OnePagePlanData + PlanSnapshot shapes)
    - scripts/onboard-fit2shine.mjs (POST helper)
    - .planning/phases/68-step-4-wizard-mvp-improvements-armstrong-co-plan-data-refresh/68-CONTEXT.md (A10 decision)
    - docs/BUSINESS_ID_PATTERNS.md (confirms plan_snapshots.business_id uses business_profiles.id per A10 wording)
    - src/app/goals/services/strategic-planning-service.ts (canonical source-of-truth for how the wizard reads business_financial_goals — mirror its column resolution for coreMetrics)
    - src/app/goals/page.tsx (confirms how `coreMetrics` is populated upstream and which columns feed each field)
    - Also run: `grep -rn "coreMetrics" src/app/goals/` to confirm every read path goes through business_financial_goals.
  </read_first>
  <action>
    Create `scripts/68-08-armstrong-plan-snapshot-baseline.mjs`:

    1. Header JSDoc — note this MUST run after 68-02..68-07 have applied.
    2. Env + `APPLY` flag.
    3. Hardcode `BUSINESS_PROFILES_ID`, `BUSINESSES_ID = 'a0bf1b0a-663e-4636-8c0d-eef62972dcbc'`, `USER_ID`, `SWOT_ANALYSES_ID`, `LABEL = 'Post 2026-05-12 session refresh'`.
    4. Helpers: apiGet, apiInsert.
    5. **Idempotency check first:**
       a. GET `plan_snapshots?business_id=eq.${BPID}&label=eq.${encodeURIComponent(LABEL)}&select=id,version_number`.
       b. If any row found → log "Baseline snapshot already exists (id=..., version=...) — skipping" and exit 0.
    6. **Compose OnePagePlanData payload:**
       a. Parallel `Promise.all` of 7 GETs listed in <interfaces>.
       b. Build the object literally matching the OnePagePlanData shape:
          - vision = strategy_data?.vision_mission?.vision_statement || ''
          - mission = strategy_data?.vision_mission?.mission_statement || ''
          - coreValues = strategy_data?.vision_mission?.core_values || []
          - strengths/weaknesses/opportunities/threats: for each cat, `swot_items.filter(i=>i.category===cat).map(i=>i.content)`
          - financialGoals: map from business_financial_goals fields (revenue_year1, gross_profit_year1, net_profit_year1, etc.); use 0 if missing
          - coreMetrics — ALL 5 fields per year are sourced from `business_financial_goals` (the live query in CONTEXT.md confirms each column exists for Armstrong: leads_per_month_year1/2/3 = 8/10/12; conversion_rate_year1/2/3 = 60/60/60; avg_transaction_value_year1/2/3 = 2000000/2000000/2000000; team_headcount_year1/2/3 = 10/12/14; owner_hours_per_week_year1/2/3 = 40/25/10 after plan 68-04 applies). Concretely:
            - coreMetrics.year1.leadsPerMonth        = business_financial_goals.leads_per_month_year1
            - coreMetrics.year2.leadsPerMonth        = business_financial_goals.leads_per_month_year2
            - coreMetrics.year3.leadsPerMonth        = business_financial_goals.leads_per_month_year3
            - coreMetrics.year{1,2,3}.conversionRate         = business_financial_goals.conversion_rate_year{1,2,3}
            - coreMetrics.year{1,2,3}.avgTransactionValue    = business_financial_goals.avg_transaction_value_year{1,2,3}
            - coreMetrics.year{1,2,3}.teamHeadcount          = business_financial_goals.team_headcount_year{1,2,3}
            - coreMetrics.year{1,2,3}.ownerHoursPerWeek      = business_financial_goals.owner_hours_per_week_year{1,2,3}
            - coreMetrics.quarter.ownerHoursPerWeek = business_financial_goals.owner_hours_per_week_current
            - coreMetrics.quarter.leadsPerMonth / conversionRate / avgTransactionValue / teamHeadcount = the corresponding `*_current` column if it exists in business_financial_goals (verify via the GET in step 4a), else explicit `null` (NOT 0 — silent zero-default would misrepresent Armstrong's data; null surfaces "not yet captured" to downstream consumers).
          - Do NOT fall back to `business_metrics` for these fields. The source-of-truth for the Step 4 wizard is `business_financial_goals` (confirmed via `grep coreMetrics src/app/goals/`).
          - kpis: business_kpis.map(k => ({ name: k.name, category: k.category, year3Target: k.year3_target ?? 0, year1Target: k.year1_target ?? 0, quarterTarget: 0 }))
          - strategicInitiatives: strategic_initiatives.map(i => ({ title: i.title, quarters: i.quarter_assigned ? [i.quarter_assigned.toUpperCase()] : [], owner: undefined }))
          - quarterlyRocks: []
          - currentQuarter: 'q4' (calendar pre-FY27 in May 2026), currentQuarterLabel: 'Pre-FY27 — Q4 FY26 (Apr-Jun 2026)'
          - yearType: 'FY'
          - planYear: 2027
          - companyName: businesses?.name || business_profiles?.company_name || ''
          - ownerGoals: { desiredHoursPerWeek: business_profiles.owner_info?.desired_hours, currentHoursPerWeek: business_profiles.owner_info?.current_hours_per_week, primaryGoal: ..., timeHorizon: ..., exitStrategy: ... } (read from owner_info object)
    7. **Compute next version_number:**
       a. GET `plan_snapshots?business_id=eq.${BPID}&select=version_number&order=version_number.desc&limit=1`.
       b. `nextVersion = (rows[0]?.version_number || 0) + 1`.
    8. Build payload `{ business_id: BPID, user_id: USER_ID, snapshot_type: 'goals_wizard_complete', version_number: nextVersion, label: LABEL, plan_data: <composed> }`.
    9. Print full payload to console (pretty-printed JSON).
    10. If `!APPLY`, exit (DRY RUN).
    11. Else POST and log the new snapshot id + version_number.
  </action>
  <verify>
    <!-- STATIC verify — runs unconditionally, no env required -->
    <automated>test -f scripts/68-08-armstrong-plan-snapshot-baseline.mjs && node --check scripts/68-08-armstrong-plan-snapshot-baseline.mjs && grep -q "'678ae542-7f0b-43d1-8784-e7341767c250'" scripts/68-08-armstrong-plan-snapshot-baseline.mjs && grep -q "'goals_wizard_complete'" scripts/68-08-armstrong-plan-snapshot-baseline.mjs</automated>
    <!-- LIVE verify — requires .env.local with NEXT_PUBLIC_SUPABASE_URL + SUPABASE_SERVICE_KEY -->
    <live_automated>test -f .env.local && (node scripts/68-08-armstrong-plan-snapshot-baseline.mjs | tee /tmp/68-08-dryrun.log && grep -E "DRY RUN|already exists|snapshot_type" /tmp/68-08-dryrun.log) || echo "SKIP: live verify requires .env.local"</live_automated>
  </verify>
  <acceptance_criteria>
    Static (always run, no env required):
    - File `scripts/68-08-armstrong-plan-snapshot-baseline.mjs` exists.
    - Contains literal `'678ae542-7f0b-43d1-8784-e7341767c250'` (BUSINESS_PROFILES_ID).
    - Contains literal `'a0bf1b0a-663e-4636-8c0d-eef62972dcbc'` (BUSINESSES_ID, for name lookup).
    - Contains literal `'goals_wizard_complete'`.
    - Contains literal `'Post 2026-05-12 session refresh'` (LABEL).
    - Contains literal `'cb6d1358-a0ec-48b8-878c-159df6b3a576'` (SWOT analysis id).
    - Contains literal `planYear: 2027`.
    - Contains literal `yearType: 'FY'`.
    - Contains the strings `'coreValues'`, `'strategicInitiatives'`, `'kpis'`, `'ownerGoals'` (proves the script assembles the full OnePagePlanData shape).

    Live (requires `.env.local` with live Supabase credentials; operator-run, gated on env presence):
    - Dry-run output contains `DRY RUN` and a JSON dump of the composed plan_data.
    - Script source contains literal `business_financial_goals` references for leads_per_month, conversion_rate, avg_transaction_value, team_headcount, AND owner_hours_per_week column families (grep finds at least one occurrence of each *_year1 column name).
    - Script source contains NO `business_metrics` table reference for coreMetrics (grep -c "business_metrics" returns 0 within the coreMetrics composition block).
    - Running `--apply` once then again prints "Baseline snapshot already exists" and exits 0 (idempotent).
  </acceptance_criteria>
  <done>Script committed; dry-run shows fully composed payload; idempotency verified.</done>
</task>

</tasks>

<verification>
- Post-apply re-query: exactly one `plan_snapshots` row for Armstrong with `label='Post 2026-05-12 session refresh'`.
- `plan_data` JSON contains coreValues array of 9 items (from 68-06), strategicInitiatives entries assigned to q1/q2/q3/q4 (from 68-02), kpis array of ≥9 (from 68-05), and ownerGoals with desiredHoursPerWeek=10 (from 68-04).
</verification>

<success_criteria>
- One canonical baseline snapshot exists.
- Composed plan_data accurately reflects the post-refresh DB state.
- Idempotent.
</success_criteria>

<output>
After completion, create `.planning/phases/68-step-4-wizard-mvp-improvements-armstrong-co-plan-data-refresh/68-08-SUMMARY.md` with dry-run + apply output and the resulting snapshot id + version_number.
</output>
