---
phase: 68-step-4-wizard-mvp-improvements-armstrong-co-plan-data-refresh
plan: 13
type: execute
wave: 6
depends_on: ["68-12"]
files_modified:
  - src/app/goals/components/Step4AnnualPlan.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Above the Available pool grid, a row of chips renders one chip per unique category in twelveMonthInitiatives plus an 'All' chip"
    - "Each chip shows count (e.g., 'Marketing (5)')"
    - "Clicking a chip filters unassignedInitiatives to that category; clicking 'All' clears the filter"
    - "Selected chip is visually distinguished (filled background using the category's palette)"
    - "Filter state is local to Step4AnnualPlan; resets to 'All' on remount"
    - "tsc + lint + build green"
  artifacts:
    - path: src/app/goals/components/Step4AnnualPlan.tsx
      provides: B6 — Available pool filterable by category chips
  key_links:
    - from: useState selectedCategory
      to: filteredUnassignedInitiatives derived list passed to Available pool render
      via: "filter on (i.category||'uncategorised').toLowerCase() === selectedCategory"
      pattern: "selectedCategory"
---

<objective>
B6: Add a category filter chip row above the Available pool grid. Each chip = one unique category present in `twelveMonthInitiatives`, plus an "All" chip (default). Clicking a chip filters `unassignedInitiatives` to that category.

Purpose: When the Available pool gets long (>10 items), operator can focus on one engine at a time.
Output: Edits to one file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/68-step-4-wizard-mvp-improvements-armstrong-co-plan-data-refresh/68-CONTEXT.md
@src/app/goals/components/Step4AnnualPlan.tsx

INTERFACES:

Existing Available pool section (~lines 1243-1287):
  <div onDragOver={...} onDrop={...} className="mt-4 bg-gray-50 rounded-lg border-2 border-dashed border-slate-300 p-4">
    <div className="flex items-center justify-between mb-3">
      <h4>Available initiatives (N)</h4>
      <p>Drag into a quarter or use + Add</p>
    </div>
    {unassignedInitiatives.length === 0 ? (...empty state...) : (
      <div className="grid ...">
        {unassignedInitiatives.map(initiative => ...card...)}
      </div>
    )}
  </div>

Target state addition (in Step4AnnualPlan body, near other useState):
  const [selectedCategory, setSelectedCategory] = useState<string>('all')

Derive filtered list (place near where unassignedInitiatives is defined, around line 366):
  const filteredUnassignedInitiatives = selectedCategory === 'all'
    ? unassignedInitiatives
    : unassignedInitiatives.filter(i => (i.category || 'uncategorised').trim().toLowerCase() === selectedCategory)

Chips data (compute via useMemo before render):
  const categoryChips = useMemo(() => {
    const counts = new Map<string, number>()
    for (const i of twelveMonthInitiatives) {
      const key = (i.category || 'uncategorised').trim().toLowerCase()
      counts.set(key, (counts.get(key) || 0) + 1)
    }
    const knownOrder = ['marketing','finance','people','systems','customer experience','cx','leadership','time','diversification']
    const known = knownOrder.filter(k => counts.has(k))
    const unknown = Array.from(counts.keys()).filter(k => !knownOrder.includes(k)).sort()
    return [...known, ...unknown].map(key => {
      const style = getCategoryStyle(key) ?? { bg: 'bg-gray-200', text: 'text-gray-700', label: key.toUpperCase().slice(0,8) }
      return { key, label: style.label, bg: style.bg, text: style.text, count: counts.get(key) || 0 }
    })
  }, [twelveMonthInitiatives])
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add selectedCategory state, chip row, and filtered pool</name>
  <files>src/app/goals/components/Step4AnnualPlan.tsx</files>
  <read_first>
    - src/app/goals/components/Step4AnnualPlan.tsx (lines 96-107 — useState block; line 366 — unassignedInitiatives derivation; lines 1243-1287 — Available pool render)
    - .planning/phases/68-step-4-wizard-mvp-improvements-armstrong-co-plan-data-refresh/68-CONTEXT.md (B6 decision)
  </read_first>
  <action>
    Edit src/app/goals/components/Step4AnnualPlan.tsx:

    1. Add new state alongside existing useStates (around lines 96-107):

       const [selectedCategory, setSelectedCategory] = useState<string>('all')

    2. Add useMemo for categoryChips right after the existing currentRemainderInfo useMemo (around line 84):

       const categoryChips = useMemo(() => {
         const counts = new Map<string, number>()
         for (const i of twelveMonthInitiatives) {
           const key = (i.category || 'uncategorised').trim().toLowerCase()
           counts.set(key, (counts.get(key) || 0) + 1)
         }
         const knownOrder = ['marketing','finance','people','systems','customer experience','cx','leadership','time','diversification']
         const known = knownOrder.filter(k => counts.has(k))
         const unknown = Array.from(counts.keys()).filter(k => !knownOrder.includes(k)).sort()
         return [...known, ...unknown].map(key => {
           const style = getCategoryStyle(key) ?? { bg: 'bg-gray-200', text: 'text-gray-700', label: key.toUpperCase().slice(0,8) }
           return { key, label: style.label, bg: style.bg, text: style.text, count: counts.get(key) || 0 }
         })
       }, [twelveMonthInitiatives])

       NOTE: This useMemo references getCategoryStyle (defined inside component per plan 68-11). Place it AFTER getCategoryStyle if the call order matters. If getCategoryStyle is a function declaration, it can be hoisted; if it's a const arrow, place categoryChips below it.

    3. Add `filteredUnassignedInitiatives` derivation right after the existing `unassignedInitiatives` line (around line 366):

       const filteredUnassignedInitiatives = selectedCategory === 'all'
         ? unassignedInitiatives
         : unassignedInitiatives.filter(i => (i.category || 'uncategorised').trim().toLowerCase() === selectedCategory)

    4. Inside the Available pool section (~line 1248), find the header row:

       <div className="flex items-center justify-between mb-3">
         <h4 className="font-semibold text-brand-navy text-sm">Available initiatives (...)</h4>
         <p className="text-xs text-gray-500">Drag into a quarter or use + Add</p>
       </div>

       Update the h4 count to use filteredUnassignedInitiatives.length (showing the visible subset).

       Insert AFTER the header row but BEFORE the empty-state / grid block:

       {categoryChips.length > 0 && (
         <div className="flex flex-wrap gap-1.5 mb-3" role="tablist" aria-label="Filter Available initiatives by category">
           <button
             type="button"
             role="tab"
             aria-selected={selectedCategory === 'all'}
             onClick={() => setSelectedCategory('all')}
             className={`px-2 py-1 text-[10px] font-semibold rounded-full border transition-colors ${
               selectedCategory === 'all'
                 ? 'bg-brand-navy text-white border-brand-navy'
                 : 'bg-white text-gray-700 border-gray-300 hover:border-brand-navy'
             }`}
           >
             All ({twelveMonthInitiatives.length})
           </button>
           {categoryChips.map(chip => (
             <button
               key={chip.key}
               type="button"
               role="tab"
               aria-selected={selectedCategory === chip.key}
               onClick={() => setSelectedCategory(chip.key)}
               className={`px-2 py-1 text-[10px] font-semibold rounded-full border transition-colors ${
                 selectedCategory === chip.key
                   ? `${chip.bg} ${chip.text} border-transparent`
                   : 'bg-white text-gray-700 border-gray-300 hover:border-gray-500'
               }`}
             >
               {chip.label} ({chip.count})
             </button>
           ))}
         </div>
       )}

    5. Replace ALL references to `unassignedInitiatives` INSIDE the Available pool render block (the grid map) with `filteredUnassignedInitiatives`. Keep the original `unassignedInitiatives` references elsewhere (e.g., quarter card + Add dropdown at ~line 1184, the assignedTitles dedupe at line 360-367, the drop handler at line 492) — they should remain on the unfiltered list so adding/removing initiatives from quarters works regardless of the chip filter.

    6. Also update the empty-state check at line 1254: `{unassignedInitiatives.length === 0 ? (` should become `{filteredUnassignedInitiatives.length === 0 ? (` so the empty-state message appears when the filtered subset is empty.

    7. Update the empty-state message text from "All initiatives assigned ✓" to a conditional: when selectedCategory is 'all', keep "All initiatives assigned ✓"; otherwise show "No initiatives in this category — try All".

       Example:
         {filteredUnassignedInitiatives.length === 0 ? (
           <p className="text-xs text-center py-4 text-gray-500">
             {selectedCategory === 'all' ? 'All initiatives assigned ✓' : 'No initiatives in this category — try All'}
           </p>
         ) : ( ... grid ... )}
  </action>
  <verify>
    <automated>grep -n "selectedCategory" src/app/goals/components/Step4AnnualPlan.tsx | wc -l &amp;&amp; grep -n "categoryChips" src/app/goals/components/Step4AnnualPlan.tsx | wc -l &amp;&amp; grep -n "filteredUnassignedInitiatives" src/app/goals/components/Step4AnnualPlan.tsx | wc -l &amp;&amp; grep -n "Filter Available initiatives by category" src/app/goals/components/Step4AnnualPlan.tsx &amp;&amp; npx tsc --noEmit 2&gt;&amp;1 | grep -E "Step4AnnualPlan|error TS" | head -10 || echo "tsc clean" &amp;&amp; npm run lint -- src/app/goals/components/Step4AnnualPlan.tsx 2&gt;&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File contains literal `const [selectedCategory, setSelectedCategory] = useState<string>('all')`.
    - File contains literal `categoryChips` definition with useMemo.
    - File contains literal `filteredUnassignedInitiatives`.
    - File contains literal aria-label `'Filter Available initiatives by category'`.
    - The empty-state branch handles both `selectedCategory === 'all'` and the filtered-empty case (grep for `'No initiatives in this category'`).
    - The quarter card `+ Add` dropdown at line ~1184 still references `unassignedInitiatives` (NOT filtered) — confirms we didn't break the assignment dropdown.
    - "npx tsc --noEmit" reports no errors.
    - "npm run lint" exits 0.
    - "npm run build" exits 0.
  </acceptance_criteria>
  <done>Chip row renders; clicking a chip filters the Available grid; tsc + lint + build green.</done>
</task>

</tasks>

<verification>
- /goals dev test: load Armstrong (post-A workstream). Available pool shows chips for Marketing, Finance, People, Systems, CX, Leadership, Time, Diversification, plus All.
- Clicking 'Diversification' filters to the 13 ideas from plan 68-03.
- Clicking 'All' restores full list.
- Dragging a card from filtered pool into a quarter still works — and the dragged item disappears from the filtered view.
</verification>

<success_criteria>
- Pool is browsable by engine without scrolling through unrelated initiatives.
- + Add dropdown on quarter cards unchanged (still shows all unassigned).
</success_criteria>

<output>
After completion, create .planning/phases/68-step-4-wizard-mvp-improvements-armstrong-co-plan-data-refresh/68-13-SUMMARY.md with chip rendering screenshot reference + filter behaviour confirmation.
</output>
