---
phase: 68-step-4-wizard-mvp-improvements-armstrong-co-plan-data-refresh
plan: 02
type: execute
wave: 1
depends_on: ["68-01"]
files_modified:
  - scripts/68-02-armstrong-initiatives-dedupe-and-assign.mjs
autonomous: true
requirements: []
must_haves:
  truths:
    - "Each canonical initiative title exists in strategic_initiatives exactly once for Armstrong (no duplicate by lower(trim(title)))"
    - "Every canonical initiative listed in A2 has the correct quarter_assigned, year_assigned=2027, fiscal_year=2027, start_date, end_date"
    - "Re-running the script with --apply does NOT delete already-deleted rows or re-write already-correct rows"
    - "Without --apply, the script prints the dedupe + assignment plan and writes nothing"
  artifacts:
    - path: scripts/68-02-armstrong-initiatives-dedupe-and-assign.mjs
      provides: A1 dedupe + A2 quarter assignment for Armstrong strategic_initiatives
  key_links:
    - from: scripts/68-02-armstrong-initiatives-dedupe-and-assign.mjs
      to: strategic_initiatives table (Supabase REST)
      via: "DELETE for q1/strategic_ideas duplicates of twelve_month titles + PATCH for quarter assignment on twelve_month rows"
      pattern: "step_type=eq.twelve_month"
---

<objective>
A1: Dedupe `strategic_initiatives` for Armstrong — keep `step_type='twelve_month'` as canonical; delete duplicates with same title under `step_type='q1'` and `step_type='strategic_ideas'`. Where a title exists ONLY under `strategic_ideas` (e.g., books, exploratory items not in the active 12-month plan), keep it.

A2: For each canonical `twelve_month` row in Armstrong's plan, write `quarter_assigned` (one of `'q1'..'q4'`), `year_assigned=2027`, `fiscal_year=2027`, plus `start_date` / `end_date` matching FY27 quarter boundaries (FY27 starts 2026-07-01 → ends 2027-06-30).

Purpose: Reflects Matt's 2026-05-12 session sequencing; eliminates the data-inflation cause of the dedupe workaround at Step4AnnualPlan.tsx:360-368.
Output: One Node script, dry-run by default, `--apply` writes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/68-step-4-wizard-mvp-improvements-armstrong-co-plan-data-refresh/68-CONTEXT.md
@scripts/onboard-fit2shine.mjs
@docs/BUSINESS_ID_PATTERNS.md

<interfaces>
Armstrong tenant identifiers (hardcoded in script):
- BUSINESS_PROFILES_ID = '678ae542-7f0b-43d1-8784-e7341767c250'  // strategic_initiatives.business_id
- USER_ID              = 'f4702002-69a6-44f1-b963-ada2a95c843b'

FY27 quarter date boundaries (yearStartMonth=7):
- Q1: 2026-07-01 → 2026-09-30 (Jul-Sep 2026)
- Q2: 2026-10-01 → 2026-12-31 (Oct-Dec 2026)
- Q3: 2027-01-01 → 2027-03-31 (Jan-Mar 2027)
- Q4: 2027-04-01 → 2027-06-30 (Apr-Jun 2027)

Canonical initiative → quarter assignments (literal title strings to match; case-insensitive match via lower(trim()); script reports any unmatched titles):

Q1 (Jul-Sep 2026):
  - "Pricing review"
  - "Progress claims update"
  - "Job profitability review"
  - "Builder Trend audit"
  - "Unpack sales process"  (also receives A11 notes — separate plan 68-07)
  - "Home warranty tracker"
  - "Financial forecast"

Q2 (Oct-Dec 2026):
  - "Website update"
  - "Testimonials (written + video)"
  - "Social media plan"
  - "Site signage refresh"
  - "New imagery"
  - "Client feedback questionnaire pilot"

Q3 (Jan-Mar 2027):
  - "Performance management process (GAP MAP)"
  - "Team-building plan"
  - "Training and development plan"
  - "Key checklists"
  - "Document key processes"
  - "Embed core values"
  - "Discuss management opportunity with Pablo"

Q4 (Apr-Jun 2027):
  - "Leadership reading list"
  - "Competitor analysis"
  - "Supplier 6-12 month review cadence"
  - "Stop-doing list creation"
  - "Ideal-week design"
  - "Time audit"
  - "2-weeks-off goal"

REST patterns (mirror onboard-fit2shine.mjs):
- GET    `${URL}/rest/v1/strategic_initiatives?business_id=eq.${BPID}&select=id,title,step_type,quarter_assigned,year_assigned,fiscal_year,start_date,end_date`
- DELETE `${URL}/rest/v1/strategic_initiatives?id=eq.${rowId}`
- PATCH  `${URL}/rest/v1/strategic_initiatives?id=eq.${rowId}` body `{quarter_assigned, year_assigned, fiscal_year, start_date, end_date}`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build dedupe + assignment script</name>
  <files>scripts/68-02-armstrong-initiatives-dedupe-and-assign.mjs</files>
  <read_first>
    - scripts/onboard-fit2shine.mjs (mirror env loading, --apply flag, headers, patch/insert helper pattern)
    - .planning/phases/68-step-4-wizard-mvp-improvements-armstrong-co-plan-data-refresh/68-CONTEXT.md (A1 + A2 decisions, Q1-Q4 lists)
    - docs/BUSINESS_ID_PATTERNS.md (confirms strategic_initiatives.business_id stores business_profiles.id)
    - src/app/goals/components/Step4AnnualPlan.tsx (lines 345-368 — dedupe-by-title workaround we are NOT removing)
  </read_first>
  <action>
    Create `scripts/68-02-armstrong-initiatives-dedupe-and-assign.mjs` mirroring the structure of `scripts/onboard-fit2shine.mjs`.

    Structure:

    1. Header JSDoc:
       - Purpose: A1 dedupe (drop q1 + strategic_ideas duplicates of twelve_month titles) + A2 quarter-assign FY27 for Armstrong
       - Targets: BUSINESS_PROFILES_ID, USER_ID hardcoded
       - Run: `node scripts/68-02-...mjs` (dry-run) / `--apply` (writes)
       - IDEMPOTENCY guarantee documented
    2. Env loading + `APPLY = process.argv.includes('--apply')` — same as onboard-fit2shine.mjs.
    3. Hardcode constants:
       - `const BUSINESS_PROFILES_ID = '678ae542-7f0b-43d1-8784-e7341767c250';`
       - `const USER_ID = 'f4702002-69a6-44f1-b963-ada2a95c843b';`
       - `const FISCAL_YEAR = 2027;`
       - `const QUARTER_BOUNDS = { q1: { start: '2026-07-01', end: '2026-09-30' }, q2: { start: '2026-10-01', end: '2026-12-31' }, q3: { start: '2027-01-01', end: '2027-03-31' }, q4: { start: '2027-04-01', end: '2027-06-30' } };`
       - `const ASSIGNMENTS = { q1: [...7 titles...], q2: [...6 titles...], q3: [...7 titles...], q4: [...7 titles...] }` — copy the literal title strings from <interfaces> above.
    4. `normalize(t) => (t || '').trim().toLowerCase()` — single source of truth for title matching.
    5. Build `titleToQuarter` Map: for each `[q, titles] of Object.entries(ASSIGNMENTS)`, set `titleToQuarter.set(normalize(title), q)`.
    6. Headers + `apiGet`, `apiPatch`, `apiDelete` helpers (mirror `patch`/`insert` from onboard-fit2shine.mjs; `apiDelete` is new — DELETE with same headers; treat 404 as success-noop).
    7. Main flow:
       a. `const rows = await apiGet(\`strategic_initiatives?business_id=eq.${BUSINESS_PROFILES_ID}&select=id,title,step_type,quarter_assigned,year_assigned,fiscal_year,start_date,end_date\`)`
       b. Group rows by `normalize(title)`.
       c. **A1 dedupe pass:** for each title group, if the group contains a `twelve_month` row, identify all `q1` / `strategic_ideas` rows with the same normalized title → mark for DELETE. (Do NOT delete a `strategic_ideas` row whose title does NOT have a `twelve_month` counterpart — those are the books/exploratory items.) Also do not delete the twelve_month row itself.
       d. **A2 assignment pass:** for each `twelve_month` row in the result, look up its quarter via `titleToQuarter.get(normalize(row.title))`. If found AND any of (`quarter_assigned`, `year_assigned`, `fiscal_year`, `start_date`, `end_date`) does NOT already match the expected values, mark for PATCH. (Idempotency: skip if all 5 fields already match.)
       e. Print three sections to console: "DEDUPE PLAN (N deletes)", "ASSIGNMENT PLAN (N patches)", "UNMATCHED TWELVE_MONTH TITLES (manual review)" — the last one lists any twelve_month rows whose title is not in `titleToQuarter` (so Matt knows what got skipped).
       f. If `!APPLY`, exit 0 with summary "DRY RUN — re-run with --apply to execute".
       g. If `APPLY`, execute deletes first (so re-running after a partial run doesn't try to delete already-gone rows), then patches.
    8. Final summary log: "Deleted N rows", "Patched N rows", "Unmatched titles: list".

    Hardcode the FOUR title arrays verbatim — do NOT abstract. The point is that the script is auditable line-by-line.

    Per scope guardrails: do NOT change anything for any tenant other than `BUSINESS_PROFILES_ID`.
  </action>
  <verify>
    <automated>node scripts/68-02-armstrong-initiatives-dedupe-and-assign.mjs | tee /tmp/68-02-dryrun.log &amp;&amp; grep -E "DEDUPE PLAN|ASSIGNMENT PLAN|DRY RUN" /tmp/68-02-dryrun.log</automated>
  </verify>
  <acceptance_criteria>
    - File `scripts/68-02-armstrong-initiatives-dedupe-and-assign.mjs` exists.
    - Contains literal `'678ae542-7f0b-43d1-8784-e7341767c250'` (BUSINESS_PROFILES_ID).
    - Contains literal `APPLY = process.argv.includes('--apply')`.
    - Contains all 4 quarter date pairs: `'2026-07-01'`, `'2026-09-30'`, `'2026-10-01'`, `'2026-12-31'`, `'2027-01-01'`, `'2027-03-31'`, `'2027-04-01'`, `'2027-06-30'`.
    - Contains the literal title `"Pricing review"` (Q1) and `"2-weeks-off goal"` (Q4) — sanity check on the assignment lists.
    - Contains literal `fiscal_year` and `year_assigned` (target columns).
    - Dry-run output contains the strings `"DEDUPE PLAN"`, `"ASSIGNMENT PLAN"`, and `"DRY RUN"`.
    - Dry-run does not write (verified by re-running it twice and confirming output is identical and no DB state changed via the snapshot from 68-01).
    - When `--apply` runs once, subsequent `--apply` runs print 0 deletes + 0 patches (idempotency).
  </acceptance_criteria>
  <done>Script committed; dry-run shows expected dedupe + assignment counts; ready for Matt to run `--apply` against prod.</done>
</task>

</tasks>

<verification>
- Post-apply re-query confirms: 0 `strategic_initiatives` rows with `business_id=BUSINESS_PROFILES_ID` AND `step_type IN ('q1','strategic_ideas')` AND `lower(trim(title))` matching any twelve_month title.
- Post-apply re-query confirms: every `twelve_month` row whose normalized title is in the ASSIGNMENTS map has `year_assigned=2027`, `fiscal_year=2027`, and the matching `quarter_assigned`, `start_date`, `end_date`.
- Re-running script after `--apply` is a no-op (idempotent).
</verification>

<success_criteria>
- Armstrong's strategic_initiatives table has exactly one canonical row per unique title from the A2 list (no triplication).
- Every Q1-Q4 initiative has correct FY27 boundaries written.
- Unmatched twelve_month titles surfaced for manual review (so we don't silently miss anything).
</success_criteria>

<output>
After completion, create `.planning/phases/68-step-4-wizard-mvp-improvements-armstrong-co-plan-data-refresh/68-02-SUMMARY.md` with:
- Dry-run output (delete count, patch count, unmatched titles)
- Apply-mode output if executed
- Confirmation snapshot from 68-01 references
</output>
