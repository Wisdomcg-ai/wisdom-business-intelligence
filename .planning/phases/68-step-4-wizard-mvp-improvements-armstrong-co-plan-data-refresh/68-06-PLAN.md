---
phase: 68-step-4-wizard-mvp-improvements-armstrong-co-plan-data-refresh
plan: 06
type: execute
wave: 1
depends_on: ["68-01"]
files_modified:
  - scripts/68-06-armstrong-values-mission-swot.mjs
autonomous: true
requirements: []
must_haves:
  truths:
    - "strategy_data.vision_mission.core_values for Armstrong is the 9-item refined list (4 originals re-worded as 'we' statements + 4 new + 'No dickheads')"
    - "strategy_data.vision_mission.mission_statement matches the final A8 wording verbatim"
    - "swot_items for analysis_id cb6d... contains the A9 new strength + new threat; updated home-warranty threat description; flexible-and-adaptable duplicate annotated"
    - "Re-running the script with --apply does NOT duplicate SWOT items"
  artifacts:
    - path: scripts/68-06-armstrong-values-mission-swot.mjs
      provides: A7 + A8 strategy_data patch + A9 swot_items patch
  key_links:
    - from: scripts/68-06-armstrong-values-mission-swot.mjs
      to: strategy_data + swot_items
      via: "PATCH strategy_data.vision_mission JSONB merge; INSERT/PATCH swot_items"
      pattern: "vision_mission"
---

<objective>
A7: Refine `strategy_data.vision_mission.core_values` for Armstrong from 5 buzzwords to 9 "we" statements (re-worded originals + 4 new lines including "No dickheads").

A8: Replace `strategy_data.vision_mission.mission_statement` with the final 2026-05-12 session wording verbatim.

A9: Touch up `swot_items` for analysis_id `cb6d1358-a0ec-48b8-878c-159df6b3a576`:
- Annotate the "Flexible & adaptable" duplicate as both strength AND weakness.
- Add strength: "Operational delivery — Marrickville 7 weeks ahead of schedule with wet weather"
- Add threat: "Trade cost inflation pushing jobs out of client budget (e.g., $175k → $200k)"
- Update existing home-warranty threat description to: "Current cap $5M; $2.2M tied at Clavellie, $700k Marrickville; zero claims history (advantage when negotiating cap increase)"

Purpose: Captures the strategic narrative from 2026-05-12 — values/mission for the One Page Plan, SWOT for next session's prep.
Output: One Node script, dry-run + `--apply`, idempotent via content-equality check.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/68-step-4-wizard-mvp-improvements-armstrong-co-plan-data-refresh/68-CONTEXT.md
@scripts/onboard-fit2shine.mjs

<interfaces>
Armstrong:
- BUSINESS_PROFILES_ID = '678ae542-7f0b-43d1-8784-e7341767c250'  // strategy_data.business_id
- USER_ID              = 'f4702002-69a6-44f1-b963-ada2a95c843b'  // swot_analyses.user_id
- SWOT_ANALYSES_ID     = 'cb6d1358-a0ec-48b8-878c-159df6b3a576'

A7 target core_values (9 items, ordered):
[
  "We are happy and have fun on every job",
  "We are always learning and teaching each other",
  "We work hard and do what we say we will do",
  "We are open and transparent — with each other and with clients",
  "We collaborate to solve problems",
  "We build long-term relationships, not transactions",
  "We do the right thing when no one's watching",
  "No dickheads",
  "We are welcoming and collaborative on site"
]

A8 mission_statement (verbatim):
"We take someone's dream that has been sketched on paper and turn it into reality, focusing on the details, overcoming the unforeseen challenges, and constantly ensuring we align with their desires, while coupling this with an amazing client experience."

A9 SWOT operations:

1. ANNOTATE "Flexible & adaptable" duplicate:
   - Find swot_items rows where lower(trim(content)) = 'flexible & adaptable' OR 'flexible and adaptable'
   - There should be one row currently under category='strengths' (or similar).
   - Action: ensure ONE row in 'strengths' with description "Flexibility wins clients but creates scope creep — same trait shows up as a weakness".
   - Then INSERT (if missing) a sibling row in 'weaknesses' with content "Flexible & adaptable" and description "Same trait that wins clients also drives scope creep when boundaries aren't set early".
   - Existence check on weakness side: lower(trim(content)) = 'flexible & adaptable' AND category='weaknesses'.

2. ADD new strength (insert if missing):
   { swot_analysis_id: SWOT_ANALYSES_ID, category: 'strengths', content: 'Operational delivery — Marrickville 7 weeks ahead of schedule with wet weather', description: null }
   Existence check: content match (case-insensitive trim).

3. ADD new threat (insert if missing):
   { swot_analysis_id: SWOT_ANALYSES_ID, category: 'threats', content: 'Trade cost inflation pushing jobs out of client budget (e.g., $175k → $200k)', description: null }

4. UPDATE existing home-warranty threat:
   - Find existing row in category='threats' whose content contains 'home warranty' or 'home-warranty' (case-insensitive).
   - PATCH description to: "Current cap $5M; $2.2M tied at Clavellie, $700k Marrickville; zero claims history (advantage when negotiating cap increase)"
   - If no such row exists, INSERT a new threat with content "Home warranty cap exposure" and the above description.

REST:
- GET   `strategy_data?business_id=eq.${BPID}&select=id,vision_mission`
- PATCH `strategy_data?business_id=eq.${BPID}` body { vision_mission: { ...existing, core_values, mission_statement } }
- GET   `swot_items?swot_analysis_id=eq.${SAID}&select=id,category,content,description`
- POST  `swot_items` body <row>
- PATCH `swot_items?id=eq.${rowId}` body <updated fields>
</interfaces>
</context>

<verification_policy>
ENV-GATING (mirrors plan 68-01's policy across the A workstream):

Acceptance criteria split into TWO groups:

1. **Static** — always run, no env required: file existence, `node --check`,
   and `grep` of literal constants in the script source.

2. **Live-execution** — requires `.env.local` with Supabase credentials.
   Invoked via the tolerant pattern
       test -f .env.local && (<live cmd>) || echo "SKIP: live verify requires .env.local"
   so the verify step does NOT fail when `.env.local` is absent.

Static acceptance criteria block on every CI/agent environment; live-execution
criteria are operator-run and gated by `.env.local`. If `.env.local` is absent
in the executor environment, the operator MUST run the live block manually
before merge and paste the output into the SUMMARY.

Pattern mirrors `scripts/onboard-fit2shine.mjs`, which prints payloads in
dry-run mode without requiring write access.
</verification_policy>

<tasks>

<task type="auto">
  <name>Task 1: Build values + mission + SWOT polish script</name>
  <files>scripts/68-06-armstrong-values-mission-swot.mjs</files>
  <read_first>
    - scripts/onboard-fit2shine.mjs (env + APPLY + headers + patch/insert pattern)
    - .planning/phases/68-step-4-wizard-mvp-improvements-armstrong-co-plan-data-refresh/68-CONTEXT.md (A7, A8, A9 decisions)
    - docs/BUSINESS_ID_PATTERNS.md (confirms swot_items inherits via swot_analysis_id; strategy_data uses business_profiles.id)
  </read_first>
  <action>
    Create `scripts/68-06-armstrong-values-mission-swot.mjs`:

    1. Header JSDoc.
    2. Env + `APPLY` flag.
    3. Hardcode `BUSINESS_PROFILES_ID`, `USER_ID`, `SWOT_ANALYSES_ID`.
    4. Define `const CORE_VALUES = [ ... 9 strings ... ]` — verbatim from <interfaces>.
    5. Define `const MISSION_STATEMENT = "..."` — verbatim from <interfaces> (with proper escaping for the apostrophes).
    6. Define `const NEW_STRENGTH_CONTENT`, `const NEW_THREAT_CONTENT`, `const HOME_WARRANTY_DESCRIPTION` as constants.
    7. Helpers: apiGet, apiPatch, apiInsert.
    8. **A7+A8 strategy_data flow:**
       a. GET strategy_data for BPID.
       b. Compute new `vision_mission = { ...existing.vision_mission, core_values: CORE_VALUES, mission_statement: MISSION_STATEMENT }`.
       c. Compare JSON.stringify(new.core_values) vs JSON.stringify(existing.core_values) AND new.mission_statement === existing.mission_statement. If both equal → log "strategy_data: no changes" and skip.
       d. Else patch (under APPLY).
    9. **A9 swot_items flow:**
       a. GET all swot_items for SWOT_ANALYSES_ID.
       b. Build `byCatContent = new Map()` keyed by `${category}|${lower(trim(content))}`.
       c. **Op 1 (flexible-adaptable):**
          - Find strength row with content match (lower(trim(content)) === 'flexible & adaptable' OR 'flexible and adaptable'):
            - If found AND description differs from target, PATCH it (under APPLY).
            - **If NOT found (defensive): INSERT a new strength row** { swot_analysis_id: SWOT_ANALYSES_ID, category: 'strengths', content: 'Flexible & adaptable', description: 'Flexibility wins clients but creates scope creep — same trait shows up as a weakness' }. Log the defensive insert: "Op 1: no matching strength row found — inserting new (defensive)".
          - Check for weakness row with same content → if missing, INSERT.
       d. **Op 2 (new strength):** if `strengths|${lower(trim(NEW_STRENGTH_CONTENT))}` not in map → INSERT.
       e. **Op 3 (new threat):** if `threats|${lower(trim(NEW_THREAT_CONTENT))}` not in map → INSERT.
       f. **Op 4 (home warranty threat update):**
          - Find any threat row whose content (case-insensitive) contains 'home warranty' OR 'home-warranty'.
          - If found and description !== HOME_WARRANTY_DESCRIPTION → PATCH.
          - If not found → INSERT new threat { content: 'Home warranty cap exposure', description: HOME_WARRANTY_DESCRIPTION }.
       g. Print plan for each op.
       h. If `!APPLY`, exit (DRY RUN).
       i. Else execute.
    10. Final summary.
  </action>
  <verify>
    <!-- STATIC verify — runs unconditionally, no env required -->
    <automated>test -f scripts/68-06-armstrong-values-mission-swot.mjs && node --check scripts/68-06-armstrong-values-mission-swot.mjs && grep -q "'cb6d1358-a0ec-48b8-878c-159df6b3a576'" scripts/68-06-armstrong-values-mission-swot.mjs && grep -q "No dickheads" scripts/68-06-armstrong-values-mission-swot.mjs</automated>
    <!-- LIVE verify — requires .env.local with NEXT_PUBLIC_SUPABASE_URL + SUPABASE_SERVICE_KEY -->
    <live_automated>test -f .env.local && (node scripts/68-06-armstrong-values-mission-swot.mjs | tee /tmp/68-06-dryrun.log && grep -E "DRY RUN|core_values|swot_items" /tmp/68-06-dryrun.log) || echo "SKIP: live verify requires .env.local"</live_automated>
  </verify>
  <acceptance_criteria>
    Static (always run, no env required):
    - File `scripts/68-06-armstrong-values-mission-swot.mjs` exists.
    - Contains literal `'cb6d1358-a0ec-48b8-878c-159df6b3a576'`.
    - Contains literal `"No dickheads"` (A7 line 8).
    - Contains literal `"We build long-term relationships, not transactions"`.
    - Contains literal `"We do the right thing when no one's watching"` (with escaped apostrophe).
    - Contains literal `"We are welcoming and collaborative on site"`.
    - Contains literal substring `"sketched on paper and turn it into reality"` (mission).
    - Contains literal `"Marrickville 7 weeks ahead of schedule"` (new strength).
    - Contains literal `"Trade cost inflation"` (new threat).
    - Contains literal `"Current cap $5M; $2.2M tied at Clavellie, $700k Marrickville"` (home warranty desc).
    - Contains literal `'strengths'`, `'weaknesses'`, `'threats'`.

    Live (requires `.env.local` with live Supabase credentials; operator-run, gated on env presence):
    - Dry-run output contains `DRY RUN`.
    - Running `--apply` once then again logs "no changes" for strategy_data and 0 inserts for swot_items.
  </acceptance_criteria>
  <done>Script committed; dry-run shows expected mutations; idempotency verified.</done>
</task>

</tasks>

<verification>
- Post-apply re-query: `strategy_data.vision_mission.core_values` is a 9-item array matching CORE_VALUES verbatim.
- Post-apply re-query: `strategy_data.vision_mission.mission_statement` matches MISSION_STATEMENT verbatim.
- Post-apply re-query of swot_items: new strength + new threat present; home-warranty threat description updated; flexible-and-adaptable appears in both strengths and weaknesses categories.
</verification>

<success_criteria>
- Values list reads as behaviour statements (not buzzwords).
- Mission matches session wording verbatim.
- SWOT additions captured.
- Idempotent.
</success_criteria>

<output>
After completion, create `.planning/phases/68-step-4-wizard-mvp-improvements-armstrong-co-plan-data-refresh/68-06-SUMMARY.md` with dry-run + apply output.
</output>
