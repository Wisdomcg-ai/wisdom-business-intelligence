---
phase: 68-step-4-wizard-mvp-improvements-armstrong-co-plan-data-refresh
plan: 11
type: execute
wave: 4
depends_on: ["68-10"]
files_modified:
  - src/app/goals/components/Step4AnnualPlan.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Initiative cards in kanban quarters render a category badge (when initiative.category is set) and a priority badge (when initiative.priority is set)"
    - "Initiative cards in the Available pool render the same category + priority badges in addition to the existing source badge"
    - "Category badge colour is determined by a stable category → palette map (Marketing/Finance/People/Systems/CX/Leadership/Time/Customer Experience etc)"
    - "Priority badge: HIGH = red, MED = amber, LOW = slate; only rendered when priority is set"
    - "tsc + lint + build green"
  artifacts:
    - path: src/app/goals/components/Step4AnnualPlan.tsx
      provides: B4 — category + priority badges on kanban + Available cards
  key_links:
    - from: kanban card render block (~line 1208)
      to: category + priority badge JSX
      via: "shared CategoryBadge + PriorityBadge inline components defined inside Step4AnnualPlan"
      pattern: "CategoryBadge"
---

<objective>
B4: Add category + priority badges to initiative cards. Today cards show only a source badge (ROADMAP / STRATEGIC / OPERATIONAL). Add:
- Category badge — colour-coded by engine (Marketing/Finance/People/Systems/CX/Leadership/Time/Customer Experience/Diversification + fallback)
- Priority badge — HIGH (red) / MED (amber) / LOW (slate). Only rendered when priority is set.

Apply to BOTH kanban cards (assigned initiatives, ~lines 1208-1234) AND Available pool cards (unassigned initiatives, ~lines 1258-1284).

Purpose: Makes the engine balance visible at the card level (sets up for the per-quarter balance bar in 68-12 and filter chips in 68-13).
Output: Edits to one file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/68-step-4-wizard-mvp-improvements-armstrong-co-plan-data-refresh/68-CONTEXT.md
@src/app/goals/components/Step4AnnualPlan.tsx
@src/app/goals/types.ts

INTERFACES:

StrategicInitiative type (from src/app/goals/types.ts) — relevant fields:
- category?: string  (free-text; conventional values: 'Marketing', 'Finance', 'People', 'Systems', 'Customer Experience', 'CX', 'Leadership', 'Time', 'Diversification', etc.)
- priority?: 'high' | 'medium' | 'low'

Category palette (stable mapping; case-insensitive lookup):
  marketing            -> { bg: 'bg-pink-100',     text: 'text-pink-700',     label: 'MKTG'  }
  finance              -> { bg: 'bg-emerald-100',  text: 'text-emerald-700',  label: 'FIN'   }
  people               -> { bg: 'bg-violet-100',   text: 'text-violet-700',   label: 'PPL'   }
  systems              -> { bg: 'bg-sky-100',      text: 'text-sky-700',      label: 'SYS'   }
  customer experience  -> { bg: 'bg-amber-100',    text: 'text-amber-700',    label: 'CX'    }
  cx                   -> { bg: 'bg-amber-100',    text: 'text-amber-700',    label: 'CX'    }
  leadership           -> { bg: 'bg-indigo-100',   text: 'text-indigo-700',   label: 'LEAD'  }
  time                 -> { bg: 'bg-orange-100',   text: 'text-orange-700',   label: 'TIME'  }
  diversification      -> { bg: 'bg-rose-100',     text: 'text-rose-700',     label: 'DIV'   }
  (default)            -> { bg: 'bg-gray-200',     text: 'text-gray-700',     label: <UPPER first 4 chars of category> }

Priority palette:
  high   -> { bg: 'bg-red-100',    text: 'text-red-700',    label: 'HIGH' }
  medium -> { bg: 'bg-amber-100',  text: 'text-amber-700',  label: 'MED'  }
  low    -> { bg: 'bg-slate-100',  text: 'text-slate-700',  label: 'LOW'  }

Existing badge styles on the Available card (lines 1265-1267 — source badge) override text colour because the card background is brand-navy or brand-orange. New badges must use the SAME `bg-white/20 text-white` override on coloured kanban cards (isRoadmap or strategic) and the standalone palette on white operational cards.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CategoryBadge + PriorityBadge helpers and wire into kanban + Available cards</name>
  <files>src/app/goals/components/Step4AnnualPlan.tsx</files>
  <read_first>
    - src/app/goals/components/Step4AnnualPlan.tsx (lines 1208-1290 — both card render blocks)
    - src/app/goals/types.ts (StrategicInitiative type to confirm category + priority field names + values)
    - .planning/phases/68-step-4-wizard-mvp-improvements-armstrong-co-plan-data-refresh/68-CONTEXT.md (B4 decision)
  </read_first>
  <action>
    Edit src/app/goals/components/Step4AnnualPlan.tsx:

    1. Define palette objects inside the Step4AnnualPlan component body (after the existing state declarations, before the existing helper functions). Add a co-located helper block, e.g. right after handleStaggerByPriority (~line 463). Use exactly these constants (copy literally):

       const CATEGORY_PALETTE: Record<string, { bg: string; text: string; label: string }> = {
         marketing:            { bg: 'bg-pink-100',    text: 'text-pink-700',    label: 'MKTG' },
         finance:              { bg: 'bg-emerald-100', text: 'text-emerald-700', label: 'FIN'  },
         people:               { bg: 'bg-violet-100',  text: 'text-violet-700',  label: 'PPL'  },
         systems:              { bg: 'bg-sky-100',     text: 'text-sky-700',     label: 'SYS'  },
         'customer experience': { bg: 'bg-amber-100',  text: 'text-amber-700',   label: 'CX'   },
         cx:                   { bg: 'bg-amber-100',   text: 'text-amber-700',   label: 'CX'   },
         leadership:           { bg: 'bg-indigo-100',  text: 'text-indigo-700',  label: 'LEAD' },
         time:                 { bg: 'bg-orange-100',  text: 'text-orange-700',  label: 'TIME' },
         diversification:      { bg: 'bg-rose-100',    text: 'text-rose-700',    label: 'DIV'  },
       }

       const PRIORITY_PALETTE: Record<string, { bg: string; text: string; label: string }> = {
         high:   { bg: 'bg-red-100',    text: 'text-red-700',    label: 'HIGH' },
         medium: { bg: 'bg-amber-100',  text: 'text-amber-700',  label: 'MED'  },
         low:    { bg: 'bg-slate-100',  text: 'text-slate-700',  label: 'LOW'  },
       }

       const getCategoryStyle = (category?: string) => {
         if (!category) return null
         const key = category.trim().toLowerCase()
         if (CATEGORY_PALETTE[key]) return CATEGORY_PALETTE[key]
         return { bg: 'bg-gray-200', text: 'text-gray-700', label: category.toUpperCase().slice(0, 4) }
       }

       const getPriorityStyle = (priority?: string) => {
         if (!priority) return null
         return PRIORITY_PALETTE[priority.toLowerCase()] ?? null
       }

       // Used to override badge colours on coloured (navy/orange) card backgrounds
       // where the contextual bg-{color}-100 + text-{color}-700 palette is unreadable.
       const overrideBadgeForColoredCard = (isColored: boolean, style: { bg: string; text: string; label: string }) => {
         if (!isColored) return style
         return { ...style, bg: 'bg-white/20', text: 'text-white' }
       }

    2. In the kanban card render block (~lines 1215-1233 — inside the items.map((initiative) => ... return JSX), find the existing card markup:

       <div key={initiative.id} draggable ... className={`group flex items-start gap-1.5 p-2 rounded border-2 cursor-move transition-all ${cardBg}`}>
         <GripVertical ... />
         <p className="text-xs font-medium leading-snug flex-1 line-clamp-2">{initiative.title}</p>
         <button ...>{X}</button>
       </div>

       Replace the inner content. Move the title + badges into a vertical wrapper so the badges sit under the title:

       <div className="flex-1 min-w-0">
         <p className="text-xs font-medium leading-snug line-clamp-2">{initiative.title}</p>
         {(() => {
           const isColored = isRoadmap || !isOperational
           const cat = getCategoryStyle(initiative.category)
           const pri = getPriorityStyle(initiative.priority)
           if (!cat && !pri) return null
           return (
             <div className="flex flex-wrap gap-1 mt-1">
               {cat && (() => {
                 const s = overrideBadgeForColoredCard(isColored, cat)
                 return <span className={`px-1 py-0.5 text-[9px] rounded font-semibold ${s.bg} ${s.text}`}>{s.label}</span>
               })()}
               {pri && (() => {
                 const s = overrideBadgeForColoredCard(isColored, pri)
                 return <span className={`px-1 py-0.5 text-[9px] rounded font-semibold ${s.bg} ${s.text}`}>{s.label}</span>
               })()}
             </div>
           )
         })()}
       </div>

       Preserve GripVertical and the X (remove) button at their existing positions in the flex row.

    3. In the Available pool card block (~lines 1268-1284), the existing structure already has a vertical wrapper. Inside the div `<div className="flex-1 min-w-0">`, add the same badge row AFTER the existing source badge (the `<span>` rendering `badgeStyle.label`):

       {(() => {
         const isColored = isRoadmap || !isOperational
         const cat = getCategoryStyle(initiative.category)
         const pri = getPriorityStyle(initiative.priority)
         if (!cat && !pri) return null
         return (
           <div className="flex flex-wrap gap-1 mt-1">
             {cat && (() => {
               const s = overrideBadgeForColoredCard(isColored, cat)
               return <span className={`px-1 py-0.5 text-[9px] rounded font-semibold ${s.bg} ${s.text}`}>{s.label}</span>
             })()}
             {pri && (() => {
               const s = overrideBadgeForColoredCard(isColored, pri)
               return <span className={`px-1 py-0.5 text-[9px] rounded font-semibold ${s.bg} ${s.text}`}>{s.label}</span>
             })()}
           </div>
         )
       })()}

       The existing source badge (ROADMAP / STRATEGIC / OPERATIONAL) stays exactly as it is — we ADD new badges underneath, do not replace.

    4. Type-check: **Confirmed in advance** — StrategicInitiative already has optional `category?: InitiativeCategory` and `priority?: InitiativePriority` at `src/app/goals/types.ts:72-73`. **No type changes are required.** Do NOT touch types.ts. If for any reason `npx tsc --noEmit` reports that `category` or `priority` is missing on `initiative` in the new JSX, that's a regression in types.ts — STOP and surface the diff before patching the types file.
  </action>
  <verify>
    <automated>grep -n "CATEGORY_PALETTE" src/app/goals/components/Step4AnnualPlan.tsx &amp;&amp; grep -n "PRIORITY_PALETTE" src/app/goals/components/Step4AnnualPlan.tsx &amp;&amp; grep -n "getCategoryStyle" src/app/goals/components/Step4AnnualPlan.tsx | wc -l &amp;&amp; grep -n "getPriorityStyle" src/app/goals/components/Step4AnnualPlan.tsx | wc -l &amp;&amp; npx tsc --noEmit 2&gt;&amp;1 | grep -E "Step4AnnualPlan|error TS" | head -10 || echo "tsc clean" &amp;&amp; npm run lint -- src/app/goals/components/Step4AnnualPlan.tsx 2&gt;&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File contains literal "CATEGORY_PALETTE" defined as Record with 'marketing', 'finance', 'people', 'systems', 'customer experience', 'cx', 'leadership', 'time', 'diversification' keys.
    - File contains literal "PRIORITY_PALETTE" with 'high', 'medium', 'low' keys.
    - File contains literal "getCategoryStyle" — referenced at least 2 times (one for kanban cards, one for Available pool).
    - File contains literal "getPriorityStyle" — referenced at least 2 times.
    - File contains literal "overrideBadgeForColoredCard".
    - Source badge code (the existing 'ROADMAP'/'STRATEGIC'/'OPERATIONAL' label) is NOT removed (grep for one of these labels still finds it).
    - "npx tsc --noEmit" reports no errors for this file.
    - "npm run lint -- src/app/goals/components/Step4AnnualPlan.tsx" exits 0.
    - "npm run build" exits 0.
  </acceptance_criteria>
  <done>Both card variants render category + priority badges when present; source badge preserved on Available cards; tsc + lint + build green.</done>
</task>

</tasks>

<verification>
- Visit /goals in dev with Armstrong (after Workstream A applies). Q1 cards show MKTG/FIN/PPL etc. badges + HIGH/MED/LOW badges where set.
- Available pool: diversification ideas (from 68-03) render the DIV category badge.
- Initiatives with no category or no priority render only what's present (no empty badge row).
</verification>

<success_criteria>
- Cards become engine-categorisable at a glance.
- Sets up the data needed for 68-12 (balance bar) and 68-13 (filter chips).
</success_criteria>

<output>
After completion, create .planning/phases/68-step-4-wizard-mvp-improvements-armstrong-co-plan-data-refresh/68-11-SUMMARY.md with screenshots or visual confirmation notes.
</output>
