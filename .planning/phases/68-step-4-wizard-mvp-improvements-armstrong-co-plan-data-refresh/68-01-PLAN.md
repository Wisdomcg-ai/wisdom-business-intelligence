---
phase: 68-step-4-wizard-mvp-improvements-armstrong-co-plan-data-refresh
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/68-01-snapshot-armstrong.mjs
  - scripts/snapshots/.gitkeep
autonomous: true
requirements: []
must_haves:
  truths:
    - "All Armstrong tenant rows from affected tables are captured to a timestamped JSON file before any write happens"
    - "Re-running the snapshot script never overwrites a prior snapshot"
    - "Snapshot file contains row counts that match a live re-query at capture time"
  artifacts:
    - path: scripts/68-01-snapshot-armstrong.mjs
      provides: One-shot read-only snapshot of all Armstrong-affected tables
    - path: scripts/snapshots/.gitkeep
      provides: Directory placeholder so scripts/snapshots/ is committed
  key_links:
    - from: scripts/68-01-snapshot-armstrong.mjs
      to: scripts/snapshots/68-armstrong-pre-write-<timestamp>.json
      via: "writeFileSync with new Date().toISOString() in filename"
      pattern: "68-armstrong-pre-write-"
---

<objective>
Capture a complete, timestamped snapshot of all Armstrong & Co plan-data tables in Supabase BEFORE any Workstream A write script runs. This is the rollback artifact — if any A-workstream script produces a bad result, the snapshot file lets us reconstruct the pre-write state.

Purpose: Risk control. All subsequent A-workstream plans depend on this snapshot existing.
Output: `scripts/68-01-snapshot-armstrong.mjs` + a snapshot JSON file under `scripts/snapshots/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/68-step-4-wizard-mvp-improvements-armstrong-co-plan-data-refresh/68-CONTEXT.md
@scripts/onboard-fit2shine.mjs
@docs/BUSINESS_ID_PATTERNS.md

<interfaces>
Armstrong tenant identifiers (hardcoded in script):
- BUSINESSES_ID         = 'a0bf1b0a-663e-4636-8c0d-eef62972dcbc'
- BUSINESS_PROFILES_ID  = '678ae542-7f0b-43d1-8784-e7341767c250'
- USER_ID               = 'f4702002-69a6-44f1-b963-ada2a95c843b'
- SWOT_ANALYSES_ID      = 'cb6d1358-a0ec-48b8-878c-159df6b3a576'

Tables to snapshot (with the column used to filter):
| Table                       | Filter                                                 |
|-----------------------------|--------------------------------------------------------|
| businesses                  | id=eq.${BUSINESSES_ID}                                 |
| business_profiles           | id=eq.${BUSINESS_PROFILES_ID}                          |
| business_financial_goals    | business_id=eq.${BUSINESS_PROFILES_ID}                 |
| business_kpis               | business_id=eq.${BUSINESS_PROFILES_ID}                 |
| strategic_initiatives       | business_id=eq.${BUSINESS_PROFILES_ID}                 |
| strategy_data               | business_id=eq.${BUSINESS_PROFILES_ID}                 |
| swot_analyses               | id=eq.${SWOT_ANALYSES_ID}                              |
| swot_items                  | swot_analysis_id=eq.${SWOT_ANALYSES_ID}                |
| plan_snapshots              | business_id=eq.${BUSINESS_PROFILES_ID}                 |

Env (same pattern as scripts/onboard-fit2shine.mjs):
- NEXT_PUBLIC_SUPABASE_URL (from .env.local)
- SUPABASE_SERVICE_KEY     (from .env.local)
Headers: { apikey, Authorization: Bearer, 'Content-Type': 'application/json' }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scripts/snapshots/.gitkeep and snapshot script</name>
  <files>scripts/68-01-snapshot-armstrong.mjs, scripts/snapshots/.gitkeep</files>
  <read_first>
    - scripts/onboard-fit2shine.mjs (mirror env-loading, headers, fetch pattern, top-of-file comment block)
    - docs/BUSINESS_ID_PATTERNS.md (confirm filter columns are correct for each table)
    - .planning/phases/68-step-4-wizard-mvp-improvements-armstrong-co-plan-data-refresh/68-CONTEXT.md (table list, identifiers)
  </read_first>
  <action>
    Create `scripts/snapshots/.gitkeep` (empty file).

    Create `scripts/68-01-snapshot-armstrong.mjs` as a Node ESM script. Structure (literal):

    1. Shebang + JSDoc header documenting:
       - Purpose ("One-off: snapshot all Armstrong & Co plan-data tables before Phase 68 Workstream A writes.")
       - Targets: hardcoded UUIDs above
       - Run: `node scripts/68-01-snapshot-armstrong.mjs` (READ-ONLY — no --apply flag; this script never writes to Supabase)
    2. `import { writeFileSync, mkdirSync, existsSync } from 'node:fs'`
    3. `import { config } from 'dotenv'; config({ path: '.env.local' });`
    4. Read `NEXT_PUBLIC_SUPABASE_URL`, `SUPABASE_SERVICE_KEY`; throw if missing (match onboard-fit2shine.mjs lines 20-22).
    5. Hardcode the four UUIDs as `const` at top.
    6. Headers object: `{ apikey: KEY, Authorization: \`Bearer \${KEY}\`, 'Content-Type': 'application/json' }`.
    7. Define `TABLES` array of `{ table, filter, label }` objects exactly matching the table list in <interfaces>.
    8. Async `fetchTable({ table, filter })` helper: GET `${URL}/rest/v1/${table}?${filter}`; throw on non-2xx; return parsed JSON array.
    9. Main: iterate `TABLES` sequentially (use `await` in a for-of loop); collect into `result[label] = rows`; also log `label: N rows` to console.
    10. Build output filename: `scripts/snapshots/68-armstrong-pre-write-${new Date().toISOString().replace(/[:.]/g, '-')}.json`.
    11. `mkdirSync('scripts/snapshots', { recursive: true })`.
    12. Refuse to overwrite: if `existsSync(outPath)` then `throw new Error('Snapshot file already exists: ' + outPath)`. (Idempotency: ISO timestamp makes this practically impossible but we still guard.)
    13. `writeFileSync(outPath, JSON.stringify({ capturedAt: new Date().toISOString(), tenant: { businesses_id, business_profiles_id, user_id, swot_analyses_id }, tables: result }, null, 2))`.
    14. Print final summary: total tables, total rows, output path.

    No `--apply` flag. This script is read-only by design (nothing in subsequent A plans should depend on a flag here).

    Per repo conventions (CLAUDE.md): npm only (script uses Node fetch — no extra deps beyond `dotenv`, already in deps).
  </action>
  <verify>
    <automated>node scripts/68-01-snapshot-armstrong.mjs &amp;&amp; ls scripts/snapshots/68-armstrong-pre-write-*.json | head -1 | xargs -I{} node -e "const d=require('./{}'); if(!d.tables.businesses) throw new Error('missing businesses'); if(!d.tables.strategic_initiatives) throw new Error('missing strategic_initiatives'); console.log('snapshot ok, tables:', Object.keys(d.tables).length)"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/68-01-snapshot-armstrong.mjs` exists.
    - `scripts/snapshots/.gitkeep` exists.
    - File contains the literal string `'a0bf1b0a-663e-4636-8c0d-eef62972dcbc'` (Armstrong businesses.id).
    - File contains the literal string `'678ae542-7f0b-43d1-8784-e7341767c250'` (Armstrong business_profiles.id).
    - File contains the literal string `'cb6d1358-a0ec-48b8-878c-159df6b3a576'` (Armstrong swot_analyses.id).
    - File contains the literal substring `'68-armstrong-pre-write-'`.
    - File contains NO `'--apply'` substring (script is read-only).
    - `grep -c "fetchTable\|fetch(" scripts/68-01-snapshot-armstrong.mjs` returns &gt;= 1.
    - Running `node scripts/68-01-snapshot-armstrong.mjs` exits 0 and produces a JSON file under `scripts/snapshots/` whose top-level `tables` key has all 9 expected sub-keys: `businesses, business_profiles, business_financial_goals, business_kpis, strategic_initiatives, strategy_data, swot_analyses, swot_items, plan_snapshots`.
  </acceptance_criteria>
  <done>Script committed; snapshot JSON file produced and verified to contain all 9 tables for Armstrong tenant.</done>
</task>

</tasks>

<verification>
- Snapshot file exists under `scripts/snapshots/` with timestamp in filename.
- `jq '.tables | keys | length' scripts/snapshots/68-armstrong-pre-write-*.json` returns 9.
- `jq '.tables.strategic_initiatives | length' scripts/snapshots/68-armstrong-pre-write-*.json` returns the current row count (expected ~65 pre-dedupe).
</verification>

<success_criteria>
- A timestamped JSON snapshot of Armstrong's 9 affected tables exists in `scripts/snapshots/`.
- Snapshot row counts match a live re-query at capture time.
- All subsequent Workstream A plans can reference this snapshot as the rollback baseline.
</success_criteria>

<output>
After completion, create `.planning/phases/68-step-4-wizard-mvp-improvements-armstrong-co-plan-data-refresh/68-01-SUMMARY.md` documenting:
- Path to the snapshot JSON file
- Row count per table at capture time
- Confirmation that no writes occurred
</output>
