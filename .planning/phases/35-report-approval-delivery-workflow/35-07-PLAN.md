---
phase: 35-report-approval-delivery-workflow
plan: 07
type: execute
wave: 4
depends_on: [35-04, 35-06]
files_modified:
  - src/app/api/monthly-report/snapshot/route.ts
  - src/app/api/monthly-report/commentary/route.ts
  - src/app/api/monthly-report/settings/route.ts
  - .planning/ROADMAP.md
autonomous: false
requirements: [APPR-01, APPR-02, APPR-03, APPR-04, APPR-05]
runtime_note: |
  All route files are Next.js App Router Node.js runtime handlers. No workflow-sandbox code.
must_haves:
  truths:
    - "Saving commentary on an approved or sent report silently reverts status to draft (D-16)"
    - "Saving the report snapshot (Save Draft) on an approved/sent report silently reverts status to draft (D-16)"
    - "Applying/changing a template via the settings endpoint on an approved/sent report silently reverts status to draft (D-16)"
    - "Xero sync endpoints are NOT modified (D-17 — background refresh must not trigger revert)"
    - "ROADMAP.md Phase 35 entry is updated to reflect Resend-direct delivery (not Make.com webhook)"
    - "A final UAT pass through the full flow confirms APPR-01..APPR-05 end-to-end"
  artifacts:
    - path: "src/app/api/monthly-report/snapshot/route.ts"
      provides: "Snapshot save endpoint — calls revertReportIfApproved after write"
      contains: "revertReportIfApproved"
    - path: "src/app/api/monthly-report/commentary/route.ts"
      provides: "Commentary save endpoint — calls revertReportIfApproved after write"
      contains: "revertReportIfApproved"
    - path: "src/app/api/monthly-report/settings/route.ts"
      provides: "Settings save endpoint — calls revertReportIfApproved after write"
      contains: "revertReportIfApproved"
    - path: ".planning/ROADMAP.md"
      provides: "Updated Phase 35 scope reflecting Resend-direct (CONTEXT.md override)"
      contains: "Resend"
  key_links:
    - from: "snapshot/route.ts"
      to: "revertReportIfApproved"
      via: "await call after existing snapshot save"
      pattern: "revertReportIfApproved"
    - from: "commentary/route.ts"
      to: "revertReportIfApproved"
      via: "await call after existing commentary save"
      pattern: "revertReportIfApproved"
    - from: "settings/route.ts"
      to: "revertReportIfApproved"
      via: "await call after existing settings save"
      pattern: "revertReportIfApproved"
---

<objective>
Close the edit-revert loop (D-16) by hooking `revertReportIfApproved()` (from Plan 35-04) into every save endpoint the coach can reach: commentary, snapshot, and settings. Also update ROADMAP.md so the documented Phase 35 scope reflects the Resend-direct decision (CONTEXT.md override of the original Make.com plan), and run a final end-to-end UAT.

Purpose: D-16 is the last server-side behavior without coverage. A coach who fixes a typo in their commentary on a sent report should see the pill flip to draft silently — this plan is where that happens. D-17 requires us to skip Xero sync routes; only coach-initiated edits count.

Output: Three route modifications + ROADMAP.md update + UAT sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-report-approval-delivery-workflow/35-CONTEXT.md
@.planning/phases/35-report-approval-delivery-workflow/35-RESEARCH.md
@.planning/phases/35-report-approval-delivery-workflow/35-04-PLAN.md
@.planning/ROADMAP.md
</context>

<interfaces>
<!-- From Plan 35-04 -->
```typescript
import { revertReportIfApproved } from '@/lib/reports/revert-report'

await revertReportIfApproved(supabase, business_id, period_month)
// Returns { reverted: boolean; previous_status?: string }
// Does nothing if status is already draft/ready_for_review
// Preserves snapshot_data + snapshot_taken_at (D-18)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Hook revertReportIfApproved into commentary + snapshot + settings save routes</name>
  <files>src/app/api/monthly-report/snapshot/route.ts, src/app/api/monthly-report/commentary/route.ts, src/app/api/monthly-report/settings/route.ts</files>
  <read_first>
    - src/app/api/monthly-report/snapshot/route.ts (entire file — understand the current save flow; the revert call goes AFTER the save succeeds)
    - src/app/api/monthly-report/commentary/route.ts (same — understand save shape + how business_id and period_month are extracted from body or query)
    - src/app/api/monthly-report/settings/route.ts (same; if file handles multiple verbs, only hook into the template-apply or settings-update path)
    - src/lib/reports/revert-report.ts (Plan 35-04 output — confirm the import path and signature)
    - .planning/phases/35-report-approval-delivery-workflow/35-CONTEXT.md D-16, D-17 — ONLY coach-initiated commentary/snapshot/template changes. Xero sync routes are OUT OF SCOPE.
    - Important negative scan: do NOT modify `src/app/api/monthly-report/sync-xero/route.ts` or any other sync endpoint. Explicitly check `ls src/app/api/monthly-report/` to confirm which files are sync-related.
  </read_first>
  <action>
    For EACH of the three route files, make a surgical addition — do NOT refactor the surrounding code.

    **Pattern:** Immediately AFTER the existing save/write succeeds (whichever supabase call writes commentary / snapshot / settings), add:
    ```typescript
    // Phase 35 D-16: Silently revert an approved or sent report to draft after a coach edit.
    // Preserves snapshot_data (D-18) so the client's already-sent email link keeps working.
    try {
      await revertReportIfApproved(supabase, business_id, period_month)
    } catch (revertErr) {
      // Do not fail the save if revert tracking fails — log and continue.
      console.error('[monthly-report/<name>] revertReportIfApproved failed:', revertErr)
    }
    ```

    Required per-file specifics:

    **`src/app/api/monthly-report/commentary/route.ts`:**
    - Add import at top: `import { revertReportIfApproved } from '@/lib/reports/revert-report'`
    - Resolve `business_id` and `period_month` from whatever the existing handler already extracts (likely request body). If the handler currently doesn't receive `period_month`, the monthly-report page's save call DOES include it — confirm via grep: `grep -rn "api/monthly-report/commentary" src/app/finances/monthly-report/`. Update the handler signature to read both from body.
    - Insert the revert block AFTER the successful commentary insert/update, BEFORE the response.
    - The handler's own success response stays unchanged.

    **`src/app/api/monthly-report/snapshot/route.ts`:**
    - Same import.
    - Resolve `business_id` and `period_month` from body (the existing snapshot payload always has these; confirm via read).
    - Insert revert block AFTER successful snapshot write (to `monthly_report_snapshots` table — NOTE this is different from `cfo_report_status.snapshot_data` per RESEARCH.md §Pitfall 8; these are two separate storage locations and this plan does NOT touch that distinction).

    **`src/app/api/monthly-report/settings/route.ts`:**
    - Same import.
    - Only insert the revert in the coach-edit branches (template apply, section toggle save, etc.). If the file also handles read-only GETs, do NOT touch them.
    - If this file handles multiple POST/PATCH shapes, add the revert inside each "something changed" branch after its own save succeeds. It's idempotent so over-calling is safe, but calling on a pure GET is wasteful.

    **Hard constraint — do NOT touch:**
    - `src/app/api/monthly-report/sync-xero/` or any Xero sync path (D-17)
    - `src/app/api/monthly-report/full-year/` (read-only derived data)
    - `src/app/api/monthly-report/generate/` (likely returns live-computed report — not a write)
    - Any `src/app/api/xero/*` endpoint
    - `src/app/api/cron/*` if present

    If you encounter unclear routes (e.g. `/debug`, `/account-mappings`), skip them — add the revert only where the user's definition of "edit" in D-17 clearly applies (commentary, snapshot, template/settings).
  </action>
  <verify>
    <automated>grep -l "revertReportIfApproved" src/app/api/monthly-report/commentary/route.ts src/app/api/monthly-report/snapshot/route.ts src/app/api/monthly-report/settings/route.ts &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "revertReportIfApproved" src/app/api/monthly-report/commentary/route.ts` returns `>= 2` (import + call)
    - `grep -c "revertReportIfApproved" src/app/api/monthly-report/snapshot/route.ts` returns `>= 2`
    - `grep -c "revertReportIfApproved" src/app/api/monthly-report/settings/route.ts` returns `>= 2`
    - `grep -c "revertReportIfApproved" src/app/api/monthly-report/sync-xero/route.ts` returns `0` (D-17 — NOT hooked into Xero sync)
    - `grep -rc "revertReportIfApproved" src/app/api/xero/` returns `0` (no Xero endpoint touched)
    - `grep -rc "revertReportIfApproved" src/app/api/cron/` returns `0` (no cron endpoint touched; command fine if dir absent)
    - `npx tsc --noEmit` exits 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Revert wired into exactly 3 save routes; Xero sync + cron paths untouched; build green.</done>
</task>

<task type="auto">
  <name>Task 2: Update ROADMAP.md Phase 35 scope to reflect Resend-direct delivery</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md lines 538-572 (current Phase 35 entry — the bit to rewrite)
    - .planning/phases/35-report-approval-delivery-workflow/35-CONTEXT.md §domain "Scope override vs ROADMAP.md"
  </read_first>
  <action>
    Edit `.planning/ROADMAP.md` — replace the Phase 35 section (starting at `### Phase 35: Report Approval + Delivery Workflow` and ending at the blank line before `### Phase 37:`) with the following updated block. Preserve the exact heading to maintain any cross-references elsewhere in the file.

    ```markdown
    ### Phase 35: Report Approval + Delivery Workflow
    **Goal:** Matt can mark a monthly report as approved inside WisdomBI, triggering an automated email to the client via Resend — replacing the current manual process of exporting from Calxa and sending separately.
    **Depends on:** Phase 33 (CFO Dashboard — defines `cfo_report_status` table and status vocabulary)
    **Requirements:** APPR-01, APPR-02, APPR-03, APPR-04, APPR-05
    **UI hint:** yes
    **Plans:** 7 plans (35-01..35-07)
    **Status:** Implementation complete

    **Scope override vs original roadmap:** Originally specified a Make.com webhook fan-out with a per-business `businesses.make_webhook_url` column. That was replaced during `/gsd:discuss-phase` with direct Resend email delivery — the platform owns deliverability; no per-client external-URL setup; audit data stays in the DB (`cfo_email_log`). REQUIREMENTS.md APPR-02/03/05 language still says "Make.com webhook"; the implementation satisfies the underlying intent ("delivery mechanism") via Resend.

    **Context:**
    The `cfo_report_status` table (created in Phase 33) already models the full status lifecycle:
    `draft → ready_for_review → approved → sent`

    Phase 35 adds the UI controls, the direct email send, and the public snapshot view that make that lifecycle real.

    **Scope (as implemented):**
    - New table: `cfo_email_log` (append-only audit of every Resend send attempt)
    - New env vars: `RESEND_API_KEY` (already deployed) + `REPORT_LINK_SECRET` (new — HMAC signing)
    - New API: `POST /api/cfo/report-status` — dispatches `mark_ready`, `approve_and_send`, `resend`, `revert_to_draft`
    - New email wrapper: `src/lib/email/send-report.ts` — `sendMonthlyReport()` with 15s deadline, PDF attachment, coach-email `from`
    - New URL helpers: `src/lib/reports/report-token.ts` + `src/lib/reports/build-report-url.ts` (HMAC-signed, Phase 36 portal-slug forward-compatible)
    - New public route: `/reports/view/[token]` — renders frozen `snapshot_data` (no login)
    - Middleware: `/reports/view` added to `publicRoutes` AND `onboardingExemptRoutes`
    - Monthly report page UI: `ReportStatusBar` component (pill + contextual action buttons)
    - Edit-revert: commentary / snapshot / settings save routes call `revertReportIfApproved()` — coach edits silently revert approved/sent reports to draft, preserving the already-sent snapshot

    **Email shape:**
    - From: assigned coach's email (e.g., `mattmalouf@wisdomcg.com.au`) — requires Resend sender verification per coach
    - To: `businesses.owner_email` (single recipient)
    - Subject: `{Business name} — {Month Year} financial report`
    - Body: greeting + "View Report" button + PDF attachment (no headline numbers, no AI narrative)
    - Attachment: existing jsPDF MonthlyReportPDFService output, generated browser-side and POSTed as base64

    **Success Criteria (implemented):**
    - "Approve & Send" posts the send to Resend and transitions status to `sent` on 2xx
    - CFO dashboard "Pending Approval" count decrements automatically
    - Resend failure leaves status at `approved` with a contextual error toast + "Resend" button
    - Approved tokens do not expire; rotating `REPORT_LINK_SECRET` is the kill-switch
    - Editing commentary/snapshot/template on an approved or sent report silently reverts to draft while preserving the original snapshot for the already-sent link

    **Deferred to future iteration:**
    - Async Resend bounce/complaint webhook handler
    - Per-report revocation column (complements the global `REPORT_LINK_SECRET` kill-switch)
    - AI narrative in email body (tied to Phase 24 maturity)
    - Multi-recipient CC/BCC support
    - Client sign-off workflow
    ```

    Do NOT touch any other phase in ROADMAP.md. Verify the Phase 37 section immediately following is intact after your edit.

    If there is a Phase 36 section that references "the report_url in the Make.com webhook payload (Phase 35)" (seen at line ~669 of the current file), update that specific line to say "the `report_url` generated by Phase 35's `buildReportUrl` helper links directly to `/portal/[slug]?month=YYYY-MM`". Do NOT rewrite the rest of Phase 36.
  </action>
  <verify>
    <automated>grep -c "Resend" .planning/ROADMAP.md &amp;&amp; grep -c "Phase 35" .planning/ROADMAP.md &amp;&amp; grep -c "Phase 37" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "### Phase 35: Report Approval + Delivery Workflow" .planning/ROADMAP.md` returns `1`
    - `grep -c "### Phase 37" .planning/ROADMAP.md` returns `>= 1` (not accidentally deleted)
    - The Phase 35 section now references "Resend" at least 3 times: `awk '/### Phase 35:/,/### Phase 37/' .planning/ROADMAP.md | grep -c "Resend"` returns `>= 3`
    - The Phase 35 section no longer asserts Make.com as the delivery mechanism (Make.com is only mentioned in the "Scope override" historical note): `awk '/### Phase 35:/,/### Phase 37/' .planning/ROADMAP.md | grep -c "Make.com webhook"` returns `<= 1`
    - `awk '/### Phase 35:/,/### Phase 37/' .planning/ROADMAP.md | grep -c "cfo_email_log"` returns `>= 1`
    - `awk '/### Phase 35:/,/### Phase 37/' .planning/ROADMAP.md | grep -c "REPORT_LINK_SECRET"` returns `>= 1`
    - `awk '/### Phase 35:/,/### Phase 37/' .planning/ROADMAP.md | grep -c "reports/view"` returns `>= 1`
    - `grep -c "make_webhook_url" .planning/ROADMAP.md` returns `0` (the original column plan is entirely removed from the current scope)
  </acceptance_criteria>
  <done>ROADMAP Phase 35 accurately reflects the shipped scope; historical Make.com override noted; Phase 37+ untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Final phase UAT — full lifecycle + edit-revert</name>
  <files>n/a (checkpoint — no file modification)</files>
  <action>Human-verify checkpoint. See <what-built> and <how-to-verify> below for exact steps. Executor pauses here for user sign-off.</action>
  <verify>
    <automated>echo "human-verify checkpoint — no automated command"</automated>
  </verify>
  <done>User types "approved" after all 18 UAT checks pass.</done>
  <what-built>
    - D-16 edit-revert wired into 3 save routes
    - ROADMAP.md Phase 35 entry updated
    - Complete Phase 35 is now live in dev
  </what-built>
  <how-to-verify>
    Walk through the full lifecycle once more with a real test business. Coach = mattmalouf@wisdomcg.com.au.

    Lifecycle test:
    1. Start fresh: ensure the test business's `cfo_report_status` row for this period is `draft` (or doesn't exist)
    2. Navigate to the monthly report -> confirm pill shows 'Draft'
    3. Click 'Mark Ready for Review' -> pill flips to 'Ready for Review' (D-01)
    4. Edit commentary text somewhere -> hit save
       - [ ] Pill should STAY at 'Ready for Review' (only reverts from approved/sent per D-16 scope)
    5. Click 'Approve & Send'
       - [ ] Pill flips to 'Sent · [date]'
       - [ ] Email arrives in the owner's inbox
       - [ ] Email PDF matches the Export PDF output
       - [ ] `/reports/view/[token]` link in email works in Incognito
    6. Edit commentary on the now-sent report -> hit save
       - [ ] Pill silently reverts to 'Draft' (D-16 — no modal, just happens)
       - [ ] Open the email link from step 5 in Incognito AGAIN -> still shows the original snapshot (D-18)
    7. Change a section toggle or apply a different template -> save
       - [ ] Same behavior — pill remains at 'Draft' (already draft, no-op for revert)
    8. Click 'Approve & Send' again -> new email arrives; `sent_at` updated; old email's link STILL renders the NEW snapshot (because token is keyed on statusId, not snapshot content — this is the trade-off per CONTEXT.md D-18)
    9. Click 'Revert to Draft' directly -> pill flips to 'Draft'; snapshot preserved

    Xero sync isolation test (D-17):
    10. Trigger a Xero sync (existing button on the page, or the cron route if running)
    11. Confirm the approved/sent report DOES NOT revert — the pill state is preserved

    Role isolation test (D-04):
    12. Log out; log in as a client user for the same business
    13. Navigate to the monthly report
    14. Confirm the pill is visible but NO action buttons (Mark Ready, Approve & Send, Resend, Revert) appear

    Audit log spot check:
    15. Query `cfo_email_log` for the test business's rows from this UAT session
        ```sql
        SELECT attempted_at, status_code, error_message, resend_message_id, recipient_email
        FROM cfo_email_log
        WHERE business_id = '<test business id>'
        ORDER BY attempted_at DESC
        LIMIT 10;
        ```
    16. Confirm:
        - [ ] Every "Approve & Send" click that succeeded has a row with status_code=200 + resend_message_id populated
        - [ ] Any failure during UAT has a row with error_message populated
        - [ ] Rows are append-only (no updates rewriting history)

    CFO dashboard cross-check (Phase 33 integration):
    17. Navigate to `/cfo`
    18. Confirm the "Pending Approval" count for the test business decremented after step 5 and again after step 8
  </how-to-verify>
  <resume-signal>Type "approved" if all 18 checks pass. If any fail, describe which step + state (pill label, error toast text, DB row state).</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits 0
- `npm run build` exits 0
- `npm test` exits 0 (all existing + new tests green)
- Manual UAT (Task 3) passes all 18 checks
</verification>

<success_criteria>
- D-16 satisfied: edit-revert fires on commentary + snapshot + settings save (Task 3 step 6)
- D-17 satisfied: Xero sync does NOT trigger revert (Task 3 step 10)
- D-18 satisfied: snapshot preserved across revert (Task 3 step 6 email link check)
- ROADMAP.md accurate — future readers see the shipped Resend-direct scope, not the historical Make.com plan
- APPR-01..APPR-05 all verifiable in a single UAT walkthrough
</success_criteria>

<output>
Create `.planning/phases/35-report-approval-delivery-workflow/35-07-SUMMARY.md` documenting:
- Exact set of save routes the revert was wired into (confirm 3; list any surprises)
- Lines changed in ROADMAP.md (counts + section boundaries)
- UAT results (any step that required follow-up)
- Phase completion note: ready for `/gsd:verify-work` and `/gsd:ship`
</output>
