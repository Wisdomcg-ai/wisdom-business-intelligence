---
phase: 44.1-atomic-save-hardening-and-staged-rollout
plan: 07
type: execute
wave: 5
depends_on:
  - "44.1-05"
  - "44.1-06"
files_modified:
  - .planning/phases/44.1-atomic-save-hardening-and-staged-rollout/44.1-07-SUMMARY.md
autonomous: false
gap_closure: false
requirements:
  - PHASE-44-D-12
  - PHASE-44-D-18

must_haves:
  truths:
    - "44.1 commits (44.1-01 through 44.1-06) are pushed to main FIRST, alone"
    - "After 24h soak with cron + manual saves exercising the new RPC, Wave 8 + Wave 9 commits push as a batch"
    - "FORECAST_INVARIANTS_STRICT env var stays unset (default-false) for the soak window"
    - "Sentry is monitored for invariant_violation_logged events during soak"
    - "Flip to FORECAST_INVARIANTS_STRICT=true happens AFTER 24-48h with zero invariant_violation_logged events for active tenants"
    - "User explicitly authorizes EACH push step — none auto-trigger"
  artifacts:
    - path: .planning/phases/44.1-atomic-save-hardening-and-staged-rollout/44.1-07-SUMMARY.md
      provides: "Final ship record: push timestamps, soak window observations, strict-flip timestamp, any incidents observed"
      min_lines: 50
  key_links: []
---

<objective>
Execute the staged push protocol per D-44.1-20. This plan is ENTIRELY a human-gated checkpoint sequence — its purpose is to make sure the actual deployment happens in the right order with explicit authorization at each step.

Push order:
1. **Push 44.1 commits to main** — `44.1-01` through `44.1-06` only. Wave 8/9 commits (`76595c4`, `9a38759`, `c7d8b56`, `d6c90bb`, `bc17883`, `e4f89d3`) STAY UNPUSHED.
2. **Wait 24h** for cron + manual saves to exercise the new UPSERT RPC.
3. **Push Wave 8 + Wave 9 commits in one batch** — six commits together.
4. **Soak 24-48h** with `FORECAST_INVARIANTS_STRICT=false` (the default; the env var should not be set in Vercel production env).
5. **Flip strict-on** — set `FORECAST_INVARIANTS_STRICT=true` in Vercel production env after clean Sentry window (zero `invariant_violation_logged` events for active tenants).

Each numbered step is a SEPARATE checkpoint. The plan completes when step 5 is signed off.

This plan is **autonomous: false** because every step requires:
- User authorization to git push (this is the entire point of the staged protocol).
- User-driven Sentry monitoring during soak.
- User decision on strict-flip timing.

Purpose: Make the production deployment correct, not fast. Eliminate the temptation to push everything and "watch what breaks".
Output: 44.1-07-SUMMARY.md with the actual deployment record.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/44.1-atomic-save-hardening-and-staged-rollout/44.1-CONTEXT.md
@.planning/phases/44.1-atomic-save-hardening-and-staged-rollout/44.1-05-SUMMARY.md
@.planning/phases/44.1-atomic-save-hardening-and-staged-rollout/44.1-06-SUMMARY.md
@.planning/phases/44.1-atomic-save-hardening-and-staged-rollout/PITR-RUNBOOK.md

<interfaces>
<!-- Reference: which Wave 8/9 commits stay unpushed until step 3 -->

Wave 8 (ForecastReadService + D-18 invariants):
- `c7d8b56` — test(44-08): replace 4 it.todo with full ForecastReadService spec
- `76595c4` — feat(44-08): ForecastReadService — canonical read API + D-18 invariants

Wave 9 (consumer migration — separate working session, may already be staged locally):
- `d6c90bb`, `bc17883`, `e4f89d3` — Wave 9 wizard / monthly report / cashflow consumer migrations
- `9a38759` — additional Wave 8/9 ancillary commit

Per D-44.1-12: Wave 8 commit `76595c4` is the existing strict implementation; Plan 44.1-04 adds the soft-fail gate as a follow-up commit on top of `76595c4`. Both ship together in step 3.

Hard constraint (D-44.1-20): "The six already-local commits (`76595c4`, `9a38759`, `c7d8b56`, `d6c90bb`, `bc17883`, `e4f89d3`) stay unpushed until step (3)."

Memory constraint: from `~/.claude/projects/.../memory/feedback_git_remote.md` — only push to `wisdom-business-intelligence` repo. Verify before pushing.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: CHECKPOINT — Step 1 push 44.1 commits to main</name>
  <what-built>
44.1-01 through 44.1-06 commits are local. Canary passed in 44.1-05. PITR runbook + 44-07 close-out shipped in 44.1-06. ROADMAP updated.

What requires human action: the git push itself, including verification of remote and branch.
  </what-built>
  <how-to-verify>
    1. **Verify remote** (constraint from project memory):
       ```bash
       git remote -v
       ```
       Confirm `origin` points to `wisdom-business-intelligence` repo. If a second remote exists, do NOT use it.

    2. **Verify branch is main:**
       ```bash
       git branch --show-current
       ```
       Must print `main`.

    3. **Confirm Wave 8/9 commits are NOT in the push set.** Check `git log --oneline origin/main..HEAD`:
       - The diff should contain ONLY 44.1-* commits.
       - Wave 8/9 commits (`76595c4`, `9a38759`, `c7d8b56`, `d6c90bb`, `bc17883`, `e4f89d3`) MUST NOT appear in this list. If they do, ABORT — those are pushing prematurely.
       - If Wave 8/9 commits appear in the diff, the implementer must either:
         (a) Reset `main` and cherry-pick only 44.1-* commits to push first, OR
         (b) Branch off and push 44.1-* via a temporary `feat/44-1-only` branch, then fast-forward main after.
       - Either path requires explicit user authorization.

    4. **Migration application status:**
       Confirm `supabase/migrations/20260429000003_save_assumptions_and_materialize_upsert.sql` was applied during 44.1-05 canary. If NOT, applying it now is a prerequisite to push (the canary itself would have failed otherwise — verify the SUMMARY).

    5. **Push:**
       ```bash
       git push origin main
       ```
       (Or the equivalent for the chosen branch strategy in step 3.)

    6. **Confirm Vercel deploy success:**
       Vercel auto-deploys on push to main. User watches for deploy success in Vercel dashboard.

    7. **Smoke test:**
       - Vercel deploy URL loads.
       - Coach can log in.
       - Wizard opens for any tenant.
       - User saves a small change in the wizard (or leaves the env idle if save isn't ergonomic).
       - Confirm no Sentry errors emerge in the first 5 minutes.
  </how-to-verify>
  <action>Verify remote (must be wisdom-business-intelligence per project memory) and branch (must be main). Confirm Wave 8/9 commits (76595c4, 9a38759, c7d8b56, d6c90bb, bc17883, e4f89d3) are NOT in the push set via `git log --oneline origin/main..HEAD`. If they appear, branch off and push 44.1-* commits via temporary branch. Push with `git push origin main`. Confirm Vercel deploy success in dashboard. Smoke-test the deploy URL (login, wizard open, save).</action>
  <verify>Vercel deploy status = Success; Sentry shows 0 new errors in first 5 minutes; one wizard save round-trip succeeds.</verify>
  <done>44.1 commits live in main; Wave 8/9 commits remain unpushed.</done>
  <resume-signal>Type "step 1 pushed — entering 24h soak" or "step 1 aborted: <reason>"</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: CHECKPOINT — Step 2 monitor 24h soak; collect findings</name>
  <what-built>
44.1 is live in main. The new UPSERT RPC has shipped. Cron sync runs nightly at 02:00 AEST and will exercise the new RPC indirectly via `forecast_pl_lines.computed_at` writes (cron writes to xero_pl_lines, not forecast_pl_lines, so the direct exercise of the RPC is wizard saves only).
  </what-built>
  <how-to-verify>
    1. **Wait 24h.** No code changes during this window — observation only.

    2. **Sentry checks** (every 6h or on alert):
       - Filter: `tag:invariant_violation_logged` — should show 0 events (this tag only fires when soft-fail mode in 44.1-04 is active, which only ships in step 3).
       - Filter: any new error in `save_assumptions_and_materialize` RPC — should show 0.
       - Filter: any new error in `/api/forecast-wizard-v4/generate` — investigate any anomalies.

    3. **Manual exercise** (encouraged, not strictly required):
       - User opens at least one wizard during the window and saves an assumption.
       - User runs the canary script in `--mode=diff` against the saved forecast to confirm no rows lost.

    4. **Decision after 24h:**
       - If Sentry clean + canary diff clean: proceed to step 3.
       - If incident observed: STOP. Phase 44.1 is in production; rollback options:
         (a) `DROP FUNCTION save_assumptions_and_materialize(uuid, jsonb, jsonb, boolean); CREATE OR REPLACE FUNCTION save_assumptions_and_materialize(uuid, jsonb, jsonb) ...` (restore the 3-arg form from 20260429000002).
         (b) Drop the partial unique index: `DROP INDEX forecast_pl_lines_forecast_account_code_partial_uidx;`.
         (c) Use PITR-RUNBOOK.md if data-loss surfaced.
  </how-to-verify>
  <action>Wait 24h. Every 6h, check Sentry for: (a) `tag:invariant_violation_logged` — should be 0 (this tag only activates when Wave 8/9 ships in step 3); (b) any new error in `save_assumptions_and_materialize` RPC — should be 0; (c) any new error in `/api/forecast-wizard-v4/generate`. Optionally have user save a wizard during the window and run canary diff against the saved forecast. Decide proceed-to-step-3 vs rollback.</action>
  <verify>Sentry shows 0 RPC errors and 0 wizard-generate 5xx errors over 24h; optional canary diff is clean.</verify>
  <done>24h soak complete with no incidents; ready for step 3.</done>
  <resume-signal>Type "soak clean — entering step 3" or "soak incident: <details>"</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: CHECKPOINT — Step 3 push Wave 8 + Wave 9 commits in one batch</name>
  <what-built>
44.1 has been live and stable for 24h. The UPSERT RPC is exercised by real wizard saves. Now Wave 8 (`ForecastReadService` + D-18 invariants) + Wave 9 (consumer migration to the service) ship together so the read path actually consumes the canonical service.
  </what-built>
  <how-to-verify>
    1. **Confirm `FORECAST_INVARIANTS_STRICT` is NOT set in Vercel production env** (or is set to anything other than `'true'`). The 44.1-04 default-soft behavior must be active during the upcoming soak.
       - Open Vercel dashboard → Project → Settings → Environment Variables.
       - Confirm `FORECAST_INVARIANTS_STRICT` either does not exist OR is set to `false`.

    2. **Verify which commits ship in this push:**
       ```bash
       git log --oneline origin/main..HEAD
       ```
       Expected: 6 commits including the Wave 8 commits (`c7d8b56`, `76595c4`) and Wave 9 commits (`d6c90bb`, `bc17883`, `e4f89d3`, plus `9a38759`).

    3. **Push:**
       ```bash
       git push origin main
       ```

    4. **Confirm Vercel deploy success.**

    5. **Sentry watch** (first 30 min):
       - Filter: `tag:invariant_violation_logged` — events EXPECTED here (legacy serial-saved forecasts will trip this when consumers read via ForecastReadService). They are LOG-ONLY because the soft-fail flag is default.
       - Filter: any new `tag:invariant: forecast_freshness` — should be ZERO (these would mean strict-on is enabled accidentally).
       - Filter: any new 500 from `/api/monthly-report/*`, `/api/forecast/cashflow/*` — investigate; soft-fail should prevent these.

    6. **Triage `invariant_violation_logged` population:**
       - For each tenant with a logged violation: capture forecast_id and run `POST /api/forecast/{forecast_id}/recompute` to bump computed_at and clear the log signal on the next read.
       - The triage list becomes a TODO for plain manual remediation; once cleared, the soak window enters its "clean" phase.
  </how-to-verify>
  <action>Confirm `FORECAST_INVARIANTS_STRICT` is NOT set in Vercel production env (or = false). Verify the 6 Wave 8/9 commits in `git log --oneline origin/main..HEAD`. Push with `git push origin main`. Confirm Vercel deploy success. Watch Sentry first 30 min for: invariant_violation_logged events (EXPECTED — soft-fail in action), strict `tag:invariant: forecast_freshness` events (UNEXPECTED — would mean strict-on accidentally enabled).</action>
  <verify>Vercel deploy success; Sentry strict-`invariant` tag count = 0; soft-fail `invariant_violation_logged` events surface stale-tenant population for triage in step 4.</verify>
  <done>Wave 8/9 live in main; soft-fail mode active; soak window begins.</done>
  <resume-signal>Type "step 3 pushed — entering 24-48h soak" or "step 3 aborted: <reason>"</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: CHECKPOINT — Step 4 soak 24-48h; remediate logged violations</name>
  <what-built>
Wave 8/9 are live with soft-fail invariants. Production reads succeed for all tenants (legacy + new). Sentry surfaces every legacy stale tenant via `invariant_violation_logged`. Each gets recomputed manually until the log is empty.
  </what-built>
  <how-to-verify>
    1. **Initial population audit** (within first hour after step 3 push):
       - Sentry filter: `tag:invariant_violation_logged: forecast_freshness` for the last hour.
       - Group by `forecast_id` — get the unique tenant list.

    2. **Remediate each tenant**:
       For each forecast_id in the population:
       ```bash
       curl -X POST -H "Cookie: <auth-cookie>" https://<production>/api/forecast/<forecast_id>/recompute
       ```
       (Or use a service-role client + the same recompute endpoint.)

    3. **Re-watch for 24h after remediation**:
       - New violations indicate either (a) a tenant who saved via legacy path during the soak — should not happen because new saves go through the new UPSERT RPC, OR (b) the recompute didn't take effect — investigate.

    4. **Acceptance for step 5 (strict flip):**
       - At least 24h of clean Sentry data (zero `invariant_violation_logged` events) for active tenants.
       - At most 48h of soak — beyond this, escalate to user for re-triage.

    5. **If 48h elapses and violations persist:**
       STOP. The flip is NOT performed. Investigation: which tenant + why is recompute not bumping computed_at to satisfy the freshness assertion?
  </how-to-verify>
  <action>Within first hour after step 3 push: Sentry filter `tag:invariant_violation_logged: forecast_freshness` for last hour, group by forecast_id. For each forecast_id, run `curl -X POST <production>/api/forecast/<forecast_id>/recompute` (auth-cookie required) to bump computed_at. Re-watch 24h after remediation. New violations = either tenant-saved-via-legacy (should not happen) or recompute didn't take effect — investigate. After 24h with zero new `invariant_violation_logged` events, ready for step 5 flip.</action>
  <verify>Zero `invariant_violation_logged` events in last 24h for active tenants.</verify>
  <done>Soak window clean; legacy stale tenants remediated; ready for strict flip.</done>
  <resume-signal>Type "soak clean — proceed to flip" or "soak persistent violations — investigation needed"</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 5: CHECKPOINT — Step 5 flip FORECAST_INVARIANTS_STRICT=true</name>
  <what-built>
24-48h of clean Sentry data. All legacy stale tenants remediated. Production reads are now safe to fail loud — the structural promise of D-18 (consumers refuse to render stale data) becomes operational.
  </what-built>
  <how-to-verify>
    1. **Set Vercel env var:**
       - Vercel dashboard → Project → Settings → Environment Variables.
       - Add `FORECAST_INVARIANTS_STRICT=true` to Production environment.
       - Click "Save". Vercel triggers a redeploy.

    2. **Confirm redeploy success.**

    3. **Sentry watch** (first 30 min after redeploy):
       - Filter: `tag:invariant: forecast_freshness` (strict tag, NOT the log-only one).
       - Expected: 0 events. If any fire, that tenant has stale data the soak missed; recompute IMMEDIATELY OR set `FORECAST_INVARIANTS_STRICT=false` and re-soak.

    4. **Smoke test**:
       - Coach loads forecast page → no 500.
       - Coach loads monthly report → no 500.
       - Coach loads cashflow tool → no 500.

    5. **Phase 44.1 close-out:**
       - Update `.planning/STATE.md` to mark Phase 44.1 complete.
       - Mark all 8 plan checkboxes in ROADMAP.md as `[x]` (44.1-01 through 44.1-08; 44.1-08 was added in revision iteration 2 to fix the converter root cause for D-44.1-06 vectors 2 and 3).
       - Mark Phase 44.1 itself as `[COMPLETE]` in ROADMAP.md.
       - Commit message: `docs(44.1): close out Phase 44.1 — atomic save hardened, invariants strict`.
  </how-to-verify>
  <action>In Vercel dashboard → Settings → Environment Variables → Production: add `FORECAST_INVARIANTS_STRICT=true` and Save (triggers redeploy). Confirm redeploy success. Watch Sentry first 30 min for `tag:invariant: forecast_freshness` (strict tag) — must be 0. If any fire, recompute IMMEDIATELY OR set `FORECAST_INVARIANTS_STRICT=false` and re-soak. Smoke-test: forecast page, monthly report, cashflow tool — all should load without 500. Update STATE.md + ROADMAP.md to mark Phase 44.1 complete (all 8 plan checkboxes 44.1-01 through 44.1-08 set to `[x]`; the Phase 44.1 entry header marked `[COMPLETE]`). Commit: `docs(44.1): close out Phase 44.1 — atomic save hardened, invariants strict`.</action>
  <verify>Vercel env shows `FORECAST_INVARIANTS_STRICT=true` in Production; redeploy success; Sentry strict-`invariant` tag count = 0; STATE.md + ROADMAP.md updated.</verify>
  <acceptance_criteria>
    - Vercel Production env contains `FORECAST_INVARIANTS_STRICT=true` (verified via dashboard read).
    - Vercel deploy logs show successful redeploy after the env-var save.
    - Sentry shows 0 events with `tag:invariant: forecast_freshness` in the 30 minutes after redeploy.
    - `grep -c "\[x\] 44.1-0" .planning/ROADMAP.md` returns at least 8 (one per plan ID 44.1-01 through 44.1-08; with 44.1-08 added in revision iteration 2). If the converter fix plan was NOT added, returns 7 — both acceptable; the count must match the number of plans in the phase.
    - `grep -c "Phase 44.1.*\[COMPLETE\]" .planning/ROADMAP.md` returns 1 (the Phase 44.1 header marked complete).
    - `.planning/STATE.md` updated to reflect Phase 44.1 completion (timestamp + decision log entry).
  </acceptance_criteria>
  <done>Phase 44.1 fully closed out: structural fix shipped, soft-fail soak passed, strict mode active.</done>
  <resume-signal>Type "strict flipped — Phase 44.1 closed" or "strict flip aborted: <details>"</resume-signal>
</task>

</tasks>

<verification>
- All 5 checkpoints completed with explicit user resume-signals.
- 44.1-07-SUMMARY.md captures the actual deployment timeline.
- ROADMAP.md and STATE.md reflect Phase 44.1 close-out.
- Sentry shows 0 `invariant_violation_logged` events for active tenants and 0 strict `invariant` events going forward.
</verification>

<success_criteria>
- Phase 44.1 has shipped to production in the staged order described in D-44.1-20.
- The structural data-loss vector in 44-07's RPC is closed.
- The freshness invariant is operational in strict mode after a clean soak.
- The PITR runbook is shelf-ready if a future loss event occurs.
- Wave 8/9 commits never shipped before the structural fix landed.
</success_criteria>

<output>
After completion, create `.planning/phases/44.1-atomic-save-hardening-and-staged-rollout/44.1-07-SUMMARY.md` documenting:
- Step 1 push timestamp + commits included.
- Step 2 soak observations (Sentry counts, any anomalies).
- Step 3 push timestamp + commits included (the Wave 8/9 batch).
- Step 4 soak observations + remediation actions (forecast_ids recomputed).
- Step 5 strict-flip timestamp + Sentry validation.
- Phase 44.1 final status: COMPLETE.
- Notes for future ops: any tenants that needed manual recompute beyond the soak window; any patterns to watch.
</output>
