---
plan: 75-03
title: R-4 Remove latent fallbacks + resolver input-echo + dead code
wave: 3
depends_on: ["75-02"]   # fallback removal is only safe once FKs are the loud backstop in prod
autonomous: true        # code-only, no migrations; full vitest gate + prod read-only verify
type: refactor
files_modified:
  - src/lib/business/resolveBusinessProfileIds.ts   # remove input-echo fallback
  # + the latent-fallback sites and dead-code files (enumerated in task 1 from DUAL-ID-VERIFIED.md)
---

<objective>
Now that R-6 cleansed the data and R-5's FKs make any mis-key a loud error, remove the defensive scaffolding
that was masking the bug class: (1) the ~10 latent `|| businesses.id` / `|| user.id` / id-try fallbacks
→ replace with the resolver + explicit empty state, (2) the resolver's input-echo fallback (kept "until
R14" — now safe), (3) verified dead code, and (4) drop the 13 false-positives from the backlog. Code-only,
no migrations. Each touched customer path verified against prod read-only before/after.
</objective>

<context>
The exact latent sites, dead code, and false-positives are LOCKED in DUAL-ID-VERIFIED.md (10 latent + 13
false-positive) and DUAL-ID-REMEDIATION-ROADMAP.md → R-4. Do not re-derive; read them and act. The resolver
plural reader (resolveBusinessProfileIds) currently PRESERVES an input-echo fallback on purpose (module
header lines ~27-31, 42-45) so money reads degrade to "no rows" — this plan removes it because 75-01 proved
0 unresolvable ids and 75-02's FKs now reject them at the DB. The singular resolveBusinessProfileId already
returns null-on-miss; leave it.
</context>

<task number="1" name="Enumerate the exact R-4 targets from the locked docs">
<read_first>
- .planning/codebase/DUAL-ID-VERIFIED.md (sections: the 10 latent, the 13 false-positive)
- .planning/codebase/DUAL-ID-REMEDIATION-ROADMAP.md → R-4 (the named sites + dead-code list)
</read_first>
<action>
Produce a checklist in 75-03-SUMMARY.md (created at the end) of: (a) each latent fallback site with
file:line, (b) each dead-code unit, (c) the 13 false-positives (to drop from backlog, no code). Roadmap
R-4 names: plan-data-assembler, strategic-sync-service, plan_snapshots split-history, the QR step
fallbacks, the `api/kpis` contract, goals resolve-business; dead code: `SnapshotService`,
`KPIService.updateKPIValue`/`deleteKPI`, `getIncompleteReviewsForWeek`, the dead `/api/kpis` POST,
sprint/operational `user_id` fallbacks. Confirm each against the current code (some may already be gone
post-#289 — mark those done).
</action>
<acceptance_criteria>
- Checklist enumerates every R-4 site with a current-state (present / already-removed) marker
</acceptance_criteria>
</task>

<task number="2" name="Replace latent fallbacks with the resolver + explicit empty state">
<action>
At each still-present latent site, replace the `profile?.id || businesses.id` / `|| user.id` / id-try loop
with `resolveBusinessProfileId(supabase, input)` and handle null explicitly (return the empty/typed state,
and surface the swallowed error via surfaceSupabaseError where the branch previously discarded one). No
behaviour change for the happy path (the resolver returns the same canonical id); the change is that an
unresolvable id now yields an explicit empty result instead of a silent wrong-key read.
</action>
<acceptance_criteria>
- Each touched site uses the shared resolver (grep: no new inline `business_profiles.select('id')` + `|| `)
- Each previously-swallowed error branch now calls surfaceSupabaseError
- `npx tsc --noEmit` clean
</acceptance_criteria>
</task>

<task number="3" name="Remove the resolver input-echo fallback">
<read_first>
- src/lib/business/resolveBusinessProfileIds.ts (the plural reader's fallback branch + module header)
</read_first>
<action>
Remove the input-echo fallback in `resolveBusinessProfileIds` (plural) so an unresolvable id returns an
explicit no-match result instead of echoing the input as both ids. Update the module header (drop the
"kept until R14" language; state it was removed in Phase 75 R-4 after R-6 cleanse + R-5 FKs). Audit all
plural-reader call sites for any that relied on the echo (none should, post-cleanse) and adjust to the
explicit empty path. Keep `resolveBusinessProfileId` (singular) unchanged — it already null-on-misses.
</action>
<acceptance_criteria>
- The echo-fallback branch is gone; module header no longer claims the echo is "preserved on purpose"
- Resolver unit tests updated: unresolvable id → no-match (not echo); `npx vitest run` for the resolver passes
</acceptance_criteria>
</task>

<task number="4" name="Delete dead code + drop false-positives">
<action>
Delete the verified-dead units from task 1 (and their now-unused imports/tests). Update DUAL-ID-VERIFIED.md
/ the backlog to mark the 13 false-positives explicitly dropped (doc-only). Run the full suite.
</action>
<acceptance_criteria>
- Deleted units have zero remaining references (grep)
- Full `vitest` green; `npx tsc --noEmit` clean; lint clean on touched files
- 75-03-SUMMARY.md records what was removed + the dropped false-positive list
</acceptance_criteria>
</task>
