---
plan: 75-02
title: R-5 Finish FK rollout (FK-Integrity Phase B) + single-branch RLS
wave: 2
depends_on: ["75-01"]   # FKs reject mis-keyed rows — 75-01 must be GREEN first
autonomous: false        # prod migrations — supervised apply only, never blind db push
type: migration
files_modified:
  - supabase/migrations/<ts>_fk_integrity_phase_b_text_to_uuid.sql
  - supabase/migrations/<ts>_fk_integrity_phase_b_dual_column.sql
  - supabase/migrations/<ts>_fk_integrity_phase_b_group_b.sql
  - supabase/migrations/<ts>_dual_id_rls_single_branch.sql
  - .planning/phases/75-dual-id-durable-tail/75-02-MIGRATION-RUNBOOK.md
---

<objective>
Make any future mis-key a LOUD error: add the remaining business_id FKs (FK-Integrity Phase B) and
collapse the dual-tolerant RLS to single-branch. Gated behind 75-01 GREEN. Applied SUPERVISED through
the normal pipeline with a pre-migration snapshot — NEVER a blind `db push`.
</objective>

<context>
FK-Integrity Phase A is DONE (17 uuid columns, migration 20260611010000). Phase B remains (this plan).
DDL targets (from FK-INTEGRITY-PLAN.md):
  - text→uuid then FK→business_profiles(id) CASCADE: activity_log, plan_snapshots, sprint_key_actions, kpi_history
  - dual-column → FK on business_profile_id→business_profiles(id); retire legacy text business_id:
    business_financial_goals, business_kpis
  - Group B → businesses(id) CASCADE: issues_list, open_loops, strategy_data, cashflow_assumptions (NULLs allowed)
MIGRATION-DRIFT WARNING (memory project_migration_drift): migrations 20260505…–20260508… are Phase 49
DB-04 audit-attribution FKs (auth.users), DELIBERATELY DEFERRED from prod — NOT dual-ID, NOT this DDL,
but they mean prod's applied history may diverge from supabase/migrations/. Auto-apply pipeline is broken.
</context>

<task number="1" name="Pre-flight: reconcile prod migration state (HARD GATE — no DDL until clean)">
<read_first>
- memory project_migration_drift (the deferred 505-508 + broken pipeline)
- supabase/migrations/ (full on-disk list, newest 20)
</read_first>
<action>
Using the Supabase MCP (list_migrations) or `supabase migration list`, diff prod's APPLIED migrations
against supabase/migrations/ on disk. Produce the divergence list (what's on disk but unapplied — expect
the 505-508 DB-04 batch + possibly others). Decide, WITH Matt, for each divergent file: apply-now /
mark-applied / leave-deferred — so the new Phase B migrations land on a known-consistent history. Record
the resolution in 75-02-MIGRATION-RUNBOOK.md. DO NOT author any Phase B migration until this is resolved.
</action>
<acceptance_criteria>
- Runbook contains the prod-vs-disk migration diff and a per-file disposition signed off by Matt
- No new migration file written before this gate is GREEN
</acceptance_criteria>
</task>

<task number="2" name="Pre-migration snapshot">
<action>
Snapshot all 10 FK-target tables (+ their RLS policy definitions) to
.planning/phases/75-.../snapshots/ as the rollback baseline. Mirror the 70-01 / 73-01 snapshot pattern.
</action>
<acceptance_criteria>
- Snapshot artifact exists and records row counts + current policy DDL for all 10 tables
</acceptance_criteria>
</task>

<task number="3" name="Phase B FK migrations (idempotent, transactional)">
<read_first>
- 75-01-CLEANSE-REPORT.md (MUST be GREEN — every FK-target column clean)
- supabase/migrations/20260611010000_fk_integrity_phase_a_uuid_business_id.sql (Phase A pattern to mirror)
</read_first>
<action>
Author the migrations (each idempotent: DROP CONSTRAINT IF EXISTS + ADD CONSTRAINT, in a transaction):
  1. text_to_uuid: `ALTER COLUMN business_id TYPE uuid USING business_id::uuid` on activity_log,
     plan_snapshots, sprint_key_actions, kpi_history; then FK → business_profiles(id) ON DELETE CASCADE.
  2. dual_column: FK on business_profile_id → business_profiles(id) ON DELETE CASCADE for
     business_financial_goals + business_kpis. (Legacy text business_id retirement: per FK plan, keep the
     column but stop writing it / plan a later drop — confirm with Matt whether to drop in this migration
     or defer; default DEFER the drop to avoid a breaking change mid-wave.)
  3. group_b: FK → businesses(id) ON DELETE CASCADE for issues_list, open_loops, strategy_data,
     cashflow_assumptions (nullable business_id allowed).
Each migration must run clean against a fresh local stack first.
</action>
<acceptance_criteria>
- `supabase db reset` (local) applies all three migrations with no error
- Re-running each migration is a no-op (idempotent)
- A deliberate wrong-key insert (businesses.id into a profile-keyed table) is REJECTED by the new FK (test)
</acceptance_criteria>
</task>

<task number="4" name="Single-branch RLS rewrite">
<read_first>
- the snapshotted policy DDL from task 2 (the current dual-tolerant policies on the 10 tables)
</read_first>
<action>
For each FK-target table whose RLS currently tolerates BOTH id-spaces, rewrite the policy to the single
canonical branch (business_profiles.id for Group A; businesses.id for Group B). Drop the now-dead
dual-branch. Keep the migration idempotent (CREATE OR REPLACE POLICY / DROP + CREATE).
</action>
<acceptance_criteria>
- No remaining policy references both `business_id`-spaces for the FK-target tables (grep the migration)
- Local stack: a profile-scoped read returns the owner's rows; a cross-id read returns none
</acceptance_criteria>
</task>

<task number="5" name="Full test gate + supervised prod apply">
<action>
Run the FULL vitest suite (the new FKs make wrong-key writes throw — expect any latent test that mis-keys
to surface here; fix or annotate). THEN apply to prod SUPERVISED via the normal pipeline (NOT blind
`db push`): apply each migration, confirm it records in the migration history, re-run the 75-01 audit
read-only against prod to confirm the FKs hold and 0 rows were rejected. Update the runbook with the
applied timestamps. Verify against prod read-only (Precision method).
</action>
<acceptance_criteria>
- Full `vitest` green (or each failure explained as a real mis-key the FK correctly exposes, then fixed)
- Prod migration history shows all four migrations applied + recorded
- Post-apply prod audit: every FK present, 0 orphan/mis-key rows, RLS single-branch
- 75-02-MIGRATION-RUNBOOK.md updated with apply log + rollback steps
</acceptance_criteria>
</task>
