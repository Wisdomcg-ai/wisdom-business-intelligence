---
phase: 47-input-validation-rollout
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/api/with-schema.ts
  - src/lib/api/__tests__/with-schema.test.ts
autonomous: true
requirements: [VALID-01]
user_setup:
  - service: vercel
    why: "ZOD_ENFORCE_ROUTES env var gates enforce mode (VALID-06). Not needed until enforce flips; observe mode is the default when the var is absent/empty."
    env_vars:
      - name: ZOD_ENFORCE_ROUTES
        source: "Vercel dashboard → Project → Settings → Environment Variables (prod + preview). Leave UNSET for observe mode. Mirror in .env.example if present."

must_haves:
  truths:
    - "withSchema(routeId, schema, handler) validates a cloned request body, never consuming the original stream"
    - "In observe mode (default), a parse failure logs a Sentry zod:would-reject warning and the original handler still runs with the raw body"
    - "In enforce mode (route in ZOD_ENFORCE_ROUTES or '*'), a parse failure returns HTTP 400 { error, issues } and the handler is NOT called"
    - "ctx/params second argument is forwarded verbatim for both sync-params and Promise-params dynamic routes"
    - "withQuerySchema validates searchParams with the same observe/enforce semantics"
    - "isEnforced reads process.env per-call so tests can toggle modes"
  artifacts:
    - path: "src/lib/api/with-schema.ts"
      provides: "withSchema, withQuerySchema, isEnforced exports"
      exports: ["withSchema", "withQuerySchema", "isEnforced"]
      min_lines: 60
    - path: "src/lib/api/__tests__/with-schema.test.ts"
      provides: "Unit coverage for observe/enforce/passthrough/params/query/isEnforced"
      min_lines: 80
  key_links:
    - from: "src/lib/api/with-schema.ts"
      to: "@sentry/nextjs"
      via: "Sentry.captureMessage('zod:would-reject', {level:'warning', tags:{route, invariant}})"
      pattern: "captureMessage\\(['\"]zod:would-reject"
    - from: "src/lib/api/with-schema.ts"
      to: "process.env.ZOD_ENFORCE_ROUTES"
      via: "isEnforced reads env per-call"
      pattern: "ZOD_ENFORCE_ROUTES"
---

<objective>
Build the fork-gate deliverable for Phase 47: `src/lib/api/with-schema.ts` — the generic, dependency-light HOF that every later wave attaches to routes. This plan is fully self-contained: the wrapper plus its unit tests, nothing wired to a real route yet. Implementing VALID-01 (RESEARCH §"Architecture Patterns" Patterns 1+2).

Purpose: Establish ONE validated abstraction so the 116-route observe-mode sweep (VALID-02..05) is mechanical and zero-behavior-change, and the enforce flip (VALID-06) is a pure env-var change.
Output: `with-schema.ts` exporting `withSchema`, `withQuerySchema`, `isEnforced` + a green unit-test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/47-input-validation-rollout/PHASE.md
@.planning/phases/47-input-validation-rollout/RESEARCH.md
@.planning/STATE.md

# Verified live constraints (do not re-explore — these are confirmed):
# - zod@4.1.13 installed; use schema.safeParseAsync (never .parse — must not throw in observe mode)
# - Both dynamic-param signatures coexist: sync `{ params: { id } }` at coach/clients/[id]/route.ts:7,
#   Promise `{ params: Promise<{ id }> }` at forecast/[id]/route.ts:11 → wrapper MUST be generic over ...rest
# - Sentry tag shape from Phase 46 SEC-07: import * as Sentry from '@sentry/nextjs'
# - Test Sentry-mock convention (verified at forecast/seed-from-prior/__tests__/route.test.ts:21-25):
#     vi.mock('@sentry/nextjs', () => ({ captureException: vi.fn(), captureMessage: vi.fn(), addBreadcrumb: vi.fn() }))
# - Path alias is @/lib/...
# - Known-ignorable timezone flake at src/__tests__/goals/plan-period-banner.test.tsx
</context>

<interfaces>
<!-- The exact contracts this plan creates. Later waves (47-02..06) consume these verbatim. -->

```typescript
// src/lib/api/with-schema.ts — clone-and-forward (Option B, zero handler churn)
import type { ZodType } from 'zod'

// routeId = path under src/app/api with /route.ts stripped, no leading slash, e.g. 'team/invite'.
// Matches the tags.route value already used in Sentry calls across the codebase.

export function withSchema<TArgs extends unknown[]>(
  routeId: string,
  schema: ZodType,
  handler: (request: Request, ...rest: TArgs) => Promise<Response> | Response
): (request: Request, ...rest: TArgs) => Promise<Response>

export function withQuerySchema<TArgs extends unknown[]>(
  routeId: string,
  schema: ZodType,
  handler: (request: Request, ...rest: TArgs) => Promise<Response> | Response
): (request: Request, ...rest: TArgs) => Promise<Response>

export function isEnforced(routeId: string): boolean
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — unit tests for withSchema/withQuerySchema/isEnforced</name>
  <files>src/lib/api/__tests__/with-schema.test.ts</files>
  <behavior>
    Write failing tests (route file does not exist yet) covering VALID-01:
    - observe (default, env unset): invalid body → Sentry.captureMessage('zod:would-reject', level 'warning', tags.route + tags.invariant='zod_would_reject') called once, AND the inner handler IS invoked, AND the handler can still read request.json() (body stream intact — proves clone-and-forward, guards Pitfall 1 "body already read").
    - observe: valid body → handler invoked, captureMessage NOT called.
    - enforce (process.env.ZOD_ENFORCE_ROUTES = 'test/route'): invalid body → response is 400 with JSON { error: 'Validation failed', issues: <flatten output with formErrors/fieldErrors> }, AND handler NOT called.
    - enforce: valid body → handler invoked, no 400.
    - params forwarding: a wrapped handler receiving a 2nd ctx arg gets it verbatim for BOTH sync `{ params: { id } }` and Promise `{ params: Promise<{id}> }` forms (assert the handler saw the same object reference). Covers Pitfall 4.
    - empty / non-JSON body: request with no body → no throw; schema decides (z.object({}) passes; a required-field schema would log in observe). Covers Pitfall 2.
    - withQuerySchema: searchParams parsed into an object; invalid query logs in observe / 400s in enforce; valid query → handler invoked.
    - isEnforced: '*' sentinel enforces any routeId; explicit comma list matches only listed ids; whitespace trimmed; reads env per-call (mutate process.env between two calls, assert the return flips).
    Use the verified Sentry mock convention. beforeEach: delete process.env.ZOD_ENFORCE_ROUTES. Build fakes: a real `new Request('http://x/y', { method:'POST', body: JSON.stringify({...}) })` and a fake handler `vi.fn(async (req) => NextResponse.json({ ok: true }))`. Assert on `code`/`path` of issues, NOT exact v4 message strings (they read "Invalid input: expected string, received number").
  </behavior>
  <action>
    Create `src/lib/api/__tests__/with-schema.test.ts`. Import { describe, it, expect, vi, beforeEach } from 'vitest'. Mock @sentry/nextjs exactly per RESEARCH §"Test mocking convention". Import { withSchema, withQuerySchema, isEnforced } from '../with-schema' (will fail to resolve — RED). Write the test groups described in <behavior>. Run and confirm RED (module not found / assertions fail). Commit: `test(47-01): add failing tests for withSchema observe/enforce/query/params`.
  </action>
  <verify>
    <automated>npx vitest run src/lib/api/__tests__/with-schema.test.ts</automated>
  </verify>
  <done>Test file exists with all groups; running it FAILS because with-schema.ts does not exist yet (RED). Committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement with-schema.ts (clone-and-forward, observe/enforce, query variant)</name>
  <files>src/lib/api/with-schema.ts</files>
  <behavior>
    Implement to satisfy all Task 1 tests:
    - withSchema: `raw = await request.clone().json()` inside try/catch (catch → raw=undefined); `result = await schema.safeParseAsync(raw)`; on !success → if isEnforced(routeId) return NextResponse.json({ error:'Validation failed', issues: result.error.flatten() }, { status: 400 }); else Sentry.captureMessage('zod:would-reject', { level:'warning', tags:{ route: routeId, invariant:'zod_would_reject' }, extra:{ issues: result.error.issues } } as any). ALWAYS return handler(request, ...rest) with the ORIGINAL request when not blocked. clone() MUST happen before any read.
    - withQuerySchema: parse `Object.fromEntries(new URL(request.url).searchParams)`, same safeParseAsync + observe/enforce branch, forward original request + ...rest.
    - isEnforced: parse process.env.ZOD_ENFORCE_ROUTES per-call (split ',', trim, filter Boolean, Set); return set.has('*') || set.has(routeId).
    - Generic over `...rest: TArgs extends unknown[]` so ctx/params is forwarded verbatim (never destructure or await ctx in the wrapper). Type request as Request (NextRequest is assignable).
  </behavior>
  <action>
    Create `src/lib/api/with-schema.ts` per RESEARCH §"Pattern 1 Option B" and §"Code Examples → query-schema variant". Use `safeParseAsync` (future-proof for async refinements, accepts sync schemas). Keep zero handler-side assumptions. Lint-clean (Phase 44 gate) — no unused imports; the `as any` on the Sentry options matches the existing 440-site convention. Run the unit test (GREEN). Then run the FULL suite to confirm no cross-file regression. Commit: `feat(47-01): implement withSchema/withQuerySchema clone-and-forward wrapper (VALID-01)`.
  </action>
  <verify>
    <automated>npx vitest run src/lib/api/__tests__/with-schema.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Unit test GREEN; tsc --noEmit clean; lint clean on the two new files; full `npx vitest run` green (ignore only the known plan-period-banner timezone flake). Both files committed.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/lib/api/__tests__/with-schema.test.ts` → all green (observe, enforce, query, params, isEnforced, empty-body).
- `npx tsc --noEmit` clean.
- `grep -n "withSchema\|withQuerySchema\|isEnforced" src/lib/api/with-schema.ts` shows all three exported.
- `grep -n "request.clone()" src/lib/api/with-schema.ts` confirms clone-and-forward (no double-read of original).
- `npx vitest run` full suite green (known timezone flake at src/__tests__/goals/plan-period-banner.test.tsx is ignorable).
</verification>

<success_criteria>
The wrapper exists, is unit-tested for both modes + the query variant + heterogeneous param forwarding, throws on no path (safeParseAsync), and changes ZERO route behavior when no route is wrapped. This is the fork-gate: VALID-02..06 cannot start until this is green and merged.
</success_criteria>

<output>
After completion, create `.planning/phases/47-input-validation-rollout/47-01-SUMMARY.md`.
</output>
