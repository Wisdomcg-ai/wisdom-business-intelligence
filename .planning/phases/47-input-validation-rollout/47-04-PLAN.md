---
phase: 47-input-validation-rollout
plan: 04
type: execute
wave: 3
depends_on: ["47-01"]
files_modified:
  - "src/app/api/forecast/**/route.ts"
  - "src/app/api/forecasts/**/route.ts"
  - "src/app/api/Xero/**/route.ts"
  - "src/app/api/consolidation/**/route.ts"
  - "src/app/api/cfo/report-status/route.ts"
autonomous: true
requirements: [VALID-04]

must_haves:
  truths:
    - "Every mutating forecast/forecasts/Xero/consolidation route + cfo/report-status is wrapped in OBSERVE mode"
    - "Each schema models the route's actual body (financial-write routes — schemas authored from real destructured fields, not guesses)"
    - "Dynamic-param financial routes (e.g. forecast/[id] Promise params) forward ctx verbatim and still compile"
    - "Zero behavior change on every production financial path (observe mode, no enforce)"
  artifacts:
    - path: "src/app/api/cfo/report-status/route.ts"
      provides: "Schema attached via wrapper"
      contains: "withSchema"
  key_links:
    - from: "src/app/api/forecast/[id]/route.ts"
      to: "src/lib/api/with-schema.ts"
      via: "wrapper forwarding Promise-params ctx verbatim"
      pattern: "from ['\"]@/lib/api/with-schema"
---

<objective>
Attach Zod schemas in OBSERVE mode to the highest-value financial-write tree (VALID-04): forecast/forecasts (~14 mutating), Xero/* (~9 mutating), consolidation/* (~6 mutating), plus cfo/report-status — ~29 routes per RESEARCH live counts. These are the routes whose input shape matters most (Phase 48 decimal-money work depends on knowing it), but the change is zero-behavior in observe mode.

Purpose: Establish would-reject baseline on the financial paths so Phase 48 can trust the input shape, while flipping NOTHING to enforce yet (financial routes flip LAST in VALID-06).
Output: ~29 financial-write routes wrapped in observe mode; all gates green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/47-input-validation-rollout/PHASE.md
@.planning/phases/47-input-validation-rollout/RESEARCH.md
@.planning/phases/47-input-validation-rollout/47-01-SUMMARY.md

# Wrapper contract from 47-01: withSchema / withQuerySchema, Option B (clone-and-forward).
# RESEARCH live counts for this tree: forecast/forecasts mutating = 14, Xero/* mutating = 9,
#   consolidation/* mutating = 6  → 29 write routes + cfo/report-status.
# Param signatures vary in this tree (RESEARCH Pitfall 4): forecast/[id]/route.ts:11 uses
#   Promise params `{ params: Promise<{ id }> }`. Wrapper `...rest` forwards verbatim — never await ctx.
# Note: 35 of the raw `.json()` hits in the codebase are upstream-response parses
#   (await xeroResp.json()). Wrap ONLY the inbound request.json() boundary, NOT those.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate the financial-write route set</name>
  <files>.planning/phases/47-input-validation-rollout/47-04-ROUTE-LIST.md</files>
  <action>
    Build the authoritative list of routes to wrap in this plan. Run:
      grep -rln "request.json()" src/app/api/forecast src/app/api/forecasts src/app/api/Xero src/app/api/consolidation src/app/api/cfo/report-status
    and separately list GET routes in those trees that read searchParams. EXCLUDE any `.json()` that is an upstream-response parse (e.g. `await xeroResp.json()`, `await authResponse.json()`) — only inbound `request.json()` counts. Write the resulting route list (route file path + routeId + verb(s) + body-vs-query) to `47-04-ROUTE-LIST.md`. Expected ~29 mutating + the query GETs in these trees. This list is the checklist for Task 2.
  </action>
  <verify>
    <automated>test -f .planning/phases/47-input-validation-rollout/47-04-ROUTE-LIST.md && grep -c "route.ts" .planning/phases/47-input-validation-rollout/47-04-ROUTE-LIST.md</automated>
  </verify>
  <done>ROUTE-LIST.md exists listing every inbound-body and query route in the forecast/forecasts/Xero/consolidation/cfo-report-status trees with routeId + verb; upstream-response .json() calls excluded.</done>
</task>

<task type="auto">
  <name>Task 2: Wrap every enumerated financial route in observe mode</name>
  <files>src/app/api/forecast/[id]/route.ts, src/app/api/consolidation/route.ts, src/app/api/cfo/report-status/route.ts</files>
  <action>
    For EACH route in 47-04-ROUTE-LIST.md: READ it, model each body-bearing verb's actual destructured fields as `const ...Schema = z.object({...})` (numeric/money fields → z.number(); ids → z.string(); model the real shape, do NOT guess — MEMORY feedback_testing). Wrap via Option B: keep the handler body verbatim, change only the export to `withSchema(routeId, Schema, handler)` (or `withQuerySchema` for query GETs). For dynamic-param routes (forecast/[id] etc.) the `...rest` ctx — including Promise params — is forwarded verbatim; do NOT await it in the wrapper or change the handler's `await params`. Import from `@/lib/api/with-schema`. routeId = path under src/app/api.
    CRITICAL path-casing note: preserve the capital-`X` `src/app/api/Xero/**` path EXACTLY in every import and routeId — production Linux is case-sensitive; a lowercase `xero` import 404s on Vercel.

    Observe mode only — add NOTHING to ZOD_ENFORCE_ROUTES. No auth/structure changes. Work through the list systematically; the <files> above are representative — the full set is 47-04-ROUTE-LIST.md.
  </action>
  <verify>
    <automated>npx tsc --noEmit && for r in $(grep -oE "src/app/api/[^ ]+route.ts" .planning/phases/47-input-validation-rollout/47-04-ROUTE-LIST.md); do grep -ql "withSchema\|withQuerySchema" "$r" || { echo "UNWRAPPED: $r"; exit 1; }; done && echo ALL_WRAPPED</automated>
  </verify>
  <done>Every route in 47-04-ROUTE-LIST.md imports + calls a wrapper; tsc clean (proves all param signatures forward); lint clean on touched files; observe mode (no enforce entries).</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite regression guard + commit</name>
  <files>src/app/api/cfo/report-status/route.ts</files>
  <action>
    Schema-substance spot-check (Warning B — a blanket z.object({}).passthrough() must NOT satisfy the gate): for the wrapped financial routes, assert each schema has NON-EMPTY typed fields (money → z.number(), ids → z.string()), e.g. `grep -A6 "z.object({" <route.ts> | grep -qE "z\.(string|number|boolean|enum|array)"`. In 47-04-SUMMARY.md list EACH wrapped route with its modeled field count.

    Then run the FULL vitest suite — this tree has the densest existing test coverage (forecast/consolidation/Xero suites). Confirm all still green in observe mode (responses unchanged). The forecast suite alone is 365 tests (STATE.md 72-02); any failure here is a real regression, not noise — fix by loosening the schema, never by altering the wrapper or handler logic. Ignore only the plan-period-banner timezone flake. Commit atomically: `feat(47-04): attach observe-mode schemas to forecast/Xero/consolidation write routes (VALID-04)`.
  </action>
  <verify>
    <automated>npx vitest run && npx tsc --noEmit</automated>
  </verify>
  <done>Substance spot-check passes (wrapped financial routes have typed money/id fields); SUMMARY lists per-route field counts; full suite green (timezone flake excepted); tsc clean; one atomic commit covering the financial-write set + the ROUTE-LIST artifact.</done>
</task>

</tasks>

<verification>
- Every route in `47-04-ROUTE-LIST.md` contains a `withSchema`/`withQuerySchema` call (Task 2 loop verify).
- `npx vitest run` full suite green (forecast 365 + consolidation + Xero suites unchanged); `npx tsc --noEmit` clean.
- No financial route added to ZOD_ENFORCE_ROUTES (they flip LAST, in VALID-06).
- No upstream-response `.json()` call was wrapped (only inbound request bodies).
</verification>

<success_criteria>
The ~29 financial-write routes + cfo/report-status from VALID-04 carry observe-mode schemas with zero behavior change, the input shape Phase 48 depends on is now logged, and dynamic Promise-param routes are proven to forward ctx through the wrapper.
</success_criteria>

<output>
After completion, create `.planning/phases/47-input-validation-rollout/47-04-SUMMARY.md`.
</output>
