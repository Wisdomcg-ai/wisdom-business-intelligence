---
phase: 47-input-validation-rollout
plan: 06
type: execute
wave: 5
depends_on: ["47-05a", "47-05b", "47-05c"]
files_modified:
  - .planning/phases/47-input-validation-rollout/47-06-ENFORCE-RUNBOOK.md
autonomous: false
requirements: [VALID-06]
user_setup:
  - service: vercel
    why: "Enforce mode is flipped entirely by editing the ZOD_ENFORCE_ROUTES env var in Vercel — no code deploy. This is the human-only step; rollback = remove the route id and redeploy env."
    env_vars:
      - name: ZOD_ENFORCE_ROUTES
        source: "Vercel → Settings → Environment Variables (production). Comma-separated route ids; '*' = enforce all. Add route ids in risk-tier order per the runbook."
    dashboard_config:
      - task: "Set a Sentry saved-search per route: message:\"zod:would-reject\" AND tags.route:\"<routeId>\" to prove 7 consecutive days of zero events before adding that route to the allowlist"
        location: "Sentry → Discover / saved searches"

must_haves:
  truths:
    - "A runbook exists giving exact Vercel CLI commands + the Sentry 7-days-zero evidence gate per route, in risk-tier order (read-only → admin write → financial)"
    - "An integration test proves a route in ZOD_ENFORCE_ROUTES returns 400 on an unknown-shape body (not 500, not silent accept)"
    - "Rollback is documented: remove the route id from ZOD_ENFORCE_ROUTES + redeploy env, no code change"
    - "By phase end ZOD_ENFORCE_ROUTES contains all routes (explicit list reaching the live count, or the '*' sentinel)"
  artifacts:
    - path: ".planning/phases/47-input-validation-rollout/47-06-ENFORCE-RUNBOOK.md"
      provides: "Per-route enforce-flip procedure, evidence gate, deploy-window rule, rollback, kill switch"
      min_lines: 40
  key_links:
    - from: "ZOD_ENFORCE_ROUTES (Vercel env)"
      to: "src/lib/api/with-schema.ts isEnforced"
      via: "env-gated 400 on parse failure"
      pattern: "ZOD_ENFORCE_ROUTES"
---

<objective>
Flip routes from observe to ENFORCE, per-route, gated by Sentry evidence and the AU/NZ deploy-window rule (VALID-06). Enforce is a pure env-var change (`ZOD_ENFORCE_ROUTES`) — no code deploy — so the deliverable here is (1) a CI-pinned integration test proving the env flip actually 400s, and (2) the operator runbook + the human checkpoints that drive the staged rollout. This plan is NON-autonomous: the actual env edits are Matt's, in the AU/NZ window, after the 7-days-zero evidence per route.

Purpose: Turn validation from "logged" into "rejected" without ever breaking a live tenant — read-only routes first, financial routes last, each reversible by deleting an env entry.
Output: enforce-mode integration test in CI + runbook; staged human-driven env flips; ZOD_ENFORCE_ROUTES eventually covering all routes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/47-input-validation-rollout/PHASE.md
@.planning/phases/47-input-validation-rollout/RESEARCH.md
@.planning/phases/47-input-validation-rollout/47-01-SUMMARY.md
@.planning/phases/47-input-validation-rollout/47-05a-SUMMARY.md
@.planning/phases/47-input-validation-rollout/47-05b-SUMMARY.md
@.planning/phases/47-input-validation-rollout/47-05c-SUMMARY.md

# isEnforced reads ZOD_ENFORCE_ROUTES per-call (47-01). Format: comma-separated route ids; '*' = all.
# Tier order (PHASE.md + STATE.md v1.1): read-only (VALID-02) flip first, admin write (VALID-03) second,
#   forecast/consolidation/Xero (VALID-04) last. Risky deploys outside Australia/NZ business hours.
# Evidence gate (success criteria #3/#4): per route, message:"zod:would-reject" AND tags.route:"<id>"
#   shows zero events for 7 consecutive days BEFORE adding it to the allowlist.
# Model: this mirrors Phase 65-04's SECTION_PERMISSION_ENFORCE cutover runbook (env-gated, human-only flip,
#   kill switch by removing the value). Reuse that shape.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integration test pinning enforce-mode 400 behavior</name>
  <files>src/lib/api/__tests__/with-schema.enforce.test.ts</files>
  <action>
    Add an integration-style test that imports the wrapper and a representative wrapped route handler shape, sets `process.env.ZOD_ENFORCE_ROUTES = '<routeId>'`, sends an unknown-shape body, and asserts: status 400, JSON body `{ error: 'Validation failed', issues: <flatten> }`, and the inner handler was NOT invoked. Add a paired case with the route id ABSENT (observe) asserting the handler IS invoked and no 400 — pinning that the env var, and only the env var, controls the flip. Use the verified Sentry mock convention; beforeEach delete the env var. This locks success criterion #5 (unknown body → 400, not 500/silent) in CI permanently so a future refactor can't regress it.
  </action>
  <verify>
    <automated>npx vitest run src/lib/api/__tests__/with-schema.enforce.test.ts</automated>
  </verify>
  <done>Test green: env-listed route → 400 + handler not called; env-absent route → handler called, no 400. tsc + lint clean. Committed: `test(47-06): pin enforce-mode 400 behavior in CI (VALID-06)`.</done>
</task>

<task type="auto">
  <name>Task 2: Write the enforce-flip runbook</name>
  <files>.planning/phases/47-input-validation-rollout/47-06-ENFORCE-RUNBOOK.md</files>
  <action>
    Author the operator runbook (model: Phase 65-04 ENFORCE-CUTOVER.md). Include:
    - Tier order with explicit route-id lists: Tier 1 = the 5 VALID-02 read-only ids; Tier 2 = the 8 VALID-03 admin ids; Tier 3 = the VALID-04 financial ids; then the VALID-05 remainder; finally the '*' sentinel (or full explicit list) to satisfy success criterion #5.
    - Per-route evidence gate: the exact Sentry saved-search `message:"zod:would-reject" AND tags.route:"<id>"` and the rule "zero events for 7 consecutive days before adding the id".
    - Exact Vercel commands to append an id to ZOD_ENFORCE_ROUTES (production) and redeploy env, e.g. `vercel env rm ZOD_ENFORCE_ROUTES production` then `vercel env add ZOD_ENFORCE_ROUTES production` with the new comma list, then `vercel --prod`. Note the var is set per-environment (prod + preview).
    - Deploy-window rule: flips only outside Australia/NZ business hours (STATE.md decision).
    - Dual-400-shape note: routes with an EXISTING legacy guard (e.g. team/invite returns `{error:'Missing required fields'}`, 400) will, once enforced, emit a DIFFERENT 400 envelope than the wrapper's `{error:'Validation failed', issues}` depending on which check fires first. The WRAPPER fires first (it wraps the handler), so an unknown-shape body yields the wrapper's `{error:'Validation failed', issues}`; a shape that passes Zod but still fails the legacy guard yields the legacy envelope. Note this dual-400-shape on guard-bearing routes so it is not a surprise during the soak.
    - Kill switch / rollback: remove the offending id from ZOD_ENFORCE_ROUTES + redeploy env — no code change, instant.
    - Post-flip verification per tier: after a flip, watch Sentry for any genuine 400-driven client error report for 24h before advancing to the next tier.
  </action>
  <verify>
    <automated>test -f .planning/phases/47-input-validation-rollout/47-06-ENFORCE-RUNBOOK.md && grep -q "ZOD_ENFORCE_ROUTES" .planning/phases/47-input-validation-rollout/47-06-ENFORCE-RUNBOOK.md && grep -q "7 consecutive days" .planning/phases/47-input-validation-rollout/47-06-ENFORCE-RUNBOOK.md</automated>
  </verify>
  <done>Runbook exists with tier order, per-route Sentry evidence gate, exact Vercel commands, AU/NZ window rule, and rollback/kill switch. Committed: `docs(47-06): enforce-flip runbook (VALID-06)`.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Staged human-driven enforce flips (per-route, evidence-gated)</name>
  <what-built>The wrapper enforces 400 when a route id is in ZOD_ENFORCE_ROUTES (pinned by Task 1's CI test), and the runbook (Task 2) gives the exact per-tier env edits, Sentry evidence gate, deploy window, and rollback.</what-built>
  <how-to-verify>
    This is the human-only enforce rollout — staged, evidence-gated, over days/weeks (NOT a single sitting). Per 47-06-ENFORCE-RUNBOOK.md:
    1. For each route, confirm the Sentry saved-search `message:"zod:would-reject" AND tags.route:"<id>"` shows zero events for 7 consecutive days.
    2. In the AU/NZ off-hours window, append that route's id(s) to ZOD_ENFORCE_ROUTES in Vercel (production) and redeploy env per the runbook commands.
    3. Advance tiers in order: VALID-02 read-only ids → VALID-03 admin ids → VALID-04 financial ids → VALID-05 remainder.
    4. After each tier, watch Sentry 24h for any genuine 400 client-impact; if any, remove that id (instant rollback) and re-investigate the schema.
    5. By phase end, ZOD_ENFORCE_ROUTES contains every route id (or '*') — verify an unknown-shape body to any route returns 400 with `error.flatten()` (success criterion #5).
  </how-to-verify>
  <resume-signal>Type "enforce-complete" once ZOD_ENFORCE_ROUTES covers all routes with the 7-days-zero evidence per tier — or describe which tier you stopped at and why.</resume-signal>
  <files>Vercel env ZOD_ENFORCE_ROUTES (production) — no repo files</files>
  <action>Operator-only: follow 47-06-ENFORCE-RUNBOOK.md to append route ids to ZOD_ENFORCE_ROUTES in risk-tier order, each gated by 7 consecutive days of zero zod:would-reject events for that route id and flipped only in the AU/NZ off-hours window. Rollback any tier by removing its ids and redeploying env (no code change).</action>
  <verify>After the final tier, an unknown-shape body to any route returns 400 with error.flatten() (not 500/silent); ZOD_ENFORCE_ROUTES contains all route ids or the * sentinel.</verify>
  <done>ZOD_ENFORCE_ROUTES covers every route id (or *) with per-route 7-days-zero evidence; no tenant disruption observed in the 24h post-flip Sentry watch per tier.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/lib/api/__tests__/with-schema.enforce.test.ts` green — env-gated 400 pinned in CI.
- `47-06-ENFORCE-RUNBOOK.md` present with tier order, per-route 7-days-zero Sentry gate, exact Vercel commands, AU/NZ window, rollback.
- (Operator, over time) ZOD_ENFORCE_ROUTES in Vercel production contains all route ids or '*'; an unknown-shape body returns 400, not 500 or silent accept.
</verification>

<success_criteria>
Enforce behavior is permanently pinned in CI; the operator has an exact, reversible, evidence-gated runbook; and — driven by the human checkpoint over the soak period — every route ends up in ZOD_ENFORCE_ROUTES in risk-tier order, satisfying VALID-06 and enforce-mode success criteria #3/#4/#5 with zero tenant disruption.
</success_criteria>

<output>
After completion, create `.planning/phases/47-input-validation-rollout/47-06-SUMMARY.md`.
</output>
