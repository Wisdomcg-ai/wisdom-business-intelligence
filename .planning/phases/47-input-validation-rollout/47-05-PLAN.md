---
phase: 47-input-validation-rollout
plan: 05
type: execute
wave: 4
depends_on: ["47-02", "47-03", "47-04"]
files_modified:
  - "src/app/api/**/route.ts"
autonomous: true
requirements: [VALID-05]

must_haves:
  truths:
    - "Every remaining route with request input (body OR searchParams) is wrapped in OBSERVE mode"
    - "The ~14 truly input-less GET routes get a permissive z.object({}) query wrapper so the grep target is uniform"
    - "After this plan, `grep -rln \"withSchema\\|withQuerySchema\" src/app/api/ | wc -l` equals the live route count (re-measured at plan time)"
    - "Zero behavior change across the whole sweep (observe mode, nothing in ZOD_ENFORCE_ROUTES)"
  artifacts:
    - path: ".planning/phases/47-input-validation-rollout/47-05-REMAINING-ROUTES.md"
      provides: "The authoritative remaining-route checklist (live count minus already-wrapped)"
      contains: "route.ts"
  key_links:
    - from: "src/app/api/**/route.ts (remaining)"
      to: "src/lib/api/with-schema.ts"
      via: "import { withSchema|withQuerySchema }"
      pattern: "from ['\"]@/lib/api/with-schema"
---

<objective>
Sweep every remaining route — the ~79 routes not covered by VALID-02/03/04 — attaching observe-mode schemas so the entire API surface has a wrapper (VALID-05). Bulk, churn-minimizing pass using Option B uniformly. End state: success-criterion grep #1 (every input route wrapped) is satisfied at the LIVE route count (130 today, not the stale 120 — RESEARCH Pitfall 5).

Purpose: Complete observe-mode adoption across 100% of the API boundary, the precondition for the VALID-06 enforce flips and for Phase 48.
Output: All remaining routes wrapped in observe mode; grep target met; all gates green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/47-input-validation-rollout/PHASE.md
@.planning/phases/47-input-validation-rollout/RESEARCH.md
@.planning/phases/47-input-validation-rollout/47-01-SUMMARY.md
@.planning/phases/47-input-validation-rollout/47-04-SUMMARY.md

# Wrapper contract from 47-01. Option B uniformly (RESEARCH Open Q1 recommendation: lowest risk across
#   the bulk sweep, honors PHASE.md no-signature-change). routeId = path under src/app/api, no leading slash.
# RESEARCH Pitfall 5: the live tree is 130 route.ts (not 120). Re-run `find src/app/api -name route.ts | wc -l`
#   at plan time and target "every route with request input has a wrapper", not the literal 120.
# RESEARCH Open Q2: the ~14 truly input-less GET routes get a permissive withQuerySchema(routeId, z.object({}))
#   so the grep is uniform and a future body addition is auto-validated.
# RESEARCH anti-pattern: do NOT wrap upstream-response .json() (await xeroResp.json()) — inbound boundary only.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compute the remaining-route checklist</name>
  <files>.planning/phases/47-input-validation-rollout/47-05-REMAINING-ROUTES.md</files>
  <action>
    Establish the live target and the remaining set. Run `find src/app/api -name route.ts | wc -l` for the LIVE count (~130) and `grep -rln "withSchema\|withQuerySchema" src/app/api | wc -l` for what 47-02/03/04 already wrapped. List every route.ts NOT yet wrapped via:
      comm -23 <(find src/app/api -name route.ts | sort) <(grep -rln "withSchema\|withQuerySchema" src/app/api | sort)
    For each, READ enough to classify: body verb (model real fields), query GET (model searchParams), or input-less GET (permissive z.object({})). Record path + routeId + classification in 47-05-REMAINING-ROUTES.md, with the LIVE count at the top so the success grep uses the real number, not 120.
  </action>
  <verify>
    <automated>test -f .planning/phases/47-input-validation-rollout/47-05-REMAINING-ROUTES.md && grep -q "LIVE" .planning/phases/47-input-validation-rollout/47-05-REMAINING-ROUTES.md</automated>
  </verify>
  <done>REMAINING-ROUTES.md lists every unwrapped route with routeId + classification and records the live route count.</done>
</task>

<task type="auto">
  <name>Task 2: Wrap all remaining body + query + input-less routes</name>
  <files>src/app/api</files>
  <action>
    Work through 47-05-REMAINING-ROUTES.md systematically. For body routes: inline `const ...Schema = z.object({...})` modeling the ACTUAL destructured fields (MEMORY feedback_testing — do not guess; READ each handler), wrap each verb via Option B (`export const POST = withSchema(routeId, Schema, handler)`, handler body untouched, keeps its own request.json()). For query GETs: withQuerySchema with a z.object matching searchParams. For input-less GETs: withQuerySchema(routeId, z.object({}), handler). Import from `@/lib/api/with-schema`. routeId = path under src/app/api. NEVER wrap upstream-response .json() (await xeroResp.json()). Observe mode only — ZOD_ENFORCE_ROUTES stays empty. No auth/structure changes anywhere.

    This touches many files — commit in reviewable sub-batches by subtree (one commit per top-level api subdir), each keeping tsc + lint green. Suggested message: `feat(47-05): observe-mode schemas — <subtree> routes (VALID-05)`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && for r in $(grep -oE "src/app/api/[^ ]+route.ts" .planning/phases/47-input-validation-rollout/47-05-REMAINING-ROUTES.md); do grep -ql "withSchema\|withQuerySchema" "$r" || { echo "UNWRAPPED: $r"; exit 1; }; done && echo ALL_REMAINING_WRAPPED</automated>
  </verify>
  <done>Every route in REMAINING-ROUTES.md imports + calls a wrapper; tsc clean; lint clean on touched files; observe mode only.</done>
</task>

<task type="auto">
  <name>Task 3: Verify full-surface grep target + full-suite regression + commit</name>
  <files>src/app/api</files>
  <action>
    Confirm the phase-wide observe-adoption target: `grep -rln "withSchema\|withQuerySchema" src/app/api/ | wc -l` must equal (or exceed, accounting for input-less GETs) the LIVE route count recorded in REMAINING-ROUTES.md — i.e. every route file now carries a wrapper. Run the FULL vitest suite (the sweep touches ~79 files — MEMORY feedback_executor_scoped_tests demands a full run). Fix genuine regressions by loosening schemas, never by altering the wrapper. Ignore only the plan-period-banner timezone flake. Ensure all sub-batch commits are made; final commit if any stragglers: `feat(47-05): complete observe-mode schema sweep across all API routes (VALID-05)`.
  </action>
  <verify>
    <automated>npx vitest run && [ "$(grep -rln 'withSchema\|withQuerySchema' src/app/api/ | wc -l)" -ge "$(find src/app/api -name route.ts | wc -l)" ] && echo FULL_SURFACE_WRAPPED</automated>
  </verify>
  <done>Wrapped-route count ≥ live route count (every route file wrapped); full suite green (timezone flake excepted); tsc + lint clean; all commits made.</done>
</task>

</tasks>

<verification>
- `grep -rln "withSchema\|withQuerySchema" src/app/api/ | wc -l` ≥ `find src/app/api -name route.ts | wc -l` (success criterion #1 at the LIVE count, not stale 120).
- `npx vitest run` full suite green; `npx tsc --noEmit` clean; lint clean on touched files.
- ZOD_ENFORCE_ROUTES still empty — entire surface in observe mode (zero behavior change).
- No upstream-response `.json()` wrapped.
</verification>

<success_criteria>
100% of the API surface carries an observe-mode wrapper at the live route count, satisfying VALID-05 and observe-mode success criterion #1. The codebase is now ready to begin per-route enforce flips (VALID-06) once Sentry shows 7 days of zero would-rejects per route.
</success_criteria>

<output>
After completion, create `.planning/phases/47-input-validation-rollout/47-05-SUMMARY.md`.
</output>
