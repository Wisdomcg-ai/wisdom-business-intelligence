---
phase: 47-input-validation-rollout
plan: 03
type: execute
wave: 2
depends_on: ["47-01"]
files_modified:
  - src/app/api/admin/clients/route.ts
  - src/app/api/admin/coaches/route.ts
  - src/app/api/admin/reset-password/route.ts
  - src/app/api/admin/clients/resend-invitation/route.ts
  - src/app/api/team/invite/route.ts
  - src/app/api/team/remove-member/route.ts
  - src/app/api/clients/send-invitation/route.ts
  - src/app/api/coach/clients/[id]/route.ts
autonomous: true
requirements: [VALID-03]

must_haves:
  truths:
    - "Each of the 8 admin/team write routes is wrapped with withSchema in OBSERVE mode"
    - "Each schema models the route's actual request body (real destructured fields, not guesses)"
    - "coach/clients/[id] (legacy SYNC params signature) forwards ctx verbatim and still works"
    - "team/invite's hand-rolled `if (!businessId || !firstName || !email || !role)` guard is preserved (observe mode does not replace it yet)"
  artifacts:
    - path: "src/app/api/team/invite/route.ts"
      provides: "InviteBodySchema attached via withSchema in observe mode"
      contains: "withSchema"
    - path: "src/app/api/coach/clients/[id]/route.ts"
      provides: "Schema attached; sync-params ctx forwarded verbatim"
      contains: "withSchema"
  key_links:
    - from: "src/app/api/team/invite/route.ts"
      to: "src/lib/api/with-schema.ts"
      via: "import { withSchema }"
      pattern: "from ['\"]@/lib/api/with-schema"
    - from: "src/app/api/coach/clients/[id]/route.ts"
      to: "src/lib/api/with-schema.ts"
      via: "withSchema wrapping handler with sync params ctx forwarded"
      pattern: "withSchema\\("
---

<objective>
Attach Zod schemas in OBSERVE mode to the 8 highest-risk admin/team write routes (VALID-03). These mutate users/clients/invitations — the first write surface — but stay zero-behavior in observe mode. Includes the legacy sync-params route `coach/clients/[id]`, the canonical test of param-forwarding on a real route.

Purpose: Prove the wrapper composes with body-mutating routes and the legacy sync-params signature, generating would-reject baseline for the admin tier (the second tier to flip to enforce in VALID-06).
Output: 8 routes wrapped in observe mode; all gates green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/47-input-validation-rollout/PHASE.md
@.planning/phases/47-input-validation-rollout/RESEARCH.md
@.planning/phases/47-input-validation-rollout/47-01-SUMMARY.md

# Wrapper contract from 47-01: withSchema(routeId, schema, handler), Option B (clone-and-forward,
#   handler keeps its own request.json()). routeId = path under src/app/api, no leading slash.
# CRITICAL param note (RESEARCH Pitfall 4 + verified live):
#   coach/clients/[id]/route.ts:7 uses LEGACY SYNC params: { params: { id: string } }
#   The wrapper's `...rest` forwards this verbatim — do NOT await it, do NOT change the handler's
#   destructuring. team/invite uses plain `Request` with NO ctx arg (verified :28).
# Reference schema for team/invite (RESEARCH §"A concrete schema for the representative offender"):
#   z.object({ businessId: z.string().min(1), firstName: z.string().min(1), lastName: z.string().optional(),
#     email: z.string().email(), phone: z.string().optional(), position: z.string().optional(),
#     role: z.string().min(1), sectionPermissions: z.record(z.string(), z.unknown()).optional(),
#     createAccount: z.boolean().optional().default(true) })
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wrap the 5 simpler admin/team routes (no dynamic params)</name>
  <files>src/app/api/admin/clients/route.ts, src/app/api/admin/coaches/route.ts, src/app/api/admin/reset-password/route.ts, src/app/api/admin/clients/resend-invitation/route.ts, src/app/api/clients/send-invitation/route.ts</files>
  <action>
    For EACH route: READ the file, enumerate which verbs (POST/PATCH/DELETE) it exports and the EXACT fields destructured from `await request.json()` in each. Author one schema per body-bearing verb modeling those real fields (required where the handler currently guards on them, `.optional()` otherwise). admin/clients exports POST/PATCH/DELETE per PHASE.md — give each its own schema (a DELETE that takes only an id, etc.).

    Wrap each verb's export via Option B: keep the handler body verbatim (it keeps `await request.json()`), change only the export to `export const POST = withSchema('admin/clients', AdminClientsPostSchema, postHandler)`. routeId = the path under src/app/api. Import from `@/lib/api/with-schema`; inline `const ...Schema = z.object({...})` at the top.

    Observe mode only — do NOT touch ZOD_ENFORCE_ROUTES. Make NO auth/CSRF/structure changes.
  </action>
  <verify>
    <automated>grep -rln "withSchema" src/app/api/admin/clients/route.ts src/app/api/admin/coaches/route.ts src/app/api/admin/reset-password/route.ts src/app/api/admin/clients/resend-invitation/route.ts src/app/api/clients/send-invitation/route.ts | wc -l | grep -qx 5 && npx tsc --noEmit</automated>
  </verify>
  <done>All 5 files import + call withSchema for every body-bearing verb; tsc clean; lint clean; responses unchanged in observe mode.</done>
</task>

<task type="auto">
  <name>Task 2: Wrap team/invite, team/remove-member, and coach/clients/[id] (legacy sync params)</name>
  <files>src/app/api/team/invite/route.ts, src/app/api/team/remove-member/route.ts, src/app/api/coach/clients/[id]/route.ts</files>
  <action>
    team/invite: attach the InviteBodySchema from the context block (it matches the destructure at :46-56) via `export const POST = withSchema('team/invite', InviteBodySchema, postHandler)`. LEAVE the existing `if (!businessId || !firstName || !email || !role)` guard in place — observe mode runs alongside it; the guard is removed only conceptually when enforce flips (VALID-06), and even then we leave the code (defense in depth). Plain Request, no ctx.

    team/remove-member: READ it, model its body (likely { memberId/userId, businessId }), wrap its mutating verb.

    coach/clients/[id]: READ it (verified GET at :5-7 uses SYNC `{ params: { id } }`; check whether it also has PATCH/DELETE with bodies). For the GET, attach `withQuerySchema('coach/clients/[id]', z.object({}), handler)` (no body; permissive). For any body verb, use withSchema. CRITICAL: the handler's 2nd arg `{ params: { id } }` is forwarded by the wrapper's `...rest` — do NOT await params, do NOT change the destructure. routeId = 'coach/clients/[id]'.

    Observe mode only; no structure changes.
  </action>
  <verify>
    <automated>grep -rln "withSchema\|withQuerySchema" src/app/api/team/invite/route.ts src/app/api/team/remove-member/route.ts src/app/api/coach/clients/[id]/route.ts | wc -l | grep -qx 3 && npx tsc --noEmit</automated>
  </verify>
  <done>All 3 wrapped; coach/clients/[id] sync-params forwarded verbatim (tsc proves the generic accepts it); team/invite guard preserved; tsc + lint clean.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite regression guard + commit</name>
  <files>src/app/api/team/invite/route.ts</files>
  <action>
    Run the FULL vitest suite (cross-route change — MEMORY feedback_executor_scoped_tests). Confirm any existing tests for these 8 routes still pass (observe mode = unchanged responses). Fix genuine regressions by loosening the schema, never by special-casing the wrapper. Then commit all 8 files atomically: `feat(47-03): attach observe-mode schemas to 8 admin/team write routes (VALID-03)`.
  </action>
  <verify>
    <automated>npx vitest run && npx tsc --noEmit</automated>
  </verify>
  <done>Full suite green (timezone flake excepted); tsc clean; one atomic commit covering all 8 routes.</done>
</task>

</tasks>

<verification>
- `grep -rln "withSchema\|withQuerySchema" src/app/api/admin/clients src/app/api/admin/coaches src/app/api/admin/reset-password src/app/api/admin/clients/resend-invitation src/app/api/team/invite src/app/api/team/remove-member src/app/api/clients/send-invitation "src/app/api/coach/clients/[id]" | wc -l` covers all 8 route files.
- coach/clients/[id] compiles with sync-params ctx forwarded (tsc clean).
- `npx vitest run` full suite green; lint clean on the 8 files.
- No route added to ZOD_ENFORCE_ROUTES.
</verification>

<success_criteria>
All 8 admin/team write routes from VALID-03 carry observe-mode schemas with zero behavior change, the legacy sync-params route is proven to forward ctx verbatim, and the admin tier now emits would-reject baseline data for its eventual enforce flip.
</success_criteria>

<output>
After completion, create `.planning/phases/47-input-validation-rollout/47-03-SUMMARY.md`.
</output>
