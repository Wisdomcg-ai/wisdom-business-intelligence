---
phase: 47-input-validation-rollout
plan: 02
type: execute
wave: 2
depends_on: ["47-01"]
files_modified:
  - src/app/api/coach/stats/route.ts
  - src/app/api/notifications/route.ts
  - src/app/api/health/route.ts
  - src/app/api/admin/check-auth/route.ts
  - src/app/api/cfo/summaries/route.ts
autonomous: true
requirements: [VALID-02]

must_haves:
  truths:
    - "Each of the 5 read-only routes is wrapped with withSchema or withQuerySchema in OBSERVE mode"
    - "Every route's existing behavior is byte-identical (observe mode, no route in ZOD_ENFORCE_ROUTES)"
    - "Routes that read searchParams use withQuerySchema; routes with neither body nor query get a permissive z.object({})"
  artifacts:
    - path: "src/app/api/coach/stats/route.ts"
      provides: "Query/body schema attached via wrapper"
      contains: "withSchema"
    - path: "src/app/api/cfo/summaries/route.ts"
      provides: "Schema attached via wrapper"
      contains: "withSchema"
  key_links:
    - from: "src/app/api/coach/stats/route.ts"
      to: "src/lib/api/with-schema.ts"
      via: "import { withSchema|withQuerySchema }"
      pattern: "from ['\"]@/lib/api/with-schema"
---

<objective>
Attach Zod schemas in OBSERVE mode to the 5 lowest-risk read-only routes (VALID-02). First real consumers of the VALID-01 wrapper — proves the integration pattern on the safest possible surface before financial routes.

Purpose: Zero-behavior-change validation on read-only endpoints, generating the first `zod:would-reject` baseline traffic and confirming the wrapper composes cleanly with real auth/handler code.
Output: 5 routes wrapped; all gates green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/47-input-validation-rollout/PHASE.md
@.planning/phases/47-input-validation-rollout/RESEARCH.md
@.planning/phases/47-input-validation-rollout/47-01-SUMMARY.md

# The wrapper contract (from 47-01):
#   withSchema(routeId, schema, handler) — body routes
#   withQuerySchema(routeId, schema, handler) — searchParams routes
#   routeId = path under src/app/api with /route.ts stripped, no leading slash
#     (coach/stats, notifications, health, admin/check-auth, cfo/summaries)
# Wrap pattern (Option B, zero handler churn): keep the existing handler function body untouched
#   (it keeps its own `await request.json()`); only change the EXPORT line, e.g.
#     export const GET = withQuerySchema('coach/stats', QuerySchema, _handler)
#   where _handler is the existing `async function GET(...)` renamed/kept as the inner fn.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wrap the 5 read-only routes in observe mode</name>
  <files>src/app/api/coach/stats/route.ts, src/app/api/notifications/route.ts, src/app/api/health/route.ts, src/app/api/admin/check-auth/route.ts, src/app/api/cfo/summaries/route.ts</files>
  <action>
    For EACH route, first READ the file to determine: which verbs it exports, whether each reads a body (`request.json()`) or searchParams (`new URL(request.url).searchParams` / `request.nextUrl.searchParams`), and the handler signature (plain Request vs NextRequest, and any ctx/params arg).

    Then, per RESEARCH §"Don't touch handler bodies" (Option B):
    - Keep the existing handler logic verbatim. Convert the named export to a wrapped const that forwards to the existing handler function. Two acceptable mechanics: (a) rename `export async function GET` to `async function getHandler` and add `export const GET = withQuerySchema('coach/stats', Schema, getHandler)`; OR (b) keep the function and re-export. Pick ONE and apply consistently within this plan.
    - Author the schema from the route's ACTUAL input (do NOT guess): for query routes use `withQuerySchema` with a `z.object({...})` matching the searchParams it reads (mark optional/permissive — these are read routes, false-rejects are noise). For routes that read NEITHER body nor query (e.g. health), attach a permissive `withQuerySchema(routeId, z.object({}), handler)` so the success grep is uniform (RESEARCH Open Question 2).
    - routeId = the path under src/app/api (coach/stats, notifications, health, admin/check-auth, cfo/summaries).
    - Import from `@/lib/api/with-schema`. Inline `const Schema = z.object({...})` at the top of each file (RESEARCH §"Recommended file layout" — inline, not a separate file).
    - Do NOT add any route to ZOD_ENFORCE_ROUTES. Observe mode only.
    - Make NO auth/structure changes (PHASE.md "no signature changes").
  </action>
  <verify>
    <automated>grep -rln "withSchema\|withQuerySchema" src/app/api/coach/stats src/app/api/notifications src/app/api/health src/app/api/admin/check-auth src/app/api/cfo/summaries | wc -l | grep -qx 5 && npx tsc --noEmit</automated>
  </verify>
  <done>All 5 route files import and call a wrapper; tsc clean; each route's response is unchanged when ZOD_ENFORCE_ROUTES is unset (observe). Lint clean on the 5 files.</done>
</task>

<task type="auto">
  <name>Task 2: Full-suite regression guard + commit</name>
  <files>src/app/api/coach/stats/route.ts, src/app/api/notifications/route.ts, src/app/api/health/route.ts, src/app/api/admin/check-auth/route.ts, src/app/api/cfo/summaries/route.ts</files>
  <action>
    Run the FULL vitest suite (not scoped — MEMORY feedback_executor_scoped_tests: a cross-route change must be validated against the whole suite). If any pre-existing route test for these 5 paths exists, confirm it still passes (the response shape is unchanged in observe mode). Fix any genuine regression by adjusting the schema to be more permissive — NEVER by special-casing the wrapper. Ignore only the known plan-period-banner timezone flake. Then commit all 5 files in one atomic commit: `feat(47-02): attach observe-mode schemas to 5 read-only routes (VALID-02)`.
  </action>
  <verify>
    <automated>npx vitest run && npx tsc --noEmit</automated>
  </verify>
  <done>Full suite green (timezone flake excepted); tsc clean; one atomic commit with the 5 routes.</done>
</task>

</tasks>

<verification>
- `grep -rln "withSchema\|withQuerySchema" src/app/api/coach/stats src/app/api/notifications src/app/api/health src/app/api/admin/check-auth src/app/api/cfo/summaries | wc -l` == 5.
- `npx vitest run` full suite green; `npx tsc --noEmit` clean; lint clean on touched files.
- No route appears in ZOD_ENFORCE_ROUTES (observe mode).
</verification>

<success_criteria>
The 5 read-only routes from VALID-02 each carry a wrapper call in observe mode with zero behavior change, contributing the first slice toward the "every input route has a schema" grep target.
</success_criteria>

<output>
After completion, create `.planning/phases/47-input-validation-rollout/47-02-SUMMARY.md`.
</output>
