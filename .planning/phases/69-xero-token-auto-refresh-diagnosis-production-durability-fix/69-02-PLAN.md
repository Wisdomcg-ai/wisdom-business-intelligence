---
phase: 69-xero-token-auto-refresh-diagnosis-production-durability-fix
plan: 02
type: execute
wave: 2
depends_on: ["69-01"]
files_modified:
  - .planning/phases/69-xero-token-auto-refresh-diagnosis-production-durability-fix/69-RECONNECT-RUNBOOK.md
autonomous: false
requirements: [PHASE69-RECONNECT]
must_haves:
  truths:
    - "All 5 known-expired Xero tenants are reconnected and have valid access tokens (expires_at > now() + 1h)"
    - "A reproducible reconnect runbook exists so future expiries can be cleared without re-deriving steps"
    - "Matt approves each tenant reconnect individually before it proceeds (no bulk auto-reconnect)"
    - "Each reconnect is verified by SQL query showing fresh expires_at and updated_at"
  artifacts:
    - path: ".planning/phases/69-xero-token-auto-refresh-diagnosis-production-durability-fix/69-RECONNECT-RUNBOOK.md"
      provides: "Step-by-step reconnect procedure for any expired Xero connection"
      contains: "Reconnect Procedure"
  key_links:
    - from: "OAuth callback at /api/Xero/callback"
      to: "xero_connections row upsert by (business_id, tenant_id)"
      via: "encrypt(refresh_token), expires_at, is_active=true"
      pattern: "saveXeroConnection"
---

<objective>
Execute interactive manual reconnect of the 5 known-expired Xero tenants found in Phase 70 audit, unblocking month-end reporting. Document the reconnect runbook so future expiries can be cleared without re-deriving steps.

Purpose: 69-03 (code fix) takes time to ship. Matt cannot wait — month-end reports for Envisage, JDS, and IICT (3 tenants) are blocked by dead tokens TODAY. This plan executes the operational unblock.

**INTERACTIVE PLAN — autonomous: false.** Matt must approve each tenant reconnect individually via the OAuth flow in his browser. The executor walks Matt through one tenant at a time and verifies completion via SQL before proceeding to the next.

Output:
- 5 reconnected Xero tenants with `expires_at > now() + 1h` and `is_active=true`
- `69-RECONNECT-RUNBOOK.md` capturing the procedure for reuse
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/69-xero-token-auto-refresh-diagnosis-production-durability-fix/69-CONTEXT.md
@.planning/phases/69-xero-token-auto-refresh-diagnosis-production-durability-fix/69-DIAGNOSIS.md
@src/app/api/Xero/callback/route.ts

<interfaces>
From src/app/api/Xero/callback/route.ts:
- OAuth callback route: `GET /api/Xero/callback?code=...&state=...`
- `saveXeroConnection(...)` upserts `xero_connections` by (business_id, tenant_id) — reconnecting same Xero org refreshes tokens in place
- On success: writes encrypted access_token + refresh_token, sets expires_at = now() + tokens.expires_in seconds, sets is_active=true

OAuth init flow (the URL Matt opens to start reconnect):
- `/api/Xero/auth?business_id={BUSINESS_ID}&return_to=/coach/dashboard`
- Multi-tenant: after Xero consent, redirects through tenant selection at `/xero-connect/select-org`

Known-expired tenants (from 69-CONTEXT.md, source of truth):
| Client | businesses.id prefix | Tenant name | Currency |
|---|---|---|---|
| Envisage | 8c8c63b2… | Malouf Family Trust | AUD |
| Just Digital | fea253dd… | Aeris Solutions Pty Ltd | AUD |
| IICT | fbc6dffd… | IICT Group Pty Ltd | AUD |
| IICT | fbc6dffd… | IICT (Aust) Pty Ltd | AUD |
| IICT | fbc6dffd… | IICT Group Limited | HKD |

Verification SQL (use Matt's standard tooling — supabase MCP or scripts/phase-70-data-audit.mjs):
```sql
SELECT business_id, tenant_id, tenant_name, expires_at, updated_at, is_active
FROM xero_connections
WHERE business_id IN ('<envisage>', '<jds>', '<iict>')
ORDER BY business_id, tenant_name;
```
Pass criteria per row: `is_active=true` AND `expires_at > now() + interval '1 hour'` AND `updated_at > now() - interval '15 minutes'` at the moment of check.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Walk Matt through reconnecting each of the 5 expired Xero tenants, one at a time, with per-tenant SQL verification</name>
  <files>.planning/phases/69-xero-token-auto-refresh-diagnosis-production-durability-fix/69-RECONNECT-RUNBOOK.md</files>
  <read_first>
    - .planning/phases/69-xero-token-auto-refresh-diagnosis-production-durability-fix/69-DIAGNOSIS.md (must read first to confirm reconnect is appropriate and not masking a deeper issue)
    - .planning/phases/69-xero-token-auto-refresh-diagnosis-production-durability-fix/69-CONTEXT.md (the 5-tenant table is the source of truth for which tenants to reconnect)
    - src/app/api/Xero/callback/route.ts:30-132 (saveXeroConnection function — understand the upsert semantics so the executor can explain to Matt what each reconnect does to the DB row)
  </read_first>
  <action>
This is a **non-autonomous interactive checkpoint task**. The executor MUST pause for Matt's explicit confirmation after EACH tenant. Do NOT batch reconnects.

### Pre-flight (executor runs once, before any reconnect)

1. **Confirm 69-DIAGNOSIS.md exists and has a verdict.** If the diagnosis named a root cause that would make a fresh reconnect ALSO expire within hours (e.g. cron not firing AND token rotation broken), STOP and tell Matt: "Reconnecting now may give us hours, not days. Recommend shipping 69-03 first or accepting that we'll need to reconnect again after the fix lands."

2. **Snapshot the current production state.** Run the production state SQL from 69-DIAGNOSIS.md to confirm the 5 tenants are still expired (no other process may have reconnected them since the audit). Record the current `expires_at` for each row.

3. **Resolve exact `businesses.id` UUIDs for each tenant** by running:
   ```sql
   SELECT b.id AS business_id, b.name, xc.tenant_name, xc.tenant_id, xc.expires_at
   FROM businesses b
   JOIN xero_connections xc ON xc.business_id = b.id
   WHERE b.name ILIKE ANY (ARRAY['%envisage%', '%just digital%', '%aeris%', '%iict%'])
   ORDER BY b.name, xc.tenant_name;
   ```
   Confirm the 5 rows match the 69-CONTEXT.md table. If counts differ, STOP and ask Matt.

### Per-tenant reconnect loop (repeat 5 times — once per row)

For each tenant in order (Envisage → JDS → IICT-Group-PtyLtd → IICT-Aust → IICT-HKD):

**Step A — Brief Matt** (output literally):
> "Next reconnect: **{tenant_name}** for business **{client_name}** (business_id: {short_business_id}). Current expires_at: {expires_at}. Reconnecting will overwrite the tokens in-place via upsert on (business_id, tenant_id). Reply 'go' to proceed, 'skip' to skip, or 'stop' to halt the entire plan."

WAIT for Matt's response. If 'skip' — log the skip in the runbook and continue. If 'stop' — write the partial runbook and exit.

**Step B — Generate the OAuth init URL for Matt.** Build the URL:
```
{APP_URL}/api/Xero/auth?business_id={business_id}&return_to=/coach/dashboard
```
Where `{APP_URL}` is the production URL (e.g. `https://wisdombi.ai` — confirm with Matt if uncertain) and `{business_id}` is the resolved UUID from pre-flight. Output to Matt:
> "Open this URL in your browser while logged in as the business owner: {URL}
> Xero will prompt for consent. For multi-tenant (IICT), the tenant-select page appears — choose **{tenant_name}** specifically.
> Reply 'done' when you see the callback complete (return to /coach/dashboard with no error toast)."

WAIT for Matt to reply 'done' OR report an error.

**Step C — Verify the reconnect landed.** Run the per-row verification SQL:
```sql
SELECT business_id, tenant_id, tenant_name, expires_at, updated_at, is_active,
       (expires_at > now() + interval '1 hour') AS expires_ok,
       (updated_at > now() - interval '15 minutes') AS updated_recently
FROM xero_connections
WHERE business_id = '{business_id}' AND tenant_id = '{tenant_id}';
```
Pass: `is_active=true AND expires_ok=true AND updated_recently=true`.

If PASS: tell Matt "✓ {tenant_name} reconnected. expires_at={new_expires_at}. Moving to next tenant." and continue.
If FAIL: pause. Show Matt the row. Ask whether to retry (return to Step B), skip, or stop.

### Post-loop — write `69-RECONNECT-RUNBOOK.md`

After the loop completes (whether 5/5 or partial), write the runbook capturing:

- **## Pre-flight** — checks 1, 2, 3 above (literal text + SQL)
- **## Per-Tenant Reconnect Loop** — Steps A, B, C verbatim with placeholders
- **## Verification SQL** — the literal SQL block from Step C, self-contained for reuse
- **## Reconnect Outcome** table:
  | Tenant | Pre-reconnect expires_at | Post-reconnect expires_at | Status |
  |---|---|---|---|
  | Envisage / Malouf Family Trust | ... | ... | ✓ / SKIPPED / FAILED |
  | JDS / Aeris Solutions Pty Ltd | ... | ... | ... |
  | IICT Group Pty Ltd | ... | ... | ... |
  | IICT (Aust) Pty Ltd | ... | ... | ... |
  | IICT Group Limited (HKD) | ... | ... | ... |
- **## Final State** — re-run the full snapshot SQL one more time at end-of-loop and paste the full result table here. Every successful row must show `is_active=true` AND `expires_at > now() + 1h` AND `updated_at > now() - 15min`.
- **## Operator Notes** — any Xero UI quirks Matt encountered (e.g. tenant-select page UX, multi-tab session issues, organization not appearing in consent screen) so the next operator doesn't rediscover them.

### Resume signals

Matt types one of these at each prompt:
- `go` / `done` — advance the loop
- `skip` — skip current tenant (record in outcome table)
- `stop` — halt the plan, write partial runbook with whatever was completed, exit
- `all done` — after the final post-loop verification, confirm runbook is correct

**No production code (`src/`) may be modified by this task — only the runbook .md file.**
  </action>
  <verify>
    <automated>test -f .planning/phases/69-xero-token-auto-refresh-diagnosis-production-durability-fix/69-RECONNECT-RUNBOOK.md && grep -q "## Pre-flight" .planning/phases/69-xero-token-auto-refresh-diagnosis-production-durability-fix/69-RECONNECT-RUNBOOK.md && grep -q "## Reconnect Outcome" .planning/phases/69-xero-token-auto-refresh-diagnosis-production-durability-fix/69-RECONNECT-RUNBOOK.md && grep -q "## Final State" .planning/phases/69-xero-token-auto-refresh-diagnosis-production-durability-fix/69-RECONNECT-RUNBOOK.md && grep -ci "envisage\|aeris\|iict" .planning/phases/69-xero-token-auto-refresh-diagnosis-production-durability-fix/69-RECONNECT-RUNBOOK.md | awk '{exit ($1 >= 5) ? 0 : 1}' && test $(git status --porcelain src/ 2>/dev/null | wc -l) -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/69-xero-token-auto-refresh-diagnosis-production-durability-fix/69-RECONNECT-RUNBOOK.md` exists
    - Runbook contains literal headings `## Pre-flight`, `## Per-Tenant Reconnect Loop`, `## Verification SQL`, `## Reconnect Outcome`, `## Final State`, `## Operator Notes`
    - Runbook contains a "Reconnect Outcome" table with at least 5 data rows (one per tenant from 69-CONTEXT.md: Envisage, JDS/Aeris, IICT Group Pty Ltd, IICT (Aust), IICT Group Limited HKD)
    - Final state SQL output is pasted in the "Final State" section showing actual production query results
    - For every row marked ✓ in the Reconnect Outcome table, the Final State section shows `is_active=true` AND `expires_at > now() + interval '1 hour'`
    - Any FAILED or SKIPPED tenants are documented in the Reconnect Outcome table with a reason in an adjacent column or note
    - Verification SQL block is included literally (greppable: `grep -c "SELECT business_id" .planning/phases/69-xero-token-auto-refresh-diagnosis-production-durability-fix/69-RECONNECT-RUNBOOK.md` ≥ 1)
    - No production code modified — `git status --porcelain src/` returns empty
  </acceptance_criteria>
  <done>5 production Xero tenants are either reconnected (with verified fresh expires_at > 1h from now AND is_active=true AND updated_at within 15 min) or explicitly documented as skipped/failed with reason, AND a reusable runbook exists at `.planning/phases/69-xero-token-auto-refresh-diagnosis-production-durability-fix/69-RECONNECT-RUNBOOK.md`.</done>
</task>

</tasks>

<verification>
- Runbook file exists at `.planning/phases/69-xero-token-auto-refresh-diagnosis-production-durability-fix/69-RECONNECT-RUNBOOK.md`
- Reconnect Outcome table includes all 5 tenants from 69-CONTEXT.md
- Final SQL snapshot captured in runbook
- For tenants marked successful: `expires_at > now() + 1h` at time of verification
- No code changes in src/
</verification>

<success_criteria>
Matt can run month-end reports for Envisage, JDS, and IICT today without "Xero token expired" errors. The runbook can be handed to any future operator (including Matt 3 months from now) and will produce a successful reconnect.
</success_criteria>

<output>
After completion, create `.planning/phases/69-xero-token-auto-refresh-diagnosis-production-durability-fix/69-02-SUMMARY.md` capturing:
- Tenants reconnected (and any skipped/failed)
- Total Matt-time spent
- Quirks discovered in the OAuth flow worth noting for 69-03 or a future UX phase
- Whether any tenant required a different reconnect path than the standard `/api/Xero/auth` flow
</output>
