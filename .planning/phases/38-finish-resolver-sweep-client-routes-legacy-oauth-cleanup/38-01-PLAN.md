---
phase: 38-finish-resolver-sweep-client-routes-legacy-oauth-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/client/chat/page.tsx
  - src/app/client/sessions/page.tsx
  - src/app/client/documents/page.tsx
  - src/app/client/analytics/page.tsx
  - src/app/client/actions/page.tsx
  - src/app/dashboard/integrations/page.tsx
  - src/app/dashboard/integrations/xero/page.tsx
  - src/app/xero-connect/page.tsx
  - src/app/api/actions/route.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Orphaned /client/* routes removed (zero references in app)"
    - "Orphaned /dashboard/integrations/* routes removed"
    - "Orphaned /xero-connect/page.tsx removed (keeping /xero-connect/select-org)"
    - "api/actions/route.ts client branch preserved (coach branch already correct)"
    - "Post-cleanup: grep -rE \"\\.eq\\('owner_id', user\\.id\\)\" src/app src/hooks returns 0 matches outside helpers"
  artifacts:
    - path: "src/app/client/"
      provides: "DELETED — orphaned client-only routes"
    - path: "src/app/dashboard/integrations/"
      provides: "DELETED — orphaned legacy OAuth entry points"
    - path: "src/app/xero-connect/page.tsx"
      provides: "DELETED — unreferenced init page; select-org/ retained"
---

<objective>
Complete the Phase 37 sweep by removing orphaned routes that still contain
the old `.eq('owner_id', user.id)` pattern. These routes are unreachable
from the running app (zero navigation references), so deletion is safe.
The `/api/actions` route is reviewed but retained (its pattern is inside
a client-only branch with a separate coach branch — not the bug class).

Goal: `grep -rE "\.eq\('owner_id', user\.id\)" src/app src/hooks` returns
zero matches outside helper functions after this plan.
</objective>

<context>
Phase 37 committed (main) eliminated the bug class in all actively-reached
pages. This phase cleans up orphaned code that the grep still matches but
which no user can reach via navigation.

Evidence of orphaning (all grepped from working tree):
- Zero `href=` or `router.push` to `/client/*` anywhere
- Zero references to `/dashboard/integrations` outside self-comments
- Only `/xero-connect/select-org` referenced (from api/Xero/callback) — the
  index page `/xero-connect/page.tsx` is unreferenced
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete orphaned /client/* routes</name>
  <files>src/app/client/</files>
  <action>
    Delete the entire directory tree recursively:
    - src/app/client/chat/
    - src/app/client/sessions/
    - src/app/client/documents/
    - src/app/client/analytics/
    - src/app/client/actions/

    Use `rm -rf` since these are orphaned files with no linked imports.
    Verify with grep after: no references from the rest of the app should
    remain.
  </action>
  <verify>
    <automated>test ! -d src/app/client &amp;&amp; echo "deleted" || echo "still exists"</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -d src/app/client` succeeds
    - `grep -rn "/client/chat\|/client/sessions\|/client/documents\|/client/analytics\|/client/actions" src --include="*.tsx" --include="*.ts"` returns 0 results
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>Directory gone, zero references elsewhere, typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 2: Delete orphaned /dashboard/integrations/*</name>
  <files>src/app/dashboard/integrations/</files>
  <action>
    Delete:
    - src/app/dashboard/integrations/page.tsx
    - src/app/dashboard/integrations/xero/page.tsx
    - The parent directory if empty afterwards.

    Both files have `.eq('owner_id', user.id)` and `window.location.href = '/api/xero/auth'` (lowercase — broken anyway).
  </action>
  <verify>
    <automated>test ! -d src/app/dashboard/integrations &amp;&amp; echo "deleted" || echo "still exists"</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -d src/app/dashboard/integrations` succeeds
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>Legacy dashboard/integrations directory removed.</done>
</task>

<task type="auto">
  <name>Task 3: Delete orphaned /xero-connect/page.tsx (keep select-org)</name>
  <files>src/app/xero-connect/page.tsx</files>
  <action>
    Delete only src/app/xero-connect/page.tsx.
    KEEP src/app/xero-connect/select-org/page.tsx — the Xero OAuth callback
    redirects there for multi-tenant selection (see api/Xero/callback/route.ts:389).
  </action>
  <verify>
    <automated>test ! -f src/app/xero-connect/page.tsx &amp;&amp; test -d src/app/xero-connect/select-org &amp;&amp; echo "ok" || echo "bad"</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f src/app/xero-connect/page.tsx` succeeds
    - `test -f src/app/xero-connect/select-org/page.tsx` succeeds (NOT deleted)
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>Orphan removed, live route retained.</done>
</task>

<task type="auto">
  <name>Task 4: Review api/actions/route.ts owner_id usage</name>
  <files>src/app/api/actions/route.ts</files>
  <action>
    Read src/app/api/actions/route.ts around line 40. The `.eq('owner_id', user.id)`
    lookup is inside the client branch of an if/else that ALSO has an
    `assigned_coach_id` branch for coaches.

    Verify: the structure routes coaches through the coach branch and NEVER
    falls through to the owner_id lookup with a coach user_id. If the structure
    is already correct, document and leave as-is. If not, fix.

    This is a server-side API route, and the pattern is structurally different
    from the bug class Phase 37 fixed (page-level fallbacks). Document in
    summary.
  </action>
  <verify>
    <automated>grep -nE "assigned_coach_id|owner_id.+user\.id" src/app/api/actions/route.ts | head -10</automated>
  </verify>
  <acceptance_criteria>
    - File still exists with both assigned_coach_id (coach) and owner_id (client) branches
    - Structural review documented in 38-01-SUMMARY.md
  </acceptance_criteria>
  <done>Route reviewed; changes (if needed) applied.</done>
</task>

<task type="auto">
  <name>Task 5: Full typecheck + build + phase-wide grep</name>
  <action>
    Run:
    ```
    npx tsc --noEmit
    npm run build
    grep -rnE "\.eq\('owner_id', user\.id\)" src/app src/hooks
    ```

    All must succeed. The grep must return zero results (or at most a
    single documented acceptable occurrence in /api/actions with its coach
    branch intact).
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; grep -rcE "\.eq\('owner_id', user\.id\)" src/app src/hooks 2>/dev/null | awk -F: '{s+=$2} END {print s+0}'</automated>
  </verify>
  <acceptance_criteria>
    - TypeScript clean
    - Build succeeds
    - Grep count == 0 OR == 1 with documented coach-branch exemption
  </acceptance_criteria>
  <done>Sweep complete.</done>
</task>

</tasks>

<verification>
```bash
# Expected post-state:
test ! -d src/app/client
test ! -d src/app/dashboard/integrations
test ! -f src/app/xero-connect/page.tsx
test -f src/app/xero-connect/select-org/page.tsx
grep -rE "\.eq\('owner_id', user\.id\)" src/app src/hooks | wc -l
# Target: 0 or 1 (if api/actions retains with coach branch intact)
```
</verification>

<success_criteria>
- All 4 tasks complete
- TypeScript and build pass
- owner_id fallback count ≤ 1 (with documented exemption)
</success_criteria>

<output>
Create 38-01-SUMMARY.md documenting:
- Files/directories deleted
- /api/actions review outcome
- Final grep count
- Git commits
</output>
