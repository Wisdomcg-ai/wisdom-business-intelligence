---
phase: 15-q4-annual-review-abridged-goals-wizard
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/goals/types.ts
  - src/app/quarterly-review/types/index.ts
  - src/app/quarterly-review/services/strategic-sync-service.ts
autonomous: true
requirements:
  - "Add status field to strategic_initiatives (planned/deferred)"
  - "Type foundation for annual review sync"

must_haves:
  truths:
    - "InitiativeStatus type includes 'deferred' and 'planned' values"
    - "StrategicInitiativeRef status field includes 'deferred' and 'planned'"
    - "StrategicInitiative source field accepts 'annual_review'"
    - "StrategicSyncService StepType includes 'current_remainder'"
  artifacts:
    - path: "src/app/goals/types.ts"
      provides: "Extended InitiativeStatus with deferred + planned"
      contains: "'deferred' | 'planned'"
    - path: "src/app/quarterly-review/types/index.ts"
      provides: "Extended StrategicInitiativeRef status"
      contains: "'deferred' | 'planned'"
    - path: "src/app/quarterly-review/services/strategic-sync-service.ts"
      provides: "Extended StepType with current_remainder"
      contains: "'current_remainder'"
  key_links:
    - from: "src/app/goals/types.ts"
      to: "src/app/quarterly-review/services/strategic-sync-service.ts"
      via: "InitiativeStatus import"
      pattern: "InitiativeStatus"
---

<objective>
Extend TypeScript types across goals and quarterly-review modules to support annual review sync: add 'deferred' and 'planned' to InitiativeStatus, add 'annual_review' as a valid initiative source, and extend StrategicSyncService StepType to include 'current_remainder'.

Purpose: Foundation types must exist before Plan 02 (sync service) and Plan 03 (detection banner) can reference them. This is a pure type + contract change with zero runtime risk.

Output: Updated type files in goals/types.ts, quarterly-review/types/index.ts, and strategic-sync-service.ts StepType.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-q4-annual-review-abridged-goals-wizard/15-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/app/goals/types.ts (line 62):
```typescript
export type InitiativeStatus = 'not_started' | 'in_progress' | 'completed' | 'cancelled' | 'on_hold'
```

From src/app/goals/types.ts (line 70-71):
```typescript
export interface StrategicInitiative {
  source: 'strategic_ideas' | 'roadmap'
```

From src/app/quarterly-review/types/index.ts (line 808-816):
```typescript
export interface StrategicInitiativeRef {
  status?: 'not_started' | 'in_progress' | 'completed' | 'cancelled' | 'on_hold';
}
```

From src/app/quarterly-review/services/strategic-sync-service.ts (line 13):
```typescript
type StepType = 'q1' | 'q2' | 'q3' | 'q4' | 'sprint';
```

From src/app/quarterly-review/services/strategic-sync-service.ts (line 36):
```typescript
let status: 'not_started' | 'in_progress' | 'completed' | 'cancelled' | 'on_hold' = 'in_progress';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend InitiativeStatus and StrategicInitiative source type</name>
  <files>src/app/goals/types.ts</files>
  <read_first>src/app/goals/types.ts</read_first>
  <action>
1. On line 62, change:
   `export type InitiativeStatus = 'not_started' | 'in_progress' | 'completed' | 'cancelled' | 'on_hold'`
   to:
   `export type InitiativeStatus = 'not_started' | 'in_progress' | 'completed' | 'cancelled' | 'on_hold' | 'deferred' | 'planned'`

2. On line 70 in the StrategicInitiative interface, change:
   `source: 'strategic_ideas' | 'roadmap'`
   to:
   `source: 'strategic_ideas' | 'roadmap' | 'annual_review'`

No other changes. These are additive union extensions -- all existing code consuming the narrower set remains valid.
  </action>
  <verify>
    <automated>cd /Users/mattmalouf/Desktop/business-coaching-platform && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "'deferred' | 'planned'" src/app/goals/types.ts
    - grep -q "'annual_review'" src/app/goals/types.ts
    - npx tsc --noEmit exits 0 (or only pre-existing errors)
  </acceptance_criteria>
  <done>InitiativeStatus includes 'deferred' and 'planned', StrategicInitiative source includes 'annual_review'</done>
</task>

<task type="auto">
  <name>Task 2: Extend StrategicInitiativeRef and StrategicSyncService StepType</name>
  <files>src/app/quarterly-review/types/index.ts, src/app/quarterly-review/services/strategic-sync-service.ts</files>
  <read_first>src/app/quarterly-review/types/index.ts, src/app/quarterly-review/services/strategic-sync-service.ts</read_first>
  <action>
1. In `src/app/quarterly-review/types/index.ts` around line 813, change:
   `status?: 'not_started' | 'in_progress' | 'completed' | 'cancelled' | 'on_hold';`
   to:
   `status?: 'not_started' | 'in_progress' | 'completed' | 'cancelled' | 'on_hold' | 'deferred' | 'planned';`

2. In `src/app/quarterly-review/services/strategic-sync-service.ts` line 13, change:
   `type StepType = 'q1' | 'q2' | 'q3' | 'q4' | 'sprint';`
   to:
   `type StepType = 'q1' | 'q2' | 'q3' | 'q4' | 'sprint' | 'current_remainder';`

3. In `src/app/quarterly-review/services/strategic-sync-service.ts` line 36, change:
   `let status: 'not_started' | 'in_progress' | 'completed' | 'cancelled' | 'on_hold' = 'in_progress';`
   to:
   `let status: InitiativeStatus = 'in_progress';`
   (This uses the imported `InitiativeStatus` type from goals/types.ts instead of the inline union, keeping it DRY. The import `import type { StrategicInitiative } from '@/app/goals/types';` already exists on line 10 -- add `InitiativeStatus` to that import.)

4. Update the import on line 10 from:
   `import type { StrategicInitiative } from '@/app/goals/types';`
   to:
   `import type { StrategicInitiative, InitiativeStatus } from '@/app/goals/types';`
  </action>
  <verify>
    <automated>cd /Users/mattmalouf/Desktop/business-coaching-platform && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "'deferred' | 'planned'" src/app/quarterly-review/types/index.ts
    - grep -q "'current_remainder'" src/app/quarterly-review/services/strategic-sync-service.ts
    - grep -q "InitiativeStatus" src/app/quarterly-review/services/strategic-sync-service.ts
    - npx tsc --noEmit exits 0 (or only pre-existing errors)
  </acceptance_criteria>
  <done>StrategicInitiativeRef status includes deferred/planned, StepType includes current_remainder, inline status union replaced with shared InitiativeStatus type</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` compiles without new errors
2. `grep "'deferred'" src/app/goals/types.ts` returns a match
3. `grep "'annual_review'" src/app/goals/types.ts` returns a match
4. `grep "'current_remainder'" src/app/quarterly-review/services/strategic-sync-service.ts` returns a match
</verification>

<success_criteria>
- All three type files updated with extended unions
- TypeScript compilation passes (no regressions)
- No runtime behavior changes (pure type additions)
</success_criteria>

<output>
After completion, create `.planning/phases/15-q4-annual-review-abridged-goals-wizard/15-01-SUMMARY.md`
</output>
