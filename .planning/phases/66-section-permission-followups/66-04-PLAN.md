---
phase: 66-section-permission-followups
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/66-section-permission-followups/66-OPS-ADMIN-SECTION-PERMISSION-AUDIT.md
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Every admin/cron/coach route that could surface $ data is enumerated with a per-route recommendation"
    - "Each route's recommendation states whether it should also run the section-permission check, with rationale"
    - "The document is a DECISION DOCUMENT — no route code is changed (per D-07) unless a trivial low-risk gap is found"
    - "Routes whose content was unverified in research are read and resolved during this plan"
  artifacts:
    - path: ".planning/phases/66-section-permission-followups/66-OPS-ADMIN-SECTION-PERMISSION-AUDIT.md"
      provides: "Per-route section-permission recommendation for ops/admin/cron routes"
      contains: "section-permission"
  key_links:
    - from: "66-OPS-ADMIN-SECTION-PERMISSION-AUDIT.md"
      to: "admin/cron/coach routes"
      via: "per-route recommendation table"
      pattern: "no gate needed|gate recommended"
---

<objective>
Produce a decision document enumerating the admin, cron, and coach routes that surface (or could surface) financial $ data, with a per-route recommendation on whether each should also run the Phase 65 section-permission check (D-07).

Purpose: Phase 65 wired the `finances` section-permission gate into user-facing finance routes only. Ops/admin/cron routes that use service-role clients and may surface $ data were left out of scope. This plan decides — and documents — whether any of them need the gate, so the section-permission story is complete and consistent across the API surface.

Output: A single markdown decision document. NO route code changes (D-07 — DECISION DOCUMENT ONLY), unless the audit surfaces a trivial, low-risk gap, in which case it is flagged for a follow-up plan rather than fixed here.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-section-permission-followups/66-CONTEXT.md
@.planning/phases/66-section-permission-followups/66-RESEARCH.md
@.planning/phases/65-section-permission-api-enforcement/65-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve the four unverified routes from research</name>
  <files>.planning/phases/66-section-permission-followups/66-OPS-ADMIN-SECTION-PERMISSION-AUDIT.md</files>
  <read_first>
    - .planning/phases/66-section-permission-followups/66-RESEARCH.md section D "Ops/Admin Section-Permission Audit" (the partial route enumeration + the four routes marked "Unknown — needs check")
    - src/app/api/coach/clients/[id]/route.ts
    - src/app/api/cron/weekly-digest/route.ts
    - src/app/api/admin/demo-client/route.ts
    - src/app/api/admin/activity/route.ts
  </read_first>
  <action>
Research section D enumerated the admin/cron/coach routes but left four marked "Unknown — needs check": `coach/clients/[id]/route.ts`, `cron/weekly-digest/route.ts`, `admin/demo-client/route.ts`, `admin/activity/route.ts`.

Read each of those four route files and determine, for each:
- Does the route read or return financial $ data (P&L amounts, forecast figures, Xero monetary values), or only metadata/status flags / non-financial data?
- Does it use a service-role client?
- Who calls it (coach UI, cron, super-admin tool)?

Create `.planning/phases/66-section-permission-followups/66-OPS-ADMIN-SECTION-PERMISSION-AUDIT.md` and record a findings sub-section for these four routes (this file is finished in Task 2 — Task 1 just establishes it with the resolved-unknowns section).

This task only READS route files and writes the markdown document — it changes NO route code.
  </action>
  <verify>
    <automated>test -f .planning/phases/66-section-permission-followups/66-OPS-ADMIN-SECTION-PERMISSION-AUDIT.md && grep -q "weekly-digest\|demo-client\|admin/activity\|coach/clients" .planning/phases/66-section-permission-followups/66-OPS-ADMIN-SECTION-PERMISSION-AUDIT.md && echo "four routes resolved"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/66-section-permission-followups/66-OPS-ADMIN-SECTION-PERMISSION-AUDIT.md` exists.
    - The document records, for each of `coach/clients/[id]`, `cron/weekly-digest`, `admin/demo-client`, `admin/activity`: whether it surfaces $ data, whether it uses a service-role client, and its caller.
    - `git status --porcelain src/app/api/` shows NO changes (read-only investigation).
  </acceptance_criteria>
  <done>The four research-unverified routes are read and their financial-data exposure is recorded in the audit document.</done>
</task>

<task type="auto">
  <name>Task 2: Complete the ops/admin section-permission decision document</name>
  <files>.planning/phases/66-section-permission-followups/66-OPS-ADMIN-SECTION-PERMISSION-AUDIT.md</files>
  <read_first>
    - .planning/phases/66-section-permission-followups/66-OPS-ADMIN-SECTION-PERMISSION-AUDIT.md (the in-progress document from Task 1)
    - .planning/phases/66-section-permission-followups/66-RESEARCH.md section D (the full route enumeration + the `coach/client-completion` recommendation with rationale)
    - .planning/phases/65-section-permission-api-enforcement/65-CONTEXT.md section "Service-role bypass policy" (the helper is for user-facing routes; service-role ops routes are explicitly out of 65 scope — this audit decides whether that holds)
  </read_first>
  <action>
Complete `.planning/phases/66-section-permission-followups/66-OPS-ADMIN-SECTION-PERMISSION-AUDIT.md` as a DECISION DOCUMENT (D-07).

Document structure:

1. **Header** — title, date, source (Phase 65 follow-up item 4, D-07), and a one-paragraph statement: this is a decision document; no route code is changed in this phase unless a trivial low-risk gap surfaces (in which case it is flagged for a separate follow-up plan, not fixed here).

2. **Decision criterion** — state the test for "needs a section-permission gate": a route needs the gate if it returns actual financial $ data (P&L amounts, forecast figures, Xero monetary values) to a user who could be a restricted member. A route does NOT need the gate if it surfaces only metadata/status flags (e.g. "forecast not started", "Xero not connected"), is cron-only with no user-facing response, or returns no financial data.

3. **Per-route table** — every admin/cron/coach route from research section D plus the four resolved in Task 1. Columns: `Route`, `Surfaces $ data? (yes/no/metadata-only/indirect)`, `Caller`, `Recommendation (no gate needed / gate recommended / flag-for-followup)`, `Rationale`. Pre-fill from research section D:
   - `coach/client-completion` — reads `financial_forecasts`/`xero_connections` existence/count only (not amounts). Recommendation: **no gate needed** — surfaces completion-status flags, not P&L numbers; a restricted member legitimately needs "forecast not started" / "Xero not connected" banners. Document this rationale per research.
   - `coach/clients`, `coach/stats`, `admin/clients`, `admin/check-auth`, `admin/coaches`, `admin/reset-password`, `admin/clients/resend-invitation` — no financial data → no gate needed.
   - `cron/daily-health-report`, `cron/reconciliation-watch` — read sync/connection health, no P&L amounts, cron-only → no gate needed.
   - `cron/refresh-xero-tokens` — token-only → no gate needed.
   - `cron/sync-all-xero` — triggers sync, returns no financial data to callers → no gate needed.
   - The four routes resolved in Task 1 — fill from the findings recorded there.

4. **Findings — resolved unknowns** — keep/refine the Task 1 sub-section detailing what each of the four previously-unknown routes does.

5. **Conclusion** — state whether any route needs the gate. Per research, the expectation is NO route requires a gate (they surface metadata, not $ figures). If the Task 1 investigation surfaces a route that genuinely returns $ data to a potentially-restricted member, mark it `flag-for-followup` and describe the trivial fix — do NOT implement it here (D-07). If nothing needs it, state explicitly that Phase 65's "ops routes out of scope" stance is confirmed correct and no follow-up phase is required.

Change NO route code under `src/app/api/`.
  </action>
  <verify>
    <automated>grep -q "no gate needed" .planning/phases/66-section-permission-followups/66-OPS-ADMIN-SECTION-PERMISSION-AUDIT.md && grep -q "coach/client-completion" .planning/phases/66-section-permission-followups/66-OPS-ADMIN-SECTION-PERMISSION-AUDIT.md && git status --porcelain src/app/api/ | grep -q . && echo "ERROR: route code changed" || echo "decision doc only — ok"</automated>
  </verify>
  <acceptance_criteria>
    - The document contains a per-route table covering every admin/cron/coach route from research section D plus the four Task 1 routes.
    - Every row has a recommendation: `no gate needed`, `gate recommended`, or `flag-for-followup`.
    - `grep -q "coach/client-completion" <file>` succeeds and that row carries the "no gate needed" recommendation with the metadata-not-amounts rationale.
    - The document has an explicit conclusion stating whether any follow-up plan is needed.
    - `git status --porcelain src/app/api/` shows NO changes (DECISION DOCUMENT ONLY — per D-07).
  </acceptance_criteria>
  <done>A complete ops/admin section-permission decision document exists with a per-route recommendation and an explicit conclusion, and no route code was changed.</done>
</task>

</tasks>

<verification>
- The decision document enumerates every admin/cron/coach route with a per-route recommendation and rationale.
- The four research-unverified routes are read and resolved.
- The document reaches an explicit conclusion on whether a follow-up plan is needed.
- `git status --porcelain src/app/api/` is empty — confirms DECISION DOCUMENT ONLY.
</verification>

<success_criteria>
- `66-OPS-ADMIN-SECTION-PERMISSION-AUDIT.md` delivers a per-route decision on section-permission gating for ops/admin/cron routes (D-07).
- No route code changed (unless a trivial gap is flagged — and even then, flagged, not fixed).
</success_criteria>

<output>
After completion, create `.planning/phases/66-section-permission-followups/66-04-SUMMARY.md`.
</output>
