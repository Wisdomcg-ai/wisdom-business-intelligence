---
phase: 66-section-permission-followups
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/66-section-permission-followups/66-SERVICE-ROLE-AUDIT.md
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Every one of the 32 Phase 65-02 finance routes has a documented service-role disposition: keep, convert, or carve"
    - "The 21 routes that carry a service-role client are each classified with a rationale"
    - "The 11 fully auth-bound routes are listed and acknowledged as no-action"
    - "The document is REPORT ONLY — no route code is changed in this plan"
    - "The future per-route conversion phase has an actionable inventory to plan from"
  artifacts:
    - path: ".planning/phases/66-section-permission-followups/66-SERVICE-ROLE-AUDIT.md"
      provides: "Per-route service-role disposition document for the 32 finance routes"
      contains: "convert"
  key_links:
    - from: "66-SERVICE-ROLE-AUDIT.md"
      to: "the 32 Phase 65-02 finance routes"
      via: "per-route disposition table"
      pattern: "keep|convert|carve"
---

<objective>
Produce a per-route service-role disposition document for the 32 finance routes that Phase 65-02 wired the section-permission gate into. For each of the 21 routes carrying a module-level service-role client, classify it as `keep` (legitimate cross-business / system-level need), `convert` (could be auth-bound + RLS with no loss), or `carve` (should move to an ops-only endpoint), each with a rationale (D-05).

Purpose: Phase 65-02 left service-role data-fetching clients in place in the finance routes. The actual conversion to auth-bound RLS reads is a separate, later phase — it carries RLS-regression risk on live tenants (Dragon, IICT, Fit2Shine, JDS) and must be planned per-route. This plan delivers the inventory and disposition that future phase will plan from.

Output: A single markdown document. NO route code changes (D-05 — REPORT ONLY).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-section-permission-followups/66-CONTEXT.md
@.planning/phases/66-section-permission-followups/66-RESEARCH.md
@.planning/phases/65-section-permission-api-enforcement/65-02-SUMMARY.md
@.planning/phases/65-section-permission-api-enforcement/65-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the service-role disposition document</name>
  <files>.planning/phases/66-section-permission-followups/66-SERVICE-ROLE-AUDIT.md</files>
  <read_first>
    - .planning/phases/66-section-permission-followups/66-RESEARCH.md section C "Service-Role Data-Fetching Audit" (the authoritative per-route inventory — 21 service-role + 11 auth-bound routes, plus disposition guidance)
    - .planning/phases/65-section-permission-api-enforcement/65-02-SUMMARY.md (the 32-route wiring; modified-files list)
    - .planning/phases/65-section-permission-api-enforcement/65-CONTEXT.md section "Service-role bypass policy (revised 2026-05-15)" (the policy this audit serves)
    - src/app/api/Xero/reconciliation/route.ts lines 1-25 (the canonical auth-bound + service-role coexistence pattern referenced as the `keep` exemplar)
  </read_first>
  <action>
Create `.planning/phases/66-section-permission-followups/66-SERVICE-ROLE-AUDIT.md`.

This is a REPORT ONLY deliverable (D-05) — author the document, change NO route code.

The authoritative source is 66-RESEARCH.md section C, which already contains the full grep-confirmed inventory. Use it directly; do not re-grep all 32 files unless a row's disposition is genuinely unclear.

Document structure:

1. **Header** — title, date, source (Phase 65-02 follow-up, item 3, D-05), and a one-paragraph statement that this is a disposition document only and the actual conversions are a deferred future phase.

2. **Policy recap** — quote the revised service-role bypass policy from 65-CONTEXT.md: the permission helper must receive an auth-bound client; data fetching after the gate may use service-role where the route legitimately needs cross-business / system-level reads (`Xero/reconciliation` canonical pattern).

3. **Disposition legend** — define `keep`, `convert`, `carve`:
   - `keep` — service-role is genuinely required (cross-tenant aggregation, system-level reads, RLS-bypassed writes). Document the specific reason.
   - `convert` — service-role is used only to read the authenticated user's own business data; auth-bound client + RLS would work equivalently.
   - `carve` — the route mixes user-facing and ops concerns and should be split into an ops-only endpoint.

4. **Section A — 11 fully auth-bound routes (no action)** — list them in a table (route, auth client var, note) per research section C. State explicitly these need no disposition because they carry no service-role client.

5. **Section B — 21 routes with a service-role client** — a table with columns: `Route`, `Service-role use`, `Disposition`, `Rationale`, `Conversion risk (low/med/high)`. Pre-fill from research section C disposition guidance:
   - `keep`: the three consolidated routes (`consolidated`, `consolidated-bs`, `consolidated-cashflow`) — cross-tenant aggregation (Dragon 2 entities, IICT 3 entities); an auth-bound client would hit RLS on sibling tenants' data. `Xero/reconciliation` — canonical pattern. Routes reading `xero_connections` / `xero_pl_lines` across multiple businesses (`bank-balances`, `capex`, `sync-balances`, `xero-actuals`, `Xero/balance-sheet`, `Xero/subscription-transactions`).
   - `convert` candidates: routes where service-role only reads the authenticated user's own business — research names `settings`, `commentary`, `subscription-detail`, `wages-detail`, `full-year`. Also assess `profiles`, `account-mappings`, `auto-map`, `generate`, `snapshot` (note `snapshot` writes and may need RLS-bypass — likely `keep`).
   - `carve`: research says none obvious — state that explicitly, but flag any route that genuinely mixes concerns.
   For each `convert` row, note what would need to be verified (which tables it reads, whether RLS policies exist for the authenticated user) so the future phase can scope per-route work.

6. **Section C — out-of-band routes** — note that `sync-xero/route.ts`, `templates/route.ts`, and `debug/route.ts` also carry service-role clients but were NOT in the Phase 65-02 32-route list; flag them as out-of-scope-but-known.

7. **Recommendation / next step** — state that the conversions are a separate future phase, planned per-route, prioritizing low-risk `convert` rows first and treating any `keep` decision as final unless RLS coverage changes.

Do not modify any file under `src/app/api/`.
  </action>
  <verify>
    <automated>test -f .planning/phases/66-section-permission-followups/66-SERVICE-ROLE-AUDIT.md && grep -qc "keep\|convert" .planning/phases/66-section-permission-followups/66-SERVICE-ROLE-AUDIT.md && git status --porcelain src/app/api/ | grep -q . && echo "ERROR: route code changed" || echo "report only — ok"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/66-section-permission-followups/66-SERVICE-ROLE-AUDIT.md` exists.
    - The document contains a table covering all 21 service-role routes; `grep -c "keep\|convert\|carve" <file>` is at least 21.
    - The 11 auth-bound routes are listed in a separate no-action section.
    - `grep -q "Xero/reconciliation" <file>` succeeds (canonical `keep` exemplar referenced).
    - `grep -q "sync-xero\|templates\|debug" <file>` succeeds (out-of-band routes acknowledged).
    - `git status --porcelain src/app/api/` shows NO changes (REPORT ONLY — no route code touched).
  </acceptance_criteria>
  <done>A complete service-role disposition document exists classifying every one of the 32 Phase 65-02 finance routes, with rationale and conversion risk per route, and no route code was changed.</done>
</task>

</tasks>

<verification>
- The disposition document exists and classifies all 32 routes (21 service-role + 11 auth-bound).
- Each service-role route has a `keep`/`convert`/`carve` disposition with rationale.
- `git status --porcelain src/app/api/` is empty — confirms REPORT ONLY.
</verification>

<success_criteria>
- `66-SERVICE-ROLE-AUDIT.md` delivers an actionable per-route inventory for the future conversion phase (D-05).
- No route code changed.
</success_criteria>

<output>
After completion, create `.planning/phases/66-section-permission-followups/66-03-SUMMARY.md`.
</output>
