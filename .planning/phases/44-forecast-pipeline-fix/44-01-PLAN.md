---
phase: 44-forecast-pipeline-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/capture-xero-fixture.ts
  - scripts/audit-xero-pl-lines-duplicates.ts
  - src/__tests__/xero/fixtures/envisage-fy26.json
  - src/__tests__/xero/fixtures/envisage-fy26-reconciler.json
  - src/__tests__/xero/fixtures/jds-fy26.json
  - src/__tests__/xero/fixtures/jds-fy26-reconciler.json
  - src/__tests__/xero/pl-by-month-parser.test.ts
  - src/__tests__/xero/pl-reconciler.test.ts
  - src/__tests__/xero/sync-orchestrator.test.ts
  - src/__tests__/services/forecast-read-service.test.ts
  - src/__tests__/services/save-and-materialize.test.ts
  - src/__tests__/api/cron-sync-all.test.ts
  - src/__tests__/components/Step3RevenueCOGS.test.tsx
autonomous: false
requirements:
  - PHASE-44-D-16
  - PHASE-44-D-17
gap_closure: false

must_haves:
  truths:
    - "Envisage FY26 P&L by Month and single-period FY total responses are captured as JSON fixtures"
    - "JDS FY26 P&L by Month and single-period FY total responses are captured as JSON fixtures"
    - "All Phase 44 test files exist as compilable scaffolds with at least one it.todo (or skipped placeholder) per Validation requirement"
    - "Pre-migration duplicate audit script runs across all 18 connected businesses and writes a remediation report"
  artifacts:
    - path: "scripts/capture-xero-fixture.ts"
      provides: "One-shot Xero fixture capture utility (PL-by-Month + reconciler total)"
    - path: "scripts/audit-xero-pl-lines-duplicates.ts"
      provides: "Duplicate-detection audit across xero_pl_lines for unique key (business_id, tenant_id, account_code, period_month)"
    - path: "src/__tests__/xero/fixtures/envisage-fy26.json"
      provides: "Envisage Australia FY26 P&L by Month recorded response"
    - path: "src/__tests__/xero/fixtures/envisage-fy26-reconciler.json"
      provides: "Envisage Australia FY26 single-period FY total response"
    - path: "src/__tests__/xero/fixtures/jds-fy26.json"
      provides: "Just Digital Signage FY26 P&L by Month recorded response"
    - path: "src/__tests__/xero/fixtures/jds-fy26-reconciler.json"
      provides: "Just Digital Signage FY26 single-period FY total response"
    - path: "src/__tests__/xero/pl-by-month-parser.test.ts"
      provides: "Parser test scaffolds (D-05, D-09, D-16, D-17)"
    - path: "src/__tests__/xero/pl-reconciler.test.ts"
      provides: "Reconciler test scaffolds (D-08)"
    - path: "src/__tests__/xero/sync-orchestrator.test.ts"
      provides: "Orchestrator test scaffolds (D-06, D-07, D-09, D-10)"
    - path: "src/__tests__/services/forecast-read-service.test.ts"
      provides: "ForecastReadService test scaffolds (D-13, D-18)"
    - path: "src/__tests__/services/save-and-materialize.test.ts"
      provides: "Atomic materialization RPC test scaffolds (D-12)"
    - path: "src/__tests__/api/cron-sync-all.test.ts"
      provides: "Cron route auth test scaffolds (D-11)"
    - path: "src/__tests__/components/Step3RevenueCOGS.test.tsx"
      provides: "Step 3 wizard cell-render test scaffolds (D-15)"
  key_links:
    - from: "scripts/capture-xero-fixture.ts"
      to: "Xero ProfitAndLoss API"
      via: "fetch with one-month base period + periods=11"
      pattern: "fromDate=.*&toDate=.*&periods=11&timeframe=MONTH"
    - from: "src/__tests__/xero/pl-by-month-parser.test.ts"
      to: "src/__tests__/xero/fixtures/envisage-fy26.json"
      via: "import statement"
      pattern: "import.*envisage-fy26"
---

<objective>
[Sub-phase A — Wave 0] Establish the Phase 44 test infrastructure: record Xero HTTP fixtures for the two oracle tenants (Envisage Australia + Just Digital Signage), create empty test files for every D-XX decision in the Validation matrix, and ship the pre-migration duplicate audit script. No production code is changed; this plan exclusively builds the regression fence that subsequent plans will fail loudly against.

Purpose: Per the Nyquist validation strategy in `44-VALIDATION.md`, every Wave 0 file listed there MUST exist as a compilable scaffold before any sync code is written. Without recorded fixtures, parser/reconciler/orchestrator tests cannot run; without the audit script, the unique-constraint migration in 44-02 will fail mid-deploy on existing duplicates.
Output: 13 new files (2 scripts + 4 fixture JSON + 7 test files). Tests are red-by-design (it.todo or skipped) until later plans implement the libs they cover.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-forecast-pipeline-fix/44-CONTEXT.md
@.planning/phases/44-forecast-pipeline-fix/44-RESEARCH.md
@.planning/phases/44-forecast-pipeline-fix/44-VALIDATION.md
@scripts/diag-envisage.ts
@scripts/dedupe-envisage-xero-pl-lines.ts
@src/__tests__/setup.ts
@src/__tests__/goals/plan-period-persistence.test.ts
@vitest.config.ts

<interfaces>
<!-- Existing patterns Phase 44 must mirror -->

From `src/__tests__/goals/plan-period-persistence.test.ts:17-30` — vi.mock for Supabase client at boundary:
```typescript
vi.mock('@/lib/supabase/client', () => ({
  createClient: () => ({ from: () => ({ upsert: vi.fn(...), select: () => ({...}) }) })
}))
```

From `scripts/diag-envisage.ts` — diagnostic script harness pattern (loads .env.local, instantiates supabase service-role client, queries Xero with a known business_id).

From `src/lib/utils/fiscal-year-utils.ts` — exports `generateFiscalMonthKeys`, `getCurrentFiscalYear` for FY range computation.

From `src/lib/xero/token-manager.ts` — exports `getValidAccessToken(businessId)` for authenticated Xero calls inside scripts.

Canonical Xero PL-by-Month URL shape (per CONTEXT D-05 Post-Research Clarification):
`https://api.xero.com/api.xro/2.0/Reports/ProfitAndLoss?fromDate={baseMonthStart}&toDate={baseMonthEnd}&periods=11&timeframe=MONTH&standardLayout=false&paymentsOnly=false`

Canonical Xero single-period FY total URL shape (the reconciler oracle):
`https://api.xero.com/api.xro/2.0/Reports/ProfitAndLoss?fromDate={fyStart}&toDate={fyEnd}&standardLayout=false&paymentsOnly=false` (no periods, no timeframe — returns one column = the FY total)

Headers required for every Xero call:
```
Authorization: Bearer {accessToken}
xero-tenant-id: {connection.tenant_id}
Accept: application/json
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build the Xero fixture capture utility</name>
  <files>scripts/capture-xero-fixture.ts</files>
  <read_first>
    - scripts/diag-envisage.ts (the harness pattern: dotenv load, supabase service-role client, business lookup, getValidAccessToken)
    - src/lib/xero/token-manager.ts (getValidAccessToken signature + return shape)
    - src/lib/utils/fiscal-year-utils.ts (generateFiscalMonthKeys, getCurrentFiscalYear)
    - src/app/api/Xero/sync-all/route.ts:191-220 (existing one-month base + periods=11 URL shape)
  </read_first>
  <action>
    Create `scripts/capture-xero-fixture.ts` as a Node CLI utility (run via `tsx` like the other scripts in `scripts/`).

    Behavior:
    1. Parse args: `--business-id=<uuid>` (required), `--fy=<2026>` (required, the FY to capture), `--label=<envisage-fy26>` (required, fixture filename slug).
    2. Load `.env.local` via `dotenv/config`.
    3. Create a Supabase service-role client using `process.env.NEXT_PUBLIC_SUPABASE_URL` + `process.env.SUPABASE_SERVICE_KEY` (note: this codebase uses `SUPABASE_SERVICE_KEY`, not `SUPABASE_SERVICE_ROLE_KEY`).
    4. Look up `xero_connections` rows for the `--business-id`, filter `is_active=true`. Use the FIRST connection (multi-tenant capture is a follow-up).
    5. Call `getValidAccessToken(connection.business_id)` to get `{ accessToken, tenantId }`.
    6. Compute the FY base month: for current FY, `baseMonthStart = first of current month`, `baseMonthEnd = last of current month`. For prior FY, base month = the LAST month of that FY (e.g. FY26 = Jul 2025 – Jun 2026 → base month June 2026).
    7. Fetch P&L by Month: GET `https://api.xero.com/api.xro/2.0/Reports/ProfitAndLoss?fromDate={baseMonthStart}&toDate={baseMonthEnd}&periods=11&timeframe=MONTH&standardLayout=false&paymentsOnly=false` with headers `Authorization: Bearer {accessToken}`, `xero-tenant-id: {tenantId}`, `Accept: application/json`.
    8. Fetch single-period FY total: GET `https://api.xero.com/api.xro/2.0/Reports/ProfitAndLoss?fromDate={fyStart}&toDate={fyEnd}&standardLayout=false&paymentsOnly=false` with same headers.
    9. Write both raw response JSONs to `src/__tests__/xero/fixtures/{label}.json` and `src/__tests__/xero/fixtures/{label}-reconciler.json`.
    10. Print a summary: row counts, first/last period, totals from each report. This becomes the "expected values" the parser/reconciler tests will assert against.

    DO NOT sanitize tenant IDs or dollar amounts (per RESEARCH.md note: fixtures live in private repo; auto-memory enforces single-remote push).

    Use `process.exit(1)` on any error.
  </action>
  <verify>
    <automated>npx tsx scripts/capture-xero-fixture.ts --help 2>&1 | grep -q "business-id" && echo "script signature OK"</automated>
  </verify>
  <acceptance_criteria>
    - File `scripts/capture-xero-fixture.ts` exists.
    - `grep -c "fromDate=.*toDate=.*periods=11&timeframe=MONTH" scripts/capture-xero-fixture.ts` returns at least 1.
    - `grep -c "getValidAccessToken" scripts/capture-xero-fixture.ts` returns 1 (uses the canonical token helper, not custom auth).
    - `grep -c "src/__tests__/xero/fixtures/" scripts/capture-xero-fixture.ts` returns at least 1 (writes to the canonical fixture path).
    - Script accepts `--business-id`, `--fy`, `--label` args.
    - `npx tsc --noEmit scripts/capture-xero-fixture.ts` exits 0 (compiles cleanly with project tsconfig).
  </acceptance_criteria>
  <done>Capture utility script ships and compiles; the next checkpoint task uses it to record real fixtures.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Capture Envisage + JDS fixtures by running the utility</name>
  <what-built>
    Two captured Xero P&L fixtures per tenant (4 JSON files total) — recorded responses from the Xero `Reports/ProfitAndLoss` endpoint for FY26 by-month and single-period totals. These become the deterministic regression fixtures every Phase 44 test runs against.
  </what-built>
  <how-to-verify>
    1. Look up business IDs:
       - Envisage Australia: query `select id, name from businesses where name ilike '%envisage%';` (note the UUID — this is a dual-ID system business, use the `businesses.id`).
       - Just Digital Signage: query `select id, name from businesses where name ilike '%just digital%';`
    2. Run from repo root:
       ```
       npx tsx scripts/capture-xero-fixture.ts --business-id=<envisage-uuid> --fy=2026 --label=envisage-fy26
       npx tsx scripts/capture-xero-fixture.ts --business-id=<jds-uuid> --fy=2026 --label=jds-fy26
       ```
    3. Verify all four files exist and are non-empty:
       ```
       ls -la src/__tests__/xero/fixtures/envisage-fy26.json src/__tests__/xero/fixtures/envisage-fy26-reconciler.json src/__tests__/xero/fixtures/jds-fy26.json src/__tests__/xero/fixtures/jds-fy26-reconciler.json
       ```
    4. Spot-check each fixture has Reports[0].Rows array with at least 5 rows (sanity check the response payload).
    5. Open the script's printed summary and verify the row count + first/last period look right for each tenant. Note these expected values — they go into the parser test assertions in Task 4.

    Expected: All 4 files exist, each is valid JSON ≥ 1 KB.

    If Envisage capture fails because of the multi-org connection, capture only the FIRST tenant's response and document the tenant_id used. Multi-org parser tests can use a synthetic second-tenant fixture if needed.
  </how-to-verify>
  <resume-signal>
    Type "fixtures captured: envisage rows=N1, jds rows=N2, first-period=YYYY-MM, last-period=YYYY-MM" or report any error encountered. The expected row counts and period bounds become test assertions in Task 4.
  </resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Build the pre-migration duplicate audit script</name>
  <files>scripts/audit-xero-pl-lines-duplicates.ts</files>
  <read_first>
    - scripts/dedupe-envisage-xero-pl-lines.ts (the existing one-off remediation pattern this audit generalises across all 18 businesses)
    - scripts/diag-envisage.ts (harness pattern: dotenv, supabase service-role client)
    - .planning/phases/44-forecast-pipeline-fix/44-RESEARCH.md (Runtime State Inventory section: pre-migration audit is REQUIRED because the unique-constraint migration in 44-02 will FAIL on existing duplicates)
  </read_first>
  <action>
    Create `scripts/audit-xero-pl-lines-duplicates.ts` as a one-shot read-only audit (NO writes — only reports findings).

    Behavior:
    1. Load `.env.local` via dotenv. Create supabase service-role client.
    2. Query `xero_pl_lines` grouped by `(business_id, tenant_id, account_code)` — find any group with COUNT(*) > 1. (Note: today the table is in WIDE format with `monthly_values JSONB`; per CONTEXT D-09 Post-Research Clarification it migrates to LONG format in 44-02. Audit BOTH possible duplicate definitions: wide-format dupes by `(business_id, tenant_id, account_code)` AND any rows that would conflict in the future long-format key `(business_id, tenant_id, account_code, period_month)` if the JSONB were exploded.)
    3. For each duplicate group, list:
       - business_id (resolve to business name via a JOIN to `businesses`)
       - tenant_id (and the org name from `xero_connections.tenant_name`)
       - account_code, account_name
       - duplicate row count
       - the row IDs (pkey) that are in conflict
    4. Write the report to `scripts/audit-xero-pl-lines-duplicates-report-{YYYY-MM-DD}.md` AND print to stdout.
    5. Print a summary line: `AUDIT COMPLETE: N businesses with M total duplicate groups; remediation required before unique constraint migration in 44-02.`
    6. Exit 0 always (audit never fails the build — its job is to surface state).

    DO NOT delete or modify any rows. This script is read-only.
  </action>
  <verify>
    <automated>grep -c "supabase.*from.*xero_pl_lines" scripts/audit-xero-pl-lines-duplicates.ts | grep -E "[1-9]" && grep -c "DELETE\|delete(" scripts/audit-xero-pl-lines-duplicates.ts | grep -E "^0$" && echo "audit script is read-only"</automated>
  </verify>
  <acceptance_criteria>
    - File `scripts/audit-xero-pl-lines-duplicates.ts` exists.
    - `grep -c "DELETE FROM\|\.delete(" scripts/audit-xero-pl-lines-duplicates.ts` returns 0 (no destructive operations).
    - `grep -c "from('xero_pl_lines')\|from(\"xero_pl_lines\")" scripts/audit-xero-pl-lines-duplicates.ts` returns at least 1.
    - `grep -c "SUPABASE_SERVICE_KEY" scripts/audit-xero-pl-lines-duplicates.ts` returns at least 1 (uses service-role client to bypass RLS, like other scripts).
    - `npx tsc --noEmit scripts/audit-xero-pl-lines-duplicates.ts` exits 0.
  </acceptance_criteria>
  <done>Audit script exists, compiles, is read-only. Will be RUN (separately) before plan 44-02 deploys its migration.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Create test scaffold files for all 7 Phase 44 test surfaces</name>
  <files>src/__tests__/xero/pl-by-month-parser.test.ts, src/__tests__/xero/pl-reconciler.test.ts, src/__tests__/xero/sync-orchestrator.test.ts, src/__tests__/services/forecast-read-service.test.ts, src/__tests__/services/save-and-materialize.test.ts, src/__tests__/api/cron-sync-all.test.ts, src/__tests__/components/Step3RevenueCOGS.test.tsx</files>
  <read_first>
    - .planning/phases/44-forecast-pipeline-fix/44-VALIDATION.md (per-decision test mapping — every "MISSING" entry in the Per-Task Verification Map must be covered by exactly one `it.todo` or `it.skip` in the corresponding file)
    - src/__tests__/goals/plan-period-persistence.test.ts (existing test file pattern: imports, vi.mock, describe/it)
    - src/__tests__/services/opex-classifier.test.ts (existing service test file pattern)
    - src/__tests__/setup.ts (the test setup the scaffold must conform to)
    - vitest.config.ts (alias resolution for `@/`)
  </read_first>
  <action>
    Create the 7 test files listed in `<files>`. Each file MUST:

    1. Begin with `import { describe, it, expect } from 'vitest'`.
    2. For test files that load fixtures, ALSO import:
       ```typescript
       import envisageFY26 from './fixtures/envisage-fy26.json'
       import jdsFY26 from './fixtures/jds-fy26.json'
       ```
       (Adjust paths/imports per file; reconciler test imports `*-reconciler.json` instead.)
    3. Have a top-level `describe()` block.
    4. Contain ONE `it.todo()` or `it.skip()` for EACH row in the `44-VALIDATION.md` Per-Task Verification Map mapped to that file. Use the EXACT test name from the `-t '...'` flag in the validation map.

    Per-file required tests (copy the test names verbatim — they MUST match `npx vitest run -t '<name>'`):

    **`src/__tests__/xero/pl-by-month-parser.test.ts`** (covers D-05, D-09, D-16, D-17):
    - `it.todo('returns 12 monthly columns')`
    - `it.todo('sparse tenant')`
    - `it.todo('envisage')`
    - `it.todo('jds')`

    **`src/__tests__/xero/pl-reconciler.test.ts`** (covers D-08):
    - `it.todo('fails loud on $0.01 mismatch')` — the assertion will use the `pl-reconciler.test.ts` filename, no `-t` filter
    - `it.todo('tolerance')` — for the ≤$0.01 rounding case

    **`src/__tests__/xero/sync-orchestrator.test.ts`** (covers D-06, D-07, D-09, D-10):
    - `it.todo('two FY windows')`
    - `it.todo('advisory lock')`
    - `it.todo('idempotent upsert')`
    - `it.todo('natural key uniqueness')`
    - `it.todo('coverage record')`

    **`src/__tests__/services/forecast-read-service.test.ts`** (covers D-13, D-18):
    - `it.todo('multi-tenant aggregate')`
    - `it.todo('parity')`
    - `it.todo('invariant')`
    - `it.todo('negative coverage')`

    **`src/__tests__/services/save-and-materialize.test.ts`** (covers D-12):
    - `it.todo('atomic')`
    - `it.todo('rollback')`

    **`src/__tests__/api/cron-sync-all.test.ts`** (covers D-11):
    - `it.todo('unauth')`

    **`src/__tests__/components/Step3RevenueCOGS.test.tsx`** (covers D-15):
    - `it.todo('renders em-dash for missing months')`
    - `it.todo('renders $0 for real-zero months')`
    - `it.todo('renders coverage banner when coverage less than expected')`
    - `it.todo('renders Based on N months subtext with N matching real-data months')`

    For component tests, also import `import { render, screen } from '@testing-library/react'` even though body uses `it.todo` (so that when later plans fill in the bodies, the imports are already in place).

    Add a header comment to each file:
    ```typescript
    /**
     * Phase 44 — Wave 0 scaffold (created by plan 44-01).
     * All tests are it.todo until plans 44-{02..11} implement the libs they cover.
     * See .planning/phases/44-forecast-pipeline-fix/44-VALIDATION.md for the decision-to-test map.
     */
    ```
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/xero src/__tests__/services src/__tests__/api/cron-sync-all.test.ts src/__tests__/components/Step3RevenueCOGS.test.tsx 2>&1 | grep -E "todo|Test Files.*passed" && echo "scaffolds load and todos register"</automated>
  </verify>
  <acceptance_criteria>
    - All 7 test files exist at the paths listed.
    - `grep -c "it.todo\|it.skip" src/__tests__/xero/pl-by-month-parser.test.ts` returns at least 4.
    - `grep -c "it.todo\|it.skip" src/__tests__/xero/sync-orchestrator.test.ts` returns at least 5.
    - `grep -c "it.todo\|it.skip" src/__tests__/services/forecast-read-service.test.ts` returns at least 4.
    - `grep -c "it.todo\|it.skip" src/__tests__/services/save-and-materialize.test.ts` returns at least 2.
    - `grep -c "it.todo\|it.skip" src/__tests__/api/cron-sync-all.test.ts` returns at least 1.
    - `grep -c "it.todo\|it.skip" src/__tests__/components/Step3RevenueCOGS.test.tsx` returns at least 4.
    - `grep -c "import.*envisage-fy26" src/__tests__/xero/pl-by-month-parser.test.ts` returns at least 1.
    - `grep -c "import.*jds-fy26" src/__tests__/xero/pl-by-month-parser.test.ts` returns at least 1.
    - `npx vitest run src/__tests__/xero src/__tests__/services src/__tests__/api src/__tests__/components/Step3RevenueCOGS.test.tsx` exits 0 (todos count as pending, not failing).
    - Full repo test suite still green: `npm run test` passes (currently 299/299; this plan adds zero implemented tests, so count stays 299/299).
  </acceptance_criteria>
  <done>Wave 0 scaffold complete. Every D-XX in 44-VALIDATION.md has a corresponding placeholder test that subsequent plans WILL fill in.</done>
</task>

</tasks>

<verification>
After all tasks:
- `ls src/__tests__/xero/fixtures/*.json | wc -l` returns 4 (envisage + envisage-reconciler + jds + jds-reconciler).
- `find src/__tests__/xero src/__tests__/services src/__tests__/api/cron-sync-all.test.ts src/__tests__/components/Step3RevenueCOGS.test.tsx -name "*.test.*" | wc -l` returns 7.
- `ls scripts/capture-xero-fixture.ts scripts/audit-xero-pl-lines-duplicates.ts | wc -l` returns 2.
- `npm run test` exits 0 (full suite green).
- `npx tsc --noEmit` exits with the existing baseline error count (no NEW tsc errors introduced — Phase 43 baseline was 16 pre-existing; verify count unchanged).
</verification>

<success_criteria>
- Wave 0 file list from 44-VALIDATION.md is 100% present.
- Fixtures captured from real Envisage + JDS Xero responses (not synthetic).
- Audit script is read-only and ready to run before 44-02 ships its unique-constraint migration.
- Every D-XX decision in CONTEXT.md (D-05 through D-18) has at least one corresponding `it.todo` in the test scaffolds, traceable through 44-VALIDATION.md.
- No production code changed; only test infrastructure + utility scripts added.
</success_criteria>

<output>
After completion, create `.planning/phases/44-forecast-pipeline-fix/44-01-SUMMARY.md` documenting:
- Fixture capture results (row counts, first/last periods, total revenue/expense/NP from each report — used as the parser test assertion targets in plan 44-03).
- Audit script first-run output (number of duplicate groups across the 18 businesses; if non-zero, list the businesses requiring remediation BEFORE 44-02 deploys).
- Any deviation from the test names in 44-VALIDATION.md and the reason.
</output>
