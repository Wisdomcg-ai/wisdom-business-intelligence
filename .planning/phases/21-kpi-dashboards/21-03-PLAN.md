---
phase: 21-kpi-dashboards
plan: 03
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - src/app/reviews/weekly/page.tsx
  - src/app/business-dashboard/components/XeroSyncButton.tsx
  - src/app/business-dashboard/page.tsx
autonomous: true
requirements:
  - KPI-03
must_haves:
  truths:
    - "Weekly review completion screen has a link to the KPI dashboard"
    - "Business dashboard has a Sync from Xero button that refreshes actuals"
    - "Sync button shows loading state and triggers a Xero sync, then refreshes chart data"
    - "Sync button does NOT auto-trigger on page load (rate limit protection)"
  artifacts:
    - path: "src/app/reviews/weekly/page.tsx"
      provides: "KPI Dashboard link on weekly review page"
      contains: "business-dashboard"
    - path: "src/app/business-dashboard/components/XeroSyncButton.tsx"
      provides: "Manual Xero sync trigger button"
      exports: ["XeroSyncButton"]
    - path: "src/app/business-dashboard/page.tsx"
      provides: "Updated page with XeroSyncButton in header"
      contains: "XeroSyncButton"
  key_links:
    - from: "src/app/reviews/weekly/page.tsx"
      to: "/business-dashboard"
      via: "Link component"
      pattern: "business-dashboard"
    - from: "src/app/business-dashboard/components/XeroSyncButton.tsx"
      to: "/api/Xero/sync"
      via: "fetch POST call"
      pattern: "fetch.*Xero.*sync"
    - from: "src/app/business-dashboard/page.tsx"
      to: "src/app/business-dashboard/components/XeroSyncButton.tsx"
      via: "component render in header actions"
      pattern: "XeroSyncButton"
---

<objective>
Bridge the weekly review and KPI dashboard by adding cross-navigation, and add a manual "Sync from Xero" button to the business dashboard so users can refresh financial data on demand.

Purpose: The weekly review (/reviews/weekly) and the KPI dashboard (/business-dashboard) are currently disconnected features. Adding a "View KPI Dashboard" link from the weekly review completion screen creates a natural workflow: complete weekly check-in, then review financial charts. The Xero sync button ensures chart data stays fresh without hitting Xero rate limits on every page load.

Output: Weekly review page gets a dashboard link, business dashboard gets a sync button in the header.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-kpi-dashboards/21-RESEARCH.md
@.planning/phases/21-kpi-dashboards/21-01-SUMMARY.md

@src/app/reviews/weekly/page.tsx
@src/app/business-dashboard/page.tsx
@src/app/api/Xero/sync/route.ts

<interfaces>
From src/app/api/Xero/sync/route.ts:
```typescript
export async function POST(request: NextRequest)
// Expects: { business_id: string } in request body
// Triggers Xero P&L sync for current month
// Returns: { success: true, data: { ... monthly metrics } }
```

From Plan 21-01 (useXeroActuals):
```typescript
export function useXeroActuals(businessId: string | undefined)
// Returns: { chartData, lastSyncedAt, isLoading, hasData }
// The hook should be callable with a refresh trigger
```

From src/app/business-dashboard/hooks/useBusinessDashboard.ts:
```typescript
// Returns businessId (string) which is the resolved business profile ID
```

Weekly review page structure:
```typescript
// Uses WeeklyReviewService with weekly_reviews table
// Has sections: Rock Progress, Weekly Priorities, Discipline Checklist, Financial Summary
// Currently NO link to business-dashboard
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Weekly review KPI link + XeroSyncButton + page integration</name>
  <files>
    src/app/reviews/weekly/page.tsx
    src/app/business-dashboard/components/XeroSyncButton.tsx
    src/app/business-dashboard/page.tsx
  </files>
  <read_first>
    src/app/reviews/weekly/page.tsx
    src/app/business-dashboard/page.tsx
    src/app/api/Xero/sync/route.ts
    src/app/business-dashboard/hooks/useXeroActuals.ts
  </read_first>
  <action>
    **Part A: XeroSyncButton component**

    Create src/app/business-dashboard/components/XeroSyncButton.tsx:
    1. 'use client' directive.
    2. Props: { businessId: string, onSyncComplete?: () => void }
    3. State: isSyncing (boolean), lastResult ('success' | 'error' | null), lastSyncedAt (string | null from props or stored).
    4. handleSync function:
       - Set isSyncing = true.
       - POST to /api/Xero/sync with body { business_id: businessId }.
       - On success: set lastResult = 'success', call onSyncComplete() if provided, show toast.success('Xero data synced') via sonner.
       - On error: set lastResult = 'error', show toast.error('Sync failed — try again later').
       - Finally: set isSyncing = false. After 3 seconds, reset lastResult to null.
    5. Render: A button with RefreshCw icon. Text "Sync Xero". When syncing, icon spins (animate-spin). When lastResult='success', show CheckCircle icon briefly. Style: same as the existing Lock/Unlock button pattern (flex items-center gap-2 px-3 sm:px-4 py-2 rounded-lg text-sm font-medium bg-white border border-gray-300 text-gray-700 hover:bg-gray-50). Disabled state when isSyncing.

    **Part B: Integrate XeroSyncButton into business-dashboard page.tsx**

    In page.tsx:
    1. Import XeroSyncButton from './components/XeroSyncButton'.
    2. Add onSyncComplete callback that triggers a page-level data refresh. The simplest approach: create a refreshKey state counter, pass setRefreshKey to XeroSyncButton's onSyncComplete, and add refreshKey to the useXeroActuals dependency if the hook supports it. If the hook does not support a refresh trigger, update useXeroActuals.ts to accept an optional refreshTrigger parameter (a number that when changed triggers a refetch) — add this to the useEffect deps. This is a minor hook change: add `refreshTrigger?: number` to the hook params, default 0, include in the fetch useEffect deps array.
    3. Render <XeroSyncButton businessId={businessId} onSyncComplete={() => setRefreshKey(k => k + 1)} /> inside the PageHeader actions div, BEFORE the Lock/Unlock button.

    **Part C: Weekly review KPI Dashboard link**

    In src/app/reviews/weekly/page.tsx:
    1. Read the full file to find where the review completion or summary section is rendered. Look for patterns like "submitted", "complete", "saved", or a section that appears after the review is filled in.
    2. Add a "View KPI Dashboard" link card near the bottom of the page (after the main review sections, or in a "Next Steps" area if one exists). The link should use Next.js Link component pointing to /business-dashboard.
    3. Style: A subtle call-to-action card. bg-gradient-to-r from-brand-navy/5 to-brand-orange/5 rounded-xl p-4 border border-gray-200. Content: TrendingUp icon (lucide), "View your KPI Dashboard" as text-sm font-semibold, "Track progress against your financial targets" as text-xs text-gray-500, and an ArrowRight icon. The whole card is a Link.
    4. Import Link from 'next/link', TrendingUp and ArrowRight from 'lucide-react'.
    5. This is a MINIMAL addition — do not restructure the weekly review page.
  </action>
  <verify>
    <automated>cd /Users/mattmalouf/Desktop/business-coaching-platform && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - test -f src/app/business-dashboard/components/XeroSyncButton.tsx
    - grep -q "Xero/sync" src/app/business-dashboard/components/XeroSyncButton.tsx
    - grep -q "XeroSyncButton" src/app/business-dashboard/page.tsx
    - grep -q "business-dashboard" src/app/reviews/weekly/page.tsx
    - grep -q "onSyncComplete" src/app/business-dashboard/components/XeroSyncButton.tsx
    - grep -q "isSyncing\|syncing" src/app/business-dashboard/components/XeroSyncButton.tsx
  </acceptance_criteria>
  <done>Weekly review page has a "View KPI Dashboard" link card. Business dashboard has a "Sync Xero" button in the header that triggers a manual sync and refreshes chart data. Neither feature auto-fetches from Xero on page load. Build passes.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes with zero errors
2. `grep "business-dashboard" src/app/reviews/weekly/page.tsx` confirms dashboard link added
3. `grep "XeroSyncButton" src/app/business-dashboard/page.tsx` confirms sync button integrated
4. `grep "Xero/sync" src/app/business-dashboard/components/XeroSyncButton.tsx` confirms sync API used
5. No grep for "useEffect.*Xero/sync" in page.tsx (ensures no auto-sync on load)
</verification>

<success_criteria>
- Weekly review page links to /business-dashboard
- Business dashboard has manual Xero sync button in header
- Sync button shows loading state, hits /api/Xero/sync POST, refreshes chart data on success
- No auto-sync on page load (Xero rate limit protection)
- Build passes clean
</success_criteria>

<output>
After completion, create `.planning/phases/21-kpi-dashboards/21-03-SUMMARY.md`
</output>
