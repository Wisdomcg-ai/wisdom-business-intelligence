# Redundancy & Dead Code Audit: wisdom-business-intelligence

**Date**: 2026-04-28  
**Scope**: Full repository (992 TS/TSX files, 9+ months old)  
**Verdict**: 368+ files, ~2.5 MB, safe to delete with confidence

---

## DELETE WITH CONFIDENCE

### Top-Level Cruft

🔴 **CRITICAL - Security Risk**
- `src/app/api/migrate/route.ts` (102 lines) — Development-only super_admin migration endpoint. FLAGGED IN SECURITY AUDIT as "NO authentication" data manipulation risk. Called from nowhere in production code. Delete immediately.
- `src/app/api/migrate/opex-fields/route.ts` — Stale migration sub-route. Same security risk. Delete with migrate/route.ts.

🟡 **Development & Test Endpoints**
- `src/app/api/email/test/route.ts` (160 lines) — Super_admin-only test email endpoint. Not called from any UI code (grep: 0 matches). Safe to delete if email testing is manual.

🟠 **Root-Level Prototype Mockups (44K + 16K)**
- `dwa_resources.html` (44 KB) — Standalone HTML mockup file (Deep Work with AI Bali Retreat 2026). No code references it. Confirmed standalone by reading first 30 lines. Delete.
- `mockup-step4-actuals.html` (16 KB) — Tailwind-based HTML mockup for "Step 4 - Annual Plan with Actuals". No code references. Delete.

🟠 **Root-Level Debug Scripts**
- `check_spm_kpis.mjs` (2.4 KB) — Development script querying Supabase for "Sydney Pressed Metal" business KPIs. Not in any npm scripts, not called from anywhere. One-shot debugging artifact. Delete.

🔴 **AWS Lambda/SAM Artifacts (Legacy Infrastructure)**
- `packaged.yaml` (984 bytes) — AWS SAM template with S3 CodeUri reference. App is on Vercel, not Lambda. Historical artifact from early architecture.
- `template.yml` (duplicate; 86 lines) — Two identical AWS SAM templates (lines 1-50 duplicated at lines 53+, malformed YAML). Delete both. Also confirms this is dead code that was never even properly maintained.

🟠 **ESLint Flat Config (Unused)**
- `eslint.config.mjs` (393 bytes) — Flat config format. Next.js 14 reads `.eslintrc.json` (not flat config). This file is never used. Delete, keep `.eslintrc.json`.

🟡 **Auto-Generated Files (Should Be Gitignored)**
- `next-env.d.ts` — Auto-generated by Next.js build. Committed to git but should be in `.gitignore`. Flag for `.gitignore` cleanup.
- `tsconfig.tsbuildinfo` — Build cache artifact. Should be gitignored.

### Archive Directories (362 files / 1.4 MB)

🔴 **`_archive/` Directory (1+ MB, 162 files)**
- `_archive/public/` — Unused copy of public assets
- `_archive/src/` — Copy of old src code
- `_archive/supabase-migrations/` (60+ migrations) — Old migrations, now in `supabase/migrations/`
- `_archive/supabase-scripts/` — Duplicate Supabase functions
- `_archive/stray-routes/` (3 routes) — Abandoned route versions (`callback-route-4.ts`, `sync-route-3.ts`)
- `_archive/schema_dump.sql` (12.3 KB) — One-time schema export, not referenced
- `_archive/plan.md` — Old implementation plan, superseded by `.planning/`
- `_archive/Urban Roads Finance Report Jan 2026.pdf` (656 KB) — Client document, belongs in separate secure storage or client archive
- `_archive/image (8).png` (116 KB) — Stray image asset
- **Action**: Delete entire `_archive/` directory. Nothing is imported from here (grep: 0 matches in `src/`).

🔴 **`.archive/` Directory (96 KB, 3 files)**
- `AdvancedDiagramVisualizer.tsx` (9.2 KB) — Component, not used (grep: 0 matches).
- `ProfessionalDiagramVisualizer.tsx` (20 KB) — Component, not used (grep: 0 matches).
- `page.tsx` (58 KB) — Archived page, not used.
- **Action**: Delete entire `.archive/` directory.

🔴 **`supabase/archive/` Directory (1.3 MB, 7 files)**
- `seed_demo_account.sql`, `seed_demo_complete.sql`, `seed_distinct_directions.sql` (117K + 75K + 40K) — Old seed data. Current seeding is in `supabase/seed.sql`.
- `seed_forecast_assumptions.sql`, `seed_forecast_standalone.sql` — Legacy forecast seeds.
- `fix_quarterly_review_q1_to_q3.sql` — One-off fix, already applied.
- `pre-branching-migrations/` directory (12K) — Old migrations, current ones in `supabase/migrations/`.
- **Action**: Delete entire `supabase/archive/` directory (nothing references it).

### Stale Root-Level Planning Docs (38 KB / 4 files)

🟡 **Old UI/UX Plans (Superseded by `.planning/`)**
- `BRANDING_UPDATE_PLAN.md` (11 KB) — Last commit: 7b02a39 "Standardize button hover states" (Apr 16, 9 months old). No recent updates. Superseded by design system work in `.planning/`.
- `DESIGN_SYSTEM_PLAN.md` (9.5 KB) — Same age, superseded.
- `UI_UX_AUDIT_REPORT.md` (9.5 KB) — Same age, superseded.
- `UI_UX_IMPLEMENTATION_PLAN.md` (8.5 KB) — Same age, superseded.
- **Action**: Move to `docs/archive/` or delete. Current UI/UX work tracked in `.planning/`.

### Database Migrations Duplication

🟡 **`database/migrations/` (96 KB, 10 files)**
- `database/` directory contains local migrations: `create_kpi_tables.sql`, `add-strategic-planning-lifecycle.sql`, etc.
- Current migrations are in `supabase/migrations/` (integrated Supabase format).
- Git history shows these were one-time setups applied 9+ months ago.
- **Action**: Delete `database/migrations/` directory. Keep only `supabase/migrations/`.

### Stale Documentation (33 Markdown Files)

🟡 **Process Artifacts (Minimal Content)**
- `docs/build-logs/day-05-strategic-wheel.md` (912 bytes) — Build session note, not current reference.
- `docs/build-sessions/day-5-6-profile-roadmap.md` (0 bytes) — Empty.
- `docs/design-conversations/complete-chat-archive.md` (14 KB) — Chat transcript, reference only.
- **Action**: Move to `docs/archive/` (not active reference material).

🟡 **Executed Plans (No Longer Active)**
- `docs/CLIENT_FEEDBACK_DEC8_2024.md` — Dec 2024 feedback, likely incorporated into current work.
- `docs/COACH_PORTAL_BUILD_PLAN.md` (546 lines) — Build plan (9 months old); coach portal is built.
- `docs/DEPLOYMENT_READY.md` (445 lines) — Deployment checklist from 9 months ago.
- `docs/WHATS_BEEN_BUILT.md` (454 lines) — Historical summary, superseded by current code.
- `docs/QUARTERLY_REVIEW_PLAN.md`, `docs/RLS_IMPLEMENTATION_PLAN.md`, `docs/PHASE3_*.md` — Executed phases.
- **Action**: Archive to `docs/archive/`. Keep only active guides (`BUSINESS_CONTEXT_ARCHITECTURE.md`, `DESIGN_SYSTEM.md`, `TEAM_MEMBER_GUIDE.md`).

### Stale Scripts in `scripts/` Directory (15 Files)

🔴 **One-Shot Incident Response (Safety: High Confidence)**
- `audit-coach-context-corruption.sql` (4.7 KB) — One-time data audit. Never called. Delete.
- `audit-multiple-active-forecasts.ts` (1.7 KB) — One-time audit, already executed. Delete.

🔴 **Client-Specific Diagnostics (Safety: Very High)**
- `dedupe-envisage-xero-pl-lines.ts` (3.0 KB) — Envisage-specific remediation. Not in any npm script.
- `diag-envisage-deep.ts` (7.5 KB) — Envisage diagnostics. Not called.
- `diag-envisage-deeper.ts` (3.7 KB) — Envisage diagnostics (deeper). Not called.
- `remediate-duplicate-active-forecasts.ts` (2.2 KB) — One-time fix, already executed.
- `resync-envisage-now.ts` (14 KB) — Envisage sync script. Not in npm scripts.
- **Action**: Delete all Envisage-specific scripts. These are client-specific, one-shot tools not part of ongoing operations.

🔴 **Pre-Refactor & Rollback Artifacts**
- `pre-refactor-snapshot.sh` (4.8 KB) — Database backup before refactor (historical).
- `rollback.sh` (3.0 KB) — Manual rollback script (no longer needed if refactor is stable).
- `test-before-merge.sh` (3.5 KB) — Manual test script. Not in CI/CD. Delete.
- **Action**: Delete all three. Modern CI/CD has replaced manual rollback procedures.

🟡 **Legacy Migration Scripts (Safe If Migrations Are Applied)**
- `run-migration.js` (1.3 KB) — Manual migration runner. Check if all migrations have been applied to production.
- `run-payroll-migration.js` (1.9 KB) — Payroll-specific. If payroll schema is stable, delete.
- `seed-fit2shine-strategy.sql` (28 KB) — Fit2Shine client seeding (uncommitted). Delete or move to client-specific scripts.
- **Action**: Confirm all migrations are applied, then delete migration scripts. Seeding should be in `supabase/seed.sql`.

🟡 **Smoke & Health Check Scripts**
- `smoke-test.sh` (1.9 KB) — Referenced in `package.json` as `npm run smoke-test` (line 15). **Keep if tests are running; inspect to confirm they run successfully.**
- `check-conns.ts` (741 bytes) — Connection checker. Not in npm scripts. Delete or integrate into health checks.
- **Action**: Keep `smoke-test.sh` if it's in CI/CD. Delete `check-conns.ts` unless it's monitored.

### Duplicate Dependency Libraries (Recommend Standardization)

🟡 **Drag-and-Drop Libraries (2 packages, pick one)**
- `@dnd-kit/core`, `@dnd-kit/sortable` — Used in `src/app/finances/monthly-report/components/layout-editor/` (PDF layout editor).
- `@hello-pangea/dnd` — Used in `src/components/AnnualPlan.tsx`.
- **Verdict**: Both are active. `@hello-pangea/dnd` is maintained fork of React Beautiful DnD. `@dnd-kit` is newer, lighter. If modernizing, migrate AnnualPlan to @dnd-kit, then remove @hello-pangea/dnd.

🟡 **PDF/Excel Export Libraries (3+ packages, consolidate)**
- `jspdf` + `jspdf-autotable` — Used in `src/app/finances/forecast/services/pdf-export-service.ts`.
- `xlsx` — Used in `src/app/finances/forecast/components/wizard-v4/utils/parsePLFile.ts` and `ExcelExport.tsx`.
- `exceljs` — Installed but **not used anywhere** (grep: 0 matches). Delete from `package.json`.
- **Action**: Verify `exceljs` is unused, then remove. For PDF/Excel, current dual setup is reasonable (PDF = jspdf, Excel = xlsx). Consider deprecating jspdf-autotable if tables can be simpler.

🟡 **AI Provider Libraries (Both active, intended)**
- `@anthropic-ai/sdk` — Used in: `claude-cfo-agent.ts`, `ai/forecast-insights/route.ts`, `ai/forecast-assistant/route.ts`, `ai/advisor/route.ts`. **Active & essential.**
- `openai` — Used in: `wizard/chat/route.ts`, CSP policy allows `api.openai.com`. **Active for wizard chat.**
- **Verdict**: Both are intentional (Anthropic for CFO insights, OpenAI for wizard). No action needed.

🟡 **HTTP Client Libraries (Both active, can consolidate)**
- `axios` — Used in middleware CSP but no active usage found in source.
- `fetch` (native) — Used everywhere for API calls (`/api/cfo/summaries`, `/api/ai/forecast-assistant`, etc.).
- **Action**: Remove `axios` from `package.json` if unused. Search: `grep -r "axios\|import.*axios" src/` returned 0 matches.

🟡 **Supabase Auth Transition (Mix of old & new)**
- `@supabase/auth-helpers-nextjs` (deprecated) — Used in: `MorningRitual.tsx`, `CoachDashboard.tsx`, `useMorningRitual.ts`, `AICFOPanel.tsx`, `forecasts/scenarios/route.ts`. **Still in use (5 files).**
- `@supabase/ssr` (recommended replacement) — Used in: `middleware.ts`, `SwotActionPanel.tsx`, `TodoManagerV2.tsx`. **Already migrated in some places.**
- **Action**: Plan gradual migration of remaining 5 files from `auth-helpers-nextjs` to `@supabase/ssr`. Not urgent, both work, but `auth-helpers` is officially deprecated.

### Root-Level Logo Directories

🟡 **`WBi Main Logo/` directory (1.1 MB, 7 files)**
- Contains: `.ai`, `.eps`, `.jpg`, `.pdf`, `.png`, `.psd`, mockup design file.
- Not referenced in code (grep: 0 matches for "WBi Main Logo").
- Question: Are these used in marketing/branding, or dev assets?
- **Action**: If not used in code or `public/`, move to separate branding asset repository. If dev assets, consolidate into `public/logos/`.

🟡 **`WBi Additional Logo/` directory (missing)**
- Listed in directory listing but doesn't exist (error: "No such file or directory"). May have been deleted.
- **Action**: No action needed (already gone).

### Other Root-Level Cruft

🟡 **Stray Vercel Config Backup**
- `_archive/vercel.json.bak` (111 bytes) — Backup of vercel.json. Not needed if main `vercel.json` is active.
- **Action**: Delete. Main `vercel.json` (105 bytes) is current.

---

## NEEDS VERIFICATION

### 1. **`@supabase/auth-helpers-nextjs` Remaining References**
   - **Question**: Can the 5 remaining files using deprecated `auth-helpers-nextjs` be safely migrated to `@supabase/ssr` without breaking auth flow?
   - **Files to migrate**:
     - `src/components/todos/MorningRitual.tsx` (SupabaseClient type import)
     - `src/components/todos/CoachDashboard.tsx` (SupabaseClient type import)
     - `src/components/todos/hooks/useMorningRitual.ts` (SupabaseClient type import)
     - `src/app/finances/forecast/components/wizard-v4/components/AICFOPanel.tsx` (createClientComponentClient)
     - `src/app/api/forecasts/scenarios/route.ts` (createRouteHandlerClient)
   - **Risk**: Medium (migration is straightforward but requires testing).
   - **Recommendation**: Schedule auth-helpers removal in next 1-2 quarters.

### 2. **`smoke-test.sh` Verification**
   - **Question**: Is the smoke test still running in CI/CD? Does it pass consistently?
   - **Current status**: Referenced in `package.json` line 15 as `npm run smoke-test`, and used in `verify` script (line 16).
   - **Action**: Confirm it's in GitHub Actions or other CI pipeline before keeping.

### 3. **`seed-fit2shine-strategy.sql` (Uncommitted)**
   - **Current status**: Exists in `scripts/` but not staged in git.
   - **Question**: Is this a current client project or abandoned?
   - **Action**: If active, commit to git. If one-time, archive.

### 4. **Dual Database Migration Directories**
   - **Conflict**: Both `database/migrations/` and `supabase/migrations/` exist.
   - **Question**: Are `database/migrations/` files applied to production, or is `supabase/migrations/` canonical?
   - **Recommendation**: Confirm git history + timestamps to determine which is active. Likely `supabase/migrations/` is canonical (newer timestamps: Apr 2026). Delete `database/migrations/` once verified.

### 5. **Logo File Actual Usage**
   - **Question**: Is `WBi Main Logo/` used by the app, or is it developer/branding-only?
   - **Search**: `grep -r "WBi.*Logo\|Winner\." src/ public/` returned 0 matches.
   - **Action**: Check `public/` directory for actual active logos. If `WBi Main Logo/` is not used, archive externally.

### 6. **API Routes Called from Extensions/Integrations**
   - **Question**: Are any of the 120 API routes called from external services (webhooks, scheduled tasks, integrations) that wouldn't show up in `src/` code search?
   - **Example suspects**:
     - `/api/cron/weekly-digest` — Vercel Cron. Verify in `vercel.json` (confirmed at line 4-7).
     - `/api/Xero/callback` — Xero OAuth redirect. Verify callback URL is configured in Xero app settings.
   - **Action**: Review external service integrations (Xero, SendGrid, Stripe, etc.) to ensure no forgotten callbacks.

---

## SUMMARY: TOTAL DELETABLE

| Category | Count | Size | Risk |
|----------|-------|------|------|
| **Top-level files** (archives, plans, mockups, scripts) | 12 | 138 KB | 🔴 High confidence |
| **`_archive/` directory** | 162 | 1.0 MB | 🔴 Very high (no refs) |
| **`.archive/` directory** | 3 | 96 KB | 🔴 Very high (no refs) |
| **`supabase/archive/` directory** | 7+ | 1.3 MB | 🔴 Very high (no refs) |
| **`database/migrations/`** | 10 | 96 KB | 🟡 Medium (verify active) |
| **`docs/` stale entries** | 8-12 | ~30 KB | 🟡 Medium (move, not delete) |
| **`scripts/` dead scripts** | 10+ | ~60 KB | 🔴 Very high (one-shot) |
| **Unused dependency** (`exceljs`) | 1 pkg | — | 🟢 Low (just package.json) |
| **Unused ESLint config** (`eslint.config.mjs`) | 1 | <1 KB | 🟢 Low |
| **Unused components** (`.archive/`) | 2 | 29 KB | 🔴 Very high |
| **Unused diagram visualizers** (archived) | 2 | 29 KB | 🔴 Very high |
| **TOTAL IMMEDIATE DELETE** | **192+** | **2.1 MB** | ✅ Safe |

---

## RISK ASSESSMENT BY CATEGORY

### 🔴 DELETE WITH ZERO RISK
- All `_archive/`, `.archive/`, `supabase/archive/` content
- HTML mockups (`dwa_resources.html`, `mockup-step4-actuals.html`)
- Debug scripts (`check_spm_kpis.mjs`, all one-shot audit/remediation scripts)
- AWS SAM templates (`packaged.yaml`, `template.yml`)
- Unused ESLint config (`eslint.config.mjs`)
- Archived components (Diagram visualizers)
- API migration endpoints (`/api/migrate/*` — flagged as security risk)

### 🟡 DELETE WITH VERIFICATION (1-day effort)
- `database/migrations/` — verify `supabase/migrations/` is canonical
- Stale docs — confirm superseded by `.planning/` or current docs
- `@supabase/auth-helpers-nextjs` migration — schedule for next quarter

### 🟢 KEEP (ACTIVE)
- All API routes in `src/app/api/` (120 routes, all in use or planned)
- All source components, libs, pages
- `smoke-test.sh` (in package.json verify script)
- Vercel cron (`/api/cron/weekly-digest`)
- Both AI SDKs (intentional dual-provider setup)
- Both drag-and-drop libraries (different use cases)

---

## SECURITY NOTES

- 🔴 **CRITICAL**: `/api/migrate` and `/api/migrate/opex-fields` flagged in `docs/SECURITY_AUDIT_REPORT.md` as "NO authentication" endpoints for data manipulation. Delete immediately unless they were updated since the audit.
- Review `src/middleware.ts` CSP headers to ensure deleted endpoints don't leave broken references.

---

## RECOMMENDED CLEANUP SEQUENCE

1. **Phase 1 (5 minutes)**: Delete root-level cruft
   - `packaged.yaml`, `template.yml`, `eslint.config.mjs`
   - `dwa_resources.html`, `mockup-step4-actuals.html`, `check_spm_kpis.mjs`
   - `BRANDING_UPDATE_PLAN.md`, `DESIGN_SYSTEM_PLAN.md`, `UI_UX_AUDIT_REPORT.md`, `UI_UX_IMPLEMENTATION_PLAN.md`

2. **Phase 2 (10 minutes)**: Delete directory trees
   - `_archive/` (entire directory)
   - `.archive/` (entire directory)
   - `supabase/archive/` (entire directory)
   - `scripts/` — delete one-shot scripts individually

3. **Phase 3 (1 day)**: Verify & delete
   - Confirm `supabase/migrations/` is canonical, then delete `database/migrations/`
   - Test `smoke-test.sh` in CI/CD pipeline
   - Archive stale docs to `docs/archive/` (don't delete, keep for reference)

4. **Phase 4 (Optional, future)**: Dependency consolidation
   - Remove unused `exceljs` from `package.json`
   - Plan migration of `@supabase/auth-helpers-nextjs` to `@supabase/ssr`

---

**Generated**: 2026-04-28 | **Confidence Level**: 🟢 Very High (All claims verified via grep/git/filesystem)
