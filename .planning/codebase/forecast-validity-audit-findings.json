{"confirmed": [{"id": "COA-01", "title": "Payroll-in-COGS businesses get their labour bill counted twice (COGS % of revenue + generated OpEx wages)", "severity": "P0", "file": "src/app/finances/forecast/components/wizard-v4/useForecastWizard.ts", "line": 780, "claim": "The wizard has no team-cost guard on the COGS side. Step 2 seeding turns EVERY prior-year COGS line into a variable %-of-revenue assumption (useForecastWizard.ts:765-784), Step3RevenueCOGS.tsx contains zero team-cost/wages/labour handling (grep for isTeamCost|team|wages|labour returns nothing), and ForecastWizardV4 auto-imports ALL Xero Payroll employees into Step 4 on first load (ForecastWizardV4.tsx:251-283). convertTeam then materializes those same people's full salaries as 'Wages & Salaries'/'Superannuation' under category 'Operating Expenses' (assumptions-to-pl-lines.ts:626-659). For any business whose direct labour sits in Cost of Sales (DIRECTCOSTS-typed wages accounts \u2014 standard for trades/construction/manufacturing), the same payroll dollars are forecast once inside the COGS %-of-revenue line and again in the generated OpEx wages lines. The isTeamCost exclusion (shouldExcludeFromOpEx, useForecastWizard.ts:83-90) is applied ONLY to state.opexLines (buildAssumptions line 2081), never to cogsLines.", "evidence": "useForecastWizard.ts:766-784: cogsLines = data.cogs.byLine.map((line) => ({ ... costBehavior: 'variable' as const, percentOfRevenue: line.percentOfRevenue ... })) \u2014 no name filtering. buildAssumptions:2080-2082 filters only opex: `state.opexLines.filter(line => !shouldExcludeFromOpEx(line, exportHasTeamData))`; cogsLines mapper (2016-2034) has no filter. assumptions-to-pl-lines.ts:626-628: `{ name: 'Wages & Salaries', code: SYS_CODES.wages, data: wagesPerMonth }` emitted with `category: 'Operating Expenses'` (line 652). Prod xero_pl_lines confirms the shape exists in the fleet: account_type='cogs' rows include 'Direct Labour - Electricians' $2,453,154 (business 86e9d84f), 'Tradies Contractors' $3,538,814 (a1657c67), 'Subcontractor Costs' $3,121,434.", "impact": "An electrical/roofing/manufacturing client with $2.4M direct labour in Cost of Sales and those workers in Xero Payroll: the generated forecast carries ~$2.4M as COGS (seeded % of revenue) AND ~$2.4M+super as OpEx wages \u2014 net profit understated by the full direct-wage bill, GP% simultaneously overstated because the labour is also re-based into OpEx. Summary and stored P&L agree (parity holds) so no guard fires; the operator only sees an inexplicably terrible bottom line.", "coa_adaptability": true, "dimension": "coa-adaptability", "verdict": {"verdict": "PARTIAL", "reasoning": "The entire code mechanism is confirmed exactly as claimed: (1) setPriorYear seeds every prior-year COGS line as variable %-of-revenue with no team/name filtering (useForecastWizard.ts:765-784, read directly); (2) shouldExcludeFromOpEx is applied only to opexLines \u2014 summary at line 1794 and buildAssumptions at line 2081 \u2014 while both the summary COGS sum (1592-1618) and the COGS assumption mapper (2016-2034) have no exclusion; (3) ForecastWizardV4.tsx:251-283 auto-imports ALL Xero Payroll employees when teamMembers is empty (needsTeam, line 168), and /api/Xero/employees reads POSTED pay runs with zero awareness of which expense account wages post to; (4) convertTeam emits Wages & Salaries + Superannuation under category 'Operating Expenses' (assumptions-to-pl-lines.ts:626-659, category at 652), and convertCOGS has no team exclusion; (5) isTeamCost/shouldExcludeFromOpEx appear only on OpEx surfaces (Step5OpEx, ExcelExport) \u2014 Step3RevenueCOGS.tsx has zero team/wage/labour handling; (6) the prior-year source (historical-pl-summary.ts) buckets purely by account_type='cogs', so DIRECTCOSTS-typed wages stay in the seeded COGS lines. Not covered by ALREADY-FIXED (PR #350 is the OpEx-side conditional exclusion only). HOWEVER, the finder's empirical evidence overstates current fleet exposure: my prod fleet scan of all xero_pl_lines COGS accounts shows the only genuine employee-wages-in-COGS line ('Direct Labour - Electricians' $2,453,154, business 86e9d84f) belongs to the seeded Precision Electrical DEMO, which has no Xero connection \u2014 so the payroll auto-import path never fires for it. The other two quoted rows ('Tradies Contractors' $3.5M, 'Subcontractor Costs' $3.1M) and every other labour-adjacent COGS line in the real fleet are subcontractor accounts paid via bills \u2014 those workers are not in Xero Payroll, so the auto-import double-count chain does not currently fire for any connected client. No stored real-client forecast today carries this double-count. The defect is real by construction for the whole payroll-in-COGS CoA class (standard trades/construction/manufacturing) \u2014 precisely Matt's adaptability concern \u2014 but its impact today is prospective (next trades client onboarded, or the demo shape) rather than instantiated.", "corrected_claim": "The wizard has no team-cost guard on the COGS side: prior-year COGS lines (including DIRECTCOSTS-typed wages accounts) are all seeded as variable %-of-revenue with no filtering, Step 3 has zero team-cost handling, Step 4 auto-imports every Xero Payroll employee, and convertTeam re-materializes their full salaries+super as OpEx 'Wages & Salaries'/'Superannuation' \u2014 so any business whose payroll direct labour sits in Cost of Sales would have that labour forecast twice (once inside the seeded COGS %, once as generated OpEx wages), with summary parity holding so no guard fires. CORRECTION to the evidence: no currently-connected real client has employee wages typed as COGS \u2014 the quoted 'Direct Labour - Electricians' $2.45M row is the seeded Precision demo (no Xero connection, so the auto-import never fires for it), and the 'Tradies Contractors'/'Subcontractor Costs' rows are bill-paid subcontractors who are not in Xero Payroll and therefore are NOT auto-imported into Step 4. The double-count is a by-construction break of the payroll-in-COGS CoA class (standard for trades/construction/manufacturing) that will bite the first such client onboarded, but no stored real-client forecast today contains it.", "corrected_severity": "P0"}}, {"id": "COA-02", "title": "Over-broad TEAM_COST_KEYWORDS silently deletes real non-wage OpEx (outsourced contractors, directors fees, FBT, allowances) from the stored forecast", "severity": "P1", "file": "src/app/finances/forecast/components/wizard-v4/utils/opex-classifier.ts", "line": 31, "claim": "TEAM_COST_KEYWORDS (opex-classifier.ts:31-55) includes 'contractor', 'subcontractor', 'labour', 'labor', 'employee', 'personnel', 'staff cost', 'director fee', 'directors fee', 'fringe benefit', 'fbt', 'allowance'. Once Step 4 carries any team data (which it nearly always does \u2014 ForecastWizardV4 auto-imports Xero Payroll employees on first load), shouldExcludeFromOpEx (useForecastWizard.ts:83-90) removes every OpEx line whose name contains any of these substrings from the assumptions export (buildAssumptions:2081). But convertTeam only models the wages+super of listed team members: supplier-paid contractors, directors fees paid by invoice, FBT, and allowances are NOT modeled in Step 4, so these accounts vanish from the materialized forecast entirely. This is the identical failure class the file header admits was already hit for WorkCover/payroll tax (lines 14-21, 'they would vanish from the forecast entirely') \u2014 but the fix carved out only those two names (TEAM_COST_OPEX_OVERRIDE_KEYWORDS:61-64), leaving every other keyword live.", "evidence": "opex-classifier.ts:37: `'contractor', 'subcontractor', 'labour', 'labor',` and :38-43: `'staff cost', 'employee', 'personnel', 'director fee', 'directors fee', ... 'fringe benefit', 'fbt', 'allowance',`. Prod OpEx accounts that match today: 'Virtual Contractors' $511,720, 'Contractors excl. Artists' $433,340, 'Contractor Expenses (Non Salary)' $220,796, 'Employment Expenses: Contractors International' $211,282, 'Employment Expenses: Directors Fees' $85,000 \u2014 all account_type='opex' in xero_pl_lines. The name 'Contractor Expenses (Non Salary)' is explicit that it is NOT payroll.", "impact": "A creative agency with $433k/yr of supplier contractors ('Contractors excl. Artists') plus any imported payroll: that line is auto-flagged team-cost, filtered out of the OpEx assumptions, and never re-emitted by convertTeam \u2014 stored forecast OpEx is understated by $433k/yr and net profit overstated by the same amount. Mitigation exists but is passive: Step 5 renders excluded lines with a move-back toggle (Step5OpEx.tsx:1522), which a 'not a numbers person' operator must notice and act on per line.", "coa_adaptability": true, "dimension": "coa-adaptability", "verdict": {"verdict": "CONFIRMED", "reasoning": "Every load-bearing element of the claim checks out in code. (1) TEAM_COST_KEYWORDS (opex-classifier.ts:31-55) contains 'contractor', 'subcontractor', 'labour', 'labor', 'staff cost', 'employee', 'personnel', 'director fee', 'directors fee', 'fringe benefit', 'fbt', 'allowance' exactly as quoted; the 2026-05-07 carve-out (TEAM_COST_OPEX_OVERRIDE_KEYWORDS, lines 61-64) covers only payroll tax/levy and workcover/workers comp. (2) shouldExcludeFromOpEx (useForecastWizard.ts:83-90) excludes any keyword-matching line whenever Step 4 has team data, and buildAssumptions (line 2081) drops those lines from the stored assumptions. (3) hasTeamData is nearly always true: ForecastWizardV4.tsx:168-284 auto-imports Xero Payroll employees on first load whenever teamMembers is empty. (4) convertTeam (assumptions-to-pl-lines.ts:626-629) emits ONLY 'Wages & Salaries' and 'Superannuation' from listed members/hires \u2014 supplier-paid contractor accounts, invoice directors fees, FBT and allowances are never re-emitted, so the excluded dollars vanish from the materialized forecast unless the operator per-line toggles them back (Step5OpEx.tsx:1522) or manually models equivalent contractor team members in Step 4 (the Xero auto-import brings payroll employees only, never AP contractors). (5) The Step 5 mitigation is passive AND mislabeled: excluded rows render greyed at opacity-50 saying 'Counted in Team Costs' (Step5OpEx.tsx:1510-1527), which is false for these accounts \u2014 nothing counts them. No validation-service or Step 8 check reconciles excluded OpEx vs team lines; summary parity means the displayed summary agrees with the (understated) stored forecast, so nothing looks inconsistent. (6) Not in the ALREADY-FIXED list: PR #350 fixed the empty-Step-4 case and the WorkCover/PayrollTax phantoms; the over-broad keyword net with team data present is untouched, and the file header (lines 14-21) documents the identical failure class it only partially fixed. (7) Empirical claim re-verified via read-only SQL (trailing 12mo, deleted_at IS NULL): Dragon Roofing 'Virtual Contractors' $407,061; Urban Road 'Contractors excl. Artists' $394,464; Digital Bond 'Contractor Expenses (Non Salary)' $126,252; Efficient Living 'Employment Expenses: Directors Fees' $70,000, 'Contractors International' $67,209; plus Attaquer, Envisage, Sydney Pressed Metal matches. Finder's dollar figures are higher (likely a full-FY or all-history window vs my trailing-12mo) but the accounts and six-figure magnitude are real. Severity P1 with coa_adaptability=true is right: the stored forecast can be materially understated for CoAs that name supplier-cost accounts with these words, but the excluded rows are visible on-screen with a recovery toggle, so it is degraded-with-passive-mitigation rather than fully silent (not P0). One nuance for the report: the 'Counted in Team Costs' row label actively misleads \u2014 for these accounts nothing counts them anywhere.", "corrected_severity": "P1"}}, {"id": "COA-03", "title": "Wage accounts that miss the keyword list stay in OpEx and double-count against generated team wages", "severity": "P1", "file": "src/app/finances/forecast/components/wizard-v4/utils/opex-classifier.ts", "line": 403, "claim": "The team/OpEx boundary is guarded ONLY by substring keywords on the account name (isTeamCost, opex-classifier.ts:403-419). A payroll account named outside the list \u2014 'Remuneration', 'Locum Fees', 'Employment Expenses: Office team', 'Offshore Team' \u2014 is not matched ('employment' does not contain 'employee'; 'team' is not a keyword), so it survives as a normal fixed OpEx line seeded at prior-year monthly average (useForecastWizard.ts:806-816), while the same employees arrive via the Xero Payroll auto-import and convertTeam emits their salaries again as 'Wages & Salaries'. The two rows coexist in the stored P&L (dedupe key is category|account_name, assumptions-to-pl-lines.ts:1008, and the names differ).", "evidence": "TEAM_COST_KEYWORDS (opex-classifier.ts:31-55) contains no 'remuneration', 'locum', 'team', 'crew', or 'employment' entries. Prod check: xero_pl_lines has account_type='opex' rows 'Employment Expenses: Office team' $516,389 (business 2f92c35d, whose other accounts are all prefixed 'Employment Expenses:' \u2014 i.e. this is that tenant's wages bucket) and 'Offshore Team' $28,856. findMatchingLine (assumptions-to-pl-lines.ts:132-149) matches the generated team line only against the literal names 'Wages & Salaries'/'Superannuation', so an unmatched Xero wages account is never merged.", "impact": "For the tenant whose office wages live in 'Employment Expenses: Office team': the stored forecast carries ~$516k as a fixed OpEx line AND the same people's salaries again under the generated 'Wages & Salaries' line \u2014 OpEx overstated by ~$516k/yr, exactly inverse of COA-02. Which direction a given client breaks depends purely on what their accountant named the account.", "coa_adaptability": true, "dimension": "coa-adaptability", "verdict": {"verdict": "PARTIAL", "reasoning": "Core mechanism CONFIRMED at every step with code + prod evidence. (1) TEAM_COST_KEYWORDS (opex-classifier.ts:31-55) has no 'remuneration'/'locum'/'team'/'crew'/'employment' entries and 'employment' does not contain 'employee', so isTeamCost (lines 403-419, pure substring matching after normalizeAccountName, which only strips expense/opex/cost/overhead prefixes) returns false for 'Employment Expenses: Office team' and 'Offshore Team'. (2) The only team/OpEx boundary is shouldExcludeFromOpEx (useForecastWizard.ts:83-90), applied at the summary rollup (line 1794) and the assumptions export (line 2081); an unmatched wage account survives both and is exported as a fixed OpEx assumption seeded at prior-year monthly average (lines 798-816). Upstream, historical-pl-summary.ts builds operating_expenses_by_category from ALL account_type='opex' xero rows with no team filtering, so wage-named accounts do reach the wizard. (3) convertTeam (assumptions-to-pl-lines.ts:626-628) emits literal 'Wages & Salaries'/'Superannuation' lines; findMatchingLine (132-149) matches only exact case-insensitive name then code/id, and the final dedupe key is category|account_name (line 1008) \u2014 the differently-named rows both persist in the stored P&L. validation-service.ts contains no wage/team reconciliation that would catch it. Prod SQL confirms the rows exist exactly as quoted. Details corrected: the $516,389 'Office team' figure is a 22-month cumulative (Jul 2024\u2013Apr 2026); per-FY it is $268,474.50 (FY2025) / $247,914.78 (FY2026), so the '~$516k/yr' impact is ~2x overstated. Also, for tenant 2f92c35d 'Office team' is a secondary wages bucket \u2014 the primary 'Employment Expenses: Salaries & Wages' ($870k FY2025) IS keyword-matched and correctly excluded, so the overstatement is the office-team slice, not all wages. Finally, the double-count is conditional on Step 4 carrying team data (with an empty team step, shouldExcludeFromOpEx excludes nothing but convertTeam also emits nothing), the Xero payroll import is an operator-invoked modal (Phase 52) rather than automatic, and Step 5 offers a manual isTeamCostOverride toggle (Step5OpEx.tsx:1841) \u2014 but the default paved path (import/enter team in Step 4, accept seeded OpEx) double-counts silently, and both the on-screen summary and the stored P&L carry the same inflated total (summary-parity holds, so nothing flags it).", "corrected_claim": "The team/OpEx boundary is keyword-substring-only (isTeamCost, opex-classifier.ts:403-419; list at 31-55 lacks 'remuneration', 'locum', 'team', 'crew', 'employment'). A wage account named outside the list survives as a normal fixed OpEx line seeded at prior-year monthly average (useForecastWizard.ts:798-816; the sole exclusion gate shouldExcludeFromOpEx at lines 83-90/1794/2081 misses it), while \u2014 whenever Step 4 has team data (Xero-payroll import modal or manual entry) \u2014 convertTeam emits the same people's pay again as 'Wages & Salaries' (assumptions-to-pl-lines.ts:626-628); findMatchingLine (132-149) and the category|account_name dedupe (1008) cannot merge the differently-named rows, so both persist in the stored P&L. Real exposure: business 2f92c35d's 'Employment Expenses: Office team' is ~$268k FY2025 / ~$248k FY2026 (the $516,389 figure is the 22-month cumulative; the tenant's primary 'Salaries & Wages' bucket IS keyword-matched); business fa0a80e8 has 'Offshore Team' ~$29k. OpEx overstated by the unmatched account's annual amount for any CoA whose wage accounts miss the keyword list; direction of error depends purely on account naming; Step 5's manual isTeamCostOverride toggle is the only mitigation and defaults silent.", "corrected_severity": "P1"}}, {"id": "COA-04", "title": "Step 5 'Refresh from Xero' account-code backfill is dead code: field-name mismatch + wrong API filter, then clears the warning flag anyway", "severity": "P1", "file": "src/app/finances/forecast/components/wizard-v4/steps/Step5OpEx.tsx", "line": 1244, "claim": "handleRefreshFromXero (Step5OpEx.tsx:1223-1261) reads `a.code` and `a.name` from the chart-of-accounts response, but the route returns objects keyed `accountCode`/`accountName` (chart-of-accounts/route.ts:231-239) \u2014 so the name-match branch never fires and `match?.code` is always undefined. The accountId branch also never matches because line.accountId is a synthetic 'opex-N' index id (ForecastWizardV4.tsx:330-335), not a Xero GUID. Compounding it, the fetch omits the `filter` param so the route defaults to filter='subscription' (route.ts:101) and returns only keyword-suggested accounts anyway. After doing nothing, the handler calls `actions.setNeedsAccountCodeRefresh(false)`, permanently dismissing the banner.", "evidence": "Step5OpEx.tsx:1237: `const accounts: Array<{ code?: string; accountId?: string; name?: string }> = payload?.accounts || [];` and :1246-1249: `if (a.name && line.name && a.name.trim()...` / `if (match?.code) return { ...line, accountCode: match.code };`. Route byCode.set (route.ts:231-239): `{ accountId: acc.AccountID, accountCode: code, accountName: acc.Name, ... }` \u2014 no `code`/`name` keys. Route :101: `const filterType = searchParams.get('filter') || 'subscription'`.", "impact": "Legacy (pre-v11) drafts whose opexLines lack accountCode can never be repaired: the operator clicks Refresh, it 'succeeds', the banner disappears, and the subscription double-count guard (which keys on accountCode \u2014 useForecastWizard.ts:1798-1802 and buildAssumptions:2082) stays silently disabled \u2014 subscription spend is then counted both in the vendor-budget line and in the original Xero OpEx line of the stored forecast.", "coa_adaptability": false, "dimension": "coa-adaptability", "verdict": {"verdict": "CONFIRMED", "reasoning": "Every element verified in code. (1) Step5OpEx.tsx:1237/1244-1249 reads a.code/a.name and assigns via match?.code, but chart-of-accounts/route.ts:231-239 returns { accountId, accountCode, accountName, ... } \u2014 no code/name keys \u2014 so the name branch never fires and match?.code is always undefined; the backfill assigns nothing, ever. (2) The accountId branch is also unreachable: OpEx lines get accountId: line.id (useForecastWizard.ts:806-809, 1538-1541) and those ids are synthetic 'opex-${idx}' (ForecastWizardV4.tsx:933, 948), never Xero AccountID GUIDs. (3) The fetch at Step5OpEx.tsx:1232 omits the filter param, so route.ts:101 defaults to 'subscription' and route.ts:245-246 returns only keyword-suggested accounts. (4) actions.setNeedsAccountCodeRefresh(false) at line 1254 runs unconditionally on fetch success; the T03 migration that sets the flag only fires when storedVersion < 11 (useForecastWizard.ts:301, 329-340), so the banner never returns. (5) Impact chain real: both double-count guards are accountCode-only (useForecastWizard.ts:1798-1804 summary rollup, 2082 buildAssumptions) with explicit legacy fall-through, and buildAssumptions emits the vendor-budget subscriptions line in addition (comment 2070-2072) \u2014 so a legacy draft with active subscription vendors stores the spend twice, and no other path backfills accountCode (the restore-from-assumptions map at ForecastWizardV4.tsx:1229-1244 drops accountCode; fresh seeding only runs on initialization). Not covered by PR #351 (that fixed Step6Subscriptions, not this handler). Only nit: the synthetic-id citation should point at useForecastWizard.ts:806-809/1538-1541 (ForecastWizardV4.tsx:330-335 is one of the sources of the 'opex-N' ids, not the accountId assignment) \u2014 immaterial to the claim. P1 stands: wrong numbers can reach a stored forecast, but only for the bounded pre-v11 legacy-draft + active-subscription-vendor population, and the double-count pre-exists the button; the defect is the dead repair plus the false success signal."}}, {"id": "COA-05", "title": "CSV/XLSX P&L import drops every account row whose name contains 'income'/'revenue'/'expense'/'cost of'/'operating'/'total'", "severity": "P1", "file": "src/app/finances/forecast/components/wizard-v4/utils/parsePLFile.ts", "line": 241, "claim": "parsePLFile's section detection runs on EVERY data row without checking whether the row carries amounts: any row whose name contains 'revenue' or 'income' is consumed as a section header and skipped (`continue`), likewise 'cost of'/'direct cost', 'operating'/'expense', and 'total'/'gross profit'/'net profit' (parsePLFile.ts:240-253). AU charts are full of account names like 'Interest Income', 'Rental Income', 'Motor Vehicle Expenses', 'Entertainment Expenses' \u2014 all silently excluded from the imported totals. Additionally, rows that reach the 'unknown' default with a positive total are dropped (lines 298-303: only pushed `if (total < 0 || lowerName.includes('expense'))` \u2014 and any 'expense'-named row was already consumed as a header, making that condition dead), so flat exports with no section headers lose every account the prefix regexes (categorizeAccount, lines 36-84: /^rent/, /^insurance/... no 'electricity', 'cleaning', 'motor vehicle') don't recognize.", "evidence": "parsePLFile.ts:241-253: `if (lowerName.includes('revenue') || lowerName.includes('income')) { currentSection = 'revenue'; continue; } else if (lowerName.includes('cost of') || lowerName.includes('direct cost')) { ... continue; } else if (lowerName.includes('operating') || lowerName.includes('expense')) { ... continue; }` \u2014 no amount check before `continue`. Lines 298-302: `default: if (total < 0 || lowerName.includes('expense')) { opexLines.push(...) }` \u2014 positive unknowns dropped.", "impact": "A prospect or non-Xero client (MYOB export, the path Precision-style demos use) uploads a P&L where 'Motor Vehicle Expenses' is $48k and 'Interest Income' $12k: both rows vanish; total OpEx and Other Income are understated with no warning, and every downstream seed (fixed monthly amounts, net profit baseline) inherits the gap. This is the wizard's only ingestion path for businesses not on Xero.", "coa_adaptability": true, "dimension": "coa-adaptability", "verdict": {"reasoning": "Read parsePLFile.ts in full. Lines 240-253 match the quote verbatim: substring-based section detection runs on every data row with no amount check, and `continue` consumes the row \u2014 so any account row containing 'revenue'/'income'/'cost of'/'direct cost'/'operating'/'expense'/'total'/'gross profit'/'net profit' is dropped with its amounts (e.g. Interest Income, Rental Income, Motor Vehicle Expenses, Superannuation Expense). Lines 298-302 confirmed: unknown-category rows with positive totals are silently dropped, and the `lowerName.includes('expense')` rescue condition is provably dead because line 247 consumes any 'expense'-named row first. categorizeAccount (lines 36-84) is prefix-anchored with no patterns for common accounts (electricity, cleaning, motor vehicle), so flat no-header exports lose them too. Reachability confirmed: Step2PriorYear.tsx:439 calls parsePLFile from the live upload UI; the preview at lines 911-925 shows only the already-understated aggregates; handleConfirmParsedData commits via destructive actions.setPriorYear, seeding Steps 3/5/6 and the forecast baseline. Only warning fires at totalRevenue===0 \u2014 partial loss is silent. No guard, second pass, or reconciliation elsewhere neutralizes it; the only test in utils/__tests__ is spreadsheet-reader.test.ts (no parser-logic coverage). Not on the ALREADY-FIXED list (no PR touches parsePLFile; deferred D8 is a different mechanism). Non-Xero businesses' only Step-2 ingestion alternative is 'Skip this step' (industry benchmarks), so the impact framing holds. Severity P1 with coa_adaptability=true is correct: silent data loss on the imported baseline for common AU/MYOB chart-of-accounts naming, but user-reviewed totals stand between it and a stored forecast, so not P0.", "verdict": "CONFIRMED"}}, {"id": "COA-06", "title": "Name-pattern demotion of revenue to Other Income breaks rental/hire/franchise-shaped businesses", "severity": "P2", "file": "src/lib/services/historical-pl-summary.ts", "line": 450, "claim": "aggregatePeriod reroutes any revenue-typed account whose name contains 'rental income', 'royalt', 'grant', 'interest income', etc. to other_income (OTHER_INCOME_NAME_PATTERNS:450-466, applied at 551-554), with the sole escape being a name that starts with 'sales' (looksLikeOtherIncome:470-480 \u2014 the JDS carve-out). A business whose CORE revenue is legitimately named 'Rental Income' (equipment hire, property management) or 'Royalties' (franchisor) gets its operating revenue baseline zeroed: seasonality_pattern comes back empty (guarded by totalRevenue > 0, line 661), every COGS percent_of_revenue computes against the residual revenue (line 679), and the wizard seeds no revenue lines while the dollars ride as a flat, growth-frozen 'Other Income' SYS line in the materialized forecast (assumptions-to-pl-lines.ts:860-862).", "evidence": "historical-pl-summary.ts:461: `'rental income',   // unless rent is core revenue (rare for SMB clients)` \u2014 the comment itself acknowledges the shape exists. :551-554: `const effectiveType = line.account_type === 'revenue' && looksLikeOtherIncome(line.account_name) ? 'other_income' : line.account_type`. Fleet check today: only 'Sales - Rental Income' (protected by the 'sales' prefix) and small 'Interest Received/Income' rows are revenue-typed, so no current tenant is bitten \u2014 this is a latent trap for the next hire/franchise client onboarded.", "impact": "Onboard an equipment-hire prospect whose $1.8M revenue account is 'Rental Income': Step 2 shows near-$0 revenue, 100%+ COGS ratios, and the forecast wizard cannot seed revenue growth at all \u2014 the exact 'not adaptable to a different CoA' failure Matt is asking about, previously seen on JDS ($70,633 understatement) before the 'sales' prefix carve-out.", "coa_adaptability": true, "dimension": "coa-adaptability", "verdict": {"verdict": "CONFIRMED", "reasoning": "Every element of the claim checks out against the actual code. (1) The demotion exists as quoted: src/lib/services/historical-pl-summary.ts:450-466 defines OTHER_INCOME_NAME_PATTERNS (incl. 'rental income' at :461 with a comment acknowledging the core-revenue shape), :470-480 looksLikeOtherIncome whose only escape is a name starting with 'sales' (JDS carve-out), and :551-554 applies it unconditionally to every revenue-typed line during aggregation. Grep confirms no other call site, per-business override, or config flag neutralizes it. (2) Downstream effects are real: demoted lines never enter revenueAccounts (:614 requires effectiveType==='revenue') so revenue_lines omits them; seasonality is guarded by totalRevenue > 0 (:661) so a fully-demoted business gets seasonality_pattern undefined; COGS percent_of_revenue (:678-680) divides by the residual totalRevenue. (3) Reachability through the real wizard flow is confirmed: ForecastWizardV4.tsx:323 seeds revenue rows from priorFY.revenue_lines, useForecastWizard.ts:2230 sets xeroOtherIncome from priorYear.otherIncome.total, and assumptions-to-pl-lines.ts:860-862 materializes it via buildFlatLine(everyYear(...)) \u2014 a flat, identical-every-year, /12-per-month 'Other Income' SYS line with no growth or seasonality. (4) The empirical fleet claim re-verified via read-only SQL on xero_pl_lines_wide_compat (the table the service reads at :238/:386): the only revenue-typed matches today are 'Sales - Rental Income' ($175,625, protected by the sales prefix), 'Interest Received' ($43,738), and 'Interest Income' ($2,013) \u2014 interest demotion is intended behavior, so no current tenant is harmed and the finding is a latent trap for the next hire/franchise/property client, exactly as claimed. (5) Not covered by the ALREADY-FIXED list \u2014 the JDS fix only added the 'sales' prefix escape. One immaterial nuance: if ALL revenue is demoted, totalRevenue is exactly 0 and COGS percent falls back to 0% rather than exploding; the 100%+ ratio symptom occurs in the realistic case of small residual revenue. P2 + coa_adaptability=true is the correct severity since no current tenant is affected."}}, {"id": "COA-07", "title": "Businesses with under 12 months of Xero history seed fixed OpEx and baselines at a fraction of run-rate", "severity": "P2", "file": "src/lib/services/historical-pl-summary.ts", "line": 651, "claim": "aggregatePeriod computes monthly_average as `acc.total / monthKeys.length` where monthKeys.length is the WINDOW length (always 12 for prior-FY), not the number of months that actually contain data. A business that connected Xero 3 months before its FY end has a 'Prior FY' whose per-account totals cover 3 months; the wizard seeds every fixed OpEx line at total/12 (setPriorYear \u2192 monthlyAvg, useForecastWizard.ts:799-813) \u2014 roughly a quarter of the real monthly cost \u2014 and the whole revenue/growth baseline is similarly understated. Coverage months_covered is surfaced in the API but nothing scales or blocks the seeds.", "evidence": "historical-pl-summary.ts:651: `monthly_average: monthKeys.length > 0 ? acc.total / monthKeys.length : 0` (monthKeys generated for the full requested window at 499-510, independent of data presence). Contrast getFiscalYearActuals (395-404), which was explicitly hardened to count only months carrying data and fail-closed at <12 \u2014 the wizard path got no such guard.", "impact": "A newly-onboarded client with 4 months of history generates a forecast whose fixed OpEx is ~1/3 of reality; the operator anchoring on seeded values bakes the understatement into the stored P&L. Bites exactly the businesses being onboarded fresh \u2014 part-year history is the norm at onboarding.", "coa_adaptability": true, "dimension": "coa-adaptability", "verdict": {"verdict": "PARTIAL", "reasoning": "Core defect CONFIRMED against actual code: historical-pl-summary.ts:651 computes monthly_average as acc.total / monthKeys.length, where monthKeys (lines 499-510) is the full requested window (always 12 for prior FY) independent of data presence; getFiscalYearActuals (lines 395-417) was hardened to count only present months and its doc comment (lines 340-343) explicitly warns months_count is \"just the window LENGTH\" \u2014 the wizard path got no such guard. useForecastWizard.ts:799-813 seeds every OpEx line as fixed with monthlyAmount = round(line.monthlyAvg || total/12), and all mapping/fallback sites (ForecastWizardV4.tsx:332/936/1688, Step2PriorYear.tsx:531) also divide by 12. No neutralizer exists: coverage.months_covered is consumed by zero wizard components (only the type at types.ts:554), and data_quality (forecast-read-service.ts:336-419) reflects sync-job status/staleness only \u2014 a young Xero org with a fresh successful sync reports 'verified'. Prod SELECT confirmed real instances: 2 of 11 businesses with P&L data have first_month=2025-04-01 (3 of 12 FY25 months), 3 more have 0 FY25 months. However, the impact framing is materially off: the sync orchestrator (sync-orchestrator.ts:525-560) always fetches the FULL prior-FY window from Xero regardless of WisdomBI connection date, so a mature Xero org connected to WisdomBI 3 months before FY end gets complete history and is NOT affected. The defect bites only businesses whose Xero ledger itself is young in the baseline FY (mid-year Xero conversions, newly-formed entities) \u2014 \"part-year history is the norm at onboarding\" is an overstatement; most of the fleet has full 12-month coverage. Severity P2 stands (seeds are operator-editable defaults; the partial annual total displayed is factually the FY total; the harm is a bad extrapolation basis silently anchoring the operator). coa_adaptability=true stands \u2014 business-shape-dependent.", "corrected_claim": "aggregatePeriod (historical-pl-summary.ts:651) divides per-account totals by the window length (12 for prior FY), not by months actually carrying data, and the wizard seeds every fixed OpEx line at that understated monthly_average (useForecastWizard.ts:799-813) with no coverage-based scaling or blocking \u2014 months_covered is surfaced by the API but consumed by no wizard component, and data_quality reflects sync status only. Correction to trigger condition: the sync always fetches the full prior-FY window from Xero regardless of WisdomBI connection date, so this does NOT affect mature-Xero businesses onboarded to WisdomBI late; it bites only businesses whose Xero ledger itself has under 12 months in the baseline FY (new-to-Xero conversions / newly-formed entities). Prod currently shows 2 businesses with exactly 3 of 12 prior-FY months (seeds ~1/4 of run-rate) and 3 with 0 \u2014 real but a minority, not \"the norm at onboarding\". getFiscalYearActuals's present-month counting + fail-closed <12 guard was never applied to the wizard path.", "corrected_severity": "P2"}}, {"id": "COA-08", "title": "Section-title classifier misroutes catalog-less rows when custom sub-section names contain classifying words ('Sales & Marketing' \u2192 revenue)", "severity": "P2", "file": "src/lib/xero/pl-by-month-parser.ts", "line": 126, "claim": "classifyAccountType is substring-based on section titles with priority other_income \u2192 other_expense \u2192 cogs \u2192 expense \u2192 income/revenue/'sales' (pl-by-month-parser.ts:126-134). A custom report sub-section titled 'Sales & Marketing' under Operating Expenses passes titleClassifiesToKnownType (contains 'sales', :147-161), clobbers the carry-forward parent title (:251-253), and classifies its rows \u2014 and all subsequent non-classifying sibling sections \u2014 as 'revenue'. Similarly a COGS section titled 'Direct Expenses' classifies to opex ('expense' checked before nothing matches cogs). The blast radius is limited because the orchestrator prefers catalog xero_type (`account_type: catalogType ?? r.account_type`, sync-orchestrator.ts:870-886), so only rows WITHOUT a catalog entry (FXGROUPID, synthetic-AID legacy rows) actually take the parser's verdict.", "evidence": "pl-by-month-parser.ts:131-132: `if (t.includes('operating expense') || t.includes('expense')) return 'opex'; if (t.includes('income') || t.includes('revenue') || t.includes('sales')) return 'revenue'` \u2014 'Sales & Marketing' contains none of the expense strings and hits 'sales' \u2192 revenue. :251-253: `if (ownTitle && titleClassifiesToKnownType(ownTitle)) { currentParentTitle = ownTitle }` \u2014 the D-44.2-14 fix only protects NON-classifying sub-titles.", "impact": "A tenant with a custom layout grouping expenses under 'Sales & Marketing' whose rows lack catalog entries would see marketing spend counted as revenue (inflating revenue and deleting opex). Today catalog coverage makes this mostly theoretical, but it is the fallback path for exactly the odd rows (FX, legacy) where it can least be checked.", "coa_adaptability": true, "dimension": "coa-adaptability", "verdict": {"verdict": "PARTIAL", "reasoning": "The classifier keyword collisions are real and verified: src/lib/xero/pl-by-month-parser.ts:126-134 classifies 'Sales & Marketing' as 'revenue' (hits 'sales' after missing every expense string) and 'Direct Expenses' as 'opex' ('expense' at :131 fires because 'direct expenses' matches none of 'cost of sales'/'cogs'/'direct cost' at :130). titleClassifiesToKnownType (:147-161) returns true for such titles, so the D-44.2-14 guard does not protect against classifying-but-wrong custom titles. The catalog bound is also confirmed: sync-orchestrator.ts:870-886 uses `account_type: catalogType ?? r.account_type` (classifyByXeroType, accounts-catalog.ts:55-77), so only FXGROUPID/SYNTH-AID rows without catalog entries take the parser verdict; a catalog fetch failure aborts the tenant (:728-734) rather than silently widening the fallback. Two details are wrong, though. (1) Wrong live path: the orchestrator never calls parsePLByMonth \u2014 it uses parsePLSinglePeriod exclusively (sync-orchestrator.ts:46,749,791). The defect is still live only because pl-single-period-parser.ts imports the same classifyAccountType/titleClassifiesToKnownType (:29-34) and applies them at :154-157 and :230-232. (2) The 'all subsequent non-classifying sibling sections also become revenue' mechanism is false for the cited parser: parsePLByMonth's `effectiveTitle = ownTitle || currentParentTitle` (:256) means a non-empty non-classifying sibling classifies by its own title into default 'opex'; only empty-titled sections inherit the clobbered title. That propagation IS real in the live parser, where currentTopLevelType carry-forward (:226-232) plus walkSection inheritance (:152-157) hands 'revenue' to every following non-classifying flat sibling. Core claim survives with corrected file/mechanism; severity and CoA-adaptability flag stand.", "corrected_claim": "COA-08 (corrected) \u2014 classifyAccountType's substring priority (pl-by-month-parser.ts:126-134, shared by both parsers) misroutes catalog-less rows for custom section titles containing classifying words: 'Sales & Marketing' under Operating Expenses \u2192 'revenue'; a COGS section titled 'Direct Expenses' \u2192 'opex'. The live defect path is pl-single-period-parser.ts (the only parser the sync orchestrator uses): walkSection :154-157 classifies the section's own rows as revenue, and the top-level forward-carry :226-232 then propagates 'revenue' to all subsequent flat sibling sections whose titles do NOT classify (e.g. 'Administration'). In the cited legacy parsePLByMonth, by contrast, only the mistitled section's own rows and subsequent EMPTY-titled sections are affected \u2014 non-empty non-classifying siblings fall to default opex via `effectiveTitle = ownTitle || currentParentTitle` (:256). Blast radius correctly bounded: the orchestrator prefers catalog xero_type (sync-orchestrator.ts:870-886, `catalogType ?? r.account_type`), so only FXGROUPID and SYNTH-AID rows (no catalog entry) actually store the parser's verdict; a catalog fetch failure aborts the tenant sync rather than widening the fallback.", "corrected_severity": "P2"}}, {"id": "FML-01", "title": "Step 5 Y2/Y3 overrides (y2Override/y3Override/y2PercentOverride/y3PercentOverride) never reach the stored forecast and are lost on reopen", "severity": "P0", "file": "src/app/finances/forecast/components/wizard-v4/useForecastWizard.ts", "line": 2080, "claim": "The operator can type an explicit Y2/Y3 annual amount (or Y2/Y3 % of revenue) per OpEx line in Step 5, and the on-screen summary honors it \u2014 but buildAssumptions does not export any of the four override fields, the ForecastAssumptions schema has no slot for them, and convertOpEx has no handling, so the materialized forecast_pl_lines carry the un-overridden formula value. Because restore also rebuilds opexLines from assumptions, the overrides additionally vanish the next time the forecast is reopened.", "evidence": "UI setter: Step5OpEx.tsx:1765-1767 `const overrideKey = activeYear === 2 ? 'y2Override' : 'y3Override'; actions.updateOpExLine(line.id, { [overrideKey]: val } as any)`. Summary honors them: useForecastWizard.ts:1808-1813 `if (yearNum === 2 && typeof line.y2Override === 'number') return sum + line.y2Override` and 1826-1830 for y2PercentOverride/y3PercentOverride. Export drops them: useForecastWizard.ts:2080-2097 maps only accountId/accountName/priorYearTotal/costBehavior/monthlyAmount/annualIncreasePct/percentOfRevenue/seasonal*/expected*/isSubscription/notes. Schema confirms: types/assumptions.ts:162-181 OpExLineAssumption has no override fields. Materializer convertOpEx (assumptions-to-pl-lines.ts:320-421) computes fixed lines as `baseAmount * (1+annualInc)^(yearNum-1)` for every year with no override path. Parity check is watch-only (generate/route.ts:352-357 'report, never block'), so the divergent forecast publishes. Worked example: fixed line $10,000/mo, 3% increase, operator overrides Y2 to $100,000. Screen/approved Y2 opex = $100,000; stored Y2 = 10,000\u00d712\u00d71.03 = $123,600 \u2014 stored NP is $23,600 lower than approved for that line alone.", "impact": "Any business where the coach hand-tunes Y2/Y3 OpEx (the whole point of the override UI) publishes a stored P&L that contradicts the approved Step 8 numbers, and the tuning silently disappears when the forecast is reopened. Same fields also skip isOneTime/oneTimeYear/startYear which the summary honors (useForecastWizard.ts:1791-1793), though no current UI writes those three.", "coa_adaptability": false, "dimension": "formulas", "verdict": {"reasoning": "Every element of the claim checks out against the code. (1) The override UI is real and reachable: Step5OpEx.tsx:1765-1767 and 1801-1803 write y2Override/y3Override via Y2Y3WorkingInput, :625/:1753 write y2/y3PercentOverride via VariablePercentInput, and :1826-1834 renders an amber asterisk for overridden lines. (2) The on-screen summary honors all four fields (useForecastWizard.ts:1808-1813, 1826-1830). (3) buildAssumptions (useForecastWizard.ts:2080-2097) exports exactly the fields the finder listed and none of the overrides; OpExLineAssumption (types/assumptions.ts:162-181) has no slot for them; convertOpEx (assumptions-to-pl-lines.ts:320-341) computes fixed lines as baseAmount*(1+annualInc)^(yearNum-1) and variable lines from percentOfRevenue with no override branch. (4) Both saveDraft (:2269-2284) and generateForecast (:2309-2322) send only buildAssumptions() output, so the server NEVER receives the overrides in any form \u2014 the only persistence is the same-browser localStorage full-state serialize (useForecastWizard.ts:419). (5) The parity guard cannot catch it: generate/route.ts:344-382 is watch-only AND compares only summary.year1 (:358-359), while the divergence is exclusively Y2/Y3 \u2014 actually worse than claimed, since the divergence is outside the watched surface entirely; meanwhile the stored summary payload (year2/year3, :2286-2288) DOES embed the overrides, so the stored forecast's summary and its materialized forecast_pl_lines contradict each other. (6) Reopen loss confirmed with one nuance: the saved-assumptions restore (ForecastWizardV4.tsx:1214-1249) rebuilds opexLines without the override fields, but when a usable same-browser localStorage draft exists the API init is skipped (:136-139) and overrides survive locally; they are lost on any other machine, after storage clear, a WIZARD_VERSION bump, or draft-owner discard \u2014 and durable server-side loss is guaranteed since the fields never leave the browser. (7) The worked example is arithmetically correct ($100,000 approved vs $123,600 stored = $23,600 divergence on one line). (8) The isOneTime/oneTimeYear/startYear aside is accurate: honored by the summary (:1791-1793) and by Step5 display math (:945-949), written by no UI, not exported. (9) Not covered by the already-fixed list \u2014 none of #350/#377-379/#380-381 touch the Y2/Y3 OpEx override fields; the 'P0-1' comment shows only the summary side was ever fixed. P0 stands under the contract ('wrong numbers in a stored forecast'): any coach who hand-tunes Y2/Y3 OpEx publishes stored Y2/Y3 P&L lines that contradict the approved Step 8 numbers, with no Sentry signal. The only overstated detail is that overrides vanish on 'the next' reopen \u2014 they persist while the same-browser localStorage draft hydrates \u2014 which does not change the core defect or severity.", "verdict": "CONFIRMED"}}, {"id": "FML-02", "title": "Partially-filled Y2/Y3 monthly revenue grids materialize as $0 (monthlyToQuarterly returns all-zero below 12 keys)", "severity": "P0", "file": "src/app/finances/forecast/components/wizard-v4/types.ts", "line": 154, "claim": "monthlyToQuarterly returns {q1:0,q2:0,q3:0,q4:0} whenever the monthly map has fewer than 12 keys, buildAssumptions treats any non-empty map as hasY2/hasY3 and exports that zero quarterly, and convertRevenue reads ONLY the quarterly fields for Y2/Y3 \u2014 so a revenue line whose Y2 tab was only partially filled stores $0 for the entire year while the on-screen summary counts the typed months.", "evidence": "types.ts:153-154 `const values = Object.values(monthly); if (values.length < 12) return { q1: 0, q2: 0, q3: 0, q4: 0 }`. useForecastWizard.ts:1991 `const hasY2 = line.year2Monthly && Object.keys(line.year2Monthly).length > 0` then 2004 `year2Quarterly: hasY2 ? monthlyToQuarterly(line.year2Monthly) : undefined`. Converter reads only quarterly: assumptions-to-pl-lines.ts:185-193 `if (forecastDuration >= 2 && revLine.year2Quarterly) { ... expandQuarterlyToMonthly ... }` \u2014 year2Monthly is exported (line 2001) but never read by convertRevenue. Summary counts the partial sum via getRevenueLineYearTotal (types.ts:168-171). Reachable: Step3 handleRevenueChange (Step3RevenueCOGS.tsx:1470-1473) writes single keys into an empty year2Monthly, e.g. a line added on the Y2 tab with 3 months typed. Worked example: year2Monthly = {\"2026-07\": 50000} \u2192 summary Y2 revenue +$50,000; stored Y2 for the line = $0 in every month.", "impact": "A coach who adds or hand-fills only some Y2/Y3 months on a revenue line publishes a forecast whose stored Y2/Y3 revenue silently drops those dollars; monthly report/cashflow then vary against a budget missing entire revenue lines. Also key-order dependent: values[0..2] are assumed to be Jul-Sep, so filling an empty Y2 grid out of chronological order scrambles months into the wrong quarters.", "coa_adaptability": true, "dimension": "formulas", "verdict": {"verdict": "CONFIRMED", "reasoning": "Every cited line exists as described and no guard neutralizes the path. (1) src/app/finances/forecast/components/wizard-v4/types.ts:151-161 \u2014 monthlyToQuarterly returns {q1:0,q2:0,q3:0,q4:0} whenever Object.values(monthly).length < 12 (guard at line 154), and builds quarters positionally from values[0..11] (insertion order), which also confirms the key-order corollary since handleRevenueChange's spread preserves first-touch order. (2) useForecastWizard.ts:1991-1992 \u2014 hasY2/hasY3 = any non-empty map; 2004-2005 export year2Quarterly/year3Quarterly = monthlyToQuarterly(partial map) = all zeros. The adjacent comment (1985-1990) documents that the PR #350-era fix covered ONLY the empty-map (0-key) case; the 1-11-key case slipped through, so this is not on the already-fixed list. (3) assumptions-to-pl-lines.ts:185-204 \u2014 convertRevenue reads ONLY year2Quarterly/year3Quarterly for Y2/Y3 (year2Monthly appears nowhere in the converter file); expandQuarterlyToMonthly (88-108) writes 0 into all 12 Y2/Y3 month keys, and the D-44.1-06 merge at line 173 lets those zeros overwrite any existing forecast_months. Same pattern hits COGS (convertCOGS 257-266 with useForecastWizard 2018-2031) \u2014 zeros there UNDERSTATE costs, overstating stored GP. (4) The real generate flow uses this converter (app/api/forecast-wizard-v4/generate/route.ts:155) and summary parity is explicitly watch-mode \u2014 \"report, never block\" (route.ts:353) \u2014 so the mismatch materializes and only emits a Sentry event. (5) Screen side confirmed: summary sums the partial monthly map via getRevenueLineYearTotal (useForecastWizard.ts:1579-1581, types.ts:164-176), so the UI shows the typed dollars while storage gets $0. (6) Reachability confirmed: Step3RevenueCOGS.tsx:1470-1478 writes one key per commit into a possibly-empty map; lines added in Step 3 start with year2Monthly:{} (Step3:1414-1415; hook 1334/1387/1407/1458); goal distribution (hook 846-847/1503-1504) fills 12 keys only when a Y2/Y3 revenue goal is set AND only for lines existing at init \u2014 so a line added later, or any business without Y2/Y3 goals, produces partial maps. Note this also bites LEGITIMATELY complete input: a seasonal Y2 revenue line hand-typed for its 4 trading months (4 keys) stores $0 for the year. (7) Restore path (ForecastWizardV4.tsx:1167/1203) restores from the monthly map, so reopened drafts keep displaying the phantom dollars. Severity P0 is correct per the contract: wrong numbers in a stored forecast, silently. The coa_adaptability flag is defensible as business-shape-dependent (seasonal/partial-year revenue lines, mid-wizard-added lines) rather than strictly chart-of-accounts-dependent."}}, {"id": "FML-03", "title": "Stored Y2/Y3 monthly shape is flattened within each quarter \u2014 seasonality destroyed for monthly consumers", "severity": "P1", "file": "src/app/finances/forecast/services/assumptions-to-pl-lines.ts", "line": 103, "claim": "Even when a Y2/Y3 monthly grid is complete, the export collapses it to 4 quarterly totals and the materializer re-expands each quarter as qValue/3 into equal months, so the stored forecast_pl_lines lose all within-quarter seasonality for Years 2 and 3 (revenue AND COGS lines). Annual and quarterly totals are preserved; monthly numbers are not.", "evidence": "expandQuarterlyToMonthly: assumptions-to-pl-lines.ts:98-104 `result[key] = round2(qValue / 3)` \u2014 even thirds, no seasonality parameter. convertRevenue Y2: lines 185-193 uses only revLine.year2Quarterly. The wizard's own Y2 grids ARE seasonal: distributeGoalRevenueMonthly (useForecastWizard.ts:1488-1498) weights each month by seasonality. Worked example: a line with Y2 Q2 = $300k distributed 50/30/20 across Oct/Nov/Dec on screen ($150k/$90k/$60k) stores as $100k/$100k/$100k.", "impact": "Every seasonal business (most of the fleet \u2014 landscaping, roofing, retail) gets flat-within-quarter Y2/Y3 budgets in the stored P&L. Monthly budget-variance in the monthly report, cashflow projections, and quarterly reviews for the out-years compare actuals against months that were never planned at those values, while Step 3 shows the seasonal shape the coach approved.", "coa_adaptability": true, "dimension": "formulas", "verdict": {"verdict": "CONFIRMED", "reasoning": "Every link in the claimed chain exists in the code as described. (1) Flattening: src/app/finances/forecast/services/assumptions-to-pl-lines.ts:88-108 expandQuarterlyToMonthly writes `result[key] = round2(qValue / 3)` (line 103 exactly) \u2014 even thirds, no seasonality input. (2) Converter ignores the monthly grids: convertRevenue Y2/Y3 (lines 185-204) and convertCOGS Y2/Y3 (lines 257-269) read ONLY revLine.year2Quarterly/year3Quarterly; the RevenueLineAssumption/COGSLineAssumption types DO carry year2Monthly/year3Monthly (types/assumptions.ts:42-45, 70-73) and buildAssumptions populates them (useForecastWizard.ts:2001-2005 revenue, 2027-2031 COGS, deriving the quarterly via monthlyToQuarterly at types.ts:151-161), but no converter code path reads them \u2014 grep confirms zero references to year2Monthly/year3Monthly in assumptions-to-pl-lines.ts. (3) The wizard's Y2/Y3 shape IS monthly and seasonal: distributeGoalRevenueMonthly (useForecastWizard.ts:1474-1504; seasonality factor at line 1493) weights each month by the line's effective seasonality, and Step3 Y2/Y3 tabs edit per-month (Step3RevenueCOGS.tsx:529, 999, 1115); YearTabs.tsx:14-15 documents Phase 57+ storing Y2/Y3 as monthly with quarterly as legacy. (4) Reachability: convertAssumptionsToPLLines is the ONLY materialization path (generate route.ts:155 \u2192 save_assumptions_and_materialize RPC; also recompute:143, seed-from-prior:218, and ExcelExport.tsx:96 \u2014 so the Excel export shows the same flat thirds). (5) Not covered by the already-fixed list: PR #350 (M1) established \"Y2/Y3 quarterly maps win next\" \u2014 that comment in convertCOGS documents the flattening as the still-current design; #350 fixed Y1 grid fidelity, not out-year monthly shape. (6) Invisible to guards: summary-parity.ts compares annual bucket totals only (revenue/GP/netProfit), so preserved annual totals mask the monthly divergence. The worked example is arithmetically correct (Oct/Nov/Dec 150k/90k/60k \u2192 q2=300k \u2192 stored 100k/100k/100k). Scope is actually slightly WIDER than claimed: commission wage lines also route Y2/Y3 through expandQuarterlyToMonthly (lineMonthlyValue, assumptions-to-pl-lines.ts:561-568), so monthly commission amounts in the Wages line are flattened too. Minor detail nit only: the distributeGoalRevenueMonthly citation (1488-1498) is the interior of the function that actually spans 1474-1504 \u2014 immaterial. Severity P1 is right: annual and quarterly totals are preserved, Y1 monthly is exact, so current-FY budget variance is unaffected; the damage is monthly-grain Y2/Y3 numbers for seasonal businesses (business-shape-dependent, hence the CoA/shape flag)."}}, {"id": "FML-04", "title": "Materializer charges 12% super on bonuses and commissions; the approved summary does not", "severity": "P1", "file": "src/app/finances/forecast/services/assumptions-to-pl-lines.ts", "line": 622, "claim": "convertTeam pools bonuses and commissions into wagesEmployeePerMonth (addWage(..., false)) and then computes the Superannuation line as wagesEmployeePerMonth \u00d7 superPct \u2014 so stored super includes 12% on every bonus and commission. The wizard summary adds bonusTotal and commission amounts to teamCosts with no super component. The stored forecast is therefore systematically more expensive than the approved summary by superPct \u00d7 (bonuses + commissions), every year.", "evidence": "Bonuses: assumptions-to-pl-lines.ts:544 `addWage(mk, bonus.amount, false)`; commissions: lines 593 and 609 `addWage(mk, ..., false)`; super: lines 617-622 `const superPct = (team.superannuationPct || 12) / 100 ... superPerMonth[mk] = round2(wagesEmployeePerMonth[mk] * superPct)`. Summary: useForecastWizard.ts:1708-1715 `teamCosts += bonusTotal` (no super term) and 1736 `teamCosts += lineRevenue * ((commission.percentOfRevenue || 0) / 100)` (no super term). Worked example: $20,000 of bonuses + a 2% commission on a $2.5M line ($50,000) \u2192 stored Superannuation exceeds the approved teamCosts by 0.12 \u00d7 70,000 = $8,400/yr; the watch-mode parity check logs it to Sentry but the forecast publishes. Note a contractor's bonus is also filed as employee wages (hardcoded `false`), attracting super it shouldn't.", "impact": "Any business using bonuses or commissions (sales-led clients) has a stored NP up to five figures below the number the coach approved on Step 8. ATO-wise the materializer is closer to correct (bonuses/commissions are OTE), which means the SCREEN is understating true cost \u2014 but either way the two halves disagree and the invariant 'wizard summary \u2261 stored P&L' is violated on every such business.", "coa_adaptability": false, "dimension": "formulas", "verdict": {"reasoning": "Every cited line exists as described: assumptions-to-pl-lines.ts pools bonuses (line 544) and commissions (lines 593, 609) into wagesEmployeePerMonth via addWage(..., false), then computes superPerMonth[mk] = wagesEmployeePerMonth[mk] \u00d7 superPct (lines 617-622), so stored Superannuation includes 12% on every bonus and commission. The wizard summary (useForecastWizard.ts:1708-1715, 1736) adds bonusTotal and commission amounts to teamCosts with no super component \u2014 super is only applied to salaries (lines 1670, 1694). Both sides use 12% (SUPER_RATE=0.12, types.ts:1075; superannuationPct exported as 12 at line 2214), so the annual delta is exactly 0.12 \u00d7 (bonuses + commissions); the $8,400 worked example checks out, including for quarterly/annual commission timing whose accrual sums to the same annual total. No guard neutralizes it: generate/route.ts lines 344-376 run checkSummaryParity in explicit watch mode ('report, never block') \u2014 Sentry captureMessage with invariant:summary-parity, then the forecast publishes. The contractor-bonus note also holds: Step4Team builds contractor rows with bonus fields and addBonus is reachable per row, while line 544 hardcodes false, so a contractor's bonus lands in the employee pool and attracts super. Not covered by the already-fixed list (PR #350 fixed bonus timing and removed WorkCover/PayrollTax phantoms; the comment block documents no super-base decision for bonuses/commissions). Severity P1 is appropriate: the materializer is closer to ATO-correct (bonuses/commissions are OTE), so the defect is the on-screen approved summary understating true cost plus a summary\u2261stored parity violation, not a corrupted stored number \u2014 'wrong numbers on-screen' = P1 under the contract.", "verdict": "CONFIRMED"}}, {"id": "FML-05", "title": "An explicit 0% increase is coerced to 3% by the summary but honored by Step 5 and the materializer \u2014 three different answers", "severity": "P1", "file": "src/app/finances/forecast/components/wizard-v4/useForecastWizard.ts", "line": 1740, "claim": "The summary uses falsy-coalescing (`|| 3`, `|| defaultIncrease`) so a deliberate 0% OpEx/subscription increase is silently replaced with 3%, while Step 5's display uses nullish-coalescing (`?? defaultGrowth`, honors 0) and the materializer uses `|| 0` (honors 0). Setting the default increase to 0% therefore produces Y2/Y3 opex and subscription figures that differ between Step 5, Step 8/saved wizard_state, and the stored P&L.", "evidence": "Summary: useForecastWizard.ts:1740 `const defaultIncrease = state.defaultOpExIncreasePct || 3` (0\u21923) and 1820 `const increaseFactor = 1 + (line.annualIncreasePct || defaultIncrease) / 100` (0\u21923); subscriptions growth 1775 uses the same defaultIncrease. Step 5 display: Step5OpEx.tsx:957 `return line.annualIncreasePct ?? defaultGrowth` (0 respected). Materializer: assumptions-to-pl-lines.ts:323 `const annualInc = (opexLine.annualIncreasePct || 0) / 100` (0 respected) and 790 `(assumptions.subscriptions?.annualGrowthPct ?? 3)` fed from snapshot `state.defaultOpExIncreasePct ?? 3` (useForecastWizard.ts:2164, 0 respected). Worked example: all-fixed OpEx $600k/yr with default increase set to 0%: Step 5 and stored Y3 = $600k; Step 8/summary Y3 = 600k\u00d71.03\u00b2 = $636.5k \u2014 $36.5k phantom cost on screen.", "impact": "A flat-cost plan (common instruction: 'hold costs flat') shows inflated Y2/Y3 opex and understated NP on the review screen and in the saved wizard_state headline, while the stored lines say the opposite. Note setDefaultOpExIncreasePct(0) also rewrites every fixed line's annualIncreasePct to 0 (useForecastWizard.ts:1090-1092), which the summary then coerces back to 3 line-by-line.", "coa_adaptability": false, "dimension": "formulas", "verdict": {"reasoning": "Every cited line verified verbatim. Summary path coerces 0\u21923 twice: useForecastWizard.ts:1740 `const defaultIncrease = state.defaultOpExIncreasePct || 3` and :1820 `line.annualIncreasePct || defaultIncrease` (the setter at :1085-1094 stamps 0 onto every fixed line, which then falls through to the coerced 3); subscriptions rollup at :1775 uses the same coerced value. Step 5 honors 0 (Step5OpEx.tsx:751 `?? 3`, :956 `?? defaultGrowth`). Materializer honors 0 (assumptions-to-pl-lines.ts:323 `|| 0`; :790 `?? 3` fed by snapshot :2164 `?? 3` which preserves 0; snapshot :2089 passes the stamped 0 through). Reachability is stronger than claimed: Step5OpEx.tsx:1362 uses `parseFloat(e.target.value) || 0`, so merely clearing the input sets the default to 0. No guard neutralizes it: the summary-parity check (generate/route.ts:358-362) compares Year 1 only and is watch-mode, while the divergence is Y2/Y3-only (growth factor ^(yearNum-1) = 1 in Y1), so it structurally cannot be caught; the inflated summary is persisted at route.ts:124 (`wizard_state: summary || null`) and Step8Review renders summary.year2/year3 directly. Worked example checks out ($600k flat \u2192 summary Y3 \u2248 $636.5k, stored Y3 = $600k). Not covered by the ALREADY-FIXED list. One wording nit: \"three different answers\" is really two distinct values across three surfaces \u2014 Step 5 display \u2261 stored P&L (honor 0) vs summary/Step 8/wizard_state (coerced 3%) \u2014 which the finder's own evidence body states correctly. Severity P1 is right: wrong Y2/Y3 opex+subscriptions and NP on the review screen and in the saved wizard_state headline, while stored forecast_pl_lines are correct; not P0 because the stored forecast that feeds reports honors the operator's 0.", "verdict": "CONFIRMED", "corrected_severity": "P1"}}, {"id": "FML-06", "title": "COGS y2y3Trend (improves/increases \u00b12%) is applied in the summary only \u2014 not exported, not materialized, lost on reopen", "severity": "P1", "file": "src/app/finances/forecast/components/wizard-v4/useForecastWizard.ts", "line": 2016, "claim": "Step 3 exposes a per-COGS-line Y2/Y3 trend selector; the summary multiplies the Y2/Y3 COGS by (1 \u00b1 2%) but the COGSLineAssumption export has no y2y3Trend field, the exported year2Quarterly is derived from the raw monthly grid without the adjustment, and the materializer never sees it. The trend also does not survive save/reopen (restore rebuilds cogsLines from assumptions).", "evidence": "UI: Step3RevenueCOGS.tsx:2465-2466 `value={line.y2y3Trend || 'same'} onChange={... updateCOGSLine(line.id, { y2y3Trend: ... })}`. Summary: useForecastWizard.ts:1598-1604 `const trendAdj = ... 'improves' ? -2 : 'increases' ? 2 : 0; ... return sum + manualSum * (1 + trendAdj / 100)`. Export: useForecastWizard.ts:2016-2034 COGSLineAssumption mapper has no y2y3Trend and 2030 `year2Quarterly: hasY2 ? monthlyToQuarterly(line.year2Monthly) : undefined` (raw grid). Restore: ForecastWizardV4.tsx:1194-1195 rebuilds cogs lines without the field. Worked example: Dragon-scale COGS ~$5.4M with 'improves': screen Y2 COGS = grid\u00d70.98 (\u2212$108k); stored Y2 COGS = grid \u2014 NP diverges by $108k.", "impact": "For any client where the coach models margin improvement via the trend selector (the designed mechanism for 'GP improves 2pp next year'), the stored forecast and every downstream report ignore the improvement, and the setting evaporates on the next wizard open.", "coa_adaptability": false, "dimension": "formulas", "verdict": {"verdict": "CONFIRMED", "reasoning": "Every element of the claim checks out against the actual code, and no neutralizing guard exists anywhere in the pipeline. (1) UI: Step3RevenueCOGS.tsx:2463-2473 renders the per-COGS-line selector when forecastDuration > 1 ('Y2/Y3: Same %/Improves ~2%/Increases ~2%') and writes y2y3Trend via updateCOGSLine \u2014 reachable in any multi-year forecast. (2) Summary: useForecastWizard.ts:1596-1617 applies trendAdj (\u00b12) on all three COGS paths \u2014 manual monthly grid (`manualSum * (1 + trendAdj/100)`, line 1604), fixed (`monthlyAmount * (1 + trendAdj/100)`, 1607), and variable (`adjustedPct = percentOfRevenue + trendAdj`, 1616) \u2014 for yearNum 2/3 only. A comment there ('P0-5') shows the summary side was deliberately fixed to honor the toggle, deepening the asymmetry. (3) Export: COGSLineAssumption (types/assumptions.ts:58-74) has no y2y3Trend field; the mapper at useForecastWizard.ts:2016-2034 exports raw year2Monthly/year3Monthly grids, raw monthlyToQuarterly conversions, and unadjusted percentOfRevenue. A repo-wide grep confirms y2y3Trend appears ONLY in Step3 UI, types.ts:193, and the summary calc \u2014 never in assumptions, materializer, or API. (4) Materializer: assumptions-to-pl-lines.ts convertCOGS (line 227ff) consumes year2Quarterly (line 257) or falls back to percentOfRevenue \u2014 zero trend references in the file. (5) The divergence is fully silent: the generate route's parity check (generate/route.ts:358-362) compares only summary?.year1 against stored lines and is watch-mode anyway, so a Y2/Y3 approved-vs-stored gap produces no Sentry event and no block. (6) Reopen loss: the only restore path is forecast.assumptions (ForecastWizardV4.tsx:1179-1205 rebuilds COGS lines without the field; grep shows no full-WizardState hydrate), so the selection reverts to 'same' on next open \u2014 the coach doesn't even see a stale-but-visible setting; it evaporates. The worked example is arithmetically right ($5.4M \u00d7 2% = $108k Y2 NP divergence on the monthly-grid path). Not covered by any ALREADY-FIXED item (#350's materializer-fidelity work exported the COGS grids but not this field; #377-379 were different COGS defects). P1 severity is appropriate as claimed: stored Y2/Y3 diverges from the approved on-screen summary, but Y1 (the primary reporting year) is exact, and the defect requires opting into a non-default trend on a multi-year forecast; one could argue P0 under 'wrong numbers in a stored forecast' but the out-year/opt-in scope makes P1 the fair call. Minor nit only: the summary's percent path applies the trend as a flat \u00b12 percentage points while the monthly path applies \u00b12% multiplicative \u2014 an internal inconsistency adjacent to, but not contradicting, the claim."}}, {"id": "FML-07", "title": "CapEx taxonomy math: operating leases charge 12 months every year ignoring start month and term; depreciation never stops at end of useful life", "severity": "P1", "file": "src/app/finances/forecast/components/wizard-v4/types.ts", "line": 578, "claim": "In getBreakdownWithTaxonomy: (a) operating_lease returns monthlyPayment \u00d7 12 for every forecast year \u2014 it ignores both the purchase month (Y1 should charge only 13\u2212month months) and term_months (payments continue after the lease ends); (b) outright_purchase and finance_lease/loan_financing depreciation charges monthsRemaining (12 in Y2/Y3) with no cumulative cap at useful_life_months, so short-life assets depreciate more than their depreciable base; (c) the legacy finance branch charges financeTotalInterest/(financeTerm/12) in every year with no end-of-term clipping.", "evidence": "types.ts:578-583 operating_lease `const exp = Math.round(monthlyPayment * 12); return { depreciation: 0, expenses: exp, total: exp }` \u2014 monthsRemaining (line 564) is computed but unused, termMonths only used as an amount fallback. Depreciation: lines 570-576 and 586-591 `const monthlyDep = depreciableBase / usefulLifeMonths; dep = Math.round(monthlyDep * monthsRemaining)` with monthsRemaining = 12 for yearNum>1, no cumulative clip. Legacy: lines 654-659 `expenses += Math.round(item.financeTotalInterest / (item.financeTerm / 12))` for every yearNum. Worked examples: (1) 24-month operating lease at $2,000/mo starting fiscal month 1 \u2192 true cost $48k over the 3-yr window, computed $72k. (2) laptop fleet $60k, useful_life_months=24, bought M1 \u2192 depreciation booked = 36 \u00d7 ($60k/24) = $90k = 150% of cost. (3) 12-month loan, $6k total interest \u2192 $6k charged in Y1, Y2 AND Y3 = $18k. The taxonomy IS live UI (Step6CapEx.tsx:295-313 writes lease_type/term_months/interest_rate). By contrast the interest helper clips correctly (types.ts:526 `Math.min(termMonths, startMonthIdx + monthsInWindow)`), and this same breakdown feeds BOTH the summary and the materializer (shared helper), so screen and stored agree \u2014 they are consistently wrong.", "impact": "Any business with leases or financed/short-life assets (trades with vehicle/equipment leases \u2014 Dragon, Urban Road class) overstates Y2/Y3 OpEx and depreciation in both the approved summary and the stored forecast; a 2-year vehicle lease inflates the 3-year cost by 50%.", "coa_adaptability": true, "dimension": "formulas", "verdict": {"verdict": "CONFIRMED", "reasoning": "Every quoted line exists as described and no downstream guard neutralizes any of the three defects. (a) operating_lease (types.ts:578-583): `const exp = Math.round(monthlyPayment * 12); return { depreciation: 0, expenses: exp, total: exp }` \u2014 charged for every yearNum; monthsRemaining (computed at line 564 from the fiscal month, confirmed 1=July by the `// 1-12 fiscal month` comment at types.ts:436) is unused in this branch, and termMonths appears only as a fallback divisor for monthlyPayment (line 581), never as an end-of-term stop. A 24-month $2,000/mo lease starting fiscal M1 books $24k in each of Y1/Y2/Y3 = $72k vs true $48k. (b) Depreciation (outright_purchase lines 570-576; finance_lease/loan_financing lines 587-591): `dep = Math.round(monthlyDep * monthsRemaining)` with monthsRemaining=12 for yearNum>1 and no cumulative cap against usefulLifeMonths \u2014 $60k asset with useful_life_months=24 bought M1 books 36 \u00d7 $2,500 = $90k = 150% of depreciable base over 3 years. (c) Legacy finance branch (lines 654-660): `expenses += Math.round(item.financeTotalInterest / (item.financeTerm / 12))` unconditionally per year \u2014 a 12-month/$6k-interest loan charges $6k in Y1, Y2 AND Y3. The comment at 634-637 says legacy is deliberately frozen to match pre-Phase-50 output (regression-locked at $23,200), but the live UI still offers \"Legacy (simplified)\" options (legacy:finance) in the Step6CapEx two-tier select, so new items can enter this path too. Reachability confirmed: Step6CapEx.tsx (~294-331) writes lease_type via the \"New (detailed accounting)\" optgroup, and conditional inputs at lines 404-486 write useful_life_months, term_months, interest_rate, residual_value. Both consumers use the same helper with no clipping: summary rollup useForecastWizard.ts:1897-1903 and materializer assumptions-to-pl-lines.ts:710-737 (buildYearlyLine just spreads depByYear/expByYear into forecast_months; grep found no cap anywhere in services/ or the generate route) \u2014 so screen and stored agree, consistently wrong, exactly as claimed. The contrast also holds: calculateAmortizedInterestForWindow clips correctly at types.ts:526 (`Math.min(termMonths, startMonthIdx + monthsInWindow)`), so the finance-lease INTEREST is right while its depreciation and the operating-lease rent are wrong. Not covered by the ALREADY-FIXED list (PR #350 addressed different materializer issues; #377-379 were COGS). One severity note for the report: under the rubric's letter this arguably rates P0, since the materializer stores the inflated numbers into forecast_pl_lines (Y2/Y3 for the depreciation/interest overruns; even a 1-year stored forecast is wrong for a mid-year operating lease, which books 12 months of rent regardless of start month). P1 is defensible only on the \"degraded for some business shapes\" reading; the filed technical content is fully accurate either way."}}, {"id": "FML-08", "title": "Seasonal OpEx Y2/Y3 computed three different ways (summary vs Step 5 vs materializer); materializer runs one growth-step hot when history exists", "severity": "P1", "file": "src/app/finances/forecast/services/assumptions-to-pl-lines.ts", "line": 349, "claim": "For a seasonal OpEx line: the summary's Y2 = priorYearAnnual \u00d7 (1+g) and ignores seasonalTargetAmount beyond Y1; Step 5's display grows from the Y1 amount (which IS the target); and the materializer, when the existing row has actual_months, sets targetAmount = totalActual \u00d7 (1+growth) \u2014 applying the growth already in Year 1 \u2014 and then multiplies (1+g)^(yearNum\u22121) on top, so stored Y2 = totalActual \u00d7 (1+g)\u00b2. Three different Y2 figures whenever target \u2260 prior or growth \u2260 0.", "evidence": "Materializer: assumptions-to-pl-lines.ts:348-349 `const targetAmount = opexLine.seasonalTargetAmount || totalActual * (1 + growthPct)` then 364-366 `const yearMultiplier = yearNum > 1 ? Math.pow(1 + growthPct, yearNum - 1) : 1; ... round2((targetAmount / 12) * (proportion * 12) * yearMultiplier)` \u2192 Y1 already carries (1+g) when no explicit target. Summary: useForecastWizard.ts:1846-1861 \u2014 Y1 uses seasonalTargetAmount if set, otherwise Y1..Y3 = `priorTotal * Math.pow(1 + growthPct / 100, yearNum - 1)` (Y1 = priorTotal, no growth); seasonalTargetAmount is never used for Y2/Y3. Step 5: Step5OpEx.tsx:973-985 Y2 = getBaseAmount(y1Amount) \u00d7 (1+rate) where y1Amount = seasonalTargetAmount when set (line 909-910). Worked example: prior $100k, target set to $120k, growth 5%: summary Y2 = 100k\u00d71.05 = $105k; Step 5 Y2 = 120k\u00d71.05 = $126k; materializer (no-history fallback path, line 377-385) Y2 = 120k\u00d71.05 = $126k; materializer (history path, target absent) Y1 = totalActual\u00d71.05 instead of totalActual.", "impact": "Seasonal-classified expenses (utilities, insurance, seasonal labour adjuncts) show one Y2/Y3 number on the OpEx step, a different one on Review, and can store a third; when the target override is used the approved summary quietly discards it for the out-years.", "coa_adaptability": true, "dimension": "formulas", "verdict": {"reasoning": "Every quoted line verified byte-for-byte. (1) Materializer assumptions-to-pl-lines.ts:349 `targetAmount = opexLine.seasonalTargetAmount || totalActual * (1 + growthPct)` plus :364-366 `yearMultiplier = pow(1+growthPct, yearNum-1)` applied to targetAmount \u2014 so history-path Y1 without a target = totalActual\u00d7(1+g) and Y2 = totalActual\u00d7(1+g)\u00b2, one growth-step hot vs the summary AND vs the materializer's own no-history branch (:377-385, Y1 = baseAnnual with pow(...,yearNum-1)), whose comment states intent 'Y1 = target (or priorYearTotal), later years compound growth'. (2) Summary useForecastWizard.ts:1846-1861: seasonalTargetAmount used only at yearNum===1; Y2/Y3 = priorTotal\u00d7(1+g)^(yearNum-1) unless y2/y3SeasonalTargetAmount are set \u2014 grep shows NOTHING ever writes those fields (only declared, types.ts:332). (3) Step5OpEx.tsx:909-913 Y1 = target if set else prior\u00d7(1+g); :960-984 Y2 = that base \u00d7(1+g). Three distinct formulas confirmed. No neutralizing guard: assumption export (useForecastWizard.ts:2080-2097) sends only seasonalGrowthPct/seasonalTargetAmount; OpExLineAssumption (types/assumptions.ts:162-181) has no per-year fields, so Step-5 y2Override (honored by summary at 1806-1813) never reaches storage. Summary-parity (generate/route.ts:358-362) checks year1 ONLY and is watch-mode \u2014 Sentry warn, never blocks (:353-374). Reachable: UPDATE-path generate feeds the forecast's own forecast_pl_lines (with synced actual_months) back as existingLines (route.ts:146-152). Not covered by #350/#377-379. Only imprecision: with a target set, Step 5 and the materializer numerically agree (target\u00d7(1+g)) and the summary is the lone outlier \u2014 two figures, as the finder's own worked example correctly shows; a simultaneous three-way numeric split needs the no-target+history case where totalActual \u2260 priorYearAnnual (stored totalActual\u00d7(1+g)\u00b2 vs Step5 prior\u00d7(1+g)\u00b2 vs summary prior\u00d7(1+g)). The headline 'computed three different ways' (three formulas) is exact; impact (Review discards the target for out-years, stored Y2/Y3 diverges from the approved summary unblocked) is real. P1 and coa_adaptability stand.", "verdict": "CONFIRMED"}}, {"id": "PROC-01", "title": "Step 6 OpEx Y2/Y3 overrides and COGS Y2/Y3 trend never reach the stored forecast and are lost on reopen", "severity": "P0", "file": "src/app/finances/forecast/components/wizard-v4/useForecastWizard.ts", "line": 2080, "claim": "The wizard UI lets the operator set per-year OpEx overrides (y2Override/y3Override annual amounts, y2PercentOverride/y3PercentOverride, isTeamCostOverride) and a COGS Y2/Y3 trend (y2y3Trend improves/increases). The on-screen summary honors all of them (useForecastWizard.ts:1808-1830 for OpEx overrides, 1596-1617 for y2y3Trend), so Step 8 Review shows Y2/Y3 numbers that include them \u2014 but buildAssumptions exports none of these fields (opexLineAssumptions mapping at 2083-2097 and COGSLineAssumption mapping at 2016-2034 omit them; OpExLineAssumption in types/assumptions.ts:162-181 has no slot for them), convertOpEx/convertCOGS in assumptions-to-pl-lines.ts (304-439, 227-298) have no code path that reads them, and the restore mapper in ForecastWizardV4.tsx:1215-1244 does not round-trip them. The summary-parity check compares Year 1 only (generate/route.ts:358-362 passes summary.year1 and Y1 month keys), so the Y2/Y3 divergence is invisible even to the watch-mode Sentry invariant.", "evidence": "Step5OpEx.tsx:314 `const overrideKey = activeYear === 2 ? 'y2Override' : 'y3Override';` (live UI writer, also 532, 625, 1765-1826). Summary: useForecastWizard.ts:1808-1812 `if (yearNum === 2 && typeof line.y2Override === 'number') { return sum + line.y2Override; }`. Export: useForecastWizard.ts:2083-2097 maps only accountId/accountName/priorYearTotal/costBehavior/monthlyAmount/annualIncreasePct/percentOfRevenue/seasonalGrowthPct/seasonalTargetAmount/expectedAnnualAmount/expectedMonths/isSubscription/notes \u2014 no y2/y3 fields. Converter: convertOpEx fixed case (assumptions-to-pl-lines.ts:321-330) applies only `Math.pow(1 + annualInc, yearNum - 1)`. COGS trend: Step3RevenueCOGS.tsx:2466 `onChange={(e) => actions.updateCOGSLine(line.id, { y2y3Trend: ... })}`; summary useForecastWizard.ts:1598-1601 applies \u00b12pp; COGSLineAssumption export (2016-2034) has no y2y3Trend. Parity route.ts:359 `checkSummaryParity(summary?.year1, generatedLines, generateFiscalMonthKeys(fiscalYear, ...))`.", "impact": "Any multi-year forecast where the coach dials a specific Y2/Y3 OpEx amount (e.g. \"rent steps to $180k in Y2\") or marks COGS as improving: Step 8 shows the adjusted Y2/Y3 net profit, the operator approves it, but forecast_pl_lines stores the formula-derived numbers instead \u2014 the stored Y2 budget the monthly report will eventually vary against is different from the approved one, and reopening the wizard silently reverts every override to the growth-formula default with no warning.", "coa_adaptability": false, "dimension": "process", "verdict": {"verdict": "PARTIAL", "reasoning": "The P0 core is fully confirmed by direct code reads. (1) Live UI writers: Step5OpEx.tsx:314/532-561 write y2Override/y3Override, :625 writes y2/y3PercentOverride, :1765-1841 wired to updateOpExLine; Step3RevenueCOGS.tsx:2465-2466 writes y2y3Trend. (2) The on-screen summary honors all of them: useForecastWizard.ts:1808-1813 (annual overrides win before formula), :1826-1831 (percent overrides), :1596-1617 (COGS trendAdj \u00b12pp applied to BOTH the manual-grid path \u2014 manualSum*(1+trendAdj/100) \u2014 and the formula path), and Step8Review renders summary.year2/year3 (Step8Review.tsx:392-393, 722-723, 856). (3) buildAssumptions exports none of these: OpEx mapping useForecastWizard.ts:2083-2097 and COGS mapping :2016-2034 omit them; OpExLineAssumption (types/assumptions.ts:162-181) and COGSLineAssumption (:58-74) have no slots; COGS Y2/Y3 grids are exported RAW without the trend multiplier. (4) convertOpEx fixed case applies only Math.pow(1+annualInc, yearNum-1) (assumptions-to-pl-lines.ts:320-330), variable uses flat percentOfRevenue for all years (:333-340); convertCOGS (:227-298) has no trend path. (5) Lost on ANY reopen including drafts: saveDraft persists only buildAssumptions() (useForecastWizard.ts:2269-2270) \u2014 there is no separate wizard-state blob \u2014 and the restore mappers (ForecastWizardV4.tsx:1185-1205, 1215-1244) do not round-trip the fields. (6) Parity is Y1-only: generate/route.ts:358-362 passes summary?.year1 and generateFiscalMonthKeys returns exactly 12 Year-1 keys (fiscal-year-utils.ts:154-167), so the Y2/Y3 divergence never fires the Sentry invariant. Not covered by the already-fixed list (#350 wired COGS monthly grids/team exclusion; the P0-1/P0-5 comments show the summary side was fixed without the export/converter/restore legs). Two details are wrong, hence PARTIAL: (a) isTeamCostOverride is overstated \u2014 it is not exported as a field, but its exclusion effect IS applied at generate time via the shouldExcludeFromOpEx filter (useForecastWizard.ts:2081, helper :84-87), so it reaches the stored forecast in the session where it is set and is only lost on reopen; (b) the title says \"Step 6 OpEx\" but OpEx is Step 5 (Step5OpEx.tsx). The gap additionally covers y2SeasonalTargetAmount/y3SeasonalTargetAmount (summary :1848-1851, never exported), which the finder did not list.", "corrected_claim": "Step 5 (not 6) OpEx per-year overrides (y2Override/y3Override annual amounts, y2PercentOverride/y3PercentOverride, and also y2SeasonalTargetAmount/y3SeasonalTargetAmount) and the Step 3 COGS y2y3Trend toggle are honored by the on-screen summary and Step 8 Review, but are never exported by buildAssumptions, have no slot in the assumption types, are never read by convertOpEx/convertCOGS, and are not round-tripped by the restore mapper \u2014 so the stored Y2/Y3 forecast_pl_lines diverge from the approved on-screen Y2/Y3 numbers whenever any of them is used, every reopen (draft or generated; saveDraft persists only buildAssumptions output) silently reverts them to the growth-formula defaults, and the Y1-only summary-parity check cannot see the divergence. Exception: isTeamCostOverride's exclusion effect DOES reach the stored forecast at generate time via the shouldExcludeFromOpEx export filter; it is only the manual classification that is lost on reopen, which can flip team-cost exclusion on a later re-generate.", "corrected_severity": "P0"}}, {"id": "PROC-02", "title": "Y2/Y3 revenue monthly seasonality is flattened to quarter-averages in the stored P&L", "severity": "P1", "file": "src/app/finances/forecast/services/assumptions-to-pl-lines.ts", "line": 185, "claim": "Step 3 lets the operator edit per-month Y2/Y3 revenue grids (year2Monthly/year3Monthly), and buildAssumptions exports those grids \u2014 but convertRevenue materializes Y2/Y3 exclusively from the quarterly maps (year2Quarterly/year3Quarterly), which expandQuarterlyToMonthly spreads as quarter-total \u00f7 3 per month. The operator's monthly seasonal shape survives on screen (restore reads year2Monthly back at ForecastWizardV4.tsx:1167) but the stored forecast_pl_lines carry a quarter-flat shape for Y2/Y3. Variable COGS, variable OpEx, and commissions all derive from revenueByMonth built from these flattened lines, so their Y2/Y3 monthly shapes flatten too. Annual totals match, so nothing flags it.", "evidence": "Converter: assumptions-to-pl-lines.ts:184-193 `// Year 2 \u2014 expand quarterly\\n if (forecastDuration >= 2 && revLine.year2Quarterly) { ... expandQuarterlyToMonthly(...) }` \u2014 no branch reads revLine.year2Monthly. expandQuarterlyToMonthly (88-108): `result[key] = round2(qValue / 3)`. Export includes both: useForecastWizard.ts:2001-2005 `year2Monthly: hasY2 ? line.year2Monthly : undefined, ... year2Quarterly: hasY2 ? monthlyToQuarterly(line.year2Monthly) : undefined`. UI writer: Step3RevenueCOGS.tsx:529 `const yearKey = activeYear === 1 ? 'year1Monthly' : activeYear === 2 ? 'year2Monthly' : 'year3Monthly';` and 667 `actions.updateRevenueLine(lineId, { year2Monthly: monthly })`.", "impact": "A seasonal business (e.g. Dragon Roofing with a summer peak) planning Y2 month-by-month: the wizard shows Dec at $500k / Feb at $200k, but the stored Y2 budget is ~$350k every month of that quarter. Once FY rolls into year 2 the monthly report's budget-vs-actual variance is wrong every single month even though the annual total is right \u2014 the same defect class as #379, still live for Y2/Y3.", "coa_adaptability": false, "dimension": "process", "verdict": {"reasoning": "Read the actual code at every cited location. convertRevenue (assumptions-to-pl-lines.ts:184-204) materializes Y2/Y3 only from year2Quarterly/year3Quarterly \u2014 no branch reads year2Monthly; a comment in useForecastWizard.ts:1985-1990 explicitly documents this. expandQuarterlyToMonthly (88-108) writes qValue/3 per month, flattening intra-quarter shape. buildAssumptions (useForecastWizard.ts:2001-2005) exports the operator's year2Monthly AND a derived year2Quarterly (monthlyToQuarterly sums per quarter, preserving totals). Step3RevenueCOGS.tsx:650-670 seeds Y2/Y3 monthly grids from prior-year seasonality by default and 523-529 allows per-month Y2/Y3 edits, so seasonal Y2/Y3 shapes exist on essentially every multi-year seasonal forecast, not just manual edits. Restore path (ForecastWizardV4.tsx:1166-1168) reads year2Monthly back, so the screen keeps the seasonal shape while forecast_pl_lines stores quarter-flat months. Downstream: revenueByMonth is built from the flattened converted lines (889-896) and feeds variable COGS (276), variable OpEx (336); commissions expand year2Quarterly directly (561-568); explicit COGS Y2/Y3 grids also flatten via convertCOGS 257-270 (defect slightly broader than claimed). Summary-parity compares category-level annual totals per year at $1 tolerance, and quarter sums are exact, so no guard flags it. Stored lines span Y2/Y3 (generate route.ts:117 sets forecast_end_month across the duration). Not covered by the already-fixed list (#350 exported year1Monthly COGS grids; #379 was Y1 variable COGS). Severity P1 stands: annual/quarter totals exact, distortion is monthly-shape-within-quarter for Y2/Y3 only, typically superseded by annual reset before Y2 goes live \u2014 but any Y2/Y3 monthly budget consumer sees a shape the operator never approved. Only detail correction: linked commissions flatten via the line's own quarterly map rather than revenueByMonth \u2014 same flat outcome.", "verdict": "CONFIRMED"}}, {"id": "PROC-03", "title": "A stale localStorage draft on a second device silently overwrites newer server assumptions on mount", "severity": "P1", "file": "src/app/finances/forecast/components/wizard-v4/ForecastWizardV4.tsx", "line": 136, "claim": "The restore path trusts any localStorage draft that has P&L line data (`hasRestoredData = !startFresh && hasUsablePLLineData(state)`) and then skips loading the server's saved assumptions entirely (they are only read when priorYear is missing, line 470). There is no timestamp comparison between the local draft and the server row. The mount-time refreshes (goals, profile, planPeriod, hydrateDuration, priorYearDisplay, currentYTD, subscriptions fetch) each mutate state, and the autosave effect fires after just three state changes (`stateVersionRef.current < 3` at line 1515), so within ~6 seconds of opening \u2014 with zero user edits \u2014 performAutoSave POSTs the stale draft's buildAssumptions output over the newer server assumptions. The same applies to two tabs in one browser and to coach-vs-client editing: the update path in generate/route.ts (215-244) has no updated_at precondition or version check \u2014 pure last-writer-wins.", "evidence": "ForecastWizardV4.tsx:136 `const hasRestoredData = !startFresh && hasUsablePLLineData(state);` \u2192 line 138-139 'skipping full API initialization'. Line 467-470: server assumptions loaded only `if (priorYearMissing && resolvedId)`. Autosave effect 1507-1545: deps include state.goals, state.subscriptions, state.plannedSpends etc. (all touched by mount refreshes), guard is only `if (stateVersionRef.current < 3) return;`. loadStateFromStorage (useForecastWizard.ts:203-409) checks businessId, fiscalYear, wizardVersion, forecastId owner \u2014 never freshness. Route update path: `.update(updatePayload).eq('id', forecastId)` with no concurrency token.", "impact": "Matt edits a client forecast on his laptop Monday (server assumptions updated); the client opens the same forecast Tuesday on a machine whose localStorage still holds last week's draft \u2014 the week-old revenue/COGS/OpEx state is restored, and the mount-time autosave writes it back over Monday's server draft before anyone types a key. Work loss is silent; the save indicator shows 'Draft saved' in green.", "coa_adaptability": false, "dimension": "process", "verdict": {"verdict": "PARTIAL", "reasoning": "Core mechanism CONFIRMED by direct code read: (1) ForecastWizardV4.tsx:136 restores any localStorage draft with usable P&L line data and skips full init; server assumptions load only when priorYear is missing (line 470), so a stale draft with priorYear present shadows newer server assumptions entirely. (2) loadStateFromStorage (useForecastWizard.ts:203-409) checks businessId/fiscalYear/forecastId-owner/wizardVersion but never freshness \u2014 and saveStateToStorage (line 419) stores no timestamp at all, so no freshness comparison is even possible. (3) generate/route.ts:229-234 updates via .update(updatePayload).eq('id', forecastId) with no updated_at/version precondition; forecastData includes assumptions (line 122) and applyDraftPublishGuard strips only revenue_goal/gross_profit_goal/net_profit_goal/wizard_state/goal_source \u2014 draft autosaves DO overwrite server assumptions, pure last-writer-wins. Coach-vs-client and two-device scenarios are reachable (identity guard at useForecastWizard.ts:226-238 admits the matching-but-stale draft). REFUTED detail: the 'zero user edits within ~6 seconds' trigger. The autosave effect (1507-1520) returns on `if (isLoading) return;` BEFORE incrementing stateVersionRef, so every mount-time refresh the finder cites (goals, duration hydrate, team, priorYearDisplay, currentYTD \u2014 all dispatched inside loadData while isLoading=true) does NOT count toward the 3-change threshold. Post-load, only the isLoading flip (run 1) and possibly a late-resolving subscription-budgets fetch (run 2) occur without user input; debouncedAutoSave is identity-stable (use-debounced-callback.ts:50-60). No demonstrated path reaches version 3 with zero edits \u2014 the stale overwrite fires on the user's first ~2 edits, not spontaneously on mount. Also a scope correction: the draft-publish guard keeps published headline columns and forecast_pl_lines untouched; the silent loss is the newer draft assumptions (work), which only reaches published numbers if someone subsequently hits Generate.", "corrected_claim": "A stale localStorage draft on a second device/browser silently shadows newer server assumptions on mount (restore path skips the server load whenever the draft has priorYear + P&L lines; no freshness check exists and no timestamp is even stored in the draft), and the user's first couple of edits \u2014 not a zero-edit mount autosave \u2014 trigger a full-state draft save that overwrites the newer server assumptions last-writer-wins (no updated_at/version precondition on the update path). Week-old numbers display immediately and the newer draft work is lost silently under a green 'Draft saved'; published numbers and forecast_pl_lines are protected by the draft-publish guard until a user hits Generate on the stale state.", "corrected_severity": "P1"}}, {"id": "PROC-04", "title": "Final Generate is non-atomic: headline published before materialization, so an RPC failure leaves the active forecast claiming numbers its P&L lines don't have", "severity": "P1", "file": "src/app/api/forecast-wizard-v4/generate/route.ts", "line": 227, "claim": "On the update path a final Generate (!isDraft) writes the FULL payload \u2014 wizard_state (the approved summary), revenue/gross/net goals, is_completed=true, completed_at \u2014 in step one (applyDraftPublishGuard returns the payload unchanged when !isDraft), and only afterwards calls save_assumptions_and_materialize. If that RPC fails, the route returns 500 (lines 325-334) without rolling back the earlier update: the forecast row \u2014 typically the currently ACTIVE forecast being regenerated \u2014 now carries a new approved summary, new goals and is_completed=true while forecast_pl_lines remain at the previous Generate. This is precisely the headline/lines divergence draft-publish-guard exists to prevent, reintroduced on the Generate failure path. The Sentry capture makes it loud, but the stored state stays inconsistent until a successful retry.", "evidence": "route.ts:227 `const updatePayload = applyDraftPublishGuard(forecastData, isDraft)` \u2014 draft-publish-guard.ts:42 `if (!isDraft) return { ...payload }` (nothing stripped). Update executes at 229-234; materialize RPC at 316-323; failure branch 325-334 returns 500 with no compensating write. forecastData included `wizard_state: summary`, `revenue_goal`, `is_completed: true` (108-131). Activation (392-406) is correctly last, but the already-active row needs no activation to be live.", "impact": "Regenerating the active forecast when the materialize RPC hits any error (e.g. the 21000 duplicate-code upsert failure the converter's code-dedupe guard exists for): the coach dashboard and completeness checker immediately read the new revenue/net-profit goals and 'completed' status, while the monthly report varies actuals against the previous forecast's lines \u2014 screen and report disagree until someone retries Generate.", "coa_adaptability": false, "dimension": "process", "verdict": {"verdict": "CONFIRMED", "reasoning": "Every element verified against actual code. (1) route.ts:227 applies applyDraftPublishGuard, and draft-publish-guard.ts:42 returns the payload unchanged when !isDraft \u2014 so a final Generate's step-one UPDATE (route.ts:229-234) writes wizard_state, revenue/gross/net goals, goal_source, assumptions, is_completed=true and completed_at (forecastData built at 108-132). (2) The materialize RPC save_assumptions_and_materialize runs afterwards at 316-323 as a SEPARATE statement; its failure branch (325-334) returns 500 with only a Sentry capture and no compensating write; activation (392-406) is never reached, but an already-active row needs no activation \u2014 it stays live with the new headline while forecast_pl_lines remain at the previous Generate. (3) Reachability through the real UI confirmed: page.tsx:699/1020 mount ForecastWizardV4 with existingForecastId; ForecastWizardV4.tsx:67 + handleComplete:1860 call generateForecast(forecastId); useForecastWizard.ts:2313-2328 posts without isDraft/createNew \u2014 the UPDATE path is exactly what a regenerate of the active forecast takes. (4) RPC failure is empirically real: assumptions-to-pl-lines.ts:1002-1024 documents the Postgres 21000 upsert failure this RPC produced in production (the dedupe guard keys on category+account_name, so code-level collisions remain possible), plus P0002/timeouts/transients; the RPC (migration 20260429000003) is internally atomic but there is no cross-statement transaction with the earlier UPDATE. (5) Not covered by the ALREADY-FIXED list \u2014 #350 and the guard fixed the draft path; the guard being a deliberate no-op for !isDraft is what reintroduces headline/lines divergence on the Generate failure path. Additional detail that strengthens the finding: the step-one UPDATE also writes assumptions (route.ts:122), so on RPC failure the stored assumptions no longer correspond to the stored lines either \u2014 even the D-12 assumptions\u2194lines atomicity is undermined on this path. Severity P1 stands: inconsistent stored state visible across surfaces (coach dashboard/completeness read new goals + completed; monthly report varies against old lines) until a successful retry, but failure-path-only, loud in Sentry, and recoverable \u2014 not a silent whole-class P0."}}, {"id": "PROC-05", "title": "July fiscal-year start is hardcoded through the whole persistence chain", "severity": "P1", "file": "src/app/api/forecast-wizard-v4/generate/route.ts", "line": 114, "claim": "The generate route computes actual_start_month/actual_end_month/forecast_start_month/forecast_end_month with literal '-07'/'-06' suffixes, buildAssumptions stamps `fiscalYearStart: '07'` unconditionally, and initializeFromXero carries an explicit code comment admitting non-July FY is unsupported ('NOTE: assumes July FY (fiscal_year_start = 7); non-July FY support is a pre-existing latent issue', useForecastWizard.ts:1278-1279). generateMonthKeys and getFYYear accept a yearStartMonth parameter but every wizard call site uses the default of 7. A business whose Xero financial year does not start in July gets its forecast window, actual-month locking, and year-boundary math keyed to the wrong months.", "evidence": "route.ts:114-117 `actual_start_month: `${fiscalYear - 2}-07`, actual_end_month: `${fiscalYear - 1}-06`, forecast_start_month: `${fiscalYear - 1}-07`, forecast_end_month: `${fiscalYear + (forecastDuration || 1) - 1}-06`,`. useForecastWizard.ts:2174 `fiscalYearStart: '07',`. useForecastWizard.ts:1278 comment quoted above. getFYYear default: assumptions-to-pl-lines.ts:114 `yearStartMonth: number = DEFAULT_YEAR_START_MONTH`.", "impact": "This is the concrete ceiling on Matt's stated concern about adaptability across businesses: any client whose org runs a non-July FY (the IICT Hong Kong org is the live candidate \u2014 HK companies commonly use Apr or Jan year-ends) would get forecast windows offset from its real FY, actuals locked into the wrong months, and Y1/Y2 boundaries mid-year. Today's AU fleet is safe; the first non-July onboarding breaks silently.", "coa_adaptability": true, "dimension": "process", "verdict": {"verdict": "CONFIRMED", "reasoning": "Every quoted code element exists as described: generate/route.ts:114-117 hardcodes '-07'/'-06' month suffixes into actual_start_month/actual_end_month/forecast_start_month/forecast_end_month; useForecastWizard.ts:2174 stamps fiscalYearStart: '07' unconditionally (types/assumptions.ts:454 same default); the admission comment is at useForecastWizard.ts:1279-1280 (one line off the claim, immaterial). No guard neutralizes it \u2014 the forecast page.tsx (lines 294-306) actually reads business_profiles.fiscal_year_start and uses it for page-level FY selection, but never passes it to ForecastWizardV4 (props are businessId/fiscalYear only), so a non-July business would get page-level FY math on its real start month while the wizard and stored forecast key everything to July \u2014 internally inconsistent as well as wrong. All wizard generateMonthKeys call sites are single-argument (default yearStartMonth=7): useForecastWizard.ts:739/830/1267/1476/2102, Step3RevenueCOGS.tsx:333/660. Extra hardcodes beyond the claim: ForecastWizardV4.tsx:1284/1323 (+6 FY-to-calendar month conversion), Step4Team.tsx:2106/3920 (`${fiscalYear-1}-07`), Step2PriorYear.tsx:689 (DEFAULT_YEAR_START_MONTH constant). Empirical check via read-only SQL: all 30 business_profiles rows have fiscal_year_start=7, confirming today's fleet is unaffected; xero_connections has no column recording the Xero org's true financial year end, so a mismatch would be structurally undetectable (supports 'breaks silently'). The IICT-HK non-July FY is plausible but unrecorded/unverified \u2014 the finder hedged this correctly as a candidate, not a fact. Not covered by any ALREADY-FIXED PR. P1 with coa_adaptability=true stands: zero businesses are wrong today, but the first non-July onboarding silently produces a forecast with offset windows, mislocked actual months, and mid-year Y1/Y2 boundaries."}}, {"id": "PROC-06", "title": "Saved assumptions are a filtered, code-stripped OpEx list \u2014 server restore permanently drops team/subscription-covered lines and disarms the double-count guard", "severity": "P1", "file": "src/app/finances/forecast/components/wizard-v4/useForecastWizard.ts", "line": 2080, "claim": "buildAssumptions exports opexLines AFTER filtering out team-cost lines (shouldExcludeFromOpEx) and subscription-covered lines, and the OpExLineAssumption shape carries no accountCode and no isTeamCostOverride. The server restore path then REPLACES state.opexLines with this filtered list (ForecastWizardV4.tsx:1246-1249 setOpExLines runs after initializeFromXero rebuilt the full list from Xero, clobbering it). Consequences after any reopen without a localStorage draft: (a) the wages/super/subscription Xero accounts are gone from Step 6 \u2014 if the operator later clears Step 4 team data or deactivates a vendor, shouldExcludeFromOpEx's hasTeamData=false fallback has no line to un-exclude, so the underlying cost silently vanishes from both buckets; (b) every restored line has accountCode undefined, so the summary's coveredAccountCodes skip (useForecastWizard.ts:1802) and the export filter (2082) can never match a newly-added vendor \u2014 the same account then double-counts in OpEx and Subscriptions with no banner (needsAccountCodeRefresh is only set by the v10\u2192v11 migration, not by this path).", "evidence": "Export filter: useForecastWizard.ts:2080-2082 `.filter(line => !shouldExcludeFromOpEx(line, exportHasTeamData)).filter(line => !(line.accountCode && exportCoveredCodes.has(line.accountCode)))`. Assumption shape: types/assumptions.ts:162-181 (no accountCode field). Restore mapper: ForecastWizardV4.tsx:1229-1244 (no accountCode key) then 1246-1249 `actionsRef.current.setOpExLines(restoredOpExLines);`. Guard: useForecastWizard.ts:1802 `if (line.accountCode && coveredAccountCodes.has(line.accountCode)) return sum;`.", "impact": "A coach reopens a client forecast on a fresh browser: Step 6 no longer lists Wages & Salaries or the Xero software-subscription accounts (confusing but numerically covered), and if they then add a new vendor budget in Step 5 for an account still present in OpEx, the summary and the generated forecast count that spend twice \u2014 the exact double-count Phase 57's accountCode mechanism was built to prevent, disabled by the restore round-trip.", "coa_adaptability": true, "dimension": "process", "verdict": {"verdict": "CONFIRMED", "reasoning": "Every load-bearing claim checks out in the code. (1) Export strips lines and codes: useForecastWizard.ts:2080-2097 builds opexLineAssumptions from state.opexLines with `.filter(line => !shouldExcludeFromOpEx(line, exportHasTeamData))` then `.filter(line => !(line.accountCode && exportCoveredCodes.has(line.accountCode)))`, and the `.map` emits no accountCode/isTeamCostOverride; the OpExLineAssumption type (wizard-v4/types/assumptions.ts:162-181) has neither field. (2) Server restore clobbers the Xero-built list: initializeFromXero DOES attach accountCode (useForecastWizard.ts:1531-1548, from the API row's account_code), but ForecastWizardV4.tsx:1139's setTimeout restore (mapper at 1215-1244, no accountCode key) then calls setOpExLines at 1248, which is a full replacement (useForecastWizard.ts:1096-1101). (3) Trigger is exactly \"reopen without a usable localStorage draft\": the full-init+restore branch is skipped when hasUsablePLLineData(state) (ForecastWizardV4.tsx:136-151), so a same-browser draft keeps its codes; fresh browser/cleared storage/version bump takes the destructive path. (4) Guard disarmed with no banner: the summary skip (useForecastWizard.ts:1802) and export skip (2082) both require line.accountCode; restored lines have undefined; vendors DO carry accountCodes from the live subscription-budgets fetch (useForecastWizard.ts:558), so a newly-added/activated vendor covering a still-present OpEx account double-counts on-screen AND in a generated forecast, and summary-parity holds because both sides double-count identically \u2014 no alarm. needsAccountCodeRefresh=true is set ONLY in loadStateFromStorage under storedVersion<11 (useForecastWizard.ts:301-341); the only other writer is Step5OpEx's manual refresh which sets it false (Step5OpEx.tsx:1254); the banner renders only when the flag is true (Step5OpEx.tsx:1296), so the restore-created legacy-shaped state shows no nudge. (5) Consequence (a) verified: team/subscription-covered lines are physically absent from restored state, so shouldExcludeFromOpEx's hasTeamData=false re-inclusion (useForecastWizard.ts:83-90, the M2 Dragon-Roofing protection) has no line to re-include \u2014 clearing Step 4 or deactivating a vendor then drops the cost from both buckets. The operator's isTeamCostOverride toggle is also lost on the round-trip. Not covered by the ALREADY-FIXED list: PR #350's M2/M6 work is precisely the machinery being disarmed here, not a fix of this restore path. Severity P1 is right rather than P0: reopen+regenerate with no further edits reproduces the same stored numbers (the filtered list plus team/vendor conversion is self-consistent), so stored-forecast wrongness requires a subsequent edit (add/activate vendor, or clear team) \u2014 conditional, but silent and parity-invisible when it happens. coa_adaptability=true is apt: it bites CoAs whose wages/super/software accounts live in OpEx and businesses using Steps 4/5."}}, {"id": "XVAL-1", "title": "Zombie NULL-code team lines double-count wages/super in a live active forecast (Digital Bond FY27: +$274k OpEx)", "severity": "P0", "file": "src/app/finances/forecast/services/assumptions-to-pl-lines.ts", "line": 651, "claim": "When a forecast contains a legacy non-manual line with account_code NULL whose name matches a converter-generated line, Generate permanently forks a duplicate: convertTeam matches the legacy row by name but replaces its NULL code via `account_code: existing?.account_code || tl.code` (line 651), and the save RPC upserts ON CONFLICT (forecast_id, account_code) while ignoring the payload's `id` \u2014 so it INSERTs a fresh SYS-TEAM-* row and never touches the legacy row. The RPC's step 5 then freshens the zombie's computed_at so the D-18 freshness invariant can't flag it, and the summary-parity check runs against `generatedLines` (the payload, generate/route.ts:358) rather than post-upsert DB state, so the divergence is invisible. Digital Bond's ACTIVE FY2027 forecast carries wages twice (245,000 legacy + 163,228 SYS-TEAM-WAGES) and super twice (29,400 + 12,507), while the wizard's own assumptions plan only 3 team members totalling $160,192 salaries and zero wage-named OpEx lines.", "evidence": "Prod query on Digital Bond's active FY2027 forecast lines: [{code:null, name:'Wages & Salaries', created 2026-06-19, updated 2026-08-17 20:40:12, y1:245000}, {code:'SYS-TEAM-WAGES', name:'Wages & Salaries', created 2026-08-17 20:40:12, y1:163228}, {code:null, name:'Superannuation', y1:29400}, {code:'SYS-TEAM-SUPER', y1:12507}] \u2014 SYS rows freshly INSERTED and legacy rows merely re-stamped in the same run (identical v_now timestamps). Assumptions jsonb: team.existingTeam=3 members, sum salaries 160,192, super 12%, and zero opex lines matching wage|salar|super|contractor. Code: assumptions-to-pl-lines.ts:651 `account_code: existing?.account_code || tl.code`; migration 20260429000003 `ON CONFLICT (\"forecast_id\", \"account_code\") WHERE \"is_manual\" = false` (insert SELECT does not use payload id) + step-5 `UPDATE ... SET computed_at = v_now ... WHERE computed_at < v_now`; generate/route.ts:358 `checkSummaryParity(summary?.year1, generatedLines, ...)`.", "impact": "Digital Bond's live active FY2027 stored forecast overstates OpEx by ~$274,400/yr ($245,000 wages + $29,400 super that the wizard never planned) \u2014 every consumer that sums forecast_pl_lines (budget tracker, monthly report budget, cashflow, dashboards) shows wages of $408k against a planned $163k. The same mechanism re-arms on ANY forecast holding a NULL-code non-manual line (Armstrong's active forecast has 2 NULL-code lines, Envisage FY26 has 1): NULL codes bypass the partial unique index, so the upsert can never update or replace them, and any name-collision with a generated line forks a permanent duplicate.", "coa_adaptability": false, "dimension": "empirical", "verdict": {"verdict": "CONFIRMED", "reasoning": "Every load-bearing element verified against actual code and live prod data. (1) assumptions-to-pl-lines.ts:651 reads exactly `account_code: existing?.account_code || tl.code`; findMatchingLine (lines 132-149) matches the legacy row case-insensitively by name, and line 647 carries its `id` into the payload, so the pass-through (lines 996-1000) treats the legacy row as consumed \u2014 yet the payload row's code becomes SYS-TEAM-WAGES because the legacy code is NULL (falsy). (2) Migration 20260429000003: the RPC INSERT column list (lines 102-137) contains no `id`; conflict target is (forecast_id, account_code) WHERE is_manual=false (line 138), and Postgres unique indexes treat NULLs as distinct, so the legacy NULL-code row can never be conflict-matched \u2014 the SYS row INSERTs fresh and the legacy row survives; no delete runs (generate/route.ts:309 deliberately omits p_force_full_replace). The code itself admits the hole at line 1028: \"null codes still insert; they just don't upsert-match\". (3) RPC step 5 (migration lines 159-164) re-stamps the zombie's computed_at/updated_at to v_now, blinding the freshness invariant. (4) generate/route.ts:358-362 runs checkSummaryParity against generatedLines (the payload), never post-upsert DB state, and is watch-mode besides. (5) Prod re-query confirmed Digital Bond active FY2027 forecast a87d33fc-c8d9-48ed-bb3a-e00a837a50f2: NULL-code Wages 244,999.92 + Super 29,400 (created 2026-06-19, computed_at re-stamped 2026-08-17 20:40:12) alongside SYS-TEAM-WAGES 163,228.08 + SYS-TEAM-SUPER 12,507.36 created at that exact instant; all is_manual=false. Assumptions: 3 members, year1Salary sum exactly 163,228, zero wage-like opex lines \u2014 the extra ~$274.4k/yr is unplanned. (6) Fleet re-arm confirmed: Armstrong & Co active FY2027 has NULL-code \"Wages & Salaries\"+\"Superannuation\" rows and no SYS-TEAM rows yet (armed, forks on next Generate); Envisage FY26 has 1. Not covered by ALREADY-FIXED: PR #350 stopped new NULL insertions but left the residue, and line 651 turns a legacy name-match into a permanent fork instead of an adoption. One impact detail is overbroad: the monthly-report budget path (monthly-report/generate/route.ts:408-475) dedupes budget lines by claim-once + name, so it shows one row (possibly the stale 245k \u2014 wrong, but not 408k); raw-summing consumers like quarterly-summary (quarterly-summary/route.ts:152-174) do show the doubled ~408k. This nuance does not change the core claim: wrong numbers stored in a live active forecast, mechanism re-arms on other businesses. P0 stands.", "corrected_claim": "As claimed, with one impact refinement: the monthly report's budget column does not show the $408k sum \u2014 it dedupes budget lines by name/claim-once and shows exactly one of the two rows (which may be the stale, unplanned $245k). Consumers that sum forecast_pl_lines raw (e.g. quarterly-summary) do show the doubled ~$408k wages / ~$41.9k super.", "corrected_severity": "P0"}}, {"id": "XVAL-2", "title": "isTeamCost keyword exclusion silently drops bill-paid contractor OpEx \u2014 Dragon's live FY27 forecast is missing ~$435k/yr Virtual Contractors", "severity": "P0", "file": "src/app/finances/forecast/components/wizard-v4/utils/opex-classifier.ts", "line": 37, "claim": "TEAM_COST_KEYWORDS includes 'contractor'/'subcontractor' (line 37), so once Step 4 has ANY team data, shouldExcludeFromOpEx (useForecastWizard.ts:83-90) auto-excludes every OpEx account whose name contains those substrings \u2014 but Step 4's Xero payroll import only produces payroll employees, so bill-paid contractor spend has no replacement and vanishes from the stored forecast. The PR #350 M2 fix (exclusion requires hasTeamData) only protects the empty-Step-4 case; the moment employees are imported, the original Dragon incident recurs. Dragon Roofing's ACTIVE FY2027 forecast proves it live: prior-year 'Virtual Contractors' ($435,481 since Jul-2025) has NO line in the stored forecast, while the keyword-missed sibling 'Consultants' ($1.15M) was correctly retained \u2014 same economic class, opposite outcome, decided purely by account naming.", "evidence": "Replay of the real isTeamCost() (vitest run against opex-classifier.ts) over all prod OpEx account names since 2025-07 flags as team-excluded: Dragon 'Virtual Contractors' $435,481; Urban Road 'Contractors excl. Artists' $433,340; Digital Bond 'Contractor Expenses (Non Salary)' $134,184 (name literally says NON-salary); Efficient Living 'Employment Expenses: Contractors International' $76,190 and 'Directors Fees' $85,000; Attaquer 'Design Contractor' $41,673; Envisage 'Contractors/Sub Contractors' $32,116. Prod query on Dragon's active FY2027 stored lines matching contractor|wage|salar|super|consult|virtual returns ONLY: Consultants y1=1,055,159; Wages & Salaries y1=363,882; Superannuation y1=43,666; Consulting & Accounting y1=3,328 \u2014 no Virtual Contractors line. Dragon assumptions: team.existingTeam=4 members, salaries 353,284 (payroll staff \u2014 cannot absorb $435k of bill-paid contractors); opex assumption lines contain no contractor-named account. Digital Bond's active forecast likewise has no 'Contractor Expenses (Non Salary)' line.", "impact": "Dragon Roofing's live FY2027 forecast understates costs by roughly $435k/yr \u2014 net profit overstated by the same amount on a $9.94M revenue plan (~4.4% of revenue). Digital Bond's forecast is missing its ~$134k/yr contractor spend. Urban Road ($433k) and Efficient Living ($76k+$85k directors fees) hit the identical trap the day they build a forecast with payroll import. Any CoA that books contract labour through bills (trades, agencies, offshore teams \u2014 at least 6 of 12 client CoAs) loses that cost by default; the only guard is the operator noticing Step 5's exclude pill.", "coa_adaptability": true, "dimension": "empirical", "verdict": {"verdict": "CONFIRMED", "reasoning": "Every link of the claimed chain exists in code and reproduces in prod on two live forecasts. (1) Mechanism: opex-classifier.ts:37 has 'contractor','subcontractor' (and :39 'director fee','directors fee') in TEAM_COST_KEYWORDS; isTeamCost (:403-419) substring-matches after normalization, with overrides only for payroll-tax/workcover (:61-64). shouldExcludeFromOpEx (useForecastWizard.ts:83-90) returns isTeamCost(name) whenever hasTeamData && no explicit operator override. (2) The exclusion reaches the STORED forecast: the assumptions export filters opexLines through shouldExcludeFromOpEx (useForecastWizard.ts:2073, 2080-2081) before building opexLineAssumptions, and the materializer's convertOpEx iterates only assumptions.opex.lines (assumptions-to-pl-lines.ts:310-314) while convertTeam (:445) emits only from Step 4 members/hires \u2014 so a keyword-excluded account with no Step 4 counterpart has zero representation in forecast_pl_lines. (3) Step 4's Xero import maps Payroll AU employees (/api/Xero/employees via xero-payroll-mapping.ts); bill-paid (accounts-payable) contractors never enter Step 4, so nothing replaces the dropped line. The only UI guard is a greyed row labeled \"Counted in Team Costs\" with a \"Team \u2715\" pill (Step5OpEx.tsx:1506-1531) \u2014 a label that is factually false for bill-paid contractors. (4) Prod evidence re-run: Dragon Roofing active FY2027 forecast 7b90633d-466b-46bc-bbfd-fa20b27f98f1 has assumptions.team.existingTeam=4, plannedHires=0 (hasTeamData=true); its 44 stored OpEx assumption lines and forecast_pl_lines contain Consultants $1,055,159, Wages & Salaries $363,882, Superannuation $43,666 (exactly 12% of wages \u2014 convertTeam output), Consulting & Accounting $3,328, and NO Virtual Contractors line in any category \u2014 while xero_pl_lines shows Virtual Contractors (codes 2300/508, both orgs) at $384,003 for FY26 (Jul-2025..Jun-2026) and $511,720 since Apr-2025. No subscription vendor covers codes 2300/508, ruling out the line-2082 coverage filter as an alternative explanation. (5) Replication: Digital Bond's active FY2027 forecast a87d33fc (team=3) stores Wages/Super lines but NO 'Contractor Expenses (Non Salary)' despite $120,322 FY26 actual. (6) Not covered by the already-fixed list: PR #350 M2 only added the hasTeamData condition \u2014 the code comment at useForecastWizard.ts:75-80 documents the ORIGINAL Dragon incident; this is the with-team-data recurrence M2 explicitly does not protect against. Minor numeric corrections (do not change severity): the finder's $435,481/$134,184 figures use a since-Jul-2025-through-Aug-2026 window; the prior-year (FY26) amounts the wizard actually consumed and dropped are ~$384,003 (Dragon, ~3.9% of the $9.94M revenue plan, not 4.4%) and ~$120,322 (Digital Bond). One residual uncertainty: stored data cannot distinguish auto-exclusion from an operator-clicked exclude toggle (the export drops overrides), but the auto path is the default, the identical pattern appears independently on two businesses, and the \"Counted in Team Costs\" label misinforms the operator either way. P0 with coa_adaptability=true stands: wrong numbers in stored active forecasts today (two businesses), and the defect class bites any CoA that books contract labour through bills."}}, {"id": "XVAL-3", "title": "Team-cost keyword false negatives: wage accounts that will DOUBLE-count against Step 4 team lines", "severity": "P1", "file": "src/app/finances/forecast/components/wizard-v4/utils/opex-classifier.ts", "line": 31, "claim": "The inverse defect of XVAL-2: real wage-carrying accounts whose names miss every TEAM_COST_KEYWORD stay in OpEx, so when the operator also imports the payroll team in Step 4, the same people are costed twice (once in the retained OpEx line, once in SYS-TEAM-WAGES). The classifier replay against all 12 production CoAs finds: Efficient Living 'Employment Expenses: Office team' $247,915/yr (no 'team' keyword exists), Sydney Pressed Metal 'Directors Costs - Justin'/'- Michael' $18,508 ('director fee' \u2260 'directors costs'), Dragon 'Staff Bonus and Incentives' $27,347 ('staff cost' \u2260 'staff bonus'; Step 4 models bonuses separately via team.bonuses).", "evidence": "Vitest replay output (isTeamCost=false for all three): 'Employment Expenses: Office team' \u2192 no-match; 'Directors Costs - Justin' \u2192 no keyword hit; 'Staff Bonus and Incentives' \u2192 no-match adhoc. Prod totals from xero_pl_lines since 2025-07: Office team 247,915; Directors Costs 6,784+11,724; Staff Bonus 27,347. TEAM_COST_KEYWORDS (opex-classifier.ts:31-55) contains 'staff cost','director fee','directors fee' but not 'team', 'directors costs', or 'staff bonus'.", "impact": "Efficient Living is the named next forecast candidate (CFO-clients plan). When their forecast is built with payroll import, ~$248k of office-team wages will be counted in BOTH the OpEx line (not excluded) and the Step-4-generated wages line \u2014 overstating OpEx by ~$248k/yr, the mirror image of XVAL-2, on the same business that also loses $161k to XVAL-2's exclusions. Nested/prefixed CoAs ('Employment Expenses: X', 'Employ - X', 'Directors Costs - <name>') are exactly the shapes the flat keyword list cannot see.", "coa_adaptability": true, "dimension": "empirical", "verdict": {"verdict": "CONFIRMED", "reasoning": "Every element verified. (1) Code: TEAM_COST_KEYWORDS (opex-classifier.ts:31-55) lacks 'team', 'directors costs', 'staff bonus', and bare 'employment'; replay via npx tsx importing the real module returns isTeamCost=false for 'Employment Expenses: Office team' (falls through to adhoc via generic 'expense' keyword), 'Directors Costs - Justin/Michael' (adhoc via 'cost'), and 'Staff Bonus and Incentives' (unclassified), while controls 'Wages and Salaries'/'Superannuation'/'Directors Fees' return true. (2) Mechanism: shouldExcludeFromOpEx (useForecastWizard.ts:83-90) excludes solely on isTeamCost(name) once team data exists; it gates the summary rollup (line 1794), the assumptions export feeding Generate (line 2081), Step 5's active list (Step5OpEx.tsx:726-742), and ExcelExport \u2014 so a false-negative wage line stays in OpEx everywhere while convertTeam() emits SYS-TEAM-WAGES/SYS-TEAM-SUPER (assumptions-to-pl-lines.ts:44-45) for the same people. Seeding (useForecastWizard.ts:1531) maps ALL prior-year OpEx lines in with monthlyAmount=total/12, no team filter, so the accounts arrive as live dollars. types.ts:824 confirms bonuses: Bonus[] is modeled in Step 4, validating the Dragon bonus double-count path. (3) Prod SQL re-run (xero_pl_lines, deleted_at IS NULL, >=2025-07): Office team $247,914.78 (10 months), Directors Costs $6,784.12+$11,723.62=$18,507.74, Staff Bonus $27,346.52 \u2014 all match. (4) Not covered by ALREADY-FIXED: PR #350 made exclusion conditional on team data (the vanishing-cost inverse) but did not touch keyword coverage. Nuances that don't overturn: the double-count is contingent on Step 4 covering the same people, and Step 5 has a manual per-line exclude toggle (Step5OpEx.tsx:1841) \u2014 but that is manual compensation for the failed auto-detection. Minor footnote: the Efficient Living $247,915 is a 10-month total (annualized ~$297k), so the '/yr' framing slightly understates exposure. P1 + coa_adaptability=true is the correct rating: stored-forecast overstatement occurs only after a Generate with team data present, and the exposure is specific to nested/prefixed CoA name shapes."}}, {"id": "XVAL-4", "title": "IICT's ACTIVE FY2027 forecast has $20k of revenue against a $5M goal and zero multi-org revenue representation", "severity": "P1", "file": "src/app/finances/forecast/components/wizard-v4/ForecastWizardV4.tsx", "line": 1589, "claim": "IICT Group's is_active=true FY2027 forecast (created 2026-05-22, pre-#350, is_completed=false, wizard_completed_at=null) is materialized with 48 lines whose revenue sums to $20,000 (one 'Other income' line; 'Commissions Received', 'Membership income', 'Membership income GST free' all $0 across 12 months) against revenue_goal $5,000,000 \u2014 and its revenue lines are AU-org accounts only, with no representation of the HK org's dominant revenue (Membership income, 18,376,448 HKD \u2248 the group's largest stream) or the second AU org's $3.67M. Confirms and quantifies the known 'IICT budget=0' gap: an abandoned, incomplete wizard session is the live budget feed.", "evidence": "Prod: financial_forecasts row 88199866-030d\u2026 {is_active:true, is_completed:false, wizard_completed_at:null, revenue_goal:5,000,000, created 2026-05-22}. forecast_pl_lines category='Revenue' for it: revenue-0 Commissions Received fsum=0, revenue-1 Membership income fsum=0, revenue-2 Membership income GST free fsum=0, revenue-3 Other income fsum=20000 (fmonths=12). xero_pl_lines for HK tenant de943481: revenue account 'Membership income' total 18,376,448 (functional_currency HKD, is_active, include_in_consolidation); AU tenant 44582ebf revenue 3,669,631.", "impact": "Every consumer reading IICT's active forecast (consolidated monthly report budget column, budget tracker, dashboards) compares actuals of a ~$5M+ AUD-equivalent group against a $20k budget \u2014 'budget=0' symptoms across the board. Because the row predates #350's draft guards, nothing now prevents it staying active; only regenerating a real forecast fixes it. This is the multi-org/multi-currency CoA shape (3 orgs, one HKD) the wizard demonstrably has not produced a valid forecast for.", "coa_adaptability": true, "dimension": "empirical", "verdict": {"verdict": "CONFIRMED", "reasoning": "Every empirical claim re-verified against prod: financial_forecasts 88199866-030d-4c01-8626-0ab1a4617cc1 (business_profiles 6c0dfadb = IICT Group) is is_active=true, is_completed=false, wizard_completed_at=null, revenue_goal $5,000,000, FY2027, created 2026-05-22 (pre-#350). It has 48 forecast_pl_lines, none deleted; its 4 Revenue lines sum to exactly $20,000 (revenue-3 'Other income' $20k; Commissions Received / Membership income / Membership income GST free all $0 across 12 forecast months). The line set exactly mirrors AU tenant 44582ebf's revenue accounts (even the $20k is that tenant's single Jun-2025 'Other income' entry). Multi-org actuals confirmed: HK tenant de943481 (functional_currency HKD, include_in_consolidation=true) has revenue 'Membership income' 18,376,447.99; AU tenant 44582ebf revenue totals 3,669,630.83. Reachability confirmed in code \u2014 no guard neutralizes it: monthly-report/generate/route.ts:189-204 selects budget by is_active+fiscal_year with NO completion filter, and the 48-line count defeats the zero-line guard at :219; consolidation engine (src/lib/consolidation/engine.ts loadTenantBudgets/loadSingleBusinessBudget) finds zero tenant-scoped forecasts for IICT (both FY2027 rows are tenant_id=null) and falls back to this active 48-line forecast \u2014 the Phase A/B guards only skip 0-line shells, not a 48-line shell with $0 values. Not covered by ALREADY-FIXED: #350 prevents new drafts activating; it did not deactivate this pre-existing row. Minor detail annotations only: the ForecastWizardV4.tsx:1589 anchor is incidental (that line is the sync-forecast fetch, not the mechanism \u2014 the real defect is the missing completion gate on consumers plus the wizard's single-org seeding); and the HK org's revenue account shares the name 'Membership income', so consolidation pairs HK actuals against the $0 forecast line by name rather than omitting it \u2014 dollar impact identical. Overlaps the KNOWN-OPEN 'IICT budget=0' gap but is worse/more specific than believed: it identifies the live mechanism (abandoned incomplete wizard session with materialized lines evades every existing guard), quantifies it ($20k vs ~$5M+ group), and shows only regeneration fixes it. P1 with coa_adaptability=true stands: wrong on-screen budget comparisons for the multi-org/multi-currency shape; not P0 since the stored numbers are abandoned user data rather than a computation writing wrong values, and it is one business (though it is the flagship multi-org shape)."}}, {"id": "XVAL-6", "title": "'Work Cover' (two-word spelling) defeats both the on-cost override and the fixed-pattern routing", "severity": "P2", "file": "src/app/finances/forecast/components/wizard-v4/utils/opex-classifier.ts", "line": 61, "claim": "The statutory on-cost handling recognizes 'workcover', 'workers comp', 'workers compensation' (TEAM_COST_OPEX_OVERRIDE_KEYWORDS line 61-64 and CLASSIFICATION_PATTERNS.fixed line 93-94) but not the spaced spelling 'work cover'. IICT's real account 'Work Cover' ($4,746/yr) matches neither list: replay classifies it adhoc/low-confidence instead of the intended fixed OpEx, so the file-header's design ('routed to the fixed OpEx bucket so they appear in Step 6') fails for this spelling. It stays in OpEx (harmless side), but the deliberate on-cost routing is spelling-fragile \u2014 the same fragility class that produced the workcover hotfix of 2026-05-07.", "evidence": "Replay output: 'IICT Group | Work Cover | 4746' appears in the NO-MATCH (adhoc/low) list, not team-excluded, not fixed. Contrast: 'WorkCover' (JDS $36,002), 'Workers Compensation' (SPM $33,453), 'Insurance Workcover' (Digital Bond) all route correctly (override \u2192 fixed). opex-classifier.ts:61-64 lists only 'workcover','workers comp','workers compensation'.", "impact": "Businesses spelling the account 'Work Cover' (IICT today) get it seeded as ad-hoc annual lump instead of fixed monthly \u2014 wrong monthly phasing, and CV pattern-analysis rescue only fires when prior-year monthly data is present. One-token fix ('work cover') but indicative: every statutory keyword list is exact-substring fragile across CoAs.", "coa_adaptability": true, "dimension": "empirical", "verdict": {"verdict": "PARTIAL", "reasoning": "Core claim CONFIRMED by code and prod data: opex-classifier.ts:61-64 (TEAM_COST_OPEX_OVERRIDE_KEYWORDS) and :93-94 (CLASSIFICATION_PATTERNS.fixed) list only 'workcover'/'workers comp'/'workers compensation' \u2014 never the spaced 'work cover'. normalizeAccountName preserves spaces, so \"Work Cover\" \u2192 \"work cover\" matches no keyword in any list (verified exhaustively, incl. industry overrides and word-boundary short keywords) and falls to adhoc/low at classifyByName:497-502. Prod xero_accounts confirms IICT Group (business_id fbc6dffd, tenant 44582ebf) has a literal 'Work Cover' account (code 500, EXPENSE class), while every other fleet spelling ('WorkCover', 'Workcover', 'Workers Compensation', 'Insurance Workcover' \u2014 and IICT's own second org's 'Insurance: Workers Comp') routes correctly. Reachable via Step5OpEx.tsx:757-815 auto-classify. Not covered by the ALREADY-FIXED list (#350 addressed synthesized materializer lines, not classification keywords). One impact detail is off: \"wrong monthly phasing\" does not hold on the default path. Step5 never seeds expectedMonths, and assumptions-to-pl-lines.ts:390-419 spreads an adhoc line with empty expectedMonths EVENLY across each forecast year \u2014 identical per-month dollars to the fixed route (annual/12). Phasing only diverges if months are later hand-selected. The real default-path harm is a low-confidence 'please review' classification, '/yr adhoc' UI treatment, and loss of the deliberately designed statutory on-cost routing \u2014 friction/robustness, exactly P2. The CV rescue qualifier in the claim is accurate (classifyExpense:598-621, needs \u22653 prior-year months and only suggests fixed when CV<0.15).", "corrected_claim": "The spaced spelling 'work cover' defeats both the on-cost override list (opex-classifier.ts:61-64) and the fixed-pattern routing (:93-94): IICT Group's real Xero account 'Work Cover' (code 500, verified in prod xero_accounts) classifies as adhoc/low-confidence instead of the designed fixed OpEx route, and the same fragility applies to every exact-substring statutory keyword list. However, the stored-forecast numbers are unaffected on the default path: auto-seeded adhoc lines have no expectedMonths, and the materializer (assumptions-to-pl-lines.ts:390-419) spreads the annual amount evenly \u2014 the same monthly dollars as fixed. Actual impact: 'Could not auto-classify \u2014 please review' friction, '/yr adhoc' UI treatment, defeat of the deliberate 2026-05-07 on-cost design, and phasing divergence only if the operator later selects specific months. One-token fix ('work cover' in both lists). coa_adaptability=true.", "corrected_severity": "P2"}}, {"id": "XVAL-7", "title": "48 production accounts (incl. $1.18M Consultancy, $917k Offshore Virtual Assistants) fall through keyword classification to ad-hoc/low-confidence", "severity": "P2", "file": "src/app/finances/forecast/components/wizard-v4/utils/opex-classifier.ts", "line": 496, "claim": "Replaying classifyByName over every active OpEx account name in prod: 48 of ~430 named accounts across the 12 CoAs match no keyword in any bucket and default to adhoc/low-confidence (classifyByName fallthrough, line 496-503). The largest are recurring monthly costs that should be fixed: IICT 'Consultancy' $1,184,213/yr and 'Offshore Virtual Assistants' $917,482/yr (IICT's two biggest OpEx lines), JDS 'Virtual Assistant' $66,188, Envisage 'Offshore Team' $28,856, JDS 'JDS Trade Show Exhibitions' $217,524. Ad-hoc seeding distributes prior-year annual as a lump (getSuggestedValue adhoc \u2192 value=priorYearAnnual '/yr') rather than monthly-fixed, and none of these scale with revenue. The CV pattern-analysis rescue (classifyExpense) only upgrades them when \u22653 months of prior-year monthly data reaches the classifier.", "evidence": "Vitest replay NO-MATCH list (48 rows) includes: IICT 'Consultancy' 1,184,213; IICT 'Offshore Virtual Assistants' 917,482; JDS 'JDS Trade Show Exhibitions' 217,524; Attaquer 'Global-e Service Fee' 134,099; Attaquer 'Warehousing - Storage' 81,772; Attaquer '2024 Strava Challenge Fee' 74,618; JDS 'Virtual Assistant' 66,188; plus 'Accounting' (bare \u2014 'accounting fee'/'accountancy' need the suffix), 'Filing Fees', 'Foreign Currency Gains and Losses' (5 businesses). Behavior distribution over all prod names: 227 adhoc / 92 fixed / 85 variable / 7 seasonal \u2014 adhoc (the fallback bucket) is the majority outcome.", "impact": "For CoAs with idiosyncratic naming (offshore labour, platform fees, event programs \u2014 every business had at least 2 fall-throughs), the wizard's default cost behavior is wrong more often than right, so Step 5's seeded phasing and growth behavior need manual correction per line; businesses without full prior-year monthly data (new onboards) get no CV rescue at all. This is the quantified core of Matt's 'not adaptable across charts of accounts' concern for OpEx: keyword coverage is good for conventional AU SMB names and degrades sharply off that path.", "coa_adaptability": true, "dimension": "empirical", "verdict": {"verdict": "PARTIAL", "reasoning": "CODE VERIFIED: opex-classifier.ts:496-503 fallthrough returns adhoc/low exactly as quoted; getSuggestedValue adhoc \u2192 {value: priorYearAnnual, unit:'/yr'} (:731-732); classifyExpense CV rescue gated on \u22653 monthly keys (:598) and only fires at CV<0.15 or CV>0.50 (:543-551). Step5OpEx.tsx:766 is the real call site and :805-807 seeds expectedAnnualAmount for adhoc lines. EMPIRICAL REPLAY (read-only SELECT on xero_pl_lines joined to business_profiles \u2014 note xero_pl_lines.business_id is profiles-space \u2014 then the ACTUAL classifyByName run via tsx over prod names, window Jul-2025\u2192Aug-2026): 11 businesses, 444 (biz,account) pairs, 32 team-excluded; distribution 228 adhoc / 92 fixed / 85 variable / 7 seasonal (finder claimed 227/92/85/7); 49 no-match (finder claimed 48). Every headline dollar figure matches to the dollar: IICT Consultancy 1,184,213; Offshore Virtual Assistants 917,482; JDS Trade Show Exhibitions 217,524; Global-e Service Fee 134,099; Warehousing - Storage 81,772; 2024 Strava Challenge Fee 74,618; Virtual Assistant 66,188; Offshore Team 28,856; bare 'Accounting' (3 businesses), 'Filing Fees' variants (6), 'Foreign Currency Gains and Losses' (5 businesses) all fall through. CORRECTIONS: (1) The finding UNDERSTATES the rescue gap: priorYearMonthly is never assigned anywhere in src \u2014 both OpEx line constructors (useForecastWizard.ts:798-816 and :1531-1548) build lines without it, and grep shows zero writers \u2014 so classifyExpense always receives undefined and the CV pattern-analysis rescue is dead code for EVERY business, not just new onboards; confirmed at runtime: classifyExpense('Consultancy', undefined) \u2192 adhoc/low. (2) The 'distributes prior-year annual as a lump' mechanism is wrong: assumptions-to-pl-lines.ts:411-418 spreads the adhoc annual EVENLY across each forecast year when no expectedMonths are selected, so Y1 monthlies equal the fixed treatment; the real deltas vs fixed are no Y2/Y3 indexation (fixed compounds annualIncreasePct at :322-327 while adhoc charges the same flat annual every year), no revenue scaling for genuinely variable offshore-labour/platform-fee costs, and a low-confidence flag forcing per-line manual review. (3) Minor: 11 CoAs in the window, not 12; 49 no-match, not 48. Not covered by any ALREADY-FIXED item (those address materializer fidelity, COGS split math, and subscriptions \u2014 not keyword coverage). Severity P2 and coa_adaptability=true both stand: defaults are heuristic and operator-reviewable, but coverage degrades sharply on idiosyncratic CoA naming, which is the quantified core of Matt's adaptability concern.", "corrected_claim": "49 of 444 active prod OpEx (business,account) pairs across 11 CoAs (window Jul-2025\u2192Aug-2026) match no keyword in classifyByName and default to adhoc/low-confidence (opex-classifier.ts:496-503), including IICT 'Consultancy' $1,184,213 and 'Offshore Virtual Assistants' $917,482 (IICT's two biggest OpEx lines), JDS 'JDS Trade Show Exhibitions' $217,524 and 'Virtual Assistant' $66,188, Attaquer 'Global-e Service Fee' $134,099, Envisage 'Offshore Team' $28,856, plus bare 'Accounting', 'Filing Fees', and 'Foreign Currency Gains and Losses' (5 businesses); name-only behavior distribution is 228 adhoc / 92 fixed / 85 variable / 7 seasonal. Worse than originally claimed, the CV pattern-analysis rescue is unreachable for ALL businesses \u2014 priorYearMonthly is never populated on any OpEx line (useForecastWizard.ts:798-816, :1531-1548; no writer anywhere in src), so classifyExpense always runs name-only. Impact correction: misclassified-to-adhoc lines do NOT materialize as a lump \u2014 assumptions-to-pl-lines.ts:411-418 spreads the annual evenly, so Y1 equals the fixed treatment; the real damage is that adhoc lines get no Y2/Y3 indexation (fixed compounds annualIncreasePct) and no revenue scaling, and every fall-through line demands manual per-line review \u2014 degrading Step 5 defaults most for CoAs with idiosyncratic naming (offshore labour, platform fees, event programs).", "corrected_severity": "P2"}}], "refuted": [], "unverified_p2": [{"id": "FML-10", "title": "Step 8 Growth Plan and Step 4 side panels recompute team/OpEx with stale formulas (wrong exponent, wrong increase source)", "severity": "P2", "file": "src/app/finances/forecast/components/wizard-v4/steps/Step8GrowthPlan.tsx", "line": 310, "claim": "Step8GrowthPlan's per-line OpEx panel uses the pre-audit seasonal exponent `^yearNum` (one growth-step hot vs the summary's `^(yearNum-1)`), only honors seasonalTargetAmount for Y1, grows new-hire salaries by the OpEx default increase instead of the per-hire increasePct, and ignores Y2/Y3 overrides and the subscription/team exclusions; Step4Team's 3-year cost panel hardcodes 1.03 for hires; Step5's adhoc Y2/Y3 display applies an increase the summary and materializer never apply.", "evidence": "Step8GrowthPlan.tsx:310 `return priorTotal * Math.pow(1 + gPct / 100, yearNum)` (summary uses yearNum-1 at useForecastWizard.ts:1860 with an explicit audit comment about this exact bug); Step8GrowthPlan.tsx:231 `const salary = hire.salary * Math.pow(1 + defaultIncrease / 100, yearsAfterStart)` where defaultIncrease = state.defaultOpExIncreasePct (line 289) vs summary's per-hire `hire.increasePct ?? 3` (useForecastWizard.ts:1692); Step4Team.tsx:1035 `const salary = hire.salary * Math.pow(1.03, yearsAfterStart)`; Step5OpEx.tsx:986-988 `case 'adhoc': return getBaseAmount() * (1 + getIncreaseRate() / 100)` vs summary's flat expectedAnnualAmount (useForecastWizard.ts:1834-1836).", "impact": "Display-only inconsistencies: the Growth Plan drill-down and Step 4/5 side panels show per-line/per-member numbers that do not sum to the Step 8 Review totals, which erodes trust during coaching sessions but does not change stored numbers.", "coa_adaptability": false, "dimension": "formulas"}, {"id": "FML-11", "title": "Step 8 Review scenario-adjusted net profit omits other income / other expense", "severity": "P2", "file": "src/app/finances/forecast/components/wizard-v4/steps/Step8Review.tsx", "line": 584, "claim": "The what-if scenario recomputation builds netProfit as grossProfit \u2212 teamCosts \u2212 subscriptions \u2212 opex \u2212 depreciation \u2212 investments, dropping the +otherIncome and \u2212xeroOtherExpense terms the canonical summary includes, so toggling any scenario shifts the displayed NP by (otherIncome \u2212 otherExpense) beyond the intended adjustment.", "evidence": "Step8Review.tsx:584-590 `const netProfit = grossProfit - teamCosts - subscriptions - opex - depreciation - investments` \u2014 compare useForecastWizard.ts:1925-1933 which ends `+ xeroOtherIncome - xeroOtherExpense`. JDS's prior-FY other income was $651 (comment at useForecastWizard.ts:1911-1913); businesses with interest income/grants larger than that see a bigger jump.", "impact": "Scenario toggles show a baseline NP that differs from the unadjusted summary NP even with zero-value toggles, by exactly the other-income/expense carry \u2014 confusing during reviews, no stored-data impact.", "coa_adaptability": false, "dimension": "formulas"}, {"id": "FML-13", "title": "Legacy capex materializer path hardcodes 5-year life and full-year Y1 depreciation, contradicting the summary's per-item life and purchase-month proration", "severity": "P2", "file": "src/app/finances/forecast/services/assumptions-to-pl-lines.ts", "line": 744, "claim": "When plannedSpends is empty and legacy capex.items exist, convertCapExDepreciation depreciates every item over a hardcoded 5 years (`item.amount / usefulLifeYears / 12` with usefulLifeYears = 5) flat across all months including months before purchase, while the summary uses item.annualDepreciation (= cost / item.usefulLifeYears) prorated by purchase month in Y1.", "evidence": "assumptions-to-pl-lines.ts:743-745 `const usefulLifeYears = 5; totalMonthlyDepreciation += round2(item.amount / usefulLifeYears / 12)` vs useForecastWizard.ts:1872-1878 `const monthsRemaining = 13 - item.month; return sum + (item.annualDepreciation * monthsRemaining) / 12`. Also the export converts capex month via generateMonthKeys(state.fiscalYearStart + 1) (useForecastWizard.ts:2102) \u2014 Year-2 keys \u2014 though the materializer's legacy path ignores month anyway.", "impact": "Only forecasts restored from legacy capexItems (no plannedSpends) \u2014 a 3-year-life item stores 40% less depreciation than approved, and a March purchase stores depreciation from July.", "coa_adaptability": false, "dimension": "formulas"}, {"id": "PROC-07", "title": "Commission timing reverts to 'monthly' on every restore", "severity": "P2", "file": "src/app/finances/forecast/components/wizard-v4/ForecastWizardV4.tsx", "line": 1379, "claim": "buildAssumptions round-trips commission.timing (useForecastWizard.ts:2213), and the converter genuinely branches on it \u2014 quarterly/annual commissions accrue and pay in the period's last month (assumptions-to-pl-lines.ts:591-613) \u2014 but the restore path hardcodes `timing: 'monthly'` when re-adding commissions. A saved quarterly or annual commission becomes monthly after one reopen; the next Generate stores a different monthly distribution (annual totals unchanged).", "evidence": "ForecastWizardV4.tsx:1375-1380 `actionsRef.current.addCommission({ teamMemberId: memberId, revenueLineId: commission.revenueLineId, percentOfRevenue: commission.percentOfRevenue, timing: 'monthly', });` \u2014 note the restorer's type annotation (1362-1368) doesn't even declare a timing field on the saved shape, though buildAssumptions writes it (2207-2213 `timing: c.timing`).", "impact": "A business paying sales commissions quarterly: after any reopen-and-regenerate cycle, the stored wages line spreads commission evenly across all 12 months instead of spiking at quarter-ends \u2014 monthly wage variance in the report shifts by the commission amount in 8 of 12 months.", "coa_adaptability": false, "dimension": "process"}, {"id": "PROC-08", "title": "defaultOpExIncreasePct is not persisted \u2014 Y2/Y3 growth silently resets to 3% on reopen", "severity": "P2", "file": "src/app/finances/forecast/components/wizard-v4/useForecastWizard.ts", "line": 2164, "claim": "state.defaultOpExIncreasePct drives subscriptions Y2/Y3 growth (line 1775-1776), unclassified-line growth (1866), and the annualIncreasePct default applied to fixed lines \u2014 but it is only exported indirectly (per-line annualIncreasePct, and snapshotted into subscriptions.annualGrowthPct at 2164). The value itself is never restored: after a server restore the state reverts to the createInitialState default of 3, so the summary's subscription growth and any later-added line's default no longer match what was approved, and the converter (which reads assumptions.subscriptions.annualGrowthPct frozen at the LAST save) can disagree with the current on-screen summary until the next save.", "evidence": "createInitialState: useForecastWizard.ts:179 `defaultOpExIncreasePct: 3`. No restore call site sets it (grep over ForecastWizardV4.tsx restore chain: goals/revenue/cogs/opex/plannedSpends only, lines 1139-1266). Summary: 1775 `const subscriptionGrowthFactor = Math.pow(1 + defaultIncrease / 100, yearNum - 1)`. Snapshot: 2164 `annualGrowthPct: state.defaultOpExIncreasePct ?? 3`.", "impact": "An operator who set 5% OpEx growth reopens the forecast: Step 6's default reads 3%, Y2/Y3 subscription totals on Review shrink versus what was approved, and the shrunken value gets written back on the next autosave \u2014 a slow, silent drift of multi-year assumptions toward the default.", "coa_adaptability": false, "dimension": "process"}, {"id": "PROC-09", "title": "Generate route never verifies the posted forecastId belongs to the posted businessId", "severity": "P2", "file": "src/app/api/forecast-wizard-v4/generate/route.ts", "line": 229, "claim": "The route checks the caller's access to businessId, resolves profileId, then updates the row matched only by `.eq('id', forecastId)` with a payload whose business_id is the resolved profileId of the POSTED business. Nothing asserts the forecast row's existing business_id matches. For a coach/admin session (RLS-permitted across clients), any client-side state bug that pairs business A's id with business B's forecastId would silently RE-HOME business B's forecast into business A \u2014 assumptions, goals and fiscal window overwritten \u2014 and the subsequent activation would deactivate business B's real active forecast for that FY. Given dual-ID/state-staleness is this codebase's #1 recurring incident class, the missing ownership assertion is the one cheap invariant that would turn that class of bug into a 403 instead of silent cross-client data corruption.", "evidence": "route.ts:103-104 `const ids = await resolveBusinessProfileIds(supabase, businessId); const profileId = ids.profileId` \u2192 108-109 `business_id: profileId` in forecastData \u2192 229-234 `.update(updatePayload).eq('id', forecastId)` with no `.eq('business_id', profileId)` and no pre-read of the row's business_id. Contrast recompute/route.ts, which loads the row and derives access from the ROW's business_id.", "impact": "No live incident traced, but the failure mode is a coach switching clients mid-session with a stale wizard state: one autosave rewrites another client's forecast row to the wrong business. A one-line `.eq('business_id', profileId)` on the update (and a row-ownership check before the RPC create path) closes it.", "coa_adaptability": false, "dimension": "process"}, {"id": "PROC-10", "title": "The recompute recovery hatch re-materializes from assumptions that draft autosaves have since overwritten", "severity": "P2", "file": "src/app/api/forecast/[id]/recompute/route.ts", "line": 1, "claim": "Draft autosaves on the update path deliberately keep writing `assumptions` (draft-publish-guard strips wizard_state/goals but not assumptions \u2014 'a draft update saves WORK'). The recompute route re-derives forecast_pl_lines from the CURRENT assumptions column ('Re-derives forecast_pl_lines from the CURRENT financial_forecasts.assumptions row', header comment). So after any mid-edit autosave, running the documented recovery hatch on the active forecast publishes half-edited draft assumptions into the live P&L lines while wizard_state still carries the previously approved summary \u2014 the exact drafts-never-materialize invariant of #350, violated through the side door the remediation runbook points at.", "evidence": "recompute/route.ts header lines 1-21: 'Re-derives forecast_pl_lines from the CURRENT financial_forecasts.assumptions row and writes via the atomic save_assumptions_and_materialize RPC.' Draft path writes assumptions: generate/route.ts:215-234 with draft-publish-guard.ts:23-32 listing only revenue_goal/gross_profit_goal/net_profit_goal/wizard_state/goal_source as stripped.", "impact": "The D-18 staleness invariant will legitimately flag forecasts whose updated_at moved past computed_at (which every draft autosave causes) \u2014 and the prescribed fix, recompute, then installs the draft's numbers as the published budget. Guard: recompute should refuse (or warn) when the row's updated_at postdates completed_at, or drafts should write assumptions to a separate column.", "coa_adaptability": false, "dimension": "process"}, {"id": "PROC-11", "title": "In-progress draft rows become unreachable when the wizard reopens without an explicit forecast id", "severity": "P2", "file": "src/app/finances/forecast/components/wizard-v4/useForecastWizard.ts", "line": 226, "claim": "The draft identity guard discards a localStorage draft whenever its owner id differs from the requested id \u2014 including requested=null. A session that started fresh gets a server draft row (is_active=false) via autosave and stamps its id on the local draft; if the wizard later reopens through an entry point that passes existingForecastId=null (forecast/page.tsx passes selectedForecastId which resets to null on completion and on several paths), the local draft is discarded (`owner !== requested` with requested null), and the mount path auto-discovers `versions.find(is_active) || versions[0]` \u2014 the ACTIVE forecast, not the draft. The operator's in-progress draft still exists server-side but nothing resurfaces it; the wizard silently opens a different forecast.", "evidence": "useForecastWizard.ts:226-238: `const requested = expectedForecastId ?? null; const owner = parsed?.forecastId; ... if (parsed && (ownerKnown ? owner !== requested : requested !== null)) { console.warn('[ForecastWizard] Discarding draft belonging to a different forecast'...); return null; }`. Auto-discovery: ForecastWizardV4.tsx:453 `const activeForecast = (data.versions || []).find((v) => v.is_active) || (data.versions || [])[0];`. page.tsx:679/705/719 `setSelectedForecastId(null)`.", "impact": "A client builds half a forecast, closes the tab, and comes back via the generic Forecast page: the wizard opens showing last quarter's active forecast instead of their half-built draft, which looks like total work loss unless they find the draft row in the version selector. Compounded by the single localStorage slot per (business, FY) shared across all scenarios.", "coa_adaptability": false, "dimension": "process"}, {"id": "XVAL-5", "title": "Stored forecast lines carry synthetic account codes ('revenue-0', 'ACCT-MISSING-<uuid>', client-random ids) \u2014 per-line budget\u2194actual joins on account_code cannot match", "severity": "P2", "file": "src/app/finances/forecast/services/assumptions-to-pl-lines.ts", "line": 209, "claim": "convertRevenue/convertCOGS/convertOpEx store the wizard's in-memory line id as account_code when no existing code is found (`account_code: existing?.account_code || revLine.accountId`, lines 209/286/427). In production this yields codes like 'revenue-0'\u2026'revenue-17' (JDS, Dragon, IICT, Digital Bond \u2014 these ids come from Step 2's `revenue-${idx}` seeding), timestamp-random ids like '1781828347010-cvgvmt63m' (Digital Bond, from a client-side id generator), and 'ACCT-MISSING-<uuid>' (Envisage FY26, from the D-44.1-05 backfill). ForecastReadService's own doc (forecast-read-service.ts:512-515) warns that consumers keying on account_code lose fidelity; any budget-vs-actual join on account_code against real Xero codes silently matches nothing for these lines, and the codes are also index-position-dependent ('revenue-3' means a different account per business and per reseed).", "evidence": "Prod query of active-forecast Revenue lines: JDS has revenue-0\u2026revenue-17 as account_code for 18 lines; Digital Bond has 'revenue-0' and '1781828347010-cvgvmt63m'; IICT revenue-0\u20263; Envisage FY2026 has ACCT-MISSING-cc31d5fa\u2026, while Envisage FY2027 has real codes (201/208/210/200) \u2014 same wizard, different seeding path, different code fidelity. Code: assumptions-to-pl-lines.ts:209 `account_code: existing?.account_code || revLine.accountId`.", "impact": "Any current or future consumer that matches stored budget lines to Xero actuals by account_code (monthly-report auto-map, per-line variance features) gets no match for most of JDS/Dragon/IICT/Digital Bond's revenue lines and falls back to name matching or shows blank budgets; a Step-2 reseed that reorders accounts silently re-labels which real account 'revenue-3' refers to. Also creates the NULL/synthetic-code substrate that XVAL-1 exploits.", "coa_adaptability": false, "dimension": "empirical"}, {"id": "XVAL-8", "title": "xero_accounts hygiene: Envisage's full CoA duplicated under tenant_id NULL; stale IICT tenant copies under 'Wisdom BI'; duplicate/blank account codes in 8 of 14 tenants", "severity": "P2", "file": "src/app/api/Xero/chart-of-accounts-full/route.ts", "line": 1, "claim": "The xero_accounts mirror contains: (a) Envisage's 148 accounts stored twice \u2014 once with tenant_id '04d9df1f\u2026' and once with tenant_id NULL \u2014 so any consumer querying xero_accounts by business_id without a tenant filter sees every Envisage account doubled; (b) the 'Wisdom BI' business carries copies of all three IICT tenants' account sets (same tenant_ids, is_active=false connections); (c) blank account codes in 10 tenants (up to 12 blank in IICT Pty Ltd) and duplicate codes in 8 tenants. The wizard's live paths (chart-of-accounts route fetches from Xero per connection; P&L flows read xero_pl_lines) dodge most of this, but xero_accounts-backed features (subscription account mapping, D8 account lists) inherit it.", "evidence": "Prod query grouping xero_accounts by business/tenant: Envisage rows {tenant '04d9df1f\u2026': 148 accounts} and {tenant null: 148 accounts, same rev/exp/directcosts counts 17/72/1}; Wisdom BI rows for tenants 1d83c9a4/44582ebf/de943481 (88/135/63 accounts) duplicating IICT Group's (94/138/67); blank_codes=12 and dup_codes=11 on IICT tenant 44582ebf; blank_codes=10/dup_codes=9 on Envisage.", "impact": "Any feature that joins or lists xero_accounts keyed only on business_id double-lists Envisage's chart (two rows per account, one with NULL tenant) and can attribute IICT accounts to Wisdom BI; account_code being blank/duplicated in 8 tenants means code-keyed dedupe guards (subscription double-count guard keys on accountCode) are structurally unreliable for those tenants.", "coa_adaptability": false, "dimension": "empirical"}], "overflow_unverified": [{"id": "COA-09", "title": "Auto-import fabricates $80,000 salaries for employees whose Xero pay data is underivable", "severity": "P2", "file": "src/app/finances/forecast/components/wizard-v4/ForecastWizardV4.tsx", "line": 272, "claim": "The first-load payroll auto-import falls back to a hard-coded $80,000 annual salary when neither annual_salary nor hourly_rate is derivable (`if (!salary) salary = 80000;`, ForecastWizardV4.tsx:267-272), and these members are stamped isFromXero \u2014 presented to the operator as Xero-sourced data. Tenants on timesheet-driven ENTEREARNINGSRATE payroll with no posted pay runs (new orgs, mid-migration) can have several such employees; convertTeam materializes the invented salaries into the stored wages line unless the operator hand-corrects each one in Step 4.", "evidence": "ForecastWizardV4.tsx:267-272: `let salary = emp.annual_salary || 0; if (!salary && emp.hourly_rate) { ... } if (!salary) salary = 80000;` followed by `isFromXero: emp.from_xero ?? true`.", "impact": "A 6-person tenant with unpopulated pay templates gets $480k/yr of invented wages seeded and labeled as from Xero \u2014 plausible-looking enough that a coach skims past it, and it lands verbatim in the materialized forecast.", "coa_adaptability": true, "dimension": "coa-adaptability"}, {"id": "COA-10", "title": "FOURWEEKLY and QUARTERLY pay calendars collapse to 'monthly', skewing derived annual salaries", "severity": "P2", "file": "src/app/finances/forecast/components/wizard-v4/utils/xero-payroll-mapping.ts", "line": 117, "claim": "mapXeroPayrollCalendarToFrequency maps FOURWEEKLY/TWICEMONTHLY/QUARTERLY to 'monthly' (xero-payroll-mapping.ts:126-130), and getDerivedAnnualSalary then multiplies the per-period hours by ANNUAL_PAY_PERIODS.monthly = 12 (:42-46, 60-69). A FOURWEEKLY hourly employee (13 real periods/yr) is annualized at 12 periods \u2192 wages understated ~7.7%; a QUARTERLY employee's per-quarter standardHours \u00d7 12 overstates 3\u00d7. The PayRun-history path has correct per-calendar factors (WEEKS_PER_PERIOD/PERIODS_PER_YEAR maps :275-291) but the PayTemplate-derived display/import path does not.", "evidence": "xero-payroll-mapping.ts:127-130: `case 'FOURWEEKLY': case 'TWICEMONTHLY': case 'MONTHLY': case 'QUARTERLY': return 'monthly';` vs :284-291 where the same calendars correctly get 13/24/12/4 periods.", "impact": "Any tenant running a four-weekly pay calendar (not rare in AU trades) imports every hourly employee at ~92% of true wages; the gap flows straight into the materialized 'Wages & Salaries' line. Self-documented as an approximation, but it silently changes stored numbers per business shape.", "coa_adaptability": true, "dimension": "coa-adaptability"}, {"id": "COA-11", "title": "Subscription account picker cannot offer code-less Xero accounts, and the keyword pre-filter hides non-obvious subscription accounts", "severity": "P2", "file": "src/app/api/Xero/chart-of-accounts/route.ts", "line": 214, "claim": "The chart-of-accounts route unions orgs by account Code and explicitly skips accounts with a blank Code (`if (!code) continue;`, route.ts:212-214) \u2014 Xero permits code-less accounts, and such an account can never be mapped to a subscription vendor (its spend then always double-counts past the accountCode-keyed guard). The default 'subscription' filter additionally hides accounts unless their NAME matches SUBSCRIPTION_ACCOUNT_KEYWORDS (:34-49) and dodges EXCLUDE_KEYWORDS (:52-70) \u2014 a chart holding SaaS spend in 'Dues & Memberships' or 'Technology Costs' shows an empty picker (the known-open D8 empty-state, confirmed still present).", "evidence": "route.ts:213-214: `// A code-less account cannot be selected or matched downstream. if (!code) continue;` and :245-247: `if (filterType === 'subscription') { accounts = accounts.filter((acc: any) => acc.isSuggested); }`.", "impact": "A client chart without account codes (Xero does not require them) silently loses the entire Step 6 subscription-mapping and double-count-prevention machinery; a chart with unconventional names sees an empty account list and a vendor budget that double-counts against its uncovered OpEx line.", "coa_adaptability": true, "dimension": "coa-adaptability"}, {"id": "FML-09", "title": "Summary COGS ignores the cost-behavior formula whenever any partial year1Monthly exists (setPriorYear seeds actual months only)", "severity": "P1", "file": "src/app/finances/forecast/components/wizard-v4/useForecastWizard.ts", "line": 1602, "claim": "calculateYearSummary treats a COGS line's monthly map as authoritative whenever it has \u22651 key and skips the %-of-revenue formula entirely \u2014 but setPriorYear seeds cogs year1Monthly with ONLY the completed actual months. Until Step 3's redistribution runs and fills the full grid, the on-screen COGS is just the YTD actual months while the materializer resolves per-month (explicit where present, formula elsewhere) and stores a full year.", "evidence": "Summary: useForecastWizard.ts:1602-1604 `if (monthly && Object.keys(monthly).length > 0) { const manualSum = Object.values(monthly).reduce(...); return sum + manualSum * (1 + trendAdj / 100) }` \u2014 no formula fallback for the missing months. Seeding: useForecastWizard.ts:770-774 (setPriorYear) `const year1Monthly: Record<string, number> = {}; ... for (const key of completedKeys) { year1Monthly[key] = Math.round(ytd[key] ?? 0) }` \u2014 actual months only. Materializer: assumptions-to-pl-lines.ts:272-281 resolves per month (explicitMonths[mk] wins, else percentOfRevenue formula). Worked example: business seeded via setPriorYear in August (1 completed month, July COGS $40k, 55% variable on $6M): screen Y1 COGS \u2248 $40k (GP ~99%), stored Y1 COGS \u2248 $40k + 11 months of formula \u2248 $3.0M.", "impact": "Conditional: bites the setPriorYear entry paths (CSV/parse confirm, saved-assumptions fallback rebuild) when currentYTD exists and the operator generates without revisiting Step 3 (where the GP-target cascade rewrites full grids). When it fires, the review screen shows a wildly inflated GP% against a correctly-stored P&L.", "coa_adaptability": true, "dimension": "formulas"}, {"id": "FML-12", "title": "Legacy engines (coach forecast page / PLForecastTable, not wizard-v4): YoY growth zeroes months beyond a partial baseline; fortnightly pay periods overcounted; Sunday payday maps to Friday", "severity": "P2", "file": "src/app/finances/forecast/services/forecasting-engine.ts", "line": 204, "claim": "(a) applyGrowthRate YoY: for forecast month index i \u2265 actualMonthKeys.length it reads forecast[forecastMonthKeys[i\u221212]], which is undefined for i<12, so with a partial baseline (e.g. 6 actual months) months 7-12 of the forecast become 0. (b) PayrollCalculator.calculatePayPeriodsInMonth returns ceil(paydayCount/2) for fortnightly \u2014 every 5-payday month yields 3 fortnights, ~28 periods/yr instead of 26 (+7.7% wage months). (c) getPayDayIndex uses `days[payDay] || 5`, so 'sunday' (index 0, falsy) becomes Friday.", "evidence": "forecasting-engine.ts:202-208 `const priorYearValue = index < actualMonthKeys.length ? (line.actual_months[actualMonthKeys[index]] || 0) : (forecast[forecastMonthKeys[index - 12]] || 0)` \u2014 index\u221212 is negative for index in [actualMonthKeys.length, 12). payroll-calculator.ts:187 `return frequency === 'fortnightly' ? Math.ceil(count / 2) : count`; payroll-calculator.ts:203 `return days[payDay.toLowerCase()] || 5`. Usage confirmed outside wizard-v4 only: PLForecastTable.tsx, coach/clients/[id]/forecast/page.tsx, PayrollTable.tsx.", "impact": "Only the legacy coach forecast page/tables hit these; a coach forecasting from a mid-year baseline with YoY growth gets a forecast whose back half is $0, and fortnightly payroll displays overstate ~8% in 5-payday months.", "coa_adaptability": true, "dimension": "formulas"}, {"id": "FML-14", "title": "July fiscal-year start hardcoded through the generate pipeline \u2014 non-July-FY businesses cannot be represented correctly", "severity": "P2", "file": "src/app/api/forecast-wizard-v4/generate/route.ts", "line": 114, "claim": "The generate route derives forecast_start/end as `${fiscalYear-1}-07` / `${fiscalYear+n-1}-06`, buildAssumptions stamps fiscalYearStart '07', and initializeFromXero carries an explicit 'assumes July FY' note \u2014 a business whose Xero org has a non-July financial year (e.g. the HK org's calendar/Dec year-end, or any Jan-start client) gets its months bucketed into the wrong fiscal years throughout the wizard, summary, and stored lines.", "evidence": "route.ts:114-117 `actual_start_month: `${fiscalYear - 2}-07`, ... forecast_end_month: `${fiscalYear + (forecastDuration || 1) - 1}-06``; useForecastWizard.ts:2174 `fiscalYearStart: '07'`; useForecastWizard.ts:1278-1279 comment 'NOTE: assumes July FY (fiscal_year_start = 7); non-July FY support is a pre-existing latent issue tracked separately (see 44.3 PLAN-CHECK \u00a7C)'. The internal math is at least self-consistent (getFYYear, generateMonthKeys, summary all use DEFAULT_YEAR_START_MONTH = 7), so numbers agree with each other \u2014 they just describe the wrong fiscal window for such a business.", "impact": "Directly answers Matt's adaptability concern at the fiscal-calendar level: the wizard is AU-July-FY-only by construction. Acknowledged in code comments as deferred; listed so the assessment records it as a live constraint, not a bug fixed elsewhere.", "coa_adaptability": true, "dimension": "formulas"}], "solid": {"coa-adaptability": ["P&L account_type assignment is layout-independent and correctly prioritized: sync-orchestrator.ts:863-886 classifies from the xero_accounts catalog's xero_type first (classifyByXeroType covers SALES/REVENUE/OTHERINCOME/DIRECTCOSTS/OVERHEADS/EXPENSE/DEPRECIATN/SUPERANNUATIONEXPENSE/WAGESEXPENSE/OTHEREXPENSE, accounts-catalog.ts:55-77) with the section parser only as fallback \u2014 a standard-layout chart whose accountant typed COGS as EXPENSE renders the same GP the client sees in Xero itself.", "Multi-org businesses (Dragon 2 orgs, IICT 3 orgs) are handled correctly in the prior-year aggregation: aggregatePeriod merges same-named accounts across tenants summing totals and per-month values (historical-pl-summary.ts:586-621, the documented fix for the drop/overwrite bug), and multi-currency businesses are routed through the FX consolidation engine (needsFxConsolidation gate at :80-195) so HKD is never summed raw into AUD; getFiscalYearActuals is genuinely fail-closed on any FX ambiguity (:364-382).", "The chart-of-accounts route unions ALL active orgs' charts concurrently with per-org failure warnings (route.ts:113-262) \u2014 a dead org degrades to a labeled partial list instead of blanking the picker.", "resolvePriorYearSecondary (resolve-prior-year-secondaries.ts) is correct on every branch: non-zero API wins, zero API preserves non-zero cache, cold-mount trusts Xero including a real 0 \u2014 verified against all four input combinations.", "The statutory on-cost carve-out is correctly ordered: TEAM_COST_OPEX_OVERRIDE_KEYWORDS ('payroll tax', 'payroll levy', 'workcover', 'workers comp/compensation') is checked BEFORE the team-keyword scan (opex-classifier.ts:403-419), and the \\bsuper\\b word-boundary guard prevents 'Superior\u2026' false positives. Superannuation defaults to the current statutory 12% (converter :617 and team.superannuationPct || 12).", "Materializer integrity guards verified present and correct: per-payload unique account_code enforcement nulls later duplicates instead of 500ing (assumptions-to-pl-lines.ts:1029-1038); RETIRED_LINE_NAMES resurrection filter only matches synthetic/SYS-coded rows so real client accounts literally named 'Payroll Tax'/'WorkCover Insurance' (JDS carries both) survive (:982-994); planned-spend depreciation uses a distinct name to avoid colliding with a real Xero 'Depreciation' account (:722-735).", "convertCOGS per-month resolution (PR-A M1) is intact: explicit operator grid (incl. locked actuals) wins per key, Y2/Y3 quarterly next, cost-behavior formula only for untouched months, and variable COGS follows forecast revenue by month (assumptions-to-pl-lines.ts:251-281) \u2014 no regression of the #377-379 fixes observed.", "Step 2 actuals-lock on re-seed is intact: setPriorYear overwrites completed months from currentYTD by account name for both revenue and COGS lines (useForecastWizard.ts:709-784), preserving the July-actuals-lock invariant on destructive refresh.", "spreadsheet-reader.ts is robust: RFC-4180 CSV (quoted fields, CRLF/CR, BOM), exceljs rich-cell normalization (formula results, hyperlinks, rich text), magic-byte format detection, and a clean actionable rejection of legacy .xls.", "pl-by-month-parser fundamentals are sound: parseAmount handles $, commas and accounting parens; parsePeriodHeader covers the four observed Xero header formats; summary rows are excluded by an exact-name set (not substring), so accounts containing 'total' in longer names are not dropped by the Xero sync path (only by the CSV import path, reported separately).", "Xero payroll re-import reconciliation (Phase 52-02 helpers) is conservative and correct: fingerprint-based 3-way diff, silent apply only for non-overridden fields, fingerprint always advanced on keep-mine so conflicts don't re-prompt, and manually-added members are never iterated for removal.", "Dual-ID discipline holds on the audited paths: pl-summary and historical-pl-summary route through resolveBusinessProfileIds/resolveXeroBusinessId with the lookup_error surface for desyncs; xero_pl_lines is written business_profiles-keyed by the orchestrator; no raw business_id joins found in the wizard read path."], "formulas": ["Y1 revenue materialization is exact to the dollar with actuals locked: initializeFromXero locks EVERY line's completed months (at $0 when the line didn't trade, useForecastWizard.ts:1298-1354), absorbs rounding residue in the last future month so per-line sums exactly equal the rounded line target (lines 1362-1381), and distributeGoalRevenueMonthly does the same for Y2/Y3 (P0-14, lines 1487-1498); convertRevenue copies year1Monthly per-key with round2 \u2014 no re-derivation.", "Summary and materializer team math agree on the core paths (recomputed by hand): salary compounding base (year1Salary = newSalary export at useForecastWizard.ts:2045 matches converter's baseSalary^(yearNum-1)); departures inclusive of the end month on both sides; bonuses calendar-month keyed with identical departure guard; per-hire increasePct (`?? 3`) honored on both sides; hire start proration (13\u2212fyMonth months vs per-month `mk < startMonth` skip) equal; commission accrue-and-pay-at-period-end preserves the summary's annual figure for seasonal lines including the Y1-share fallback for empty out-years.", "The #377/#378/#379 COGS fixes are intact: COGS mix % is measured against the pinned GP-target-implied total (handleCogsMixChange, Step3RevenueCOGS.tsx:1943-1976 with the Dragon post-mortem encoded), variable COGS months follow revenue weights not prior-year seasonality (redistributeCOGSToHitGPTarget line 1793-1797), Y1 actual COGS months lock from Xero YTD by account name (lines 1780-1812), and per-line annual targets absorb residue on the last variable line.", "Super rate is 0.12 everywhere that matters (types.ts SUPER_RATE = 0.12, constants.ts SUPERANNUATION.DEFAULT_RATE = 0.12), contractors are excluded from super on both the summary and the converter, and casual annual salary = hourlyRate \u00d7 hoursPerWeek \u00d7 weeksPerYear is computed and committed into hire.salary by the Step 4 UI (calculateCasualAnnual, Step4Team.tsx:180-181) so the summary's hire.salary path and the converter's casual fallback agree.", "The generate route guards hold: EMPTY_FORECAST 422 blocks a 0-line final generate before any row is touched (route.ts:164-174); drafts insert is_active=false and never materialize (route.ts:191-213, 289); activation happens last, only after materialization succeeds (route.ts:392-406); the atomic save_assumptions_and_materialize RPC prevents assumptions/lines divergence on failure.", "checkSummaryParity's own math is correct: other income re-aggregated on the revenue side matching where the materializer files it, unknown categories default to expense (fails toward visible divergence), month-key restriction prevents 3-year-lines-vs-1-year-summary false positives, $1 tolerance is consistent with the converter's cent-rounding. Caveat: it is watch-mode only, which is why findings FML-01/02/04/05/06 ship instead of blocking.", "Loan math: calculateLoanPayment PMT formula is the standard amortization formula with correct 0-term/0-rate guards; calculateAmortizedInterestForWindow walks the real schedule, clips at termMonths, and its declining-interest behavior recomputes correctly ($100K @ 5%/5yr \u2192 Y1\u2248$4.7K); the shared getPlannedSpendPLBreakdown helper is used by BOTH the summary and the materializer, so CapEx screen and stored numbers cannot drift from each other (the taxonomy defects in FML-07 are consistently shared).", "Rounding discipline is adequate at the whole-forecast scale: expandQuarterlyToMonthly loses \u22642c per quarter, adhoc spreads lose \u22646c/yr, per-month Math.round in handleGrowthChange leaves \u2264$6/line/yr without residue absorption \u2014 all inside the $1-per-bucket parity tolerance except pathological 100+-line charts; no compounding drift across years because each year is derived from annual figures, not from the prior year's rounded months.", "The Phase 51 line-distribution helpers (getRevenueLineMonthlyDistribution / getEffectiveSeasonality) are math-sound (locked-actuals preserved, remaining target normalized over remaining seasonality); note getRevenueLineMonthlyDistribution is currently dead code \u2014 no caller imports it \u2014 so its lack of residue absorption is not a live defect.", "Subscription rollup agrees between summary, export, and materializer for the normal case: active-vendor filtering before snapshot, \u03a3(monthly\u00d712), identical accountCode-based OpEx exclusion applied in both the summary reduce and the assumptions export, and the covered-twin retirement in the converter prevents double-count in stored lines (the only disagreement is the 0%-growth nullish issue in FML-05)."], "process": ["Draft vs Generate semantics (#350) hold with no regression: draft creates insert is_active=false (generate/route.ts:191-204), the forecastId+createNew+isDraft hole is closed (`isDraft && (!forecastId || createNew)`), draft updates strip revenue/GP/NP goals + wizard_state + goal_source via applyDraftPublishGuard, materialization runs only when `assumptions && generatedLines && !isDraft` (route.ts:289), and activation is deliberately LAST \u2014 after lines land \u2014 so a materialize failure can never install an active zero-line forecast (route.ts:385-406).", "0-line forecasts cannot generate: client gate in handleComplete (ForecastWizardV4.tsx:1843 hasUsablePLLineData) mirrors the server EMPTY_FORECAST 422 (route.ts:164-174), which derives lines BEFORE any row write. wizard-init-guard.ts requires actual P&L line data to skip full init, closing the Dragon/Efficient-Living failed-seed trap; its unit test exists at __tests__/wizard-init-guard.test.ts.", "is_active uniqueness is sound under concurrency: create_active_forecast_locked and activate_forecast_locked share the same pg_advisory_xact_lock key on (business, fiscal_year) and run deactivate+activate in one transaction (migration 20260812090000), with the unique_active_forecast_per_fy partial index as backstop; the SECURITY DEFINER function follows the revoke/grant hygiene rule.", "Summary-parity watch runs on every final Generate for Year 1 (route.ts:357-382) with an invariant-tagged Sentry message, and the check itself can never break a publish. (Its Y1-only scope is the gap exploited by PROC-01, but the Y1 wiring is correct.)", "The localStorage draft lifecycle is well defended within its design: version soft-migration no longer wipes drafts on WIZARD_VERSION bumps (only refuses downgrades), the v10\u2192v11 step-swap migration remaps currentStep, activeYear is clamped to forecastDuration, startFresh clears the slot before the useState initializer can leak old state, quota-full failures surface a one-shot toast, and Generate clears the draft so a pre-generate snapshot can't restore forever (PR-A H5, ForecastWizardV4.tsx:1864).", "convertTeam faithfully mirrors the wizard summary's payroll math: salaries compound from year1Salary, super (team.superannuationPct, SUPER_RATE=0.12 \u2014 current statutory rate) applies to employees only, bonuses are calendar-month keyed and departure-guarded, commissions accrue per timing period, and the WorkCover/PayrollTax phantom lines stay removed with RETIRED_LINE_NAMES protection that correctly spares real Xero accounts of the same names (assumptions-to-pl-lines.ts:445-663, 982-994).", "Y1 revenue materialization fidelity holds: initializeFromXero locks ALL lines' completed months to YTD actuals (at $0 for non-trading lines), absorbs rounding residue in the last future month so per-line sums exactly hit the rounded target, and buildAssumptions omits empty Y2/Y3 maps so untouched year tabs cannot store $0 years (useForecastWizard.ts:1291-1390, 1985-2006).", "Autosave orchestration guards the known races: the isRestoringAssumptionsRef flag suppresses autosave during the async restore chain with finally-cleanup on both paths (P0-3), performAutoSave is excluded while isSaving/isAutoSaving/isReadOnly, step navigation flushes pending subscription saves and blocks navigation on flush failure, and Step-1 forward navigation is hard-gated on a Y1 revenue goal.", "Dual-ID discipline is respected on every route touched: generate resolves businessId\u2192profileId before writing financial_forecasts (route.ts:103), versions GET queries `.in('business_id', ids.all)`, recompute resolves from the ROW's business_id, and ForecastReadService resolves at the boundary and paginates xero_pl_lines to defeat the 1000-row cap.", "The 21000 duplicate-account_code upsert failure is pre-empted in the converter (unique-code guard nulls later duplicates, assumptions-to-pl-lines.ts:1029-1038), and stale-line resurrection is prevented precisely (retired names + subscription-covered codes) rather than via the dangerous p_force_full_replace full-delete."], "empirical": ["Revenue materialization is exact to the dollar on every completed client forecast: Y1 window (actuals-preferred) revenue vs revenue_goal \u2014 Armstrong 7,500,000=7,500,000; Digital Bond 1,500,000=1,500,000; Dragon 9,943,737=9,943,737 (down to the odd dollar); Envisage FY27 1,200,000=1,200,000; JDS 10,800,000=10,800,000. The apparent 3-4x inflation in raw sums is just the 36-month (3-year) horizon, not double-counting.", "No duplicate (forecast_id, account_code, account_name) groups among active forecasts' lines; no orphaned forecast_pl_lines (every non-deleted line joins a live forecast); no business has two is_active forecasts for the same fiscal year (checked across all financial_forecasts, deleted_at null).", "Draft/materialize discipline post-#350 holds: only two non-active non-completed forecasts have materialized lines, both explained \u2014 Armstrong FY2027 (created 2026-05-28, pre-#350) and Precision Electrical Group FY2026 Budget (2026-07-29, the script-seeded demo). No post-#350 wizard-created drafts have lines.", "Section-based P&L classification (pl-by-month-parser classifyAccountType) produces a coherent type distribution for all 14 synced tenants \u2014 every operating tenant has revenue+opex; COGS presence matches business type (JDS 29 COGS accounts/$10.3M, Attaquer 11, Armstrong 7; service businesses near zero). The 'Sales -' prefix guard protecting JDS 'Sales - Rental Income' from other-income demotion is in place (historical-pl-summary.ts:470-479).", "Cost-behavior keyword classification is correct on conventional AU SMB names across all 12 CoAs: all marketing/advertising variants \u2192 variable, all utilities (Electricity, Light Power Heating, Light & Power) \u2192 seasonal, rent/insurance/subscriptions/telephone \u2192 fixed, travel/repairs/training \u2192 adhoc. The wage/super core (Wages, 'Employ - Wages & Salaries', 'Employment Expenses: Salaries & Wages', 'Administration Wages', Superannuation variants) is caught as team cost in every business, and the payroll-tax/workcover OpEx override works for the standard spellings (JDS Payroll Tax $71k, WorkCover $36k, SPM Workers Compensation $33k all correctly retained in OpEx as fixed).", "Multi-currency isolation: IICT is the only business with a non-AUD org (HK, HKD, active, include_in_consolidation=true), so the FX-engine gate (needsFxConsolidation) is exercised by exactly one business; no stored forecast contains HKD amounts summed as AUD (IICT's forecast contains no HK-org lines at all \u2014 that absence is finding XVAL-4, but there is no silent cross-currency summing). getFiscalYearActuals' fail-closed non-AUD skip is correctly conservative for IICT.", "Multi-tenant name-merge in aggregatePeriod (historical-pl-summary.ts:595-612) handles Dragon's 2 orgs: Dragon's completed forecast reconciles to the dollar and both tenants' P&L rows are present in xero_pl_lines (Dragon Pty Ltd + Easy Hail Claim, distinct tenant_ids, joined data feeding one business).", "sync_jobs/xero_pl_lines dual-ID discipline: xero_pl_lines.business_id is uniformly business_profiles-space (join to businesses returned zero rows; join to business_profiles returned all 14 tenants), consistent with the documented id-space rules."]}}